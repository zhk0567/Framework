# Back-end / PHP

与 [`Back-end/Node`](../Node) 等在 **`GET /api/health`**、**`GET /api/info`** 及 **`/` 呈现页** 上对齐。**Laravel / Symfony** 本仓库为 **PHP 内置服务器 + `router.php`** 形态，完整框架见各目录说明中的 **Composer / CLI**。

| 子目录 | 技术 | 默认端口 |
|--------|------|----------|
| [Laravel](Laravel) | Laravel（HTTP 对齐 + 官方 CLI 说明） | **3081** |
| [Symfony](Symfony) | Symfony（HTTP 对齐 + 官方 CLI 说明） | **3082** |

**环境**：**PHP 8.2+**。命令见根目录 [README.md](../../README.md) 与各 `*-PHP.md`。
