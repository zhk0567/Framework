[package]
name = "framework_rocket_guide"
version = "0.1.0"
edition = "2021"
description = "Rocket 最小 HTTP 示例；与 Axum / actix-web / Warp 并排"

[dependencies]
rocket = { version = "0.5", features = ["json"] }
serde_json = "1"
