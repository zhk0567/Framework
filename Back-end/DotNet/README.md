# Back-end / DotNet

与 [`Back-end/Node`](../Node)、[`Back-end/Go`](../Go)、[`Back-end/JVM`](../JVM) 在 **`GET /api/health`**、**`GET /api/info`** 及 **`/` 呈现页** 上对齐。

| 子目录 | 技术 | 默认端口 |
|--------|------|----------|
| [AspNetCore](AspNetCore) | ASP.NET Core · Minimal API | **3080** |

**环境**：**.NET SDK 9**（或按各 `*.csproj` 调整）。命令见根目录 [README.md](../../README.md) 与各 `*-DotNet.md`。
