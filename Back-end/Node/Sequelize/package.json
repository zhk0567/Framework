{
  "name": "framework-back-end-sequelize",
  "private": true,
  "version": "1.0.0",
  "description": "Fastify + Sequelize + pg-mem（内存 PostgreSQL，无 sqlite 原生编译）",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/static": "^8.0.3",
    "fastify": "^5.2.1",
    "pg-mem": "^3.0.14",
    "sequelize": "^6.37.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
