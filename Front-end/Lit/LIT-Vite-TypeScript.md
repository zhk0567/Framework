# Lit（Vite + TypeScript）

**Web Components** 与 `LitElement`；`@state`、模板 `html` 标签函数、`:host` 样式。

```powershell
Set-Location -LiteralPath 'f:\Study\Framework\Front-end\Lit'
npm install
npm run dev
```

入口：`index.html` → `lit-showcase` 自定义元素。
