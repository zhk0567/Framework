# Backbone.js（Vite + TypeScript）

**Model / Collection / View + Events**；依赖 **jQuery** 操作 DOM（`Backbone.$`）。

```powershell
Set-Location -LiteralPath 'f:\Study\Framework\Front-end\Backbone'
npm install
npm run dev
```
