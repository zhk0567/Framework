# Qwik（Qwik City + Vite）

**可恢复（resumability）**、推迟执行；路由由 **Qwik City** 提供，`src/routes/index.tsx` 为展台页。

```powershell
Set-Location -LiteralPath 'f:\Study\Framework\Front-end\Qwik'
npm install
npm start
```

构建：`npm run build`；预览：`npm run preview`。
