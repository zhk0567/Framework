# Ember.js（Ember CLI）

**约定优于配置**；本目录由 `ember-cli` 生成（包名为 `ember-demo`），构建为 **broccoli** 管线。

```powershell
Set-Location -LiteralPath 'f:\Study\Framework\Front-end\Ember'
npm install
npm start
```

默认 `http://127.0.0.1:4200/`。静态说明见 `app/templates/application.hbs`；扩展交互可新增 Glimmer 组件与路由。
