# Alpine.js（Vite + TypeScript）

HTML 指令式增强（`x-data`、`x-for`、`x-model`）；亦可 **CDN 零构建** 使用，本目录用 Vite 仅作打包。

```powershell
Set-Location -LiteralPath 'f:\Study\Framework\Front-end\Alpine'
npm install
npm run dev
```
