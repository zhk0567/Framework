{
  "name": "capacitor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev --port 5206 --strictPort",
    "build": "tsc && vite build",
    "preview": "vite preview --port 5206",
    "cap:sync": "npm run build && npx cap sync"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@capacitor/cli": "^8.3.3",
    "@capacitor/core": "^8.3.3"
  }
}
