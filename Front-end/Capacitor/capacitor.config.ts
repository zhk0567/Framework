import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.framework.capshowcase',
  appName: 'Capacitor showcase',
  webDir: 'dist'
};

export default config;
