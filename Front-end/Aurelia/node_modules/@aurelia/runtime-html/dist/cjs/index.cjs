"use strict";

var t = require("@aurelia/kernel");

var e = require("@aurelia/template-compiler");

var s = require("@aurelia/metadata");

var i = require("@aurelia/runtime");

var n = require("@aurelia/platform-browser");

var r = require("@aurelia/expression-parser");

typeof SuppressedError === "function" ? SuppressedError : function(t, e, s) {
    var i = new Error(s);
    return i.name = "SuppressedError", i.error = t, i.suppressed = e, i;
};

const {default: l, oneTime: h, toView: a, fromView: c, twoWay: u} = e.BindingMode;

const f = s.Metadata.get;

const d = s.Metadata.has;

const p = s.Metadata.define;

const clearMetadata = (t, ...e) => {
    for (const i of e) {
        s.Metadata.delete(i, t);
    }
};

const {annotation: m} = t.Protocol;

const g = m.keyFor;

const x = Object;

const v = String;

const y = x.prototype;

const w = y.hasOwnProperty;

const b = x.freeze;

const k = x.assign;

const C = x.getOwnPropertyNames;

const S = x.keys;

const B = /*@__PURE__*/ t.createLookup();

const isDataAttribute = (e, s, i) => {
    if (B[s] === true) {
        return true;
    }
    if (!t.isString(s)) {
        return false;
    }
    const n = s.slice(0, 5);
    return B[s] = n === "aria-" || n === "data-" || i.isStandardSvgAttribute(e, s);
};

const rethrow = t => {
    throw t;
};

const A = Reflect.defineProperty;

const defineHiddenProp = (t, e, s) => {
    A(t, e, {
        enumerable: false,
        configurable: true,
        writable: true,
        value: s
    });
    return s;
};

const addSignalListener = (t, e, s) => t.addSignalListener(e, s);

const removeSignalListener = (t, e, s) => t.removeSignalListener(e, s);

const R = "Interpolation";

const T = "IsIterator";

const E = "IsFunction";

const L = "IsProperty";

const M = "pending";

const D = "running";

const q = i.AccessorType.Observer;

const P = i.AccessorType.Node;

const _ = i.AccessorType.Layout;

const createMappedError = (t, ...e) => {
    const s = v(t).padStart(4, "0");
    return new Error(`AUR${s}:${e.map(v)}`);
};

function bindable(e, s) {
    let i = void 0;
    function decorator(e, s) {
        let n;
        switch (s.kind) {
          case "getter":
          case "field":
            {
                const t = s.name;
                if (typeof t !== "string") throw createMappedError(227);
                n = t;
                break;
            }

          case "class":
            if (i == null) throw createMappedError(228);
            if (typeof i == "string") {
                n = i;
            } else {
                const t = i.name;
                if (!t) throw createMappedError(229);
                if (typeof t !== "string") throw createMappedError(227);
                n = t;
            }
            break;
        }
        const r = i == null || typeof i === "string" ? {
            name: n
        } : i;
        const l = s.metadata[I] ??= t.createLookup();
        l[n] = BindableDefinition.create(n, r);
    }
    if (arguments.length > 1) {
        i = {};
        decorator(e, s);
        return;
    } else if (t.isString(e)) {
        i = e;
        return decorator;
    }
    i = e === void 0 ? {} : e;
    return decorator;
}

const I = /*@__PURE__*/ g("bindables");

const V = b({
    name: I,
    keyFrom: t => `${I}:${t}`,
    from(...e) {
        const s = {};
        const i = Array.isArray;
        function addName(t) {
            s[t] = BindableDefinition.create(t);
        }
        function addDescription(t, e) {
            s[t] = e instanceof BindableDefinition ? e : BindableDefinition.create(t, e === true ? {} : e);
        }
        function addList(e) {
            if (i(e)) {
                e.forEach(e => t.isString(e) ? addName(e) : addDescription(e.name, e));
            } else if (e instanceof BindableDefinition) {
                s[e.name] = e;
            } else if (e !== void 0) {
                S(e).forEach(t => addDescription(t, e[t]));
            }
        }
        e.forEach(addList);
        return s;
    },
    getAll(e) {
        const s = [];
        const i = t.getPrototypeChain(e);
        let n = i.length;
        let r;
        while (--n >= 0) {
            r = i[n];
            const t = f(I, r);
            if (t == null) continue;
            s.push(...Object.values(t));
        }
        return s;
    },
    i(e, s) {
        let i = f(I, s);
        if (i == null) {
            p(i = t.createLookup(), s, I);
        }
        i[e.name] = e;
    }
});

class BindableDefinition {
    constructor(t, e, s, i, n) {
        this.attribute = t;
        this.callback = e;
        this.mode = s;
        this.name = i;
        this.set = n;
    }
    static toAttr(t) {
        return this.h[t] ??= t.replace(/([A-Z])/g, (t, e) => `-${e.toLowerCase()}`);
    }
    static create(s, i = {}) {
        const n = i.mode ?? a;
        return new BindableDefinition(i.attribute ?? BindableDefinition.toAttr(s), i.callback ?? `${s}Changed`, t.isString(n) ? e.BindingMode[n] ?? l : n, i.name ?? s, i.set ?? getInterceptor(i));
    }
}

BindableDefinition.h = {};

function coercer(t, e) {
    e.addInitializer(function() {
        F.define(this, e.name);
    });
}

const F = {
    key: /*@__PURE__*/ g("coercer"),
    define(t, e) {
        p(t[e].bind(t), t, F.key);
    },
    for(t) {
        return f(F.key, t);
    }
};

function getInterceptor(e = {}) {
    const s = e.type ?? null;
    if (s == null) {
        return t.noop;
    }
    let i;
    switch (s) {
      case Number:
      case Boolean:
      case String:
      case BigInt:
        i = s;
        break;

      default:
        {
            const e = s.coerce;
            i = typeof e === "function" ? e.bind(s) : F.for(s) ?? t.noop;
            break;
        }
    }
    return i === t.noop ? i : createCoercer(i, e.nullable);
}

function createCoercer(t, e) {
    return function(s, i) {
        if (!i?.enableCoercion) return s;
        return (e ?? (i?.coerceNullish ?? false ? false : true)) && s == null ? s : t(s, i);
    };
}

const H = t.DI.createInterface;

const O = t.Registration.singleton;

const $ = t.Registration.aliasTo;

const N = t.Registration.instance;

t.Registration.callback;

t.Registration.transient;

const registerResolver = (t, e, s) => t.registerResolver(e, s);

function alias(...t) {
    return function(e, s) {
        s.addInitializer(function() {
            const e = g("aliases");
            const s = f(e, this);
            if (s === void 0) {
                p(t, this, e);
            } else {
                s.push(...t);
            }
        });
    };
}

function registerAliases(t, e, s, i) {
    for (let n = 0, r = t.length; n < r; ++n) {
        $(s, e.keyFrom(t[n])).register(i);
    }
}

const W = "custom-element";

const j = "custom-attribute";

const z = "__au_static_resource__";

const getDefinitionFromStaticAu = (t, e, s, i = z) => {
    let n = f(i, t);
    if (n == null) {
        if (t.$au?.type === e) {
            n = s(t.$au, t);
            p(n, t, i);
        }
    }
    return n;
};

const clearStaticAuDefinition = t => {
    clearMetadata(t, z);
};

function bindingBehavior(t) {
    return function(e, s) {
        s.addInitializer(function() {
            K.define(t, this);
        });
        return e;
    };
}

class BindingBehaviorDefinition {
    constructor(t, e, s, i) {
        this.Type = t;
        this.name = e;
        this.aliases = s;
        this.key = i;
    }
    static create(e, s) {
        let i;
        let n;
        if (t.isString(e)) {
            i = e;
            n = {
                name: i
            };
        } else {
            i = e.name;
            n = e;
        }
        return new BindingBehaviorDefinition(s, t.firstDefined(getBehaviorAnnotation(s, "name"), i), t.mergeArrays(getBehaviorAnnotation(s, "aliases"), n.aliases, s.aliases), K.keyFrom(i));
    }
    register(t, e) {
        const s = this.Type;
        const i = typeof e === "string" ? getBindingBehaviorKeyFrom(e) : this.key;
        const n = this.aliases;
        if (!t.has(i, false)) {
            t.register(t.has(s, false) ? null : O(s, s), $(s, i), ...n.map(t => $(s, getBindingBehaviorKeyFrom(t))));
        }
    }
}

const U = "binding-behavior";

const G = /*@__PURE__*/ t.getResourceKeyFor(U);

const getBehaviorAnnotation = (t, e) => f(g(e), t);

const getBindingBehaviorKeyFrom = t => `${G}:${t}`;

const K = /*@__PURE__*/ b({
    name: G,
    keyFrom: getBindingBehaviorKeyFrom,
    isType(e) {
        return t.isFunction(e) && (d(G, e) || e.$au?.type === U);
    },
    define(e, s) {
        const i = BindingBehaviorDefinition.create(e, s);
        const n = i.Type;
        p(i, n, G, t.resourceBaseName);
        return n;
    },
    getDefinition(t) {
        const e = f(G, t) ?? getDefinitionFromStaticAu(t, U, BindingBehaviorDefinition.create);
        if (e === void 0) {
            throw createMappedError(151, t);
        }
        return e;
    },
    find(t, e) {
        const s = t.find(U, e);
        return s == null ? null : f(G, s) ?? getDefinitionFromStaticAu(s, U, BindingBehaviorDefinition.create) ?? null;
    },
    get(e, s) {
        return e.get(t.resource(getBindingBehaviorKeyFrom(s)));
    }
});

const X = new Map;

const createConfig = t => ({
    type: U,
    name: t
});

class BindingModeBehavior {
    bind(t, e) {
        X.set(e, e.mode);
        e.mode = this.mode;
    }
    unbind(t, e) {
        e.mode = X.get(e);
        X.delete(e);
    }
}

class OneTimeBindingBehavior extends BindingModeBehavior {
    get mode() {
        return h;
    }
}

OneTimeBindingBehavior.$au = createConfig("oneTime");

class ToViewBindingBehavior extends BindingModeBehavior {
    get mode() {
        return a;
    }
}

ToViewBindingBehavior.$au = createConfig("toView");

class FromViewBindingBehavior extends BindingModeBehavior {
    get mode() {
        return c;
    }
}

FromViewBindingBehavior.$au = createConfig("fromView");

class TwoWayBindingBehavior extends BindingModeBehavior {
    get mode() {
        return u;
    }
}

TwoWayBindingBehavior.$au = createConfig("twoWay");

const Q = new WeakMap;

const Y = 200;

class DebounceBindingBehavior {
    constructor() {
        this.p = t.resolve(t.IPlatform);
    }
    bind(e, s, i, n) {
        const r = {
            type: "debounce",
            delay: i ?? Y,
            now: this.p.performanceNow,
            signals: t.isString(n) ? [ n ] : n ?? t.emptyArray
        };
        const l = s.limit?.(r);
        if (l == null) ; else {
            Q.set(s, l);
        }
    }
    unbind(t, e) {
        Q.get(e)?.dispose();
        Q.delete(e);
    }
}

DebounceBindingBehavior.$au = {
    type: U,
    name: "debounce"
};

const Z = /*@__PURE__*/ H("ISignaler", t => t.singleton(Signaler));

class Signaler {
    constructor() {
        this.signals = t.createLookup();
    }
    dispatchSignal(t) {
        const e = this.signals[t];
        if (e === undefined) {
            return;
        }
        let s;
        for (s of e.keys()) {
            s.handleChange(undefined, undefined);
        }
    }
    addSignalListener(t, e) {
        (this.signals[t] ??= new Set).add(e);
    }
    removeSignalListener(t, e) {
        this.signals[t]?.delete(e);
    }
}

class SignalBindingBehavior {
    constructor() {
        this.u = new Map;
        this.C = t.resolve(Z);
    }
    bind(t, e, ...s) {
        if (!("handleChange" in e)) {
            throw createMappedError(817);
        }
        if (s.length === 0) {
            throw createMappedError(818);
        }
        this.u.set(e, s);
        let i;
        for (i of s) {
            addSignalListener(this.C, i, e);
        }
    }
    unbind(t, e) {
        const s = this.u.get(e);
        this.u.delete(e);
        let i;
        for (i of s) {
            removeSignalListener(this.C, i, e);
        }
    }
}

SignalBindingBehavior.$au = {
    type: U,
    name: "signal"
};

const J = new WeakMap;

const tt = 200;

class ThrottleBindingBehavior {
    constructor() {
        ({performanceNow: this.B} = t.resolve(t.IPlatform));
    }
    bind(e, s, i, n) {
        const r = {
            type: "throttle",
            delay: i ?? tt,
            now: this.B,
            signals: t.isString(n) ? [ n ] : n ?? t.emptyArray
        };
        const l = s.limit?.(r);
        if (l == null) ; else {
            J.set(s, l);
        }
    }
    unbind(t, e) {
        J.get(e)?.dispose();
        J.delete(e);
    }
}

ThrottleBindingBehavior.$au = {
    type: U,
    name: "throttle"
};

const et = /*@__PURE__*/ H("IAppTask");

class $AppTask {
    constructor(t, e, s) {
        this.c = void 0;
        this.slot = t;
        this.k = e;
        this.cb = s;
    }
    register(t) {
        return this.c = t.register(N(et, this));
    }
    run() {
        const t = this.k;
        const e = this.cb;
        return t === null ? e() : e(this.c.get(t));
    }
}

const st = b({
    creating: createAppTaskSlotHook("creating"),
    hydrating: createAppTaskSlotHook("hydrating"),
    hydrated: createAppTaskSlotHook("hydrated"),
    activating: createAppTaskSlotHook("activating"),
    activated: createAppTaskSlotHook("activated"),
    deactivating: createAppTaskSlotHook("deactivating"),
    deactivated: createAppTaskSlotHook("deactivated")
});

function createAppTaskSlotHook(e) {
    function appTaskFactory(s, i) {
        if (t.isFunction(i)) {
            return new $AppTask(e, s, i);
        }
        return new $AppTask(e, null, s);
    }
    return appTaskFactory;
}

const it = t.IPlatform;

class Refs {}

const nt = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    let e = false;
    return new class {
        get hideProp() {
            return e;
        }
        set hideProp(t) {
            e = t;
        }
        get(e, s) {
            return t.get(e)?.[s] ?? null;
        }
        set(s, i, n) {
            const r = t.get(s) ?? (t.set(s, new Refs), t.get(s));
            if (i in r) {
                throw new Error(`Node already associated with a controller, remove the ref "${i}" first before associating with another controller`);
            }
            if (!e) {
                s.$au ??= r;
            }
            return r[i] = n;
        }
        clear(e) {
            const s = t.get(e);
            if (s == null) return;
            t.delete(e);
            if (e.$au != null) {
                delete e.$au;
            }
        }
    };
})();

const ot = /*@__PURE__*/ H("INode");

function watch(e, s, i) {
    if (e == null) {
        throw createMappedError(772);
    }
    return function decorator(n, r) {
        const l = r.kind === "class";
        let h;
        let a;
        if (l) {
            if (!t.isFunction(s) && (s == null || !(s in n.prototype))) {
                throw createMappedError(773, `${v(s)}@${n.name}}`);
            }
            a = s;
            h = i ?? {};
        } else {
            if (!t.isFunction(n) || r.static) {
                throw createMappedError(774, r.name);
            }
            a = n;
            h = s ?? {};
        }
        const c = new WatchDefinition(e, a, h.flush);
        if (l) {
            addDefinition(n);
        } else {
            let t = false;
            r.addInitializer(function() {
                if (!t) {
                    t = true;
                    addDefinition(this.constructor);
                }
            });
        }
        function addDefinition(t) {
            rt.add(t, c);
            if (isAttributeType(t)) {
                getAttributeDefinition(t).watches.push(c);
            }
            if (isElementType(t)) {
                getElementDefinition(t).watches.push(c);
            }
        }
    };
}

class WatchDefinition {
    constructor(t, e, s = "async") {
        this.expression = t;
        this.callback = e;
        this.flush = s;
    }
}

const rt = /*@__PURE__*/ (() => {
    const e = new WeakMap;
    return b({
        add(t, s) {
            let i = e.get(t);
            if (i == null) {
                e.set(t, i = []);
            }
            i.push(s);
        },
        getDefinitions(s) {
            return e.get(s) ?? t.emptyArray;
        }
    });
})();

function customAttribute(t) {
    return function(e, s) {
        s.addInitializer(function() {
            defineAttribute(t, this);
        });
        return e;
    };
}

function templateController(e) {
    return function(s, i) {
        i.addInitializer(function() {
            defineAttribute(t.isString(e) ? {
                isTemplateController: true,
                name: e
            } : {
                isTemplateController: true,
                ...e
            }, this);
        });
        return s;
    };
}

class CustomAttributeDefinition {
    get type() {
        return j;
    }
    constructor(t, e, s, i, n, r, l, h, a, c, u) {
        this.Type = t;
        this.name = e;
        this.aliases = s;
        this.key = i;
        this.isTemplateController = n;
        this.bindables = r;
        this.noMultiBindings = l;
        this.watches = h;
        this.dependencies = a;
        this.containerStrategy = c;
        this.defaultProperty = u;
    }
    static create(e, s) {
        let i;
        let n;
        if (t.isString(e)) {
            i = e;
            n = {
                name: i
            };
        } else {
            i = e.name;
            n = e;
        }
        for (const t of Object.values(V.from(n.bindables))) {
            V.i(t, s);
        }
        return new CustomAttributeDefinition(s, t.firstDefined(getAttributeAnnotation(s, "name"), i), t.mergeArrays(getAttributeAnnotation(s, "aliases"), n.aliases, s.aliases), getAttributeKeyFrom(i), t.firstDefined(getAttributeAnnotation(s, "isTemplateController"), n.isTemplateController, s.isTemplateController, false), V.from(...V.getAll(s), getAttributeAnnotation(s, "bindables"), s.bindables, n.bindables), t.firstDefined(getAttributeAnnotation(s, "noMultiBindings"), n.noMultiBindings, s.noMultiBindings, false), t.mergeArrays(rt.getDefinitions(s), s.watches), t.mergeArrays(getAttributeAnnotation(s, "dependencies"), n.dependencies, s.dependencies), t.firstDefined(getAttributeAnnotation(s, "containerStrategy"), n.containerStrategy, s.containerStrategy, "reuse"), t.firstDefined(getAttributeAnnotation(s, "defaultProperty"), n.defaultProperty, s.defaultProperty, "value"));
    }
    register(t, e) {
        const s = this.Type;
        const i = typeof e === "string" ? getAttributeKeyFrom(e) : this.key;
        const n = this.aliases;
        if (t.has(i, false)) {
            console.warn(createMappedError(154, this.name));
        }
        t.register(O(i, s), ...n.map(t => $(i, getAttributeKeyFrom(t))));
    }
    toString() {
        return `au:ca:${this.name}`;
    }
}

const lt = "custom-attribute";

const ht = /*@__PURE__*/ t.getResourceKeyFor(lt);

const getAttributeKeyFrom = t => `${ht}:${t}`;

const getAttributeAnnotation = (t, e) => f(g(e), t);

const isAttributeType = e => t.isFunction(e) && (d(ht, e) || e.$au?.type === lt);

const findAttributeControllerFor = (t, e) => nt.get(t, getAttributeKeyFrom(e)) ?? void 0;

const defineAttribute = (e, s) => {
    const i = CustomAttributeDefinition.create(e, s);
    const n = i.Type;
    p(i, n, ht, t.resourceBaseName);
    return n;
};

const getAttributeDefinition = t => {
    const e = f(ht, t) ?? getDefinitionFromStaticAu(t, lt, CustomAttributeDefinition.create);
    if (e === void 0) {
        throw createMappedError(759, t);
    }
    return e;
};

const findClosestControllerByName = (e, s) => {
    let i = "";
    let n = "";
    if (t.isString(s)) {
        i = getAttributeKeyFrom(s);
        n = s;
    } else {
        const t = getAttributeDefinition(s);
        i = t.key;
        n = t.name;
    }
    let r = e;
    while (r !== null) {
        const t = nt.get(r, i);
        if (t?.is(n)) {
            return t;
        }
        r = getEffectiveParentNode(r);
    }
    return null;
};

const at = /*@__PURE__*/ b({
    name: ht,
    keyFrom: getAttributeKeyFrom,
    isType: isAttributeType,
    for: findAttributeControllerFor,
    closest: findClosestControllerByName,
    define: defineAttribute,
    getDefinition: getAttributeDefinition,
    annotate(t, e, s) {
        p(s, t, g(e));
    },
    getAnnotation: getAttributeAnnotation,
    find(t, e) {
        const s = t.find(lt, e);
        return s === null ? null : f(ht, s) ?? getDefinitionFromStaticAu(s, lt, CustomAttributeDefinition.create) ?? null;
    }
});

const ct = /*@__PURE__*/ H("ILifecycleHooks");

class LifecycleHooksEntry {
    constructor(t, e) {
        this.definition = t;
        this.instance = e;
    }
}

class LifecycleHooksDefinition {
    constructor(t, e) {
        this.Type = t;
        this.propertyNames = e;
    }
    static create(t, e) {
        const s = new Set;
        let i = e.prototype;
        while (i !== y) {
            for (const t of C(i)) {
                if (t !== "constructor" && !t.startsWith("_")) {
                    s.add(t);
                }
            }
            i = Object.getPrototypeOf(i);
        }
        return new LifecycleHooksDefinition(e, s);
    }
}

const ut = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    const e = new WeakMap;
    return b({
        define(t, s) {
            const i = LifecycleHooksDefinition.create(t, s);
            const n = i.Type;
            e.set(n, i);
            return {
                register(t) {
                    O(ct, n).register(t);
                }
            };
        },
        resolve(s) {
            let i = t.get(s);
            if (i === void 0) {
                t.set(s, i = new LifecycleHooksLookupImpl);
                const n = s.root;
                const r = n === s ? s.getAll(ct) : s.has(ct, false) ? n.getAll(ct).concat(s.getAll(ct)) : n.getAll(ct);
                let l;
                let h;
                let a;
                let c;
                let u;
                for (l of r) {
                    h = e.get(l.constructor);
                    a = new LifecycleHooksEntry(h, l);
                    for (c of h.propertyNames) {
                        u = i[c];
                        if (u === void 0) {
                            i[c] = [ a ];
                        } else {
                            u.push(a);
                        }
                    }
                }
            }
            return i;
        }
    });
})();

class LifecycleHooksLookupImpl {}

function lifecycleHooks(e, s) {
    function decorator(e, s) {
        const i = s?.metadata ?? (e[Symbol.metadata] ??= Object.create(null));
        i[t.registrableMetadataKey] = ut.define({}, e);
        return e;
    }
    return e == null ? decorator : decorator(e, s);
}

function isSSRTemplateController(t) {
    return "type" in t;
}

function isSSRScope(t) {
    return !("type" in t);
}

const ft = /*@__PURE__*/ H("ISSRContext");

function adoptSSRView(t, e, s, i, n) {
    const r = t.views[0];
    if (r == null) {
        return null;
    }
    const l = r.nodeCount ?? 1;
    const h = partitionSiblingNodes(i, [ l ]);
    if (h.length === 0 || h[0].length === 0) {
        return null;
    }
    const a = FragmentNodeSequence.adoptSiblings(n, h[0]);
    const c = e.createAdopted(s, a, r);
    c.setLocation(i);
    return {
        view: c,
        viewScope: r
    };
}

function adoptSSRViews(t, e, s, i, n) {
    const r = t.views;
    const l = r.length;
    const h = Array(l);
    for (let t = 0; t < l; ++t) {
        h[t] = r[t]?.nodeCount ?? 1;
    }
    const a = partitionSiblingNodes(i, h);
    const c = Array(l);
    for (let t = 0; t < l; ++t) {
        const l = a[t] ?? [];
        const h = FragmentNodeSequence.adoptSiblings(n, l);
        const u = e.createAdopted(s, h, r[t]);
        u.setLocation(i);
        c[t] = u;
    }
    return {
        views: c,
        viewScopes: r
    };
}

function hydrateSSRDefinition(t) {
    const e = new Map;
    for (const s of t.expressions) {
        e.set(s.id, s.ast);
    }
    const {instructions: s} = translateDefinition(t.definition, t.nestedHtmlTree, e);
    return {
        template: t.template,
        instructions: s,
        needsCompile: false
    };
}

function translateDefinition(t, e, s) {
    const i = [];
    for (let n = 0; n < t.nestedTemplates.length; n++) {
        const r = t.nestedTemplates[n];
        const l = e[n];
        if (r && l) {
            const t = translateDefinition(r, l.nested, s);
            i.push({
                template: l.html,
                instructions: t.instructions,
                name: r.name,
                needsCompile: false
            });
        }
    }
    const n = {
        exprMap: s,
        nestedDefs: i
    };
    const r = t.instructions.map(t => t.map(t => translateInstruction(t, n)));
    return {
        instructions: r,
        nestedDefs: i
    };
}

function translateInstruction(t, s) {
    switch (t.type) {
      case e.itPropertyBinding:
        {
            const i = getExpr(s.exprMap, t.exprId);
            const n = {
                type: e.itPropertyBinding,
                from: i,
                to: t.to,
                mode: t.mode
            };
            return n;
        }

      case e.itTextBinding:
        {
            const i = t.exprIds.map(t => getExpr(s.exprMap, t));
            const n = r.createInterpolation(t.parts, i);
            const l = {
                type: e.itTextBinding,
                from: n
            };
            return l;
        }

      case e.itInterpolation:
        {
            const i = t.exprIds.map(t => getExpr(s.exprMap, t));
            const n = r.createInterpolation(t.parts, i);
            const l = {
                type: e.itInterpolation,
                from: n,
                to: t.to
            };
            return l;
        }

      case e.itListenerBinding:
        {
            const i = getExpr(s.exprMap, t.exprId);
            const n = {
                type: e.itListenerBinding,
                from: i,
                to: t.to,
                capture: t.capture,
                modifier: t.modifier ?? null
            };
            return n;
        }

      case e.itRefBinding:
        {
            const i = getExpr(s.exprMap, t.exprId);
            const n = {
                type: e.itRefBinding,
                from: i,
                to: t.to
            };
            return n;
        }

      case e.itSetProperty:
        {
            const s = {
                type: e.itSetProperty,
                value: t.value,
                to: t.to
            };
            return s;
        }

      case e.itSetAttribute:
        {
            const s = {
                type: e.itSetAttribute,
                value: t.value ?? "",
                to: t.to
            };
            return s;
        }

      case e.itHydrateElement:
        {
            const i = t.instructions.map(t => translateInstruction(t, s));
            const n = {
                type: e.itHydrateElement,
                res: t.res,
                props: i,
                projections: null,
                containerless: t.containerless ?? false,
                captures: void 0,
                data: {}
            };
            return n;
        }

      case e.itHydrateAttribute:
        {
            const i = t.instructions.map(t => translateInstruction(t, s));
            const n = {
                type: e.itHydrateAttribute,
                res: t.res,
                alias: t.alias,
                props: i
            };
            return n;
        }

      case e.itHydrateTemplateController:
        {
            const i = s.nestedDefs[t.templateIndex];
            if (!i) {
                throw new Error(`Missing nested template at index ${t.templateIndex}`);
            }
            const n = t.instructions.map(t => translateInstruction(t, s));
            const r = {
                name: i.name,
                type: "custom-element",
                template: i.template,
                instructions: i.instructions,
                needsCompile: false
            };
            const l = {
                type: e.itHydrateTemplateController,
                def: r,
                res: t.res,
                alias: void 0,
                props: n
            };
            return l;
        }

      case e.itHydrateLetElement:
        {
            const i = t.bindings.map(t => {
                const i = getExpr(s.exprMap, t.exprId);
                const n = {
                    type: e.itLetBinding,
                    from: i,
                    to: t.to
                };
                return n;
            });
            const n = {
                type: e.itHydrateLetElement,
                instructions: i,
                toBindingContext: t.toBindingContext
            };
            return n;
        }

      case e.itIteratorBinding:
        {
            const i = getExpr(s.exprMap, t.exprId);
            const n = [];
            if (t.aux) {
                for (const i of t.aux) {
                    const t = getExpr(s.exprMap, i.exprId);
                    const r = {
                        type: e.itMultiAttr,
                        value: t,
                        to: i.name,
                        command: "bind"
                    };
                    n.push(r);
                }
            }
            const r = {
                type: e.itIteratorBinding,
                forOf: i,
                to: t.to,
                props: n
            };
            return r;
        }

      default:
        throw new Error(`Unknown instruction type: ${t.type}`);
    }
}

function getExpr(t, e) {
    const s = t.get(e);
    if (!s) {
        throw new Error(`Expression not found: ${e}`);
    }
    return s;
}

function valueConverter(t) {
    return function(e, s) {
        s.addInitializer(function() {
            mt.define(t, this);
        });
        return e;
    };
}

class ValueConverterDefinition {
    constructor(t, e, s, i) {
        this.Type = t;
        this.name = e;
        this.aliases = s;
        this.key = i;
    }
    static create(e, s) {
        let i;
        let n;
        if (t.isString(e)) {
            i = e;
            n = {
                name: i
            };
        } else {
            i = e.name;
            n = e;
        }
        return new ValueConverterDefinition(s, t.firstDefined(getConverterAnnotation(s, "name"), i), t.mergeArrays(getConverterAnnotation(s, "aliases"), n.aliases, s.aliases), mt.keyFrom(i));
    }
    register(t, e) {
        const s = this.Type;
        const i = typeof e === "string" ? getValueConverterKeyFrom(e) : this.key;
        const n = this.aliases;
        if (!t.has(i, false)) {
            t.register(t.has(s, false) ? null : O(s, s), $(s, i), ...n.map(t => $(s, getValueConverterKeyFrom(t))));
        }
    }
}

const dt = "value-converter";

const pt = /*@__PURE__*/ t.getResourceKeyFor(dt);

const getConverterAnnotation = (t, e) => f(g(e), t);

const getValueConverterKeyFrom = t => `${pt}:${t}`;

const mt = b({
    name: pt,
    keyFrom: getValueConverterKeyFrom,
    isType(e) {
        return t.isFunction(e) && (d(pt, e) || e.$au?.type === dt);
    },
    define(e, s) {
        const i = ValueConverterDefinition.create(e, s);
        const n = i.Type;
        p(i, n, pt, t.resourceBaseName);
        return n;
    },
    getDefinition(t) {
        const e = f(pt, t) ?? getDefinitionFromStaticAu(t, dt, ValueConverterDefinition.create);
        if (e === void 0) {
            throw createMappedError(152, t);
        }
        return e;
    },
    annotate(t, e, s) {
        p(s, t, g(e));
    },
    getAnnotation: getConverterAnnotation,
    find(t, e) {
        const s = t.find(dt, e);
        return s == null ? null : f(pt, s) ?? getDefinitionFromStaticAu(s, dt, ValueConverterDefinition.create) ?? null;
    },
    get(e, s) {
        return e.get(t.resource(getValueConverterKeyFrom(s)));
    }
});

class BindingTargetSubscriber {
    constructor(t, e) {
        this.v = void 0;
        this.b = t;
        this.A = e;
    }
    flush() {
        if (this.b.isBound) {
            this.b.updateSource(this.v);
        }
    }
    handleChange(t, e) {
        const s = this.b;
        if (t !== i.astEvaluate(s.ast, s.s, s, null)) {
            this.v = t;
            this.A.add(this);
        }
    }
}

const gt = /*@__PURE__*/ (() => {
    function useScope(t) {
        this.s = t;
    }
    return t => {
        defineHiddenProp(t.prototype, "useScope", useScope);
    };
})();

const xt = /*@__PURE__*/ (() => {
    class ResourceLookup {}
    const t = new WeakMap;
    const e = new WeakMap;
    const s = new WeakMap;
    function evaluatorGet(t) {
        return this.l.get(t);
    }
    function evaluatorGetBehavior(t, s) {
        let i = e.get(t);
        if (i == null) {
            e.set(t, i = new ResourceLookup);
        }
        return i[s] ??= K.get(t.l, s);
    }
    function evaluatorBindBehavior(t, e, i) {
        const n = evaluatorGetBehavior(this, t);
        if (n == null) {
            throw createMappedError(101, t);
        }
        let r = s.get(this);
        if (r == null) {
            s.set(this, r = {});
        }
        if (r[t]) {
            throw createMappedError(102, t);
        }
        n.bind?.(e, this, ...i);
    }
    function evaluatorUnbindBehavior(t, e) {
        const i = evaluatorGetBehavior(this, t);
        const n = s.get(this);
        i?.unbind?.(e, this);
        if (n != null) {
            n[t] = false;
        }
    }
    function evaluatorGetConverter(e, s) {
        let i = t.get(e);
        if (i == null) {
            t.set(e, i = new ResourceLookup);
        }
        return i[s] ??= mt.get(e.l, s);
    }
    function evaluatorBindConverter(t) {
        const e = evaluatorGetConverter(this, t);
        if (e == null) {
            throw createMappedError(103, t);
        }
        const s = e.signals;
        if (s != null) {
            const t = this.l.get(Z);
            const e = s.length;
            let i = 0;
            for (;i < e; ++i) {
                t.addSignalListener(s[i], this);
            }
        }
    }
    function evaluatorUnbindConverter(t) {
        const e = evaluatorGetConverter(this, t);
        if (e?.signals === void 0) {
            return;
        }
        const s = this.l.get(Z);
        let i = 0;
        for (;i < e.signals.length; ++i) {
            s.removeSignalListener(e.signals[i], this);
        }
    }
    function evaluatorUseConverter(t, e, s, i) {
        const n = evaluatorGetConverter(this, t);
        if (n == null) {
            throw createMappedError(103, t);
        }
        const r = n.withContext === true;
        let l = null;
        if (r) {
            const t = this.l.get(_e);
            const e = t.controller;
            const s = e.viewModel;
            l = {
                source: s,
                binding: this
            };
        }
        switch (e) {
          case "toView":
            {
                if ("toView" in n) {
                    return r ? n.toView(s, l, ...i) : n.toView(s, ...i);
                }
                return s;
            }

          case "fromView":
            {
                if ("fromView" in n) {
                    return r ? n.fromView?.(s, l, ...i) : n.fromView?.(s, ...i);
                }
                return s;
            }
        }
    }
    return t => {
        const e = t.prototype;
        defineHiddenProp(e, "get", evaluatorGet);
        defineHiddenProp(e, "bindBehavior", evaluatorBindBehavior);
        defineHiddenProp(e, "unbindBehavior", evaluatorUnbindBehavior);
        defineHiddenProp(e, "bindConverter", evaluatorBindConverter);
        defineHiddenProp(e, "unbindConverter", evaluatorUnbindConverter);
        defineHiddenProp(e, "useConverter", evaluatorUseConverter);
    };
})();

const vt = /*@__PURE__*/ H("IFlushQueue", t => t.singleton(FlushQueue));

class FlushQueue {
    constructor() {
        this.R = false;
        this.T = new Set;
    }
    get count() {
        return this.T.size;
    }
    add(t) {
        this.T.add(t);
        if (this.R) {
            return;
        }
        this.R = true;
        try {
            this.T.forEach(flushItem);
        } finally {
            this.R = false;
        }
    }
    clear() {
        this.T.clear();
        this.R = false;
    }
}

const flushItem = function(t, e, s) {
    s.delete(t);
    t.flush();
};

const yt = /*@__PURE__*/ (() => {
    const t = new WeakSet;
    const debounced = (t, e, s) => {
        let n;
        let r;
        let l;
        let h = false;
        const callOriginalCallback = () => e(l);
        const fn = e => {
            l = e;
            if (s.isBound) {
                r = n;
                n = i.queueAsyncTask(callOriginalCallback, {
                    delay: t.delay
                });
                r?.cancel();
            } else {
                callOriginalCallback();
            }
        };
        const a = fn.dispose = () => {
            r?.cancel();
            n?.cancel();
            r = n = void 0;
        };
        fn.flush = () => {
            h = n?.status === M;
            a();
            if (h) {
                callOriginalCallback();
            }
        };
        return fn;
    };
    const throttled = (t, e, s) => {
        let n;
        let r;
        let l = 0;
        let h = 0;
        let a;
        let c = false;
        const now = () => t.now();
        const callOriginalCallback = () => e(a);
        const fn = e => {
            a = e;
            if (s.isBound) {
                h = now() - l;
                r = n;
                if (h > t.delay) {
                    l = now();
                    callOriginalCallback();
                } else {
                    n = i.queueAsyncTask(() => {
                        l = now();
                        callOriginalCallback();
                    }, {
                        delay: t.delay - h
                    });
                }
                r?.cancel();
            } else {
                callOriginalCallback();
            }
        };
        const u = fn.dispose = () => {
            r?.cancel();
            n?.cancel();
            r = n = void 0;
        };
        fn.flush = () => {
            c = n?.status === M;
            u();
            if (c) {
                callOriginalCallback();
            }
        };
        return fn;
    };
    return (e, s) => {
        defineHiddenProp(e.prototype, "limit", function(e) {
            if (t.has(this)) {
                throw createMappedError(9996);
            }
            t.add(this);
            const i = s(this, e);
            const n = e.signals;
            const r = n.length > 0 ? this.get(Z) : null;
            const l = this[i];
            const callOriginal = (...t) => l.call(this, ...t);
            const h = e.type === "debounce" ? debounced(e, callOriginal, this) : throttled(e, callOriginal, this);
            const a = r ? {
                handleChange: h.flush
            } : null;
            this[i] = h;
            if (r) {
                n.forEach(t => addSignalListener(r, t, a));
            }
            return {
                dispose: () => {
                    if (r) {
                        n.forEach(t => removeSignalListener(r, t, a));
                    }
                    t.delete(this);
                    h.dispose();
                    delete this[i];
                }
            };
        });
    };
})();

const wt = ((t = new WeakSet) => e => function() {
    if (!t.has(this)) {
        t.add(this);
        e.call(this);
    }
})();

class AttributeBinding {
    constructor(t, e, s, i, n, r, l, h, a) {
        this.targetAttribute = r;
        this.targetProperty = l;
        this.mode = h;
        this.strict = a;
        this.isBound = false;
        this.s = void 0;
        this.L = false;
        this.v = void 0;
        this.boundFn = false;
        this.M = false;
        this.l = e;
        this.ast = i;
        this.q = t;
        this.target = n;
        this.oL = s;
        if ((this.M = l.indexOf(" ") > -1) && !AttributeBinding.P.has(l)) {
            AttributeBinding.P.set(l, l.split(" "));
        }
    }
    updateTarget(e) {
        const s = this.target;
        const i = this.targetAttribute;
        const n = this.targetProperty;
        switch (i) {
          case "class":
            if (this.M) {
                const t = !!e;
                for (const e of AttributeBinding.P.get(n)) {
                    s.classList.toggle(e, t);
                }
            } else {
                s.classList.toggle(n, !!e);
            }
            break;

          case "style":
            {
                let i = "";
                let r = v(e);
                if (t.isString(r) && r.includes("!important")) {
                    i = "important";
                    r = r.replace("!important", "");
                }
                s.style.setProperty(n, r, i);
                break;
            }

          default:
            {
                if (e == null) {
                    s.removeAttribute(i);
                } else {
                    s.setAttribute(i, v(e));
                }
            }
        }
    }
    handleChange() {
        if (!this.isBound) return;
        if (this.L) return;
        this.L = true;
        i.queueTask(() => {
            this.L = false;
            if (!this.isBound) return;
            this.obs.version++;
            const t = i.astEvaluate(this.ast, this.s, this, (this.mode & a) > 0 ? this : null);
            this.obs.clear();
            if (t !== this.v) {
                this.v = t;
                this.updateTarget(t);
            }
        });
    }
    handleCollectionChange() {
        this.handleChange();
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        i.astBind(this.ast, t, this);
        if (this.mode & (a | h)) {
            this.updateTarget(this.v = i.astEvaluate(this.ast, t, this, (this.mode & a) > 0 ? this : null));
        }
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        i.astUnbind(this.ast, this.s, this);
        this.s = void 0;
        this.v = void 0;
        this.obs.clearAll();
    }
}

AttributeBinding.mix = wt(() => {
    gt(AttributeBinding);
    yt(AttributeBinding, () => "updateTarget");
    i.connectable(AttributeBinding, null);
    xt(AttributeBinding);
});

AttributeBinding.P = new Map;

class InterpolationBinding {
    constructor(t, e, s, i, n, r, l, h) {
        this.ast = i;
        this.target = n;
        this.targetProperty = r;
        this.mode = l;
        this.strict = h;
        this.isBound = false;
        this.s = void 0;
        this.L = false;
        this.q = t;
        this.oL = s;
        this._ = s.getAccessor(n, r);
        const a = i.expressions;
        const c = this.partBindings = Array(a.length);
        const u = a.length;
        let f = 0;
        for (;u > f; ++f) {
            c[f] = new InterpolationPartBinding(a[f], n, r, e, s, h, this);
        }
    }
    I() {
        if (!this.isBound || this.q.state > Te) return;
        const t = this.q.state !== Re && (this._.type & _) > 0;
        if (t) {
            if (this.L) return;
            this.L = true;
            i.queueTask(() => {
                this.L = false;
                if (!this.isBound || this.q.state > Te) return;
                this.updateTarget();
            });
        } else {
            this.updateTarget();
        }
    }
    updateTarget() {
        const t = this.partBindings;
        const e = this.ast;
        const s = this.target;
        const i = this.targetProperty;
        const n = e.parts;
        const r = t.length;
        let l = "";
        let h = 0;
        if (r === 1) {
            l = n[0] + t[0].V() + n[1];
        } else {
            l = n[0];
            for (;r > h; ++h) {
                l += t[h].V() + n[h + 1];
            }
        }
        this._.setValue(l, s, i);
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        const e = this.partBindings;
        const s = e.length;
        let i = 0;
        for (;s > i; ++i) {
            e[i].bind(t);
        }
        this.updateTarget();
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.s = void 0;
        const t = this.partBindings;
        const e = t.length;
        let s = 0;
        for (;e > s; ++s) {
            t[s].unbind();
        }
    }
    useAccessor(t) {
        this._ = t;
    }
}

class InterpolationPartBinding {
    constructor(t, e, s, i, n, r, l) {
        this.ast = t;
        this.target = e;
        this.targetProperty = s;
        this.strict = r;
        this.owner = l;
        this.mode = a;
        this.isBound = false;
        this.v = "";
        this.boundFn = false;
        this.D = false;
        this.l = i;
        this.oL = n;
    }
    updateTarget() {
        this.owner.I();
    }
    handleChange() {
        if (!this.isBound) return;
        this.D = true;
        this.updateTarget();
    }
    handleCollectionChange() {
        if (!this.isBound) return;
        this.D = true;
        this.updateTarget();
    }
    V() {
        if (!this.D) return this.v;
        this.obs.version++;
        const e = i.astEvaluate(this.ast, this.s, this, (this.mode & a) > 0 ? this : null);
        this.obs.clear();
        this.v = e;
        if (t.isArray(e)) {
            this.observeCollection(e);
        }
        this.D = false;
        return this.v;
    }
    bind(e) {
        if (this.isBound) {
            if (this.s === e) return;
            this.unbind();
        }
        this.s = e;
        i.astBind(this.ast, e, this);
        this.v = i.astEvaluate(this.ast, this.s, this, (this.mode & a) > 0 ? this : null);
        if (t.isArray(this.v)) {
            this.observeCollection(this.v);
        }
        this.D = false;
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.v = void 0;
        this.D = false;
        i.astUnbind(this.ast, this.s, this);
        this.s = void 0;
        this.obs.clearAll();
    }
}

InterpolationPartBinding.mix = wt(() => {
    gt(InterpolationPartBinding);
    yt(InterpolationPartBinding, () => "updateTarget");
    i.connectable(InterpolationPartBinding, null);
    xt(InterpolationPartBinding);
});

class ContentBinding {
    constructor(t, e, s, i, n, r, l) {
        this.p = i;
        this.ast = n;
        this.target = r;
        this.strict = l;
        this.isBound = false;
        this.mode = a;
        this.L = false;
        this.v = "";
        this.F = false;
        this.boundFn = false;
        this.l = e;
        this.q = t;
        this.oL = s;
    }
    updateTarget(t) {
        const e = this.target;
        const s = this.v;
        this.v = t;
        if (this.F) {
            s.parentNode?.removeChild(s);
            this.F = false;
        }
        if (t instanceof this.p.Node) {
            e.parentNode?.insertBefore(t, e);
            t = "";
            this.F = true;
        }
        e.textContent = v(t ?? "");
    }
    handleChange() {
        if (!this.isBound || this.q.state > Te) return;
        if (this.L) return;
        this.L = true;
        i.queueTask(() => {
            this.L = false;
            if (!this.isBound || this.q.state > Te) return;
            this.obs.version++;
            const e = i.astEvaluate(this.ast, this.s, this, (this.mode & a) > 0 ? this : null);
            this.obs.clear();
            if (e !== this.v) {
                if (t.isArray(e)) {
                    this.observeCollection(e);
                }
                this.updateTarget(e);
            }
        });
    }
    handleCollectionChange() {
        if (!this.isBound || this.q.state > Te) return;
        if (this.L) return;
        this.L = true;
        i.queueTask(() => {
            this.L = false;
            if (!this.isBound || this.q.state > Te) return;
            this.obs.version++;
            const e = this.v = i.astEvaluate(this.ast, this.s, this, (this.mode & a) > 0 ? this : null);
            this.obs.clear();
            if (t.isArray(e)) {
                this.observeCollection(e);
            }
            this.updateTarget(e);
        });
    }
    bind(e) {
        if (this.isBound) {
            if (this.s === e) return;
            this.unbind();
        }
        this.s = e;
        i.astBind(this.ast, e, this);
        const s = this.v = i.astEvaluate(this.ast, this.s, this, (this.mode & a) > 0 ? this : null);
        if (t.isArray(s)) {
            this.observeCollection(s);
        }
        this.updateTarget(s);
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        i.astUnbind(this.ast, this.s, this);
        if (this.F) {
            this.v.parentNode?.removeChild(this.v);
        }
        this.s = void 0;
        this.obs.clearAll();
    }
}

ContentBinding.mix = wt(() => {
    gt(ContentBinding);
    yt(ContentBinding, () => "updateTarget");
    i.connectable(ContentBinding, null);
    xt(ContentBinding);
});

class LetBinding {
    constructor(t, e, s, i, n, r, l) {
        this.ast = i;
        this.targetProperty = n;
        this.isBound = false;
        this.s = void 0;
        this.target = null;
        this.boundFn = false;
        this.l = e;
        this.oL = s;
        this.strict = l;
        this.H = r;
        this.q = t;
    }
    updateTarget() {
        this.target[this.targetProperty] = this.v;
    }
    handleChange() {
        if (!this.isBound || this.q.state > Te) return;
        this.obs.version++;
        this.v = i.astEvaluate(this.ast, this.s, this, this);
        this.obs.clear();
        this.updateTarget();
    }
    handleCollectionChange() {
        this.handleChange();
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        this.target = this.H ? t.bindingContext : t.overrideContext;
        i.astBind(this.ast, t, this);
        this.v = i.astEvaluate(this.ast, this.s, this, this);
        this.updateTarget();
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        i.astUnbind(this.ast, this.s, this);
        this.s = void 0;
        this.obs.clearAll();
    }
}

LetBinding.mix = wt(() => {
    gt(LetBinding);
    yt(LetBinding, () => "updateTarget");
    i.connectable(LetBinding, null);
    xt(LetBinding);
});

class PropertyBinding {
    constructor(t, e, s, i, n, r, l, h) {
        this.ast = i;
        this.target = n;
        this.targetProperty = r;
        this.mode = l;
        this.strict = h;
        this.isBound = false;
        this.s = void 0;
        this._ = void 0;
        this.L = false;
        this.O = null;
        this.boundFn = false;
        this.l = e;
        this.q = t;
        this.oL = s;
    }
    updateTarget(t) {
        this._.setValue(t, this.target, this.targetProperty);
    }
    updateSource(t) {
        i.astAssign(this.ast, this.s, this, null, t);
    }
    handleChange() {
        if (!this.isBound || this.q.state > Te) return;
        const t = this.q.state !== Re && (this._.type & _) > 0;
        if (t) {
            if (this.L) return;
            this.L = true;
            i.queueTask(() => {
                this.L = false;
                if (!this.isBound) return;
                this.$();
            });
        } else {
            this.$();
        }
    }
    $() {
        this.obs.version++;
        const t = i.astEvaluate(this.ast, this.s, this, (this.mode & a) > 0 ? this : null);
        this.obs.clear();
        this.updateTarget(t);
    }
    handleCollectionChange() {
        this.handleChange();
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        i.astBind(this.ast, t, this);
        const e = this.oL;
        const s = this.mode;
        let n = this._;
        if (!n) {
            if (s & c) {
                n = e.getObserver(this.target, this.targetProperty);
            } else {
                n = e.getAccessor(this.target, this.targetProperty);
            }
            this._ = n;
        }
        const r = (s & a) > 0;
        if (s & (a | h)) {
            this.updateTarget(i.astEvaluate(this.ast, this.s, this, r ? this : null));
        }
        if (s & c) {
            n.subscribe(this.O ??= new BindingTargetSubscriber(this, this.l.get(vt)));
            if (!r) {
                this.updateSource(n.getValue(this.target, this.targetProperty));
            }
        }
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        if (this.O) {
            this._.unsubscribe(this.O);
            this.O = null;
        }
        i.astUnbind(this.ast, this.s, this);
        this.s = void 0;
        this.obs.clearAll();
    }
    useTargetObserver(t) {
        this._?.unsubscribe(this);
        (this._ = t).subscribe(this);
    }
    useTargetSubscriber(t) {
        if (this.O != null) {
            throw createMappedError(9995);
        }
        this.O = t;
    }
}

PropertyBinding.mix = wt(() => {
    gt(PropertyBinding);
    yt(PropertyBinding, t => t.mode & c ? "updateSource" : "updateTarget");
    i.connectable(PropertyBinding, null);
    xt(PropertyBinding);
});

class RefBinding {
    constructor(t, e, s, i, n) {
        this.oL = e;
        this.ast = s;
        this.target = i;
        this.strict = n;
        this.isBound = false;
        this.s = void 0;
        this.l = t;
    }
    updateSource() {
        if (this.isBound) {
            this.obs.version++;
            i.astAssign(this.ast, this.s, this, this, this.target);
            this.obs.clear();
        } else {
            i.astAssign(this.ast, this.s, this, null, null);
        }
    }
    handleChange() {
        if (this.isBound) {
            this.updateSource();
        }
    }
    handleCollectionChange() {
        if (this.isBound) {
            this.updateSource();
        }
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        i.astBind(this.ast, t, this);
        this.isBound = true;
        this.updateSource();
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.obs.clearAll();
        if (i.astEvaluate(this.ast, this.s, this, null) === this.target) {
            this.updateSource();
        }
        i.astUnbind(this.ast, this.s, this);
        this.s = void 0;
    }
}

RefBinding.mix = wt(() => {
    i.connectable(RefBinding, null);
    yt(RefBinding, () => "updateSource");
    gt(RefBinding);
    xt(RefBinding);
});

class ListenerBindingOptions {
    constructor(t, e = false, s) {
        this.prevent = t;
        this.capture = e;
        this.onError = s;
    }
}

class ListenerBinding {
    constructor(t, e, s, i, n, r, l) {
        this.ast = e;
        this.target = s;
        this.targetEvent = i;
        this.strict = l;
        this.isBound = false;
        this.self = false;
        this.boundFn = true;
        this.N = null;
        this.l = t;
        this.W = n;
        this.N = r;
    }
    callSource(e) {
        const s = this.s.overrideContext;
        s.$event = e;
        let n = i.astEvaluate(this.ast, this.s, this, null);
        delete s.$event;
        if (t.isFunction(n)) {
            n = n(e);
        }
        if (n !== true && this.W.prevent) {
            e.preventDefault();
        }
    }
    handleEvent(t) {
        if (this.self) {
            if (this.target !== t.composedPath()[0]) {
                return;
            }
        }
        if (this.N?.(t) !== false) {
            try {
                this.callSource(t);
            } catch (e) {
                this.W.onError(t, e);
            }
        }
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        i.astBind(this.ast, t, this);
        this.target.addEventListener(this.targetEvent, this, this.W);
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        i.astUnbind(this.ast, this.s, this);
        this.s = void 0;
        this.target.removeEventListener(this.targetEvent, this, this.W);
    }
}

ListenerBinding.mix = wt(function() {
    gt(ListenerBinding);
    yt(ListenerBinding, () => "callSource");
    xt(ListenerBinding);
});

const bt = /*@__PURE__*/ H("IEventModifier");

const kt = /*@__PURE__*/ H("IKeyMapping", t => t.instance({
    meta: b([ "ctrl", "alt", "shift", "meta" ]),
    keys: {
        escape: "Escape",
        enter: "Enter",
        space: "Space",
        tab: "tab",
        ...Array.from({
            length: 25
        }).reduce((t, e, s) => {
            let i = String.fromCharCode(s + 65);
            t[s + 65] = i;
            i = String.fromCharCode(s + 97);
            t[s + 97] = t[i] = i;
            return t;
        }, {})
    }
}));

class ModifiedMouseEventHandler {
    constructor() {
        this.type = [ "click", "mousedown", "mousemove", "mouseup", "dblclick", "contextmenu" ];
        this.j = t.resolve(kt);
        this.U = [ "left", "middle", "right" ];
    }
    static register(t) {
        t.register(O(bt, ModifiedMouseEventHandler));
    }
    getHandler(t) {
        const e = t.split(/[:+.]/);
        return t => {
            let s = false;
            let i = false;
            let n;
            for (n of e) {
                switch (n) {
                  case "prevent":
                    s = true;
                    continue;

                  case "stop":
                    i = true;
                    continue;

                  case "left":
                  case "middle":
                  case "right":
                    if (t.button !== this.U.indexOf(n)) return false;
                    continue;
                }
                if (this.j.meta.includes(n) && t[`${n}Key`] !== true) {
                    return false;
                }
            }
            if (s) t.preventDefault();
            if (i) t.stopPropagation();
            return true;
        };
    }
}

class ModifiedKeyboardEventHandler {
    constructor() {
        this.j = t.resolve(kt);
        this.type = [ "keydown", "keyup" ];
    }
    static register(t) {
        t.register(O(bt, ModifiedKeyboardEventHandler));
    }
    getHandler(t) {
        const e = t.split(/[:+.]/);
        return t => {
            let s = false;
            let i = false;
            let n;
            for (n of e) {
                switch (n) {
                  case "prevent":
                    s = true;
                    continue;

                  case "stop":
                    i = true;
                    continue;
                }
                if (this.j.meta.includes(n)) {
                    if (t[`${n}Key`] !== true) {
                        return false;
                    }
                    continue;
                }
                const e = this.j.keys[n];
                if (e !== t.key) {
                    return false;
                }
            }
            if (s) t.preventDefault();
            if (i) t.stopPropagation();
            return true;
        };
    }
}

class ModifiedEventHandler {
    constructor() {
        this.type = [ "$ALL" ];
    }
    static register(t) {
        t.register(O(bt, ModifiedEventHandler));
    }
    getHandler(t) {
        const e = t.split(/[:+.]/);
        return t => {
            let s = false;
            let i = false;
            let n;
            for (n of e) {
                switch (n) {
                  case "prevent":
                    s = true;
                    continue;

                  case "stop":
                    i = true;
                    continue;
                }
            }
            if (s) t.preventDefault();
            if (i) t.stopPropagation();
            return true;
        };
    }
}

const Ct = /*@__PURE__*/ H("IEventModifierHandler", t => t.instance({
    getHandler: () => null
}));

class EventModifier {
    constructor() {
        this.G = t.resolve(t.all(bt)).reduce((e, s) => {
            const i = t.isArray(s.type) ? s.type : [ s.type ];
            i.forEach(t => e[t] = s);
            return e;
        }, {});
    }
    static register(t) {
        t.register(O(Ct, EventModifier));
    }
    getHandler(e, s) {
        return t.isString(s) ? (this.G[e] ?? this.G.$ALL)?.getHandler(s) ?? null : null;
    }
}

const St = {
    register(t) {
        t.register(EventModifier, ModifiedMouseEventHandler, ModifiedKeyboardEventHandler, ModifiedEventHandler);
    }
};

const Bt = /*@__PURE__*/ H("IViewFactory");

class ViewFactory {
    constructor(t, e) {
        this.isCaching = false;
        this.h = null;
        this.K = -1;
        this.name = e.name;
        this.container = t;
        this.def = e;
    }
    setCacheSize(e, s) {
        if (e) {
            if (e === "*") {
                e = ViewFactory.maxCacheSize;
            } else if (t.isString(e)) {
                e = parseInt(e, 10);
            }
            if (this.K === -1 || !s) {
                this.K = e;
            }
        }
        if (this.K > 0) {
            this.h = [];
        } else {
            this.h = null;
        }
        this.isCaching = this.K > 0;
    }
    canReturnToCache(t) {
        return this.h != null && this.h.length < this.K;
    }
    tryReturnToCache(t) {
        if (this.canReturnToCache(t)) {
            this.h.push(t);
            return true;
        }
        return false;
    }
    create(t) {
        const e = this.h;
        let s;
        if (e != null && e.length > 0) {
            s = e.pop();
            return s;
        }
        s = Controller.$view(this, t);
        return s;
    }
    createAdopted(t, e, s) {
        return Controller.$viewAdopted(this, t, e, s);
    }
}

ViewFactory.maxCacheSize = 65535;

const At = /*@__PURE__*/ (() => {
    const createComment = (t, e) => t.document.createComment(e);
    return t => {
        const e = createComment(t, "au-end");
        e.$start = createComment(t, "au-start");
        return e;
    };
})();

const insertManyBefore = (t, e, s) => {
    if (t === null) {
        return;
    }
    const i = s.length;
    let n = 0;
    while (i > n) {
        t.insertBefore(s[n], e);
        ++n;
    }
};

const createMutationObserver = (t, e) => new t.ownerDocument.defaultView.MutationObserver(e);

const isElement = t => t.nodeType === 1;

const Rt = "default";

const Tt = "au-slot";

const Et = /*@__PURE__*/ H("IAuSlotsInfo");

class AuSlotsInfo {
    constructor(t) {
        this.projectedSlots = t;
    }
}

const Lt = /*@__PURE__*/ H("IAuSlotWatcher");

class AuSlotWatcherBinding {
    constructor(e, s, i, n) {
        this.X = new Set;
        this.Y = t.emptyArray;
        this.isBound = false;
        this.cb = (this.o = e)[s];
        this.slotName = i;
        this.Z = n;
    }
    bind() {
        this.isBound = true;
    }
    unbind() {
        this.isBound = false;
    }
    getValue() {
        return this.Y;
    }
    watch(t) {
        if (!this.X.has(t)) {
            this.X.add(t);
            t.subscribe(this);
        }
    }
    unwatch(t) {
        if (this.X.delete(t)) {
            t.unsubscribe(this);
        }
    }
    handleSlotChange(t, e) {
        if (!this.isBound) {
            return;
        }
        const s = this.Y;
        const i = [];
        const n = this.Z;
        let r;
        let l;
        for (r of this.X) {
            for (l of r === t ? e : r.nodes) {
                if (n === "$all" || isElement(l) && (n === "*" || l.matches(n))) {
                    i[i.length] = l;
                }
            }
        }
        if (i.length !== s.length || i.some((t, e) => t !== s[e])) {
            this.Y = i;
            this.cb?.call(this.o, i);
            this.subs.notify(i, s);
        }
    }
    get() {
        throw createMappedError(99, "get");
    }
}

class SlottedLifecycleHooks {
    constructor(t) {
        this.J = t;
    }
    register(t) {
        N(ct, this).register(t);
    }
    hydrating(t, e) {
        const s = this.J;
        const i = new AuSlotWatcherBinding(t, s.callback ?? `${v(s.name)}Changed`, s.slotName ?? "default", s.query ?? "*");
        A(t, s.name, {
            enumerable: true,
            configurable: true,
            get: k(() => i.getValue(), {
                getObserver: () => i
            }),
            set: () => {}
        });
        N(Lt, i).register(e.container);
        e.addBinding(i);
    }
}

function slotted(t, e) {
    if (!Mt) {
        Mt = true;
        i.subscriberCollection(AuSlotWatcherBinding, null);
        lifecycleHooks()(SlottedLifecycleHooks, null);
    }
    const s = g("dependencies");
    function decorator(i, n) {
        if (n.kind !== "field") throw createMappedError(9990);
        const r = typeof t === "object" ? t : {
            query: t,
            slotName: e,
            name: ""
        };
        r.name = n.name;
        const l = n.metadata[s] ??= [];
        l.push(new SlottedLifecycleHooks(r));
    }
    return decorator;
}

let Mt = false;

class SpreadBinding {
    static create(s, i, n, r, l, h, a, c) {
        const u = [];
        const f = r.renderers;
        const getHydrationContext = t => {
            let e = t;
            let i = s;
            while (i != null && e > 0) {
                i = i.parent;
                --e;
            }
            if (i == null) {
                throw createMappedError(9999);
            }
            return i;
        };
        const renderSpreadInstruction = s => {
            const r = getHydrationContext(s);
            const d = new SpreadBinding(r);
            const p = l.compileSpread(r.controller.definition, r.instruction?.captures ?? t.emptyArray, r.controller.container, i, n);
            let m;
            for (m of p) {
                switch (m.type) {
                  case e.itSpreadTransferedBinding:
                    renderSpreadInstruction(s + 1);
                    break;

                  case e.itSpreadElementProp:
                    f[m.instruction.type].render(d, findElementControllerFor(i), m.instruction, h, a, c);
                    break;

                  default:
                    f[m.type].render(d, i, m, h, a, c);
                }
            }
            u.push(d);
        };
        renderSpreadInstruction(0);
        return u;
    }
    get container() {
        return this.locator;
    }
    get definition() {
        return this.$controller.definition;
    }
    get state() {
        return this.$controller.state;
    }
    constructor(t) {
        this.isBound = false;
        this.tt = [];
        this.locator = (this.$controller = (this.et = t).controller).container;
    }
    get(t) {
        return this.locator.get(t);
    }
    bind(t) {
        if (this.isBound) return;
        this.isBound = true;
        const e = this.scope = this.et.controller.scope.parent ?? void 0;
        if (e == null) {
            throw createMappedError(9999);
        }
        this.tt.forEach(t => t.bind(e));
    }
    unbind() {
        this.tt.forEach(t => t.unbind());
        this.isBound = false;
    }
    addBinding(t) {
        this.tt.push(t);
    }
    addChild(t) {
        if (t.vmKind !== Se) {
            throw createMappedError(9998);
        }
        this.$controller.addChild(t);
    }
}

class SpreadValueBinding {
    constructor(t, e, s, i, n, r, l) {
        this.target = e;
        this.targetKeys = s;
        this.ast = i;
        this.strict = l;
        this.isBound = false;
        this.s = void 0;
        this.boundFn = false;
        this.st = {};
        this.it = new WeakMap;
        this.q = t;
        this.oL = n;
        this.l = r;
    }
    updateTarget() {
        this.obs.version++;
        const t = i.astEvaluate(this.ast, this.s, this, this);
        this.obs.clear();
        this.nt(t, true);
    }
    handleChange() {
        if (!this.isBound) return;
        this.updateTarget();
    }
    handleCollectionChange() {
        if (!this.isBound) return;
        this.updateTarget();
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.isBound = true;
        this.s = t;
        i.astBind(this.ast, t, this);
        const e = i.astEvaluate(this.ast, t, this, this);
        this.nt(e, false);
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        i.astUnbind(this.ast, this.s, this);
        this.s = void 0;
        let t;
        for (t in this.st) {
            this.st[t].unbind();
        }
    }
    nt(s, n) {
        let l;
        if (!t.isObject(s)) {
            for (l in this.st) {
                this.st[l]?.unbind();
            }
            return;
        }
        let h;
        let a = this.it.get(s);
        if (a == null) {
            this.it.set(s, a = i.Scope.fromParent(this.s, s));
        }
        for (l of this.targetKeys) {
            h = this.st[l];
            if (l in s) {
                if (h == null) {
                    h = this.st[l] = new PropertyBinding(this.q, this.l, this.oL, SpreadValueBinding.ot[l] ??= r.createAccessScopeExpression(l, 0), this.target, l, e.BindingMode.toView, this.strict);
                }
                h.bind(a);
            } else if (n) {
                h?.unbind();
            }
        }
    }
}

SpreadValueBinding.mix = wt(() => {
    gt(SpreadValueBinding);
    yt(SpreadValueBinding, () => "updateTarget");
    i.connectable(SpreadValueBinding, null);
    xt(SpreadValueBinding);
});

SpreadValueBinding.ot = {};

const addListener = (t, e, s, i) => {
    t.addEventListener(e, s, i);
};

const removeListener = (t, e, s, i) => {
    t.removeEventListener(e, s, i);
};

const mixinNodeObserverUseConfig = t => {
    let e;
    const s = t.prototype;
    defineHiddenProp(s, "subscribe", function(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            for (e of this.cf.events) {
                addListener(this.rt, e, this);
            }
            this.lt = true;
            this.ht?.();
        }
    });
    defineHiddenProp(s, "unsubscribe", function(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            for (e of this.cf.events) {
                removeListener(this.rt, e, this);
            }
            this.lt = false;
            this.ct?.();
        }
    });
    defineHiddenProp(s, "useConfig", function(t) {
        this.cf = t;
        if (this.lt) {
            for (e of this.cf.events) {
                removeListener(this.rt, e, this);
            }
            for (e of this.cf.events) {
                addListener(this.rt, e, this);
            }
        }
    });
};

const mixinNoopSubscribable = e => {
    defineHiddenProp(e.prototype, "subscribe", t.noop);
    defineHiddenProp(e.prototype, "unsubscribe", t.noop);
};

class ClassAttributeAccessor {
    get doNotCache() {
        return true;
    }
    constructor(t, e = {}) {
        this.obj = t;
        this.mapping = e;
        this.type = P | _;
        this.v = "";
        this.ut = {};
        this.ft = 0;
    }
    getValue() {
        return this.v;
    }
    setValue(t) {
        if (t !== this.v) {
            this.v = t;
            this.dt();
        }
    }
    dt() {
        const t = this.ut;
        const e = ++this.ft;
        const s = this.obj.classList;
        const i = getClassesToAdd(this.v);
        const n = i.length;
        let r = 0;
        let l;
        if (n > 0) {
            for (;r < n; r++) {
                l = i[r];
                l = this.mapping[l] || l;
                if (l.length === 0) {
                    continue;
                }
                t[l] = this.ft;
                s.add(l);
            }
        }
        if (e === 1) {
            return;
        }
        for (l in t) {
            l = this.mapping[l] || l;
            if (t[l] === e) {
                continue;
            }
            s.remove(l);
        }
    }
}

(() => {
    mixinNoopSubscribable(ClassAttributeAccessor);
})();

function getClassesToAdd(e) {
    if (t.isString(e)) {
        return splitClassString(e);
    }
    if (typeof e !== "object") {
        return t.emptyArray;
    }
    if (t.isArray(e)) {
        const s = e.length;
        if (s > 0) {
            const t = [];
            let i = 0;
            for (;s > i; ++i) {
                t.push(...getClassesToAdd(e[i]));
            }
            return t;
        } else {
            return t.emptyArray;
        }
    }
    const s = [];
    let i;
    for (i in e) {
        if (Boolean(e[i])) {
            if (i.includes(" ")) {
                s.push(...splitClassString(i));
            } else {
                s.push(i);
            }
        }
    }
    return s;
}

function splitClassString(e) {
    const s = e.match(/\S+/g);
    if (s === null) {
        return t.emptyArray;
    }
    return s;
}

const fromHydrationContext = e => ({
    $isResolver: true,
    resolve(s, i) {
        return i.get(_e).controller.container.get(t.own(e));
    }
});

const Dt = /*@__PURE__*/ H("IRenderer");

function renderer(e, s) {
    const i = s?.metadata ?? (e[Symbol.metadata] ??= Object.create(null));
    i[t.registrableMetadataKey] = {
        register(t) {
            O(Dt, e).register(t);
        }
    };
    return e;
}

function ensureExpression(e, s, i) {
    if (t.isString(s)) {
        return e.parse(s, i);
    }
    return s;
}

function getTarget(t) {
    if (t.viewModel != null) {
        return t.viewModel;
    }
    return t;
}

function getRefTarget(t, e) {
    if (e === "element") {
        return t;
    }
    switch (e) {
      case "controller":
        return findElementControllerFor(t);

      case "view":
        throw createMappedError(750);

      case "component":
        return findElementControllerFor(t).viewModel;

      default:
        {
            const s = findAttributeControllerFor(t, e);
            if (s !== void 0) {
                return s.viewModel;
            }
            const i = findElementControllerFor(t, {
                name: e
            });
            if (i === void 0) {
                throw createMappedError(751, e);
            }
            return i.viewModel;
        }
    }
}

const qt = /*@__PURE__*/ renderer(class SetPropertyRenderer {
    constructor() {
        this.target = e.itSetProperty;
    }
    render(t, e, s) {
        const i = getTarget(e);
        if (i.$observers?.[s.to] !== void 0) {
            i.$observers[s.to].setValue(s.value);
        } else {
            i[s.to] = s.value;
        }
    }
}, null);

const Pt = /*@__PURE__*/ renderer(class CustomElementRenderer {
    constructor() {
        this.r = t.resolve(oe);
        this.target = e.itHydrateElement;
    }
    render(t, e, s, i, n, r, l) {
        let h;
        let a;
        let c;
        const u = s.res;
        const f = s.projections;
        const d = t.container;
        switch (typeof u) {
          case "string":
            h = Je.find(d, u);
            if (h == null) {
                throw createMappedError(752, s, t);
            }
            break;

          default:
            h = u;
        }
        const p = s.containerless || h.containerless;
        const m = p ? convertToRenderLocation(e) : null;
        const g = createElementContainer(i, t, e, s, m, f == null ? void 0 : new AuSlotsInfo(S(f)));
        a = g.invoke(h.Type);
        c = Controller.$el(g, a, e, s, h, m, l);
        const x = this.r.renderers;
        const v = s.props;
        const y = v.length;
        let w = 0;
        let b;
        while (y > w) {
            b = v[w];
            x[b.type].render(t, c, b, i, n, r);
            ++w;
        }
        t.addChild(c);
    }
}, null);

const _t = /*@__PURE__*/ renderer(class CustomAttributeRenderer {
    constructor() {
        this.r = t.resolve(oe);
        this.target = e.itHydrateAttribute;
    }
    render(t, e, s, i, n, r) {
        let l = t.container;
        let h;
        switch (typeof s.res) {
          case "string":
            h = at.find(l, s.res);
            if (h == null) {
                throw createMappedError(753, s, t);
            }
            break;

          default:
            h = s.res;
        }
        const a = invokeAttribute(i, h, t, e, s, void 0, void 0);
        const c = Controller.$attr(a.ctn, a.vm, e, h);
        nt.set(e, h.key, c);
        const u = this.r.renderers;
        const f = s.props;
        const d = f.length;
        let p = 0;
        let m;
        while (d > p) {
            m = f[p];
            u[m.type].render(t, c, m, i, n, r);
            ++p;
        }
        t.addChild(c);
    }
}, null);

const It = /*@__PURE__*/ renderer(class TemplateControllerRenderer {
    constructor() {
        this.r = t.resolve(oe);
        this.target = e.itHydrateTemplateController;
    }
    render(t, e, s, i, n, r, l) {
        let h = t.container;
        let a;
        switch (typeof s.res) {
          case "string":
            a = at.find(h, s.res);
            if (a == null) {
                throw createMappedError(754, s, t);
            }
            break;

          default:
            a = s.res;
        }
        const c = this.r.getViewFactory(s.def, a.containerStrategy === "new" ? h.createChild({
            inheritParentResources: true
        }) : h);
        const u = convertToRenderLocation(e);
        const f = invokeAttribute(i, a, t, e, s, c, u);
        const d = Controller.$attr(f.ctn, f.vm, e, a, l);
        nt.set(u, a.key, d);
        f.vm.link?.(t, d, e, s);
        const p = this.r.renderers;
        const m = s.props;
        const g = m.length;
        let x = 0;
        let v;
        while (g > x) {
            v = m[x];
            p[v.type].render(t, d, v, i, n, r);
            ++x;
        }
        t.addChild(d);
    }
}, null);

const Vt = /*@__PURE__*/ renderer(class LetElementRenderer {
    constructor() {
        this.target = e.itHydrateLetElement;
        LetBinding.mix();
    }
    render(t, e, s, i, n, r) {
        e.remove();
        const l = s.instructions;
        const h = s.toBindingContext;
        const a = t.container;
        const c = l.length;
        let u;
        let f;
        let d = 0;
        while (c > d) {
            u = l[d];
            f = ensureExpression(n, u.from, L);
            t.addBinding(new LetBinding(t, a, r, f, u.to, h, t.strict ?? false));
            ++d;
        }
    }
}, null);

const Ft = /*@__PURE__*/ renderer(class RefBindingRenderer {
    constructor() {
        this.target = e.itRefBinding;
        RefBinding.mix();
    }
    render(t, e, s, i, n, r) {
        t.addBinding(new RefBinding(t.container, r, ensureExpression(n, s.from, L), getRefTarget(e, s.to), t.strict ?? false));
    }
}, null);

const Ht = /*@__PURE__*/ renderer(class InterpolationBindingRenderer {
    constructor() {
        this.target = e.itInterpolation;
        InterpolationPartBinding.mix();
    }
    render(t, e, s, i, n, r) {
        const l = t.container;
        const h = new InterpolationBinding(t, l, r, ensureExpression(n, s.from, R), getTarget(e), s.to, a, t.strict ?? false);
        if (s.to === "class" && h.target.nodeType > 0) {
            const t = l.get(fromHydrationContext(Ne));
            h.useAccessor(new ClassAttributeAccessor(h.target, t));
        }
        t.addBinding(h);
    }
}, null);

const Ot = /*@__PURE__*/ renderer(class PropertyBindingRenderer {
    constructor() {
        this.target = e.itPropertyBinding;
        PropertyBinding.mix();
    }
    render(t, e, s, i, n, r) {
        const l = t.container;
        const h = new PropertyBinding(t, l, r, ensureExpression(n, s.from, L), getTarget(e), s.to, s.mode, t.strict ?? false);
        if (s.to === "class" && h.target.nodeType > 0) {
            const t = l.get(fromHydrationContext(Ne));
            h.useTargetObserver(new ClassAttributeAccessor(h.target, t));
        }
        t.addBinding(h);
    }
}, null);

const $t = /*@__PURE__*/ renderer(class IteratorBindingRenderer {
    constructor() {
        this.target = e.itIteratorBinding;
        PropertyBinding.mix();
    }
    render(t, e, s, i, n, r) {
        t.addBinding(new PropertyBinding(t, t.container, r, ensureExpression(n, s.forOf, T), getTarget(e), s.to, a, t.strict ?? false));
    }
}, null);

const Nt = /*@__PURE__*/ renderer(class TextBindingRenderer {
    constructor() {
        this.target = e.itTextBinding;
        ContentBinding.mix();
    }
    render(t, e, s, i, n, r) {
        t.addBinding(new ContentBinding(t, t.container, r, i, ensureExpression(n, s.from, L), e, t.strict ?? false));
    }
}, null);

const Wt = H("IListenerBindingOptions", e => e.singleton(class {
    constructor() {
        this.p = t.resolve(it);
        this.prevent = false;
        this.onError = (t, e) => {
            const s = new this.p.CustomEvent("au-event-error", {
                cancelable: true,
                detail: {
                    event: t,
                    error: e
                }
            });
            this.p.window.dispatchEvent(s);
            if (s.defaultPrevented) {
                return;
            }
            throw e;
        };
    }
}));

const jt = /*@__PURE__*/ renderer(class ListenerBindingRenderer {
    constructor() {
        this.target = e.itListenerBinding;
        this.gt = t.resolve(Ct);
        this.xt = t.resolve(Wt);
        ListenerBinding.mix();
    }
    render(t, e, s, i, n) {
        t.addBinding(new ListenerBinding(t.container, ensureExpression(n, s.from, E), e, s.to, new ListenerBindingOptions(this.xt.prevent, s.capture, this.xt.onError), this.gt.getHandler(s.to, s.modifier), t.strict ?? false));
    }
}, null);

const zt = /*@__PURE__*/ renderer(class SetAttributeRenderer {
    constructor() {
        this.target = e.itSetAttribute;
    }
    render(t, e, s) {
        e.setAttribute(s.to, s.value);
    }
}, null);

const Ut = /*@__PURE__*/ renderer(class SetClassAttributeRenderer {
    constructor() {
        this.target = e.itSetClassAttribute;
    }
    render(t, e, s) {
        addClasses(e.classList, s.value);
    }
}, null);

const Gt = /*@__PURE__*/ renderer(class SetStyleAttributeRenderer {
    constructor() {
        this.target = e.itSetStyleAttribute;
    }
    render(t, e, s) {
        e.style.cssText += s.value;
    }
}, null);

const Kt = /*@__PURE__*/ renderer(class StylePropertyBindingRenderer {
    constructor() {
        this.target = e.itStylePropertyBinding;
        PropertyBinding.mix();
    }
    render(t, e, s, i, n, r) {
        t.addBinding(new PropertyBinding(t, t.container, r, ensureExpression(n, s.from, L), e.style, s.to, a, t.strict ?? false));
    }
}, null);

const Xt = /*@__PURE__*/ renderer(class AttributeBindingRenderer {
    constructor() {
        this.target = e.itAttributeBinding;
        AttributeBinding.mix();
    }
    render(t, e, s, i, n, r) {
        const l = t.container;
        const h = l.has(Ne, false) ? l.get(Ne) : null;
        t.addBinding(new AttributeBinding(t, l, r, ensureExpression(n, s.from, L), e, s.attr, h == null ? s.to : s.to.split(/\s/g).map(t => h[t] ?? t).join(" "), a, t.strict ?? false));
    }
}, null);

const Qt = /*@__PURE__*/ renderer(class SpreadRenderer {
    constructor() {
        this.vt = t.resolve(e.ITemplateCompiler);
        this.r = t.resolve(oe);
        this.target = e.itSpreadTransferedBinding;
    }
    render(t, e, s, i, n, r) {
        SpreadBinding.create(t.container.get(_e), e, void 0, this.r, this.vt, i, n, r).forEach(e => t.addBinding(e));
    }
}, null);

const Yt = /*@__PURE__*/ renderer(class SpreadValueRenderer {
    constructor() {
        this.target = e.itSpreadValueBinding;
        SpreadValueBinding.mix();
    }
    render(t, e, s, i, n, r) {
        const l = s.target;
        if (l === "$bindables") {
            t.addBinding(new SpreadValueBinding(t, e.viewModel, S(e.definition.bindables), n.parse(s.from, L), r, t.container, t.strict ?? false));
        } else {
            throw createMappedError(820, l);
        }
    }
}, null);

function addClasses(t, e) {
    const s = e.length;
    let i = 0;
    for (let n = 0; n < s; ++n) {
        if (e.charCodeAt(n) === 32) {
            if (n !== i) {
                t.add(e.slice(i, n));
            }
            i = n + 1;
        } else if (n + 1 === s) {
            t.add(e.slice(i));
        }
    }
}

const Zt = "IController";

const Jt = "IInstruction";

const te = "IRenderLocation";

const ee = "ISlotsInfo";

function createElementContainer(s, i, n, r, l, h) {
    const a = i.container.createChild();
    registerHostNode(a, n, s);
    registerResolver(a, Pe, new t.InstanceProvider(Zt, i));
    registerResolver(a, e.IInstruction, new t.InstanceProvider(Jt, r));
    registerResolver(a, $e, l == null ? se : new RenderLocationProvider(l));
    registerResolver(a, Bt, ie);
    registerResolver(a, Et, h == null ? ne : new t.InstanceProvider(ee, h));
    return a;
}

class ViewFactoryProvider {
    get $isResolver() {
        return true;
    }
    constructor(t) {
        this.f = t;
    }
    resolve() {
        const e = this.f;
        if (e === null) {
            throw createMappedError(755);
        }
        if (!t.isString(e.name) || e.name.length === 0) {
            throw createMappedError(756);
        }
        return e;
    }
}

function invokeAttribute(s, i, n, r, l, h, a, c) {
    const u = n instanceof Controller ? n : n.$controller;
    const f = u.container.createChild();
    registerHostNode(f, r, s);
    registerResolver(f, Pe, new t.InstanceProvider(Zt, u));
    registerResolver(f, e.IInstruction, new t.InstanceProvider(Jt, l));
    registerResolver(f, $e, a == null ? se : new t.InstanceProvider(te, a));
    registerResolver(f, Bt, h == null ? ie : new ViewFactoryProvider(h));
    registerResolver(f, Et, ne);
    return {
        vm: f.invoke(i.Type),
        ctn: f
    };
}

class RenderLocationProvider {
    get name() {
        return "IRenderLocation";
    }
    get $isResolver() {
        return true;
    }
    constructor(t) {
        this.l = t;
    }
    resolve() {
        return this.l;
    }
}

const se = new RenderLocationProvider(null);

const ie = new ViewFactoryProvider(null);

const ne = new t.InstanceProvider(ee, new AuSlotsInfo(t.emptyArray));

const oe = /*@__PURE__*/ H("IRendering", t => t.singleton(Rendering));

class Rendering {
    get renderers() {
        return this.yt ??= this.wt.getAll(Dt, false).reduce((t, e) => {
            t[e.target] ??= e;
            return t;
        }, t.createLookup());
    }
    constructor() {
        this.bt = new WeakMap;
        this.kt = new WeakMap;
        const e = this.wt = t.resolve(t.IContainer).root;
        const s = this.p = e.get(it);
        this.ep = e.get(r.IExpressionParser);
        this.oL = e.get(i.IObserverLocator);
        this.Ct = new FragmentNodeSequence(s, s.document.createDocumentFragment());
        this.St = e.has(ft, true) ? e.get(ft).preserveMarkers : false;
    }
    compile(t, s) {
        const i = s.get(e.ITemplateCompiler);
        const n = this.bt;
        let r = n.get(t);
        if (r == null) {
            n.set(t, r = CustomElementDefinition.create(t.needsCompile ? i.compile(t, s) : t));
        }
        return r;
    }
    getViewFactory(t, e) {
        return new ViewFactory(e, CustomElementDefinition.getOrCreate(t));
    }
    createNodes(e) {
        if (e.enhance === true) {
            return new FragmentNodeSequence(this.p, e.template);
        }
        let s;
        let i = false;
        const n = this.kt;
        const r = this.p;
        const l = r.document;
        if (n.has(e)) {
            s = n.get(e);
        } else {
            const h = e.template;
            let a;
            if (h == null) {
                s = null;
            } else if (h instanceof r.Node) {
                if (h.nodeName === "TEMPLATE") {
                    s = h.content;
                    i = true;
                } else {
                    (s = l.createDocumentFragment()).appendChild(h.cloneNode(true));
                }
            } else {
                a = l.createElement("template");
                if (t.isString(h)) {
                    a.innerHTML = h;
                }
                s = a.content;
                i = true;
            }
            n.set(e, s);
        }
        if (s == null) {
            return this.Ct;
        }
        const h = i ? l.importNode(s, true) : l.adoptNode(s.cloneNode(true));
        return new FragmentNodeSequence(this.p, h, this.St);
    }
    adoptNodes(t) {
        return FragmentNodeSequence.adoptChildren(this.p, t);
    }
    render(t, s, i, n) {
        const r = i.instructions;
        const l = this.renderers;
        const h = s.length;
        const a = r.length;
        const c = t.ssrScope;
        const u = c != null;
        const f = c?.children;
        let d = 0;
        let p = 0;
        let m = 0;
        let g = a;
        let x;
        let v;
        let y;
        if (!u && h !== a) {
            throw createMappedError(757, h, a);
        }
        if (n != null) {
            x = i.surrogates;
            if ((g = x.length) > 0) {
                m = 0;
                while (g > m) {
                    v = x[m];
                    l[v.type].render(t, n, v, this.p, this.ep, this.oL);
                    ++m;
                }
            }
        }
        if (a > 0) {
            while (a > p) {
                x = r[p];
                y = s[p];
                m = 0;
                g = x.length;
                while (g > m) {
                    v = x[m];
                    const s = v.type;
                    const i = s === e.itHydrateTemplateController || s === e.itHydrateElement;
                    const n = i && f != null ? f[d++] : undefined;
                    l[s].render(t, y, v, this.p, this.ep, this.oL, n);
                    ++m;
                }
                ++p;
            }
        }
    }
}

function cssModules(...t) {
    return new CSSModulesProcessorRegistry(t);
}

class CSSModulesProcessorRegistry {
    constructor(t) {
        this.modules = t;
    }
    register(s) {
        let i = s.get(t.own(Ne));
        if (i == null) {
            s.register(N(Ne, i = t.createLookup()));
        }
        {
            k(i, ...this.modules);
        }
        class CompilingHook {
            compiling(t) {
                const processElement = t => {
                    const e = t.getAttributeNode("class");
                    if (!e?.value) return;
                    const s = e.value.split(/\s+/g).map(t => i[t] || t).join(" ");
                    e.value = s;
                };
                const processContainer = t => {
                    t.querySelectorAll("[class]").forEach(processElement);
                    t.querySelectorAll("template").forEach(t => processContainer(t.content));
                };
                processElement(t);
                processContainer(t.tagName === "TEMPLATE" ? t.content : t);
            }
        }
        s.register(e.TemplateCompilerHooks.define(CompilingHook));
    }
}

function shadowCSS(...t) {
    return new ShadowDOMRegistry(t);
}

const re = /*@__PURE__*/ H("IShadowDOMStyleFactory", t => t.cachedCallback(t => {
    if (AdoptedStyleSheetsStyles.supported(t.get(it))) {
        return t.get(AdoptedStyleSheetsStylesFactory);
    }
    return t.get(StyleElementStylesFactory);
}));

class ShadowDOMRegistry {
    constructor(t) {
        this.css = t;
    }
    register(t) {
        const e = t.get(he);
        const s = t.get(re);
        t.register(N(le, s.createStyles(this.css, e)));
    }
}

class AdoptedStyleSheetsStylesFactory {
    constructor() {
        this.p = t.resolve(it);
        this.cache = new Map;
    }
    createStyles(t, e) {
        return new AdoptedStyleSheetsStyles(this.p, t, this.cache, e);
    }
}

class StyleElementStylesFactory {
    constructor() {
        this.p = t.resolve(it);
    }
    createStyles(t, e) {
        return new StyleElementStyles(this.p, t, e);
    }
}

const le = /*@__PURE__*/ H("IShadowDOMStyles");

const he = /*@__PURE__*/ H("IShadowDOMGlobalStyles", e => e.instance({
    applyTo: t.noop
}));

class AdoptedStyleSheetsStyles {
    constructor(t, e, s, i = null) {
        this.sharedStyles = i;
        this.styleSheets = e.map(e => {
            let i;
            if (e instanceof t.CSSStyleSheet) {
                i = e;
            } else {
                i = s.get(e);
                if (i === void 0) {
                    i = new t.CSSStyleSheet;
                    i.replaceSync(e);
                    s.set(e, i);
                }
            }
            return i;
        });
    }
    static supported(t) {
        return "adoptedStyleSheets" in t.ShadowRoot.prototype;
    }
    applyTo(t) {
        if (this.sharedStyles !== null) {
            this.sharedStyles.applyTo(t);
        }
        t.adoptedStyleSheets = [ ...t.adoptedStyleSheets, ...this.styleSheets ];
    }
}

class StyleElementStyles {
    constructor(t, e, s = null) {
        this.p = t;
        this.localStyles = e;
        this.sharedStyles = s;
    }
    applyTo(t) {
        const e = this.localStyles;
        const s = this.p;
        for (let i = e.length - 1; i > -1; --i) {
            const n = s.document.createElement("style");
            n.innerHTML = e[i];
            t.prepend(n);
        }
        if (this.sharedStyles !== null) {
            this.sharedStyles.applyTo(t);
        }
    }
}

const ae = {
    shadowDOM(e) {
        return st.creating(t.IContainer, t => {
            if (e.sharedStyles != null) {
                const s = t.get(re);
                t.register(N(he, s.createStyles(e.sharedStyles, null)));
            }
        });
    }
};

const {enter: ce, exit: ue} = i.ConnectableSwitcher;

const {wrap: fe, unwrap: de} = i.ProxyObservable;

class ComputedWatcher {
    get value() {
        return this.v;
    }
    constructor(t, e, s, i, n = "async") {
        this.obj = t;
        this.$get = s;
        this.isBound = false;
        this.L = false;
        this.Bt = 0;
        this.v = void 0;
        this.cb = i;
        this.oL = e;
        this.At = n;
    }
    handleChange() {
        this.run();
    }
    handleCollectionChange() {
        this.run();
    }
    bind() {
        if (this.isBound) return;
        this.compute();
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.obs.clearAll();
    }
    run() {
        if (!this.isBound) return;
        if (this.At === "sync") {
            this.Rt();
            return;
        }
        if (this.L) return;
        this.L = true;
        i.queueTask(() => {
            this.L = false;
            this.Rt();
        });
    }
    Rt() {
        if (!this.isBound) return;
        const e = this.obj;
        const s = this.v;
        if (++this.Bt > 100) {
            throw createMappedError(655);
        }
        const i = this.compute();
        if (!t.areEqual(i, s)) {
            this.cb.call(e, i, s, e);
        }
        if (!this.L) {
            this.Bt = 0;
        }
    }
    compute() {
        this.obs.version++;
        try {
            ce(this);
            return this.v = de(this.$get.call(void 0, fe(this.obj), this));
        } finally {
            this.obs.clear();
            ue(this);
        }
    }
}

(() => {
    i.connectable(ComputedWatcher, null);
})();

class ExpressionWatcher {
    get value() {
        return this.v;
    }
    constructor(t, e, s, i, n, r = "async") {
        this.scope = t;
        this.l = e;
        this.oL = s;
        this.isBound = false;
        this.L = false;
        this.boundFn = false;
        this.obj = t.bindingContext;
        this.Tt = i;
        this.cb = n;
        this.At = r;
    }
    handleChange() {
        this.run();
    }
    handleCollectionChange() {
        this.run();
    }
    run() {
        if (!this.isBound) return;
        if (this.At === "sync") {
            this.Rt();
            return;
        }
        if (this.L) return;
        this.L = true;
        i.queueTask(() => {
            this.L = false;
            this.Rt();
        });
    }
    Rt() {
        if (!this.isBound) return;
        const e = this.Tt;
        const s = this.obj;
        const n = this.v;
        this.obs.version++;
        const r = i.astEvaluate(e, this.scope, this, this);
        this.obs.clear();
        if (!t.areEqual(r, n)) {
            this.v = r;
            this.cb.call(s, r, n, s);
        }
    }
    bind() {
        if (this.isBound) return;
        this.obs.version++;
        this.v = i.astEvaluate(this.Tt, this.scope, this, this);
        this.obs.clear();
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.obs.clearAll();
        this.v = void 0;
    }
}

(() => {
    i.connectable(ExpressionWatcher, null);
    xt(ExpressionWatcher);
})();

class Controller {
    get lifecycleHooks() {
        return this.Et;
    }
    get isActive() {
        return (this.state & (Re | Te)) > 0 && (this.state & Ee) === 0;
    }
    get name() {
        if (this.parent === null) {
            switch (this.vmKind) {
              case Se:
                return `[${this.definition.name}]`;

              case Ce:
                return this.definition.name;

              case Be:
                return this.viewFactory.name;
            }
        }
        switch (this.vmKind) {
          case Se:
            return `${this.parent.name}>[${this.definition.name}]`;

          case Ce:
            return `${this.parent.name}>${this.definition.name}`;

          case Be:
            return this.viewFactory.name === this.parent.definition?.name ? `${this.parent.name}[view]` : `${this.parent.name}[view:${this.viewFactory.name}]`;
        }
    }
    get viewModel() {
        return this.Lt;
    }
    set viewModel(t) {
        this.Lt = t;
        this.Mt = t == null || this.vmKind === Be ? HooksDefinition.none : new HooksDefinition(t);
    }
    get strict() {
        return this.definition?.strict;
    }
    constructor(t, e, s, i, n, r, l) {
        this.container = t;
        this.vmKind = e;
        this.definition = s;
        this.viewFactory = i;
        this.host = r;
        this.head = null;
        this.tail = null;
        this.next = null;
        this.parent = null;
        this.bindings = null;
        this.children = null;
        this.scope = null;
        this.isBound = false;
        this.Dt = false;
        this.mountTarget = me;
        this.shadowRoot = null;
        this.nodes = null;
        this.location = null;
        this.Et = null;
        this.state = Ae;
        this.qt = false;
        this.$initiator = null;
        this.$resolve = void 0;
        this.$reject = void 0;
        this.$promise = void 0;
        this.Pt = 0;
        this._t = 0;
        this.It = 0;
        this.Lt = n;
        this.Mt = e === Be ? HooksDefinition.none : new HooksDefinition(n);
        this.location = l;
        this.r = t.root.get(oe);
    }
    static getCached(t) {
        return pe.get(t);
    }
    static getCachedOrThrow(t) {
        const e = Controller.getCached(t);
        if (e === void 0) {
            throw createMappedError(500, t);
        }
        return e;
    }
    static $el(e, s, i, n, r = void 0, l = null, h = null) {
        if (pe.has(s)) {
            return pe.get(s);
        }
        {
            r = r ?? getElementDefinition(s.constructor);
        }
        registerResolver(e, r.Type, new t.InstanceProvider(r.key, s, r.Type));
        const a = new Controller(e, Ce, r, null, s, i, l);
        const c = e.get(t.optional(_e));
        if (r.dependencies.length > 0) {
            e.register(...r.dependencies);
        }
        registerResolver(e, _e, new t.InstanceProvider("IHydrationContext", new HydrationContext(a, n, c)));
        pe.set(s, a);
        if (h != null) {
            a.ssrScope = h;
        }
        if (n == null || n.hydrate !== false) {
            a.hE(n);
        }
        return a;
    }
    static $attr(e, s, i, n, r) {
        if (pe.has(s)) {
            return pe.get(s);
        }
        n = n ?? getAttributeDefinition(s.constructor);
        registerResolver(e, n.Type, new t.InstanceProvider(n.key, s, n.Type));
        const l = new Controller(e, Se, n, null, s, i, null);
        if (n.dependencies.length > 0) {
            e.register(...n.dependencies);
        }
        pe.set(s, l);
        if (r != null) {
            l.ssrScope = r;
        }
        l.Vt();
        return l;
    }
    static $view(t, e = void 0, s = null) {
        const i = new Controller(t.container, Be, null, t, null, s, null);
        i.parent = e ?? null;
        i.Ft();
        return i;
    }
    static $viewAdopted(t, e, s, i) {
        const n = new Controller(t.container, Be, null, t, null, null, null);
        n.parent = e ?? null;
        if (i != null) {
            n.ssrScope = i;
        }
        n.Ht(s);
        return n;
    }
    hE(e) {
        const s = this.container;
        const n = this.Lt;
        const r = this.definition;
        this.scope = i.Scope.create(n, null, true);
        if (r.watches.length > 0) {
            createWatchers(this, s, r, n);
        }
        createObservers(this, r, n);
        this.Et = ut.resolve(s);
        s.register(r.Type);
        if (r.injectable !== null) {
            registerResolver(s, r.injectable, new t.InstanceProvider("definition.injectable", n));
        }
        if (e == null || e.hydrate !== false) {
            this.hS();
            this.hC();
        }
    }
    hS() {
        if (this.Et.hydrating != null) {
            this.Et.hydrating.forEach(callHydratingHook, this);
        }
        if (this.Mt.Ot) {
            this.Lt.hydrating(this);
        }
        const t = this.definition;
        const e = this.$t = this.r.compile(t, this.container);
        const s = e.shadowOptions;
        const i = e.hasSlots;
        const n = e.containerless;
        const r = this.host;
        let l = this.location;
        if (n && l == null) {
            l = this.location = convertToRenderLocation(r);
        }
        He(r, Qe, this);
        He(r, t.key, this);
        if (s !== null || i) {
            if (l != null) {
                throw createMappedError(501);
            }
            He(this.shadowRoot = r.attachShadow(s ?? ke), Qe, this);
            He(this.shadowRoot, t.key, this);
            this.mountTarget = xe;
        } else if (l != null) {
            if (r !== l) {
                He(l, Qe, this);
                He(l, t.key, this);
            }
            this.mountTarget = ve;
        } else {
            this.mountTarget = ge;
        }
        this.Lt.$controller = this;
        if (this.ssrScope != null) {
            this.nodes = this.r.adoptNodes(r);
        } else {
            this.nodes = this.r.createNodes(e);
        }
        if (this.Et.hydrated !== void 0) {
            this.Et.hydrated.forEach(callHydratedHook, this);
        }
        if (this.Mt.Nt) {
            this.Lt.hydrated(this);
        }
    }
    hC() {
        const t = this.nodes.findTargets();
        this.r.render(this, t, this.$t, this.host);
        if (this.Et.created !== void 0) {
            this.Et.created.forEach(callCreatedHook, this);
        }
        if (this.Mt.Wt) {
            this.Lt.created(this);
        }
    }
    Vt() {
        const t = this.definition;
        const e = this.Lt;
        if (t.watches.length > 0) {
            createWatchers(this, this.container, t, e);
        }
        createObservers(this, t, e);
        e.$controller = this;
        this.Et = ut.resolve(this.container);
        if (this.Et.created !== void 0) {
            this.Et.created.forEach(callCreatedHook, this);
        }
        if (this.Mt.Wt) {
            this.Lt.created(this);
        }
    }
    Ft() {
        this.$t = this.r.compile(this.viewFactory.def, this.container);
        this.r.render(this, (this.nodes = this.r.createNodes(this.$t)).findTargets(), this.$t, this.host);
    }
    Ht(t) {
        this.$t = this.r.compile(this.viewFactory.def, this.container);
        this.nodes = t;
        const e = t.findTargets();
        this.r.render(this, e, this.$t, this.host);
    }
    activate(e, s, i) {
        switch (this.state) {
          case Ae:
          case Le:
            if (!(s === null || s.isActive)) {
                return;
            }
            this.state = Re;
            break;

          case Ee:
            if (!(s === null || s.isActive)) {
                return;
            }
            this.Dt = false;
            this.isBound = false;
            this.head = this.tail = this.next = null;
            this.Pt = 0;
            this._t = 0;
            this.It = 0;
            this.state = Re;
            break;

          case Te:
            return;

          case De:
            throw createMappedError(502, this.name);

          default:
            throw createMappedError(503, this.name, stringifyState(this.state));
        }
        this.parent = s;
        switch (this.vmKind) {
          case Ce:
            this.scope.parent = i ?? null;
            break;

          case Se:
            this.scope = i ?? null;
            break;

          case Be:
            if (i === void 0 || i === null) {
                throw createMappedError(504, this.name);
            }
            this.scope = i;
            break;
        }
        this.$initiator = e;
        this.jt();
        let n = void 0;
        if (this.vmKind !== Be && this.Et.binding != null) {
            n = t.onResolveAll(...this.Et.binding.map(callBindingHook, this));
        }
        if (this.Mt.zt) {
            n = t.onResolveAll(n, this.Lt.binding(this.$initiator, this.parent));
        }
        if (t.isPromise(n)) {
            this.Ut();
            n.then(() => {
                this.Dt = true;
                if (this.state !== Re) {
                    this.Gt();
                } else {
                    this.bind();
                }
            }).catch(t => {
                this.Kt(t);
            });
            return this.$promise;
        }
        this.Dt = true;
        this.bind();
        return this.$promise;
    }
    bind() {
        let e = 0;
        let s = 0;
        let i = void 0;
        if (this.bindings !== null) {
            e = 0;
            s = this.bindings.length;
            while (s > e) {
                this.bindings[e].bind(this.scope);
                ++e;
            }
        }
        if (this.vmKind !== Be && this.Et.bound != null) {
            i = t.onResolveAll(...this.Et.bound.map(callBoundHook, this));
        }
        if (this.Mt.Xt) {
            i = t.onResolveAll(i, this.Lt.bound(this.$initiator, this.parent));
        }
        if (t.isPromise(i)) {
            this.Ut();
            i.then(() => {
                this.isBound = true;
                if (this.state !== Re) {
                    this.Gt();
                } else {
                    this.Qt();
                }
            }).catch(t => {
                this.Kt(t);
            });
            return;
        }
        this.isBound = true;
        this.Qt();
    }
    Yt(...t) {
        switch (this.mountTarget) {
          case ge:
            this.host.append(...t);
            break;

          case xe:
            this.shadowRoot.append(...t);
            break;

          case ve:
            {
                let e = 0;
                for (;e < t.length; ++e) {
                    this.location.parentNode.insertBefore(t[e], this.location);
                }
                break;
            }
        }
    }
    Qt() {
        switch (this.mountTarget) {
          case ge:
            this.nodes.appendTo(this.host, this.definition != null && this.definition.enhance);
            break;

          case xe:
            {
                const t = this.container;
                const e = t.has(le, false) ? t.get(le) : t.get(he);
                e.applyTo(this.shadowRoot);
                this.nodes.appendTo(this.shadowRoot);
                break;
            }

          case ve:
            this.nodes.insertBefore(this.location);
            break;
        }
        let e = 0;
        let s = void 0;
        if (this.vmKind !== Be && this.Et.attaching != null) {
            s = t.onResolveAll(...this.Et.attaching.map(callAttachingHook, this));
        }
        if (this.Mt.Zt) {
            s = t.onResolveAll(s, this.Lt.attaching(this.$initiator, this.parent));
        }
        if (t.isPromise(s)) {
            this.Ut();
            this.jt();
            s.then(() => {
                this.Gt();
            }).catch(t => {
                this.Kt(t);
            });
        }
        if (this.children !== null) {
            for (;e < this.children.length; ++e) {
                void this.children[e].activate(this.$initiator, this, this.scope);
            }
        }
        this.Gt();
    }
    deactivate(e, s) {
        let i = void 0;
        switch (this.state & ~Me) {
          case Te:
            this.state = Ee;
            break;

          case Re:
            this.state = Ee;
            i = this.$promise?.catch(t.noop);
            break;

          case Ee:
            return this.$promise;

          case Ae:
          case Le:
          case De:
          case Le | De:
            return;

          default:
            throw createMappedError(505, this.name, this.state);
        }
        this.$initiator = e;
        if (e === this) {
            this.Jt();
        }
        const n = t.isPromise(i) && e !== this;
        if (n) {
            e.Ut();
            e.Jt();
        }
        let r = 0;
        let l;
        if (this.children !== null) {
            for (r = 0; r < this.children.length; ++r) {
                void this.children[r].deactivate(e, this);
            }
        }
        return t.onResolve(i, () => {
            if (this.isBound) {
                if (this.vmKind !== Be && this.Et.detaching != null) {
                    l = t.onResolveAll(...this.Et.detaching.map(callDetachingHook, this));
                }
                if (this.Mt.te) {
                    l = t.onResolveAll(l, this.Lt.detaching(this.$initiator, this.parent));
                }
            }
            if (t.isPromise(l)) {
                this.Ut();
                e.Jt();
                l.then(() => {
                    e.ee();
                }).catch(t => {
                    e.Kt(t);
                });
            }
            if (e.head === null) {
                e.head = this;
            } else {
                e.tail.next = this;
            }
            e.tail = this;
            if (e !== this) {
                if (n) {
                    e.ee();
                }
                return;
            }
            this.ee();
            return this.$promise;
        });
    }
    removeNodes() {
        switch (this.vmKind) {
          case Ce:
          case Be:
            this.nodes.remove();
            this.nodes.unlink();
        }
    }
    unbind() {
        let t = 0;
        if (this.bindings !== null) {
            for (;t < this.bindings.length; ++t) {
                this.bindings[t].unbind();
            }
        }
        this.parent = null;
        switch (this.vmKind) {
          case Se:
            this.scope = null;
            break;

          case Be:
            this.scope = null;
            if ((this.state & Me) === Me && !this.viewFactory.tryReturnToCache(this) && this.$initiator === this) {
                this.dispose();
            }
            break;

          case Ce:
            this.scope.parent = null;
            break;
        }
        this.state = Le;
        this.$initiator = null;
        this.se();
    }
    Ut() {
        if (this.$promise === void 0) {
            this.$promise = new Promise((t, e) => {
                this.$resolve = t;
                this.$reject = e;
            });
            if (this.$initiator !== this) {
                this.parent.Ut();
            }
        }
    }
    se() {
        if (this.$promise !== void 0) {
            Ie = this.$resolve;
            this.$resolve = this.$reject = this.$promise = void 0;
            Ie();
            Ie = void 0;
        }
    }
    Kt(t) {
        if (this.$promise !== void 0) {
            Ve = this.$reject;
            this.$resolve = this.$reject = this.$promise = void 0;
            Ve(t);
            Ve = void 0;
        }
        if (this.$initiator !== this) {
            this.parent.Kt(t);
        }
    }
    jt() {
        ++this.Pt;
        if (this.$initiator !== this) {
            this.parent.jt();
        }
    }
    Gt() {
        if (this.state !== Re) {
            --this.Pt;
            this.se();
            if (this.$initiator !== this) {
                this.parent.Gt();
            }
            return;
        }
        if (--this.Pt === 0) {
            if (this.vmKind !== Be && this.Et.attached != null) {
                Fe = t.onResolveAll(...this.Et.attached.map(callAttachedHook, this));
            }
            if (this.Mt.ie) {
                Fe = t.onResolveAll(Fe, this.Lt.attached(this.$initiator));
            }
            if (t.isPromise(Fe)) {
                this.Ut();
                Fe.then(() => {
                    this.state = Te;
                    this.se();
                    if (this.$initiator !== this) {
                        this.parent.Gt();
                    }
                }).catch(t => {
                    this.Kt(t);
                });
                Fe = void 0;
                return;
            }
            Fe = void 0;
            this.state = Te;
            this.se();
        }
        if (this.$initiator !== this) {
            this.parent.Gt();
        }
    }
    Jt() {
        ++this._t;
    }
    ee() {
        if (--this._t === 0) {
            this.ne();
            this.removeNodes();
            let e = this.$initiator.head;
            let s = void 0;
            while (e !== null) {
                if (e !== this) {
                    if (e.debug) {
                        e.logger.trace(`detach()`);
                    }
                    e.removeNodes();
                }
                if (e.Dt) {
                    if (e.vmKind !== Be && e.Et.unbinding != null) {
                        s = t.onResolveAll(...e.Et.unbinding.map(callUnbindingHook, e));
                    }
                    if (e.Mt.oe) {
                        if (e.debug) {
                            e.logger.trace("unbinding()");
                        }
                        s = t.onResolveAll(s, e.viewModel.unbinding(e.$initiator, e.parent));
                    }
                }
                if (t.isPromise(s)) {
                    this.Ut();
                    this.ne();
                    s.then(() => {
                        this.re();
                    }).catch(t => {
                        this.Kt(t);
                    });
                }
                s = void 0;
                e = e.next;
            }
            this.re();
        }
    }
    ne() {
        ++this.It;
    }
    re() {
        if (--this.It === 0) {
            let t = this.$initiator.head;
            let e = null;
            while (t !== null) {
                if (t !== this) {
                    t.Dt = false;
                    t.isBound = false;
                    t.unbind();
                }
                e = t.next;
                t.next = null;
                t = e;
            }
            this.head = this.tail = null;
            this.Dt = false;
            this.isBound = false;
            this.unbind();
        }
    }
    addBinding(t) {
        if (this.bindings === null) {
            this.bindings = [ t ];
        } else {
            this.bindings[this.bindings.length] = t;
        }
    }
    addChild(t) {
        if (this.children === null) {
            this.children = [ t ];
        } else {
            this.children[this.children.length] = t;
        }
    }
    is(t) {
        switch (this.vmKind) {
          case Se:
          case Ce:
            {
                return this.definition.name === t;
            }

          case Be:
            return this.viewFactory.name === t;
        }
    }
    setHost(t) {
        if (this.vmKind === Ce) {
            He(t, Qe, this);
            He(t, this.definition.key, this);
        }
        this.host = t;
        this.mountTarget = ge;
        return this;
    }
    setShadowRoot(t) {
        if (this.vmKind === Ce) {
            He(t, Qe, this);
            He(t, this.definition.key, this);
        }
        this.shadowRoot = t;
        this.mountTarget = xe;
        return this;
    }
    setLocation(t) {
        if (this.vmKind === Ce) {
            He(t, Qe, this);
            He(t, this.definition.key, this);
        }
        this.location = t;
        this.mountTarget = ve;
        return this;
    }
    release() {
        this.state |= Me;
    }
    dispose() {
        if ((this.state & De) === De) {
            return;
        }
        this.state |= De;
        if (this.Mt.le) {
            this.Lt.dispose();
        }
        if (this.children !== null) {
            this.children.forEach(callDispose);
            this.children = null;
        }
        this.scope = null;
        this.nodes = null;
        this.location = null;
        this.viewFactory = null;
        if (this.Lt !== null) {
            pe.delete(this.Lt);
            this.Lt = null;
        }
        this.Lt = null;
        this.host = null;
        this.shadowRoot = null;
        this.container.disposeResolvers();
    }
    accept(t) {
        if (t(this) === true) {
            return true;
        }
        if (this.Mt.he && this.Lt.accept(t) === true) {
            return true;
        }
        if (this.children !== null) {
            const {children: e} = this;
            for (let s = 0, i = e.length; s < i; ++s) {
                if (e[s].accept(t) === true) {
                    return true;
                }
            }
        }
    }
}

const pe = new WeakMap;

const me = 0;

const ge = 1;

const xe = 2;

const ve = 3;

const ye = b({
    none: me,
    host: ge,
    shadowRoot: xe,
    location: ve
});

const we = t.optionalResource(i.ICoercionConfiguration);

function createObservers(e, s, n) {
    const r = s.bindables;
    const l = C(r);
    const h = l.length;
    if (h === 0) return;
    const a = e.container.get(i.IObserverLocator);
    const c = "propertiesChanged" in n;
    const u = e.vmKind === Be ? void 0 : e.container.get(we);
    const f = c ? (() => {
        let t = {};
        let s = false;
        let r = 0;
        const callPropertiesChanged = () => {
            if (!s) {
                s = true;
                i.queueTask(() => {
                    s = false;
                    const i = t;
                    t = {};
                    r = 0;
                    if (e.isBound) {
                        n.propertiesChanged?.(i);
                        if (r > 0) {
                            callPropertiesChanged();
                        }
                    }
                });
            }
        };
        return (e, s, i) => {
            t[e] = {
                newValue: s,
                oldValue: i
            };
            r++;
            callPropertiesChanged();
        };
    })() : t.noop;
    for (let s = 0; s < h; ++s) {
        const i = l[s];
        const h = r[i];
        const d = h.callback;
        const p = a.getObserver(n, i);
        if (h.set !== t.noop) {
            if (p.useCoercer?.(h.set, u) !== true) {
                throw createMappedError(507, i);
            }
        }
        if (d in n || n.propertyChanged != null || c) {
            const callback = (t, s) => {
                if (e.isBound) {
                    n[d]?.(t, s);
                    n.propertyChanged?.(i, t, s);
                    f(i, t, s);
                }
            };
            if (p.useCallback?.(callback) !== true) {
                throw createMappedError(508, i);
            }
        }
    }
}

const be = new Map;

const getAccessScopeAst = t => {
    let e = be.get(t);
    if (e == null) {
        e = r.createAccessScopeExpression(t, 0);
        be.set(t, e);
    }
    return e;
};

function createWatchers(e, s, n, l) {
    const h = s.get(i.IObserverLocator);
    const a = s.get(r.IExpressionParser);
    const c = n.watches;
    const u = e.vmKind === Ce ? e.scope : i.Scope.create(l, null, true);
    const f = c.length;
    let d;
    let p;
    let m;
    let g;
    let x = 0;
    for (;f > x; ++x) {
        ({expression: d, callback: p, flush: g} = c[x]);
        p = t.isFunction(p) ? p : Reflect.get(l, p);
        if (!t.isFunction(p)) {
            throw createMappedError(506, p);
        }
        if (t.isFunction(d)) {
            e.addBinding(new ComputedWatcher(l, h, d, p, g));
        } else {
            m = t.isString(d) ? a.parse(d, L) : getAccessScopeAst(d);
            e.addBinding(new ExpressionWatcher(u, s, h, m, p, g));
        }
    }
}

function isCustomElementController(t) {
    return t instanceof Controller && t.vmKind === Ce;
}

function isCustomElementViewModel(t) {
    return isElementType(t?.constructor);
}

class HooksDefinition {
    constructor(t) {
        this.ae = "define" in t;
        this.Ot = "hydrating" in t;
        this.Nt = "hydrated" in t;
        this.Wt = "created" in t;
        this.zt = "binding" in t;
        this.Xt = "bound" in t;
        this.Zt = "attaching" in t;
        this.ie = "attached" in t;
        this.te = "detaching" in t;
        this.oe = "unbinding" in t;
        this.le = "dispose" in t;
        this.he = "accept" in t;
    }
}

HooksDefinition.none = new HooksDefinition({});

const ke = {
    mode: "open"
};

const Ce = "customElement";

const Se = "customAttribute";

const Be = "synthetic";

const Ae = 0;

const Re = 1;

const Te = 2;

const Ee = 4;

const Le = 8;

const Me = 16;

const De = 32;

const qe = /*@__PURE__*/ b({
    none: Ae,
    activating: Re,
    activated: Te,
    deactivating: Ee,
    deactivated: Le,
    released: Me,
    disposed: De
});

function stringifyState(t) {
    const e = [];
    if ((t & Re) === Re) {
        e.push("activating");
    }
    if ((t & Te) === Te) {
        e.push("activated");
    }
    if ((t & Ee) === Ee) {
        e.push("deactivating");
    }
    if ((t & Le) === Le) {
        e.push("deactivated");
    }
    if ((t & Me) === Me) {
        e.push("released");
    }
    if ((t & De) === De) {
        e.push("disposed");
    }
    return e.length === 0 ? "none" : e.join("|");
}

const Pe = /*@__PURE__*/ H("IController");

const _e = /*@__PURE__*/ H("IHydrationContext");

class HydrationContext {
    constructor(t, e, s) {
        this.instruction = e;
        this.parent = s;
        this.controller = t;
    }
}

function callDispose(t) {
    t.dispose();
}

function callCreatedHook(t) {
    t.instance.created(this.Lt, this);
}

function callHydratingHook(t) {
    t.instance.hydrating(this.Lt, this);
}

function callHydratedHook(t) {
    t.instance.hydrated(this.Lt, this);
}

function callBindingHook(t) {
    return t.instance.binding(this.Lt, this["$initiator"], this.parent);
}

function callBoundHook(t) {
    return t.instance.bound(this.Lt, this["$initiator"], this.parent);
}

function callAttachingHook(t) {
    return t.instance.attaching(this.Lt, this["$initiator"], this.parent);
}

function callAttachedHook(t) {
    return t.instance.attached(this.Lt, this["$initiator"]);
}

function callDetachingHook(t) {
    return t.instance.detaching(this.Lt, this["$initiator"], this.parent);
}

function callUnbindingHook(t) {
    return t.instance.unbinding(this.Lt, this["$initiator"], this.parent);
}

let Ie;

let Ve;

let Fe;

const He = nt.set;

const Oe = /*@__PURE__*/ H("IEventTarget", t => t.cachedCallback(t => {
    if (t.has(es, true)) {
        return t.get(es).host;
    }
    return t.get(it).document;
}));

const $e = /*@__PURE__*/ H("IRenderLocation");

const Ne = /*@__PURE__*/ H("ICssClassMapping");

const We = new WeakMap;

function getEffectiveParentNode(t) {
    if (We.has(t)) {
        return We.get(t);
    }
    let e = 0;
    let s = t.nextSibling;
    while (s !== null) {
        if (s.nodeType === 8) {
            switch (s.textContent) {
              case "au-start":
                ++e;
                break;

              case "au-end":
                if (e-- === 0) {
                    return s;
                }
            }
        }
        s = s.nextSibling;
    }
    if (t.parentNode === null && t.nodeType === 11) {
        const e = findElementControllerFor(t, {
            optional: true
        });
        if (e == null) {
            return null;
        }
        if (e.mountTarget === ye.shadowRoot) {
            return getEffectiveParentNode(e.host);
        }
    }
    return t.parentNode;
}

function setEffectiveParentNode(t, e) {
    if (t.platform !== void 0 && !(t instanceof t.platform.Node)) {
        const s = t.childNodes;
        for (let t = 0, i = s.length; t < i; ++t) {
            We.set(s[t], e);
        }
    } else {
        We.set(t, e);
    }
}

function convertToRenderLocation(t) {
    if (isRenderLocation(t)) {
        return t;
    }
    const e = t.ownerDocument.createComment("au-end");
    const s = e.$start = t.ownerDocument.createComment("au-start");
    const i = t.parentNode;
    if (i !== null) {
        i.replaceChild(e, t);
        i.insertBefore(s, e);
    }
    return e;
}

function isRenderLocation(t) {
    return t.textContent === "au-end";
}

function findMatchingEndMarker(t) {
    let e = 1;
    let s = t.nextSibling;
    while (s !== null) {
        if (s.nodeType === 8) {
            const t = s.textContent;
            if (t === "au-start") {
                e++;
            } else if (t === "au-end") {
                if (--e === 0) {
                    return s;
                }
            }
        }
        s = s.nextSibling;
    }
    return null;
}

function partitionSiblingNodes(t, e) {
    const s = t;
    const i = t.$start;
    if (i == null) {
        return [];
    }
    const n = [];
    let r = i.nextSibling;
    for (let t = 0, i = e.length; i > t; ++t) {
        const i = e[t];
        const l = [];
        for (let t = 0; t < i && r != null && r !== s; ++t) {
            l.push(r);
            r = r.nextSibling;
        }
        n.push(l);
    }
    return n;
}

function collectMarkerComments(t) {
    const e = [];
    const s = t.ownerDocument.createTreeWalker(t, 128);
    let i;
    while ((i = s.nextNode()) !== null) {
        if (i.textContent === "au") {
            e.push(i);
        }
    }
    return e;
}

class FragmentNodeSequence {
    get firstChild() {
        return this.ce;
    }
    get lastChild() {
        return this.ue;
    }
    constructor(t, e, s) {
        this.platform = t;
        this.next = void 0;
        this.fe = false;
        this.de = false;
        this.ref = null;
        const i = collectMarkerComments(this.f = e);
        let n = 0;
        let r = i.length;
        let l = this.t = Array(r);
        let h;
        let a;
        while (r > n) {
            a = i[n];
            h = a.nextSibling;
            if (!s) {
                a.remove();
            }
            if (h.nodeType === 8) {
                const t = h;
                h = h.nextSibling;
                h.$start = t;
            }
            l[n] = h;
            ++n;
        }
        const c = e.childNodes;
        const u = this.childNodes = Array(r = c.length);
        n = 0;
        while (r > n) {
            u[n] = c[n];
            ++n;
        }
        this.ce = e.firstChild;
        this.ue = e.lastChild;
    }
    static adoptChildren(t, e) {
        const s = t.document.createDocumentFragment();
        const i = new FragmentNodeSequence(t, s);
        const n = e.childNodes;
        const r = Array(n.length);
        for (let t = 0, e = n.length; e > t; ++t) {
            r[t] = n[t];
        }
        i.childNodes = r;
        i.ce = r[0] ?? null;
        i.ue = r[r.length - 1] ?? null;
        i.fe = true;
        i.t = i.pe(e);
        return i;
    }
    static adoptSiblings(t, e) {
        const s = t.document.createDocumentFragment();
        const i = new FragmentNodeSequence(t, s);
        i.childNodes = e;
        i.ce = e[0] ?? null;
        i.ue = e[e.length - 1] ?? null;
        i.fe = true;
        i.t = i.me(e);
        return i;
    }
    me(t) {
        const e = [];
        const s = [];
        const collectFromChildren = t => {
            let e = 0;
            for (let i = 0, n = t.length; n > i; ++i) {
                const n = t[i];
                if (n.nodeType === 8) {
                    const t = n.textContent;
                    if (t === "au") {
                        if (e === 0) {
                            s.push(n);
                        }
                        continue;
                    } else if (t === "au-start") {
                        e++;
                        continue;
                    } else if (t === "au-end") {
                        e--;
                        continue;
                    }
                }
                if (e > 0) continue;
                if (n.nodeType === 1) {
                    const t = n;
                    if (t.tagName.includes("-")) {
                        continue;
                    }
                    collectFromChildren(t.childNodes);
                }
            }
        };
        for (let e = 0, i = t.length; i > e; ++e) {
            const i = t[e];
            if (i.nodeType === 8 && i.textContent === "au") {
                s.push(i);
            } else if (i.nodeType === 1) {
                const t = i;
                if (!t.tagName.includes("-")) {
                    collectFromChildren(t.childNodes);
                }
            }
        }
        for (let t = 0, i = s.length; i > t; ++t) {
            const i = s[t];
            let n = i.nextSibling;
            if (n.nodeType === 8 && n.textContent === "au-start") {
                const t = n;
                const e = findMatchingEndMarker(n);
                if (e !== null) {
                    n = e;
                    n.$start = t;
                }
            }
            e[t] = n;
        }
        return e;
    }
    pe(t) {
        const e = [];
        const s = [];
        const collectSiblings = t => {
            let e = 0;
            for (let i = 0, n = t.length; n > i; ++i) {
                const n = t[i];
                if (n.nodeType === 8) {
                    const t = n.textContent;
                    if (t === "au") {
                        if (e === 0) {
                            s.push(n);
                        }
                        continue;
                    } else if (t === "au-start") {
                        e++;
                        continue;
                    } else if (t === "au-end") {
                        e--;
                        continue;
                    }
                }
                if (e > 0) continue;
                if (n.nodeType === 1) {
                    const t = n;
                    if (t.tagName.includes("-")) {
                        continue;
                    }
                    collectSiblings(t.childNodes);
                }
            }
        };
        collectSiblings(t.childNodes);
        for (let t = 0, i = s.length; i > t; ++t) {
            const i = s[t];
            let n = i.nextSibling;
            if (n.nodeType === 8 && n.textContent === "au-start") {
                const t = n;
                const e = findMatchingEndMarker(n);
                if (e !== null) {
                    n = e;
                    n.$start = t;
                }
            }
            e[t] = n;
        }
        return e;
    }
    findTargets() {
        return this.t;
    }
    insertBefore(t) {
        if (this.de && !!this.ref) {
            this.addToLinked();
        } else {
            const e = t.parentNode;
            if (this.fe) {
                let s = this.ce;
                let i;
                const n = this.ue;
                while (s != null) {
                    i = s.nextSibling;
                    e.insertBefore(s, t);
                    if (s === n) {
                        break;
                    }
                    s = i;
                }
            } else {
                this.fe = true;
                t.parentNode.insertBefore(this.f, t);
            }
        }
    }
    appendTo(t, e = false) {
        if (this.fe) {
            let e = this.ce;
            let s;
            const i = this.ue;
            while (e != null) {
                s = e.nextSibling;
                t.appendChild(e);
                if (e === i) {
                    break;
                }
                e = s;
            }
        } else {
            this.fe = true;
            if (!e) {
                t.appendChild(this.f);
            }
        }
    }
    remove() {
        if (this.fe) {
            this.fe = false;
            const t = this.f;
            const e = this.ue;
            let s;
            let i = this.ce;
            while (i !== null) {
                s = i.nextSibling;
                t.appendChild(i);
                if (i === e) {
                    break;
                }
                i = s;
            }
        }
    }
    addToLinked() {
        const t = this.ref;
        const e = t.parentNode;
        if (this.fe) {
            let s = this.ce;
            let i;
            const n = this.ue;
            while (s != null) {
                i = s.nextSibling;
                e.insertBefore(s, t);
                if (s === n) {
                    break;
                }
                s = i;
            }
        } else {
            this.fe = true;
            e.insertBefore(this.f, t);
        }
    }
    unlink() {
        this.de = false;
        this.next = void 0;
        this.ref = void 0;
    }
    link(t) {
        this.de = true;
        if (isRenderLocation(t)) {
            this.ref = t;
        } else {
            this.next = t;
            this.ge();
        }
    }
    ge() {
        if (this.next !== void 0) {
            this.ref = this.next.firstChild;
        } else {
            this.ref = void 0;
        }
    }
}

const je = /*@__PURE__*/ H("IWindow", t => t.callback(t => t.get(it).window));

const ze = /*@__PURE__*/ H("ILocation", t => t.callback(t => t.get(je).location));

const Ue = /*@__PURE__*/ H("IHistory", t => t.callback(t => t.get(je).history));

const registerHostNode = (e, s, i = e.get(it)) => {
    registerResolver(e, i.HTMLElement, registerResolver(e, i.Element, registerResolver(e, ot, new t.InstanceProvider("ElementResolver", s))));
    return e;
};

function customElement(t) {
    return function(e, s) {
        s.addInitializer(function() {
            defineElement(t, this);
        });
        return e;
    };
}

function useShadowDOM(e, s) {
    if (e === void 0) {
        return function(t, e) {
            e.addInitializer(function() {
                annotateElementMetadata(this, "shadowOptions", {
                    mode: "open"
                });
            });
        };
    }
    if (!t.isFunction(e)) {
        return function(t, s) {
            s.addInitializer(function() {
                annotateElementMetadata(this, "shadowOptions", e);
            });
        };
    }
    s.addInitializer(function() {
        annotateElementMetadata(this, "shadowOptions", {
            mode: "open"
        });
    });
}

function containerless(t, e) {
    if (t === void 0) {
        return function(t, e) {
            e.addInitializer(function() {
                markContainerless(t);
            });
        };
    }
    e.addInitializer(function() {
        markContainerless(t);
    });
}

function markContainerless(t) {
    const e = f(Qe, t);
    if (e === void 0) {
        annotateElementMetadata(t, "containerless", true);
        return;
    }
    e.containerless = true;
}

const Ge = new WeakMap;

class CustomElementDefinition {
    get type() {
        return W;
    }
    constructor(t, e, s, i, n, r, l, h, a, c, u, f, d, p, m, g, x, v, y) {
        this.Type = t;
        this.name = e;
        this.aliases = s;
        this.key = i;
        this.capture = n;
        this.template = r;
        this.instructions = l;
        this.dependencies = h;
        this.injectable = a;
        this.needsCompile = c;
        this.surrogates = u;
        this.bindables = f;
        this.containerless = d;
        this.shadowOptions = p;
        this.hasSlots = m;
        this.enhance = g;
        this.watches = x;
        this.strict = v;
        this.processContent = y;
    }
    static create(e, s = null) {
        if (s === null) {
            const i = e;
            if (t.isString(i)) {
                throw createMappedError(761, e);
            }
            const n = t.fromDefinitionOrDefault("name", i, Ye);
            if (t.isFunction(i.Type)) {
                s = i.Type;
            } else {
                s = Ze(t.pascalCase(n));
            }
            for (const t of Object.values(V.from(i.bindables))) {
                V.i(t, s);
            }
            return new CustomElementDefinition(s, n, t.mergeArrays(i.aliases), t.fromDefinitionOrDefault("key", i, () => getElementKeyFrom(n)), t.fromAnnotationOrDefinitionOrTypeOrDefault("capture", i, s, returnFalse), t.fromAnnotationOrDefinitionOrTypeOrDefault("template", i, s, returnNull), t.mergeArrays(i.instructions), t.mergeArrays(getElementAnnotation(s, "dependencies"), i.dependencies), t.fromDefinitionOrDefault("injectable", i, returnNull), t.fromDefinitionOrDefault("needsCompile", i, returnTrue), t.mergeArrays(i.surrogates), V.from(getElementAnnotation(s, "bindables"), i.bindables), t.fromAnnotationOrDefinitionOrTypeOrDefault("containerless", i, s, returnFalse), t.fromDefinitionOrDefault("shadowOptions", i, returnNull), t.fromDefinitionOrDefault("hasSlots", i, returnFalse), t.fromDefinitionOrDefault("enhance", i, returnFalse), t.fromDefinitionOrDefault("watches", i, returnEmptyArray), t.fromDefinitionOrDefault("strict", i, returnUndefined), t.fromAnnotationOrTypeOrDefault("processContent", s, returnNull));
        }
        if (t.isString(e)) {
            return new CustomElementDefinition(s, e, t.mergeArrays(getElementAnnotation(s, "aliases"), s.aliases), getElementKeyFrom(e), t.fromAnnotationOrTypeOrDefault("capture", s, returnFalse), t.fromAnnotationOrTypeOrDefault("template", s, returnNull), t.mergeArrays(getElementAnnotation(s, "instructions"), s.instructions), t.mergeArrays(getElementAnnotation(s, "dependencies"), s.dependencies), t.fromAnnotationOrTypeOrDefault("injectable", s, returnNull), t.fromAnnotationOrTypeOrDefault("needsCompile", s, returnTrue), t.mergeArrays(getElementAnnotation(s, "surrogates"), s.surrogates), V.from(...V.getAll(s), getElementAnnotation(s, "bindables"), s.bindables), t.fromAnnotationOrTypeOrDefault("containerless", s, returnFalse), t.fromAnnotationOrTypeOrDefault("shadowOptions", s, returnNull), t.fromAnnotationOrTypeOrDefault("hasSlots", s, returnFalse), t.fromAnnotationOrTypeOrDefault("enhance", s, returnFalse), t.mergeArrays(rt.getDefinitions(s), s.watches), t.fromAnnotationOrTypeOrDefault("strict", s, returnUndefined), t.fromAnnotationOrTypeOrDefault("processContent", s, returnNull));
        }
        const i = t.fromDefinitionOrDefault("name", e, Ye);
        for (const t of Object.values(V.from(e.bindables))) {
            V.i(t, s);
        }
        return new CustomElementDefinition(s, i, t.mergeArrays(getElementAnnotation(s, "aliases"), e.aliases, s.aliases), getElementKeyFrom(i), t.fromAnnotationOrDefinitionOrTypeOrDefault("capture", e, s, returnFalse), t.fromAnnotationOrDefinitionOrTypeOrDefault("template", e, s, returnNull), t.mergeArrays(getElementAnnotation(s, "instructions"), e.instructions, s.instructions), t.mergeArrays(getElementAnnotation(s, "dependencies"), e.dependencies, s.dependencies), t.fromAnnotationOrDefinitionOrTypeOrDefault("injectable", e, s, returnNull), t.fromAnnotationOrDefinitionOrTypeOrDefault("needsCompile", e, s, returnTrue), t.mergeArrays(getElementAnnotation(s, "surrogates"), e.surrogates, s.surrogates), V.from(...V.getAll(s), getElementAnnotation(s, "bindables"), s.bindables, e.bindables), t.fromAnnotationOrDefinitionOrTypeOrDefault("containerless", e, s, returnFalse), t.fromAnnotationOrDefinitionOrTypeOrDefault("shadowOptions", e, s, returnNull), t.fromAnnotationOrDefinitionOrTypeOrDefault("hasSlots", e, s, returnFalse), t.fromAnnotationOrDefinitionOrTypeOrDefault("enhance", e, s, returnFalse), t.mergeArrays(e.watches, rt.getDefinitions(s), s.watches), t.fromAnnotationOrDefinitionOrTypeOrDefault("strict", e, s, returnUndefined), t.fromAnnotationOrDefinitionOrTypeOrDefault("processContent", e, s, returnNull));
    }
    static getOrCreate(t) {
        if (t instanceof CustomElementDefinition) {
            return t;
        }
        if (Ge.has(t)) {
            return Ge.get(t);
        }
        const e = CustomElementDefinition.create(t);
        Ge.set(t, e);
        p(e, e.Type, Qe);
        return e;
    }
    register(t, e) {
        const s = this.Type;
        const i = typeof e === "string" ? getElementKeyFrom(e) : this.key;
        const n = this.aliases;
        if (t.has(i, false)) {
            console.warn(createMappedError(153, this.name));
            return;
        }
        t.register(O(i, s), ...n.map(t => $(i, getElementKeyFrom(t))));
    }
    toString() {
        return `au:ce:${this.name}`;
    }
}

const Ke = {
    name: undefined,
    searchParents: false,
    optional: false
};

const returnNull = () => null;

const returnUndefined = () => void 0;

const returnFalse = () => false;

const returnTrue = () => true;

const returnEmptyArray = () => t.emptyArray;

const Xe = "custom-element";

const Qe = /*@__PURE__*/ t.getResourceKeyFor(Xe);

const getElementKeyFrom = t => `${Qe}:${t}`;

const Ye = /*@__PURE__*/ (t => () => `unnamed-${++t}`)(0);

const annotateElementMetadata = (t, e, s) => {
    p(s, t, g(e));
};

const defineElement = (e, s) => {
    const i = CustomElementDefinition.create(e, s);
    const n = i.Type;
    p(i, n, Qe, t.resourceBaseName);
    return n;
};

const isElementType = e => t.isFunction(e) && (d(Qe, e) || e.$au?.type === Xe);

const findElementControllerFor = (t, e = Ke) => {
    if (e.name === void 0 && e.searchParents !== true) {
        const s = nt.get(t, Qe);
        if (s === null) {
            if (e.optional === true) {
                return null;
            }
            throw createMappedError(762, t);
        }
        return s;
    }
    if (e.name !== void 0) {
        if (e.searchParents !== true) {
            const s = nt.get(t, Qe);
            if (s === null) {
                throw createMappedError(763, t);
            }
            if (s.is(e.name)) {
                return s;
            }
            return void 0;
        }
        let s = t;
        let i = false;
        while (s !== null) {
            const t = nt.get(s, Qe);
            if (t !== null) {
                i = true;
                if (t.is(e.name)) {
                    return t;
                }
            }
            s = getEffectiveParentNode(s);
        }
        if (i) {
            return void 0;
        }
        throw createMappedError(764, t);
    }
    let s = t;
    while (s !== null) {
        const t = nt.get(s, Qe);
        if (t !== null) {
            return t;
        }
        s = getEffectiveParentNode(s);
    }
    throw createMappedError(765, t);
};

const getElementAnnotation = (t, e) => f(g(e), t);

const getElementDefinition = t => {
    const e = f(Qe, t) ?? getDefinitionFromStaticAu(t, Xe, CustomElementDefinition.create);
    if (e == null) {
        throw createMappedError(760, t);
    }
    return e;
};

const createElementInjectable = () => {
    const t = {
        $isInterface: false,
        register() {
            return {
                $isResolver: true,
                resolve(e, s) {
                    if (s.has(t, true)) {
                        return s.get(t);
                    } else {
                        return null;
                    }
                }
            };
        }
    };
    return t;
};

const Ze = /*@__PURE__*/ function() {
    const t = {
        value: "",
        writable: false,
        enumerable: false,
        configurable: true
    };
    const e = {};
    return function(s, i = e) {
        const n = class Anonymous {};
        t.value = s;
        A(n, "name", t);
        if (i !== e) {
            k(n.prototype, i);
        }
        return n;
    };
}();

const clearElementDefinition = t => {
    clearMetadata(t, Qe);
    clearStaticAuDefinition(t);
};

const Je = /*@__PURE__*/ b({
    name: Qe,
    keyFrom: getElementKeyFrom,
    isType: isElementType,
    for: findElementControllerFor,
    define: defineElement,
    getDefinition: getElementDefinition,
    annotate: annotateElementMetadata,
    getAnnotation: getElementAnnotation,
    generateName: Ye,
    createInjectable: createElementInjectable,
    generateType: Ze,
    find(t, e) {
        const s = t.find(Xe, e);
        return s == null ? null : f(Qe, s) ?? getDefinitionFromStaticAu(s, Xe, CustomElementDefinition.create) ?? null;
    },
    clearDefinition: clearElementDefinition
});

const ts = /*@__PURE__*/ g("processContent");

function processContent(e) {
    return e === void 0 ? function(t, e) {
        if (!e.static || e.kind !== "method") throw createMappedError(766, t);
        e.addInitializer(function() {
            p(t, this, ts);
        });
    } : function(s, i) {
        i.addInitializer(function() {
            if (t.isString(e) || t.isSymbol(e)) {
                e = this[e];
            }
            if (!t.isFunction(e)) throw createMappedError(766, e);
            const s = f(Qe, this);
            if (s !== void 0) {
                s.processContent = e;
            } else {
                p(e, this, ts);
            }
        });
        return s;
    };
}

function capture(e) {
    return function(s, i) {
        const n = t.isFunction(e) ? e : true;
        i.addInitializer(function() {
            annotateElementMetadata(this, "capture", n);
            if (isElementType(this)) {
                getElementDefinition(this).capture = n;
            }
        });
    };
}

const es = /*@__PURE__*/ H("IAppRoot");

class AppRoot {
    get controller() {
        return this.q;
    }
    constructor(e, s, i, n = false) {
        this.config = e;
        this.container = s;
        this.xe = void 0;
        this.ve = n;
        const r = this.host = e.host;
        i.prepare(this);
        registerResolver(s, Oe, new t.InstanceProvider("IEventTarget", r));
        registerHostNode(s, r, this.platform = this.ye(s, r));
        this.xe = t.onResolve(this.we("creating"), () => {
            if (!e.allowActionlessForm !== false) {
                r.addEventListener("submit", t => {
                    const e = t.target;
                    const s = !e.getAttribute("action");
                    if (e.tagName === "FORM" && s) {
                        t.preventDefault();
                    }
                }, false);
            }
            const i = n ? s : s.createChild();
            const l = e.component;
            let h;
            if (t.isFunction(l)) {
                h = i.invoke(l);
                N(l, h);
            } else {
                h = e.component;
            }
            const a = {
                hydrate: false,
                projections: null
            };
            const c = n ? CustomElementDefinition.create({
                name: Ye(),
                template: this.host,
                enhance: true,
                strict: e.strictBinding
            }) : void 0;
            const u = this.q = Controller.$el(i, h, r, a, c, null, e.ssrScope);
            u.hE(a);
            return t.onResolve(this.we("hydrating"), () => {
                u.hS();
                return t.onResolve(this.we("hydrated"), () => {
                    u.hC();
                    this.xe = void 0;
                });
            });
        });
    }
    activate() {
        return t.onResolve(this.xe, () => t.onResolve(this.we("activating"), () => t.onResolve(this.q.activate(this.q, null, void 0), () => this.we("activated"))));
    }
    deactivate() {
        return t.onResolve(this.we("deactivating"), () => t.onResolve(this.q.deactivate(this.q, null), () => this.we("deactivated")));
    }
    we(e) {
        const s = this.container;
        const i = this.ve && !s.has(et, false) ? [] : s.getAll(et);
        return t.onResolveAll(...i.reduce((t, s) => {
            if (s.slot === e) {
                t.push(s.run());
            }
            return t;
        }, []));
    }
    ye(t, e) {
        let s;
        if (!t.has(it, false)) {
            if (e.ownerDocument.defaultView === null) {
                throw createMappedError(769);
            }
            s = new n.BrowserPlatform(e.ownerDocument.defaultView);
            t.register(N(it, s));
        } else {
            s = t.get(it);
        }
        return s;
    }
    dispose() {
        this.q?.dispose();
    }
}

const ss = /*@__PURE__*/ H("IAurelia");

class Aurelia {
    get isRunning() {
        return this.ir;
    }
    get isStarting() {
        return this.be;
    }
    get isStopping() {
        return this.ke;
    }
    get root() {
        if (this.Ce == null) {
            if (this.next == null) {
                throw createMappedError(767);
            }
            return this.next;
        }
        return this.Ce;
    }
    constructor(e = t.DI.createContainer()) {
        this.container = e;
        this.ir = false;
        this.be = false;
        this.ke = false;
        this.Ce = void 0;
        this.next = void 0;
        this.Se = void 0;
        this.Be = void 0;
        if (e.has(ss, true) || e.has(Aurelia, true)) {
            throw createMappedError(768);
        }
        registerResolver(e, ss, new t.InstanceProvider("IAurelia", this));
        registerResolver(e, Aurelia, new t.InstanceProvider("Aurelia", this));
        registerResolver(e, es, this.Ae = new t.InstanceProvider("IAppRoot"));
    }
    register(...t) {
        this.container.register(...t);
        return this;
    }
    app(t) {
        this.next = new AppRoot(t, this.container, this.Ae);
        return this;
    }
    enhance(e) {
        const s = e.container ?? this.container.createChild();
        const i = registerResolver(s, es, new t.InstanceProvider("IAppRoot"));
        const n = new AppRoot({
            host: e.host,
            component: e.component
        }, s, i, true);
        return t.onResolve(n.activate(), () => n);
    }
    hydrate(e) {
        const s = e.container ?? this.container.createChild();
        const i = new AppRoot({
            host: e.host,
            component: e.component,
            ssrScope: e.ssrScope
        }, s, this.Ae, false);
        return t.onResolve(i.activate(), () => i);
    }
    start(e = this.next) {
        if (e == null) {
            throw createMappedError(770);
        }
        if (t.isPromise(this.Se)) {
            return this.Se;
        }
        return this.Se = t.onResolve(this.stop(), () => {
            if (!nt.hideProp) {
                Reflect.set(e.host, "$aurelia", this);
            }
            this.Ae.prepare(this.Ce = e);
            this.be = true;
            return t.onResolve(e.activate(), () => {
                this.ir = true;
                this.be = false;
                this.Se = void 0;
                this.Re(e, "au-started", e.host);
            });
        });
    }
    stop(e = false) {
        if (t.isPromise(this.Be)) {
            return this.Be;
        }
        if (this.ir === true) {
            const s = this.Ce;
            this.ir = false;
            this.ke = true;
            return this.Be = t.onResolve(s.deactivate(), () => t.onResolve(i.tasksSettled(), () => {
                Reflect.deleteProperty(s.host, "$aurelia");
                if (e) {
                    s.dispose();
                }
                this.Ce = void 0;
                this.Ae.dispose();
                this.ke = false;
                this.Be = void 0;
                this.Re(s, "au-stopped", s.host);
            }));
        }
    }
    dispose() {
        if (this.ir || this.ke) {
            throw createMappedError(771);
        }
        this.container.dispose();
    }
    Re(t, e, s) {
        const i = new t.platform.window.CustomEvent(e, {
            detail: this,
            bubbles: true,
            cancelable: true
        });
        s.dispatchEvent(i);
    }
}

const is = /*@__PURE__*/ H("ISVGAnalyzer", t => t.singleton(NoopSVGAnalyzer));

const o = e => {
    const s = t.createLookup();
    e = t.isString(e) ? e.split(" ") : e;
    let i;
    for (i of e) {
        s[i] = true;
    }
    return s;
};

class NoopSVGAnalyzer {
    isStandardSvgAttribute(t, e) {
        return false;
    }
}

class SVGAnalyzer {
    static register(t) {
        t.register(O(this, this), $(this, is));
    }
    constructor() {
        this.Te = k(t.createLookup(), {
            a: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage target transform xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            altGlyph: o("class dx dy externalResourcesRequired format glyphRef id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rotate style systemLanguage x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            altglyph: t.createLookup(),
            altGlyphDef: o("id xml:base xml:lang xml:space"),
            altglyphdef: t.createLookup(),
            altGlyphItem: o("id xml:base xml:lang xml:space"),
            altglyphitem: t.createLookup(),
            animate: o("accumulate additive attributeName attributeType begin by calcMode dur end externalResourcesRequired fill from id keySplines keyTimes max min onbegin onend onload onrepeat repeatCount repeatDur requiredExtensions requiredFeatures restart systemLanguage to values xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            animateColor: o("accumulate additive attributeName attributeType begin by calcMode dur end externalResourcesRequired fill from id keySplines keyTimes max min onbegin onend onload onrepeat repeatCount repeatDur requiredExtensions requiredFeatures restart systemLanguage to values xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            animateMotion: o("accumulate additive begin by calcMode dur end externalResourcesRequired fill from id keyPoints keySplines keyTimes max min onbegin onend onload onrepeat origin path repeatCount repeatDur requiredExtensions requiredFeatures restart rotate systemLanguage to values xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            animateTransform: o("accumulate additive attributeName attributeType begin by calcMode dur end externalResourcesRequired fill from id keySplines keyTimes max min onbegin onend onload onrepeat repeatCount repeatDur requiredExtensions requiredFeatures restart systemLanguage to type values xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            circle: o("class cx cy externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup r requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            clipPath: o("class clipPathUnits externalResourcesRequired id requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            "color-profile": o("id local name rendering-intent xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            cursor: o("externalResourcesRequired id requiredExtensions requiredFeatures systemLanguage x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            defs: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            desc: o("class id style xml:base xml:lang xml:space"),
            ellipse: o("class cx cy externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rx ry style systemLanguage transform xml:base xml:lang xml:space"),
            feBlend: o("class height id in in2 mode result style width x xml:base xml:lang xml:space y"),
            feColorMatrix: o("class height id in result style type values width x xml:base xml:lang xml:space y"),
            feComponentTransfer: o("class height id in result style width x xml:base xml:lang xml:space y"),
            feComposite: o("class height id in in2 k1 k2 k3 k4 operator result style width x xml:base xml:lang xml:space y"),
            feConvolveMatrix: o("bias class divisor edgeMode height id in kernelMatrix kernelUnitLength order preserveAlpha result style targetX targetY width x xml:base xml:lang xml:space y"),
            feDiffuseLighting: o("class diffuseConstant height id in kernelUnitLength result style surfaceScale width x xml:base xml:lang xml:space y"),
            feDisplacementMap: o("class height id in in2 result scale style width x xChannelSelector xml:base xml:lang xml:space y yChannelSelector"),
            feDistantLight: o("azimuth elevation id xml:base xml:lang xml:space"),
            feFlood: o("class height id result style width x xml:base xml:lang xml:space y"),
            feFuncA: o("amplitude exponent id intercept offset slope tableValues type xml:base xml:lang xml:space"),
            feFuncB: o("amplitude exponent id intercept offset slope tableValues type xml:base xml:lang xml:space"),
            feFuncG: o("amplitude exponent id intercept offset slope tableValues type xml:base xml:lang xml:space"),
            feFuncR: o("amplitude exponent id intercept offset slope tableValues type xml:base xml:lang xml:space"),
            feGaussianBlur: o("class height id in result stdDeviation style width x xml:base xml:lang xml:space y"),
            feImage: o("class externalResourcesRequired height id preserveAspectRatio result style width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            feMerge: o("class height id result style width x xml:base xml:lang xml:space y"),
            feMergeNode: o("id xml:base xml:lang xml:space"),
            feMorphology: o("class height id in operator radius result style width x xml:base xml:lang xml:space y"),
            feOffset: o("class dx dy height id in result style width x xml:base xml:lang xml:space y"),
            fePointLight: o("id x xml:base xml:lang xml:space y z"),
            feSpecularLighting: o("class height id in kernelUnitLength result specularConstant specularExponent style surfaceScale width x xml:base xml:lang xml:space y"),
            feSpotLight: o("id limitingConeAngle pointsAtX pointsAtY pointsAtZ specularExponent x xml:base xml:lang xml:space y z"),
            feTile: o("class height id in result style width x xml:base xml:lang xml:space y"),
            feTurbulence: o("baseFrequency class height id numOctaves result seed stitchTiles style type width x xml:base xml:lang xml:space y"),
            filter: o("class externalResourcesRequired filterRes filterUnits height id primitiveUnits style width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            font: o("class externalResourcesRequired horiz-adv-x horiz-origin-x horiz-origin-y id style vert-adv-y vert-origin-x vert-origin-y xml:base xml:lang xml:space"),
            "font-face": o("accent-height alphabetic ascent bbox cap-height descent font-family font-size font-stretch font-style font-variant font-weight hanging id ideographic mathematical overline-position overline-thickness panose-1 slope stemh stemv strikethrough-position strikethrough-thickness underline-position underline-thickness unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical widths x-height xml:base xml:lang xml:space"),
            "font-face-format": o("id string xml:base xml:lang xml:space"),
            "font-face-name": o("id name xml:base xml:lang xml:space"),
            "font-face-src": o("id xml:base xml:lang xml:space"),
            "font-face-uri": o("id xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            foreignObject: o("class externalResourcesRequired height id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform width x xml:base xml:lang xml:space y"),
            g: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            glyph: o("arabic-form class d glyph-name horiz-adv-x id lang orientation style unicode vert-adv-y vert-origin-x vert-origin-y xml:base xml:lang xml:space"),
            glyphRef: o("class dx dy format glyphRef id style x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            glyphref: t.createLookup(),
            hkern: o("g1 g2 id k u1 u2 xml:base xml:lang xml:space"),
            image: o("class externalResourcesRequired height id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup preserveAspectRatio requiredExtensions requiredFeatures style systemLanguage transform width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            line: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform x1 x2 xml:base xml:lang xml:space y1 y2"),
            linearGradient: o("class externalResourcesRequired gradientTransform gradientUnits id spreadMethod style x1 x2 xlink:arcrole xlink:href xlink:role xlink:title xlink:type xml:base xml:lang xml:space y1 y2"),
            marker: o("class externalResourcesRequired id markerHeight markerUnits markerWidth orient preserveAspectRatio refX refY style viewBox xml:base xml:lang xml:space"),
            mask: o("class externalResourcesRequired height id maskContentUnits maskUnits requiredExtensions requiredFeatures style systemLanguage width x xml:base xml:lang xml:space y"),
            metadata: o("id xml:base xml:lang xml:space"),
            "missing-glyph": o("class d horiz-adv-x id style vert-adv-y vert-origin-x vert-origin-y xml:base xml:lang xml:space"),
            mpath: o("externalResourcesRequired id xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            path: o("class d externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup pathLength requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            pattern: o("class externalResourcesRequired height id patternContentUnits patternTransform patternUnits preserveAspectRatio requiredExtensions requiredFeatures style systemLanguage viewBox width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            polygon: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup points requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            polyline: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup points requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            radialGradient: o("class cx cy externalResourcesRequired fx fy gradientTransform gradientUnits id r spreadMethod style xlink:arcrole xlink:href xlink:role xlink:title xlink:type xml:base xml:lang xml:space"),
            rect: o("class externalResourcesRequired height id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rx ry style systemLanguage transform width x xml:base xml:lang xml:space y"),
            script: o("externalResourcesRequired id type xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            set: o("attributeName attributeType begin dur end externalResourcesRequired fill id max min onbegin onend onload onrepeat repeatCount repeatDur requiredExtensions requiredFeatures restart systemLanguage to xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            stop: o("class id offset style xml:base xml:lang xml:space"),
            style: o("id media title type xml:base xml:lang xml:space"),
            svg: o("baseProfile class contentScriptType contentStyleType externalResourcesRequired height id onabort onactivate onclick onerror onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup onresize onscroll onunload onzoom preserveAspectRatio requiredExtensions requiredFeatures style systemLanguage version viewBox width x xml:base xml:lang xml:space y zoomAndPan"),
            switch: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            symbol: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup preserveAspectRatio style viewBox xml:base xml:lang xml:space"),
            text: o("class dx dy externalResourcesRequired id lengthAdjust onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rotate style systemLanguage textLength transform x xml:base xml:lang xml:space y"),
            textPath: o("class externalResourcesRequired id lengthAdjust method onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures spacing startOffset style systemLanguage textLength xlink:arcrole xlink:href xlink:role xlink:title xlink:type xml:base xml:lang xml:space"),
            title: o("class id style xml:base xml:lang xml:space"),
            tref: o("class dx dy externalResourcesRequired id lengthAdjust onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rotate style systemLanguage textLength x xlink:arcrole xlink:href xlink:role xlink:title xlink:type xml:base xml:lang xml:space y"),
            tspan: o("class dx dy externalResourcesRequired id lengthAdjust onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rotate style systemLanguage textLength x xml:base xml:lang xml:space y"),
            use: o("class externalResourcesRequired height id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            view: o("externalResourcesRequired id preserveAspectRatio viewBox viewTarget xml:base xml:lang xml:space zoomAndPan"),
            vkern: o("g1 g2 id k u1 u2 xml:base xml:lang xml:space")
        });
        this.Ee = o("a altGlyph animate animateColor circle clipPath defs ellipse feBlend feColorMatrix feComponentTransfer feComposite feConvolveMatrix feDiffuseLighting feDisplacementMap feFlood feGaussianBlur feImage feMerge feMorphology feOffset feSpecularLighting feTile feTurbulence filter font foreignObject g glyph glyphRef image line linearGradient marker mask missing-glyph path pattern polygon polyline radialGradient rect stop svg switch symbol text textPath tref tspan use");
        this.Le = o("alignment-baseline baseline-shift clip-path clip-rule clip color-interpolation-filters color-interpolation color-profile color-rendering color cursor direction display dominant-baseline enable-background fill-opacity fill-rule fill filter flood-color flood-opacity font-family font-size-adjust font-size font-stretch font-style font-variant font-weight glyph-orientation-horizontal glyph-orientation-vertical image-rendering kerning letter-spacing lighting-color marker-end marker-mid marker-start mask opacity overflow pointer-events shape-rendering stop-color stop-opacity stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width stroke text-anchor text-decoration text-rendering unicode-bidi visibility word-spacing writing-mode");
        const e = t.resolve(it);
        this.SVGElement = e.globalThis.SVGElement;
        const s = e.document.createElement("div");
        s.innerHTML = "<svg><altGlyph /></svg>";
        if (s.firstElementChild.nodeName === "altglyph") {
            const t = this.Te;
            let e = t.altGlyph;
            t.altGlyph = t.altglyph;
            t.altglyph = e;
            e = t.altGlyphDef;
            t.altGlyphDef = t.altglyphdef;
            t.altglyphdef = e;
            e = t.altGlyphItem;
            t.altGlyphItem = t.altglyphitem;
            t.altglyphitem = e;
            e = t.glyphRef;
            t.glyphRef = t.glyphref;
            t.glyphref = e;
        }
    }
    isStandardSvgAttribute(t, e) {
        if (!(t instanceof this.SVGElement)) {
            return false;
        }
        return this.Ee[t.nodeName] === true && this.Le[e] === true || this.Te[t.nodeName]?.[e] === true;
    }
}

class AttrMapper {
    constructor() {
        this.fns = [];
        this.Me = t.createLookup();
        this.De = t.createLookup();
        this.svg = t.resolve(is);
        this.useMapping({
            LABEL: {
                for: "htmlFor"
            },
            IMG: {
                usemap: "useMap"
            },
            INPUT: {
                maxlength: "maxLength",
                minlength: "minLength",
                formaction: "formAction",
                formenctype: "formEncType",
                formmethod: "formMethod",
                formnovalidate: "formNoValidate",
                formtarget: "formTarget",
                inputmode: "inputMode"
            },
            TEXTAREA: {
                maxlength: "maxLength"
            },
            TD: {
                rowspan: "rowSpan",
                colspan: "colSpan"
            },
            TH: {
                rowspan: "rowSpan",
                colspan: "colSpan"
            }
        });
        this.useGlobalMapping({
            accesskey: "accessKey",
            contenteditable: "contentEditable",
            tabindex: "tabIndex",
            textcontent: "textContent",
            innerhtml: "innerHTML",
            scrolltop: "scrollTop",
            scrollleft: "scrollLeft",
            readonly: "readOnly"
        });
    }
    useMapping(e) {
        let s;
        let i;
        let n;
        let r;
        for (n in e) {
            s = e[n];
            i = this.Me[n] ??= t.createLookup();
            for (r in s) {
                if (i[r] !== void 0) {
                    throw createError(r, n);
                }
                i[r] = s[r];
            }
        }
    }
    useGlobalMapping(t) {
        const e = this.De;
        for (const s in t) {
            if (e[s] !== void 0) {
                throw createError(s, "*");
            }
            e[s] = t[s];
        }
    }
    useTwoWay(t) {
        this.fns.push(t);
    }
    isTwoWay(t, e) {
        return shouldDefaultToTwoWay(t, e) || this.fns.length > 0 && this.fns.some(s => s(t, e));
    }
    map(t, e) {
        return this.Me[t.nodeName]?.[e] ?? this.De[e] ?? (isDataAttribute(t, e, this.svg) ? e : null);
    }
}

AttrMapper.register = t.createImplementationRegister(e.IAttrMapper);

function shouldDefaultToTwoWay(t, e) {
    switch (t.nodeName) {
      case "INPUT":
        switch (t.type) {
          case "checkbox":
          case "radio":
            return e === "checked";

          default:
            return e === "value" || e === "files" || e === "value-as-number" || e === "value-as-date";
        }

      case "TEXTAREA":
      case "SELECT":
        return e === "value";

      default:
        switch (e) {
          case "textcontent":
          case "innerhtml":
            return t.hasAttribute("contenteditable");

          case "scrolltop":
          case "scrollleft":
            return true;

          default:
            return false;
        }
    }
}

function createError(t, e) {
    return createMappedError(719, t, e);
}

const ns = {
    register(t) {
        t.register(e.TemplateCompiler, AttrMapper, ResourceResolver);
    }
};

class BindablesInfo {
    constructor(t, e, s) {
        this.attrs = t;
        this.bindables = e;
        this.primary = s;
    }
}

class ResourceResolver {
    constructor() {
        this.qe = new WeakMap;
        this.Pe = new WeakMap;
    }
    el(t, e) {
        let s = this.qe.get(t);
        if (s == null) {
            this.qe.set(t, s = new RecordCache);
        }
        return e in s._e ? s._e[e] : s._e[e] = Je.find(t, e);
    }
    attr(t, e) {
        let s = this.qe.get(t);
        if (s == null) {
            this.qe.set(t, s = new RecordCache);
        }
        return e in s.Ie ? s.Ie[e] : s.Ie[e] = at.find(t, e);
    }
    bindables(e) {
        let s = this.Pe.get(e);
        if (s == null) {
            const i = e.bindables;
            const n = t.createLookup();
            let r;
            let l;
            let h;
            let a;
            const c = e.type === "custom-attribute";
            const u = c ? e.defaultProperty : null;
            for (l in i) {
                r = i[l];
                a = r.attribute;
                if (u != null && l === u) {
                    h = r;
                }
                n[a] = BindableDefinition.create(l, r);
            }
            if (c && h == null) {
                const t = u;
                h = n[t] = BindableDefinition.create(t);
            }
            this.Pe.set(e, s = new BindablesInfo(n, i, h ?? null));
        }
        return s;
    }
}

ResourceResolver.register = t.createImplementationRegister(e.IResourceResolver);

class RecordCache {
    constructor() {
        this._e = t.createLookup();
        this.Ie = t.createLookup();
    }
}

const os = t.createLookup();

class AttributeNSAccessor {
    static forNs(t) {
        return os[t] ??= new AttributeNSAccessor(t);
    }
    constructor(t) {
        this.ns = t;
        this.type = P | _;
    }
    getValue(t, e) {
        return t.getAttributeNS(this.ns, e);
    }
    setValue(t, e, s) {
        if (t == null) {
            e.removeAttributeNS(this.ns, s);
        } else {
            e.setAttributeNS(this.ns, s, t);
        }
    }
}

(() => {
    mixinNoopSubscribable(AttributeNSAccessor);
})();

class DataAttributeAccessor {
    constructor() {
        this.type = P | _;
    }
    getValue(t, e) {
        return t.getAttribute(e);
    }
    setValue(t, e, s) {
        if (t == null) {
            e.removeAttribute(s);
        } else {
            e.setAttribute(s, t);
        }
    }
}

(() => {
    mixinNoopSubscribable(DataAttributeAccessor);
})();

const rs = /*@__PURE__*/ new DataAttributeAccessor;

class SelectValueObserver {
    static Ve(t) {
        const e = [];
        if (t.length === 0) {
            return e;
        }
        const s = t.length;
        let i = 0;
        let n;
        while (s > i) {
            n = t[i];
            if (n.selected) {
                e[e.length] = w.call(n, "model") ? n.model : n.value;
            }
            ++i;
        }
        return e;
    }
    static Fe(t, e) {
        return t === e;
    }
    constructor(t, e, s, i) {
        this.type = P | q | _;
        this.v = void 0;
        this.ov = void 0;
        this.He = false;
        this.Oe = void 0;
        this.$e = void 0;
        this.iO = false;
        this.lt = false;
        this.rt = t;
        this.oL = i;
        this.cf = s;
    }
    getValue() {
        return this.iO ? this.v : this.rt.multiple ? SelectValueObserver.Ve(this.rt.options) : this.rt.value;
    }
    setValue(t) {
        this.ov = this.v;
        this.v = t;
        this.He = t !== this.ov;
        this.Ne(t instanceof Array ? t : null);
        this.dt();
    }
    dt() {
        if (this.He) {
            this.He = false;
            this.syncOptions();
        }
    }
    handleCollectionChange() {
        this.syncOptions();
    }
    syncOptions() {
        const e = this.v;
        const s = this.rt;
        const i = t.isArray(e);
        const n = s.matcher ?? SelectValueObserver.Fe;
        const r = s.options;
        let l = r.length;
        while (l-- > 0) {
            const t = r[l];
            const s = w.call(t, "model") ? t.model : t.value;
            if (i) {
                t.selected = e.findIndex(t => !!n(s, t)) !== -1;
                continue;
            }
            t.selected = !!n(s, e);
        }
    }
    syncValue() {
        const t = this.rt;
        const e = t.options;
        const s = e.length;
        const i = this.v;
        let n = 0;
        if (t.multiple) {
            if (!(i instanceof Array)) {
                return true;
            }
            let r;
            const l = t.matcher || SelectValueObserver.Fe;
            const h = [];
            while (n < s) {
                r = e[n];
                if (r.selected) {
                    h.push(w.call(r, "model") ? r.model : r.value);
                }
                ++n;
            }
            let a;
            n = 0;
            while (n < i.length) {
                a = i[n];
                if (h.findIndex(t => !!l(a, t)) === -1) {
                    i.splice(n, 1);
                } else {
                    ++n;
                }
            }
            n = 0;
            while (n < h.length) {
                a = h[n];
                if (i.findIndex(t => !!l(a, t)) === -1) {
                    i.push(a);
                }
                ++n;
            }
            return false;
        }
        let r = null;
        let l;
        while (n < s) {
            l = e[n];
            if (l.selected) {
                r = w.call(l, "model") ? l.model : l.value;
                break;
            }
            ++n;
        }
        this.ov = this.v;
        this.v = r;
        return true;
    }
    ht() {
        (this.$e = createMutationObserver(this.rt, this.We.bind(this))).observe(this.rt, {
            childList: true,
            subtree: true,
            characterData: true
        });
        this.Ne(this.v instanceof Array ? this.v : null);
        this.iO = true;
    }
    ct() {
        this.$e.disconnect();
        this.Oe?.unsubscribe(this);
        this.$e = this.Oe = void 0;
        this.iO = false;
    }
    Ne(t) {
        this.Oe?.unsubscribe(this);
        this.Oe = void 0;
        if (t != null) {
            if (!this.rt.multiple) {
                throw createMappedError(654);
            }
            (this.Oe = this.oL.getArrayObserver(t)).subscribe(this);
        }
    }
    handleEvent() {
        const t = this.syncValue();
        if (t) {
            this.At();
        }
    }
    We(t) {
        this.syncOptions();
        const e = this.syncValue();
        if (e) {
            this.At();
        }
    }
    At() {
        const t = this.ov;
        this.ov = this.v;
        this.subs.notify(this.v, t);
    }
}

(() => {
    mixinNodeObserverUseConfig(SelectValueObserver);
    i.subscriberCollection(SelectValueObserver, null);
})();

const ls = "--";

class StyleAttributeAccessor {
    constructor(t) {
        this.obj = t;
        this.type = P | _;
        this.v = "";
        this.ov = "";
        this.styles = {};
        this.version = 0;
        this.He = false;
    }
    getValue() {
        return this.obj.style.cssText;
    }
    setValue(t) {
        this.v = t;
        this.He = t !== this.ov;
        this.dt();
    }
    je(t) {
        const e = [];
        const s = /url\([^)]+$/;
        let i = 0;
        let n = "";
        let r;
        let l;
        let h;
        let a;
        while (i < t.length) {
            r = t.indexOf(";", i);
            if (r === -1) {
                r = t.length;
            }
            n += t.substring(i, r);
            i = r + 1;
            if (s.test(n)) {
                n += ";";
                continue;
            }
            l = n.indexOf(":");
            h = n.substring(0, l).trim();
            a = n.substring(l + 1).trim();
            e.push([ h, a ]);
            n = "";
        }
        return e;
    }
    ze(e) {
        let s;
        let i;
        const n = [];
        for (i in e) {
            s = e[i];
            if (s == null) {
                continue;
            }
            if (t.isString(s)) {
                if (i.startsWith(ls)) {
                    n.push([ i, s ]);
                    continue;
                }
                n.push([ t.kebabCase(i), s ]);
                continue;
            }
            n.push(...this.Ue(s));
        }
        return n;
    }
    Ge(e) {
        const s = e.length;
        if (s > 0) {
            const t = [];
            let i = 0;
            for (;s > i; ++i) {
                t.push(...this.Ue(e[i]));
            }
            return t;
        }
        return t.emptyArray;
    }
    Ue(e) {
        if (t.isString(e)) {
            return this.je(e);
        }
        if (e instanceof Array) {
            return this.Ge(e);
        }
        if (e instanceof Object) {
            return this.ze(e);
        }
        return t.emptyArray;
    }
    dt() {
        if (this.He) {
            this.He = false;
            const t = this.v;
            const e = this.styles;
            const s = this.Ue(t);
            let i;
            let n = this.version;
            this.ov = t;
            let r;
            let l;
            let h;
            let a = 0;
            const c = s.length;
            for (;a < c; ++a) {
                r = s[a];
                l = r[0];
                h = r[1];
                this.setProperty(l, h);
                e[l] = n;
            }
            this.styles = e;
            this.version += 1;
            if (n === 0) {
                return;
            }
            n -= 1;
            for (i in e) {
                if (!w.call(e, i) || e[i] !== n) {
                    continue;
                }
                this.obj.style.removeProperty(i);
            }
        }
    }
    setProperty(e, s) {
        let i = "";
        if (s != null && t.isFunction(s.indexOf) && s.includes("!important")) {
            i = "important";
            s = s.replace("!important", "");
        }
        this.obj.style.setProperty(e, s, i);
    }
    bind() {
        this.v = this.ov = this.obj.style.cssText;
    }
}

(() => {
    mixinNoopSubscribable(StyleAttributeAccessor);
})();

class ValueAttributeObserver {
    constructor(t, e, s) {
        this.type = P | q | _;
        this.v = "";
        this.ov = "";
        this.He = false;
        this.lt = false;
        this.rt = t;
        this.k = e;
        this.cf = s;
    }
    getValue() {
        return this.v;
    }
    setValue(e) {
        if (t.areEqual(e, this.v)) {
            return;
        }
        this.ov = this.v;
        this.v = e;
        this.He = true;
        if (!this.cf.readonly) {
            this.dt();
        }
    }
    dt() {
        if (this.He) {
            this.He = false;
            this.rt[this.k] = this.v ?? this.cf.default;
            this.At();
        }
    }
    handleEvent() {
        this.ov = this.v;
        this.v = this.rt[this.k];
        if (this.ov !== this.v) {
            this.He = false;
            this.At();
        }
    }
    ht() {
        this.v = this.ov = this.rt[this.k];
    }
    At() {
        const t = this.ov;
        this.ov = this.v;
        this.subs.notify(this.v, t);
    }
}

(() => {
    mixinNodeObserverUseConfig(ValueAttributeObserver);
    i.subscriberCollection(ValueAttributeObserver, null);
})();

const hs = (() => {
    const e = "http://www.w3.org/1999/xlink";
    const s = "http://www.w3.org/XML/1998/namespace";
    const i = "http://www.w3.org/2000/xmlns/";
    return k(t.createLookup(), {
        "xlink:actuate": [ "actuate", e ],
        "xlink:arcrole": [ "arcrole", e ],
        "xlink:href": [ "href", e ],
        "xlink:role": [ "role", e ],
        "xlink:show": [ "show", e ],
        "xlink:title": [ "title", e ],
        "xlink:type": [ "type", e ],
        "xml:lang": [ "lang", s ],
        "xml:space": [ "space", s ],
        xmlns: [ "xmlns", i ],
        "xmlns:xlink": [ "xlink", i ]
    });
})();

const as = new i.PropertyAccessor;

as.type = P | _;

class NodeObserverLocator {
    constructor() {
        this.allowDirtyCheck = true;
        this.Ke = t.createLookup();
        this.Xe = t.createLookup();
        this.Qe = t.createLookup();
        this.Ye = t.createLookup();
        this.Ze = t.resolve(t.IServiceLocator);
        this.p = t.resolve(it);
        this.Je = t.resolve(i.IDirtyChecker);
        this.svg = t.resolve(is);
        const e = [ "change", "input" ];
        const s = {
            events: e,
            default: ""
        };
        this.useConfig({
            INPUT: {
                value: s,
                valueAsNumber: {
                    events: e,
                    default: 0
                },
                checked: {
                    type: CheckedObserver,
                    events: e
                },
                files: {
                    events: e,
                    readonly: true
                }
            },
            SELECT: {
                value: {
                    type: SelectValueObserver,
                    events: [ "change" ],
                    default: ""
                }
            },
            TEXTAREA: {
                value: s
            }
        });
        const n = {
            events: [ "change", "input", "blur", "keyup", "paste" ],
            default: ""
        };
        const r = {
            events: [ "scroll" ],
            default: 0
        };
        this.useConfigGlobal({
            scrollTop: r,
            scrollLeft: r,
            textContent: n,
            innerHTML: n
        });
        this.overrideAccessorGlobal("css", "style", "class");
        this.overrideAccessor({
            INPUT: [ "value", "checked", "model" ],
            SELECT: [ "value" ],
            TEXTAREA: [ "value" ]
        });
    }
    handles(t, e) {
        return t instanceof this.p.Node;
    }
    useConfig(e, s, i) {
        const n = this.Ke;
        let r;
        if (t.isString(e)) {
            r = n[e] ??= t.createLookup();
            if (r[s] == null) {
                r[s] = i;
            } else {
                throwMappingExisted(e, s);
            }
        } else {
            for (const i in e) {
                r = n[i] ??= t.createLookup();
                const l = e[i];
                for (s in l) {
                    if (r[s] == null) {
                        r[s] = l[s];
                    } else {
                        throwMappingExisted(i, s);
                    }
                }
            }
        }
    }
    useConfigGlobal(t, e) {
        const s = this.Xe;
        if (typeof t === "object") {
            for (const e in t) {
                if (s[e] == null) {
                    s[e] = t[e];
                } else {
                    throwMappingExisted("*", e);
                }
            }
        } else {
            if (s[t] == null) {
                s[t] = e;
            } else {
                throwMappingExisted("*", t);
            }
        }
    }
    getAccessor(e, s, i) {
        if (s in this.Ye || s in (this.Qe[e.tagName] ?? t.emptyObject)) {
            return this.getObserver(e, s, i);
        }
        switch (s) {
          case "src":
          case "href":
          case "role":
          case "minLength":
          case "maxLength":
          case "placeholder":
          case "size":
          case "pattern":
          case "title":
          case "popovertarget":
          case "popovertargetaction":
            return rs;

          default:
            {
                const t = hs[s];
                if (t !== undefined) {
                    return AttributeNSAccessor.forNs(t[1]);
                }
                if (isDataAttribute(e, s, this.svg)) {
                    return rs;
                }
                return as;
            }
        }
    }
    overrideAccessor(e, s) {
        let i;
        if (t.isString(e)) {
            i = this.Qe[e] ??= t.createLookup();
            i[s] = true;
        } else {
            for (const s in e) {
                for (const n of e[s]) {
                    i = this.Qe[s] ??= t.createLookup();
                    i[n] = true;
                }
            }
        }
    }
    overrideAccessorGlobal(...t) {
        for (const e of t) {
            this.Ye[e] = true;
        }
    }
    getNodeObserverConfig(t, e) {
        return this.Ke[t.tagName]?.[e] ?? this.Xe[e];
    }
    getNodeObserver(t, e, s) {
        const n = this.Ke[t.tagName]?.[e] ?? this.Xe[e];
        let r;
        if (n != null) {
            r = new (n.type ?? ValueAttributeObserver)(t, e, n, s, this.Ze);
            if (!r.doNotCache) {
                i.getObserverLookup(t)[e] = r;
            }
            return r;
        }
        return null;
    }
    getObserver(t, e, s) {
        switch (e) {
          case "class":
            return new ClassAttributeAccessor(t);

          case "css":
          case "style":
            return new StyleAttributeAccessor(t);
        }
        const n = this.getNodeObserver(t, e, s);
        if (n != null) {
            return n;
        }
        const r = hs[e];
        if (r !== undefined) {
            return AttributeNSAccessor.forNs(r[1]);
        }
        if (isDataAttribute(t, e, this.svg)) {
            return rs;
        }
        if (e in t.constructor.prototype) {
            if (this.allowDirtyCheck) {
                return this.Je.createProperty(t, e);
            }
            throw createMappedError(652, e);
        } else {
            return new i.SetterObserver(t, e);
        }
    }
}

NodeObserverLocator.register = t.createImplementationRegister(i.INodeObserverLocator);

function getCollectionObserver(t, e) {
    if (t instanceof Array) {
        return e.getArrayObserver(t);
    }
    if (t instanceof Map) {
        return e.getMapObserver(t);
    }
    if (t instanceof Set) {
        return e.getSetObserver(t);
    }
}

function throwMappingExisted(t, e) {
    throw createMappedError(653, t, e);
}

function defaultMatcher(t, e) {
    return t === e;
}

class CheckedObserver {
    constructor(t, e, s, i) {
        this.type = P | q | _;
        this.v = void 0;
        this.ov = void 0;
        this.ts = void 0;
        this.es = void 0;
        this.lt = false;
        this.rt = t;
        this.oL = i;
        this.cf = s;
    }
    getValue() {
        return this.v;
    }
    setValue(t) {
        const e = this.v;
        if (t === e) {
            return;
        }
        this.v = t;
        this.ov = e;
        this.ss();
        this.rs();
        this.At();
    }
    handleCollectionChange() {
        this.rs();
    }
    handleChange(t, e) {
        this.rs();
    }
    rs() {
        const e = this.v;
        const s = this.rt;
        const i = w.call(s, "model") ? s.model : s.value;
        const n = s.type === "radio";
        const r = s.matcher !== void 0 ? s.matcher : defaultMatcher;
        if (n) {
            s.checked = !!r(e, i);
        } else if (e === true) {
            s.checked = true;
        } else {
            let n = false;
            if (t.isArray(e)) {
                n = e.findIndex(t => !!r(t, i)) !== -1;
            } else if (e instanceof Set) {
                for (const t of e) {
                    if (r(t, i)) {
                        n = true;
                        break;
                    }
                }
            } else if (e instanceof Map) {
                for (const t of e) {
                    const e = t[0];
                    const s = t[1];
                    if (r(e, i) && s === true) {
                        n = true;
                        break;
                    }
                }
            }
            s.checked = n;
        }
    }
    handleEvent() {
        let e = this.ov = this.v;
        const s = this.rt;
        const i = w.call(s, "model") ? s.model : s.value;
        const n = s.checked;
        const r = s.matcher !== void 0 ? s.matcher : defaultMatcher;
        if (s.type === "checkbox") {
            if (t.isArray(e)) {
                const t = e.findIndex(t => !!r(t, i));
                if (n && t === -1) {
                    e.push(i);
                } else if (!n && t !== -1) {
                    e.splice(t, 1);
                }
                return;
            } else if (e instanceof Set) {
                const t = {};
                let s = t;
                for (const t of e) {
                    if (r(t, i) === true) {
                        s = t;
                        break;
                    }
                }
                if (n && s === t) {
                    e.add(i);
                } else if (!n && s !== t) {
                    e.delete(s);
                }
                return;
            } else if (e instanceof Map) {
                let t;
                for (const s of e) {
                    const e = s[0];
                    if (r(e, i) === true) {
                        t = e;
                        break;
                    }
                }
                e.set(t, n);
                return;
            }
            e = n;
        } else if (n) {
            e = i;
        } else {
            return;
        }
        this.v = e;
        this.At();
    }
    ht() {
        this.ss();
    }
    ct() {
        this.v = this.ov = void 0;
        this.ts?.unsubscribe(this);
        this.es?.unsubscribe(this);
        this.ts = this.es = void 0;
    }
    At() {
        cs = this.ov;
        this.ov = this.v;
        this.subs.notify(this.v, cs);
        cs = void 0;
    }
    ss() {
        const t = this.rt;
        (this.es ??= t.$observers?.model ?? t.$observers?.value)?.subscribe(this);
        this.ts?.unsubscribe(this);
        this.ts = void 0;
        if (t.type === "checkbox") {
            (this.ts = getCollectionObserver(this.v, this.oL))?.subscribe(this);
        }
    }
}

(() => {
    mixinNodeObserverUseConfig(CheckedObserver);
    i.subscriberCollection(CheckedObserver, null);
})();

let cs = void 0;

class AttrBindingBehavior {
    bind(t, e) {
        if (!(e instanceof PropertyBinding)) {
            throw createMappedError(9994, e);
        }
        e.useTargetObserver(rs);
    }
}

AttrBindingBehavior.$au = {
    type: U,
    name: "attr"
};

class SelfBindingBehavior {
    bind(t, e) {
        if (!("handleEvent" in e)) {
            throw createMappedError(801);
        }
        e.self = true;
    }
    unbind(t, e) {
        e.self = false;
    }
}

SelfBindingBehavior.$au = {
    type: U,
    name: "self"
};

class UpdateTriggerBindingBehavior {
    constructor() {
        this.oL = t.resolve(i.IObserverLocator);
        this.ls = t.resolve(i.INodeObserverLocator);
    }
    bind(t, e, ...s) {
        if (!(this.ls instanceof NodeObserverLocator)) {
            throw createMappedError(9993);
        }
        if (s.length === 0) {
            throw createMappedError(802);
        }
        if (!(e instanceof PropertyBinding) || !(e.mode & c)) {
            throw createMappedError(803);
        }
        const i = this.ls.getNodeObserverConfig(e.target, e.targetProperty);
        if (i == null) {
            throw createMappedError(9992, e);
        }
        const n = this.ls.getNodeObserver(e.target, e.targetProperty, this.oL);
        n.useConfig({
            readonly: i.readonly,
            default: i.default,
            events: s
        });
        e.useTargetObserver(n);
    }
}

UpdateTriggerBindingBehavior.$au = {
    type: U,
    name: "updateTrigger"
};

class If {
    constructor() {
        this.elseFactory = void 0;
        this.elseView = void 0;
        this.ifView = void 0;
        this.view = void 0;
        this.value = false;
        this.cache = true;
        this.pending = void 0;
        this.cs = false;
        this.us = 0;
        this.ds = t.resolve(Bt);
        this.l = t.resolve($e);
        this.p = t.resolve(it);
    }
    attaching(t, e) {
        const s = this.$controller.ssrScope;
        if (s != null && isSSRTemplateController(s) && s.type === "if") {
            return this.ps(s);
        }
        return this.gs(this.value);
    }
    detaching(e, s) {
        this.cs = true;
        return t.onResolve(this.pending, () => {
            this.cs = false;
            this.pending = void 0;
            void this.view?.deactivate(e, this.$controller);
        });
    }
    valueChanged(t, e) {
        if (!this.$controller.isActive) return;
        t = !!t;
        e = !!e;
        if (t !== e) return this.gs(t);
    }
    gs(e) {
        const s = this.view;
        const i = this.$controller;
        const n = this.us++;
        const isCurrent = () => !this.cs && this.us === n + 1;
        let r;
        return t.onResolve(this.pending, () => this.pending = t.onResolve(s?.isActive ? s.deactivate(s, i) : void 0, () => {
            if (!isCurrent()) {
                return;
            }
            if (e) {
                r = this.view = this.ifView = this.cache && this.ifView != null ? this.ifView : this.ds.create(i);
            } else {
                r = this.view = this.elseView = this.cache && this.elseView != null ? this.elseView : this.elseFactory?.create(i);
            }
            if (r == null) {
                return;
            }
            r.setLocation(this.l);
            const t = r.activate(r, i, i.scope);
            if (t instanceof Promise) {
                return t.then(() => {
                    if (isCurrent()) {
                        this.pending = void 0;
                    }
                }, () => {
                    if (isCurrent()) {
                        this.pending = void 0;
                    }
                    void r.deactivate(r, i);
                });
            }
            if (isCurrent()) {
                this.pending = void 0;
            }
        }));
    }
    ps(t) {
        const e = this.$controller;
        const s = t.state?.value === true;
        const i = s ? this.ds : this.elseFactory;
        if (i == null || t.views.length === 0) {
            e.ssrScope = void 0;
            return;
        }
        const n = adoptSSRView(t, i, e, this.l, this.p);
        if (n == null) {
            e.ssrScope = void 0;
            return;
        }
        const {view: r} = n;
        if (s) {
            this.view = this.ifView = r;
        } else {
            this.view = this.elseView = r;
        }
        e.ssrScope = void 0;
        return r.activate(r, e, e.scope);
    }
    dispose() {
        this.ifView?.dispose();
        this.elseView?.dispose();
        this.ifView = this.elseView = this.view = void 0;
    }
    accept(t) {
        if (this.view?.accept(t) === true) {
            return true;
        }
    }
}

If.$au = {
    type: lt,
    name: "if",
    isTemplateController: true,
    bindables: {
        value: true,
        cache: {
            set: t => t === "" || !!t && t !== "false"
        }
    }
};

class Else {
    constructor() {
        this.f = t.resolve(Bt);
    }
    link(t, e, s, i) {
        const n = t.children;
        const r = n[n.length - 1];
        if (r instanceof If) {
            r.elseFactory = this.f;
        } else if (r.viewModel instanceof If) {
            r.viewModel.elseFactory = this.f;
        } else {
            throw createMappedError(810);
        }
    }
}

Else.$au = {
    type: "custom-attribute",
    name: "else",
    isTemplateController: true
};

function dispose(t) {
    t.dispose();
}

const us = [ "BindingBehavior", "ValueConverter" ];

class Repeat {
    constructor() {
        this.views = [];
        this.key = null;
        this.contextual = true;
        this.xs = [];
        this.ys = [];
        this.ws = [];
        this.bs = new Map;
        this.ks = void 0;
        this.Cs = false;
        this.Ss = false;
        this.Bs = null;
        this.As = void 0;
        this.Rs = false;
        this.Ts = false;
        this.l = t.resolve($e);
        this.Es = t.resolve(Pe);
        this.f = t.resolve(Bt);
        this.Ls = t.resolve(ms);
        this.p = t.resolve(it);
        const s = t.resolve(e.IInstruction);
        const i = s.props[0].props;
        for (let e = 0, s = i.length; e < s; ++e) {
            const s = i[e];
            const {to: n, value: l, command: h} = s;
            if (n === "key") {
                if (h === null) {
                    this.key = l;
                } else if (h === "bind") {
                    this.key = typeof l === "string" ? t.resolve(r.IExpressionParser).parse(l, L) : l;
                } else {
                    throw createMappedError(775, h);
                }
            } else if (n === "contextual") {
                if (h === null) {
                    this.contextual = l === "false" ? false : !!l;
                } else if (h === "bind") {
                    this.Ms = typeof l === "string" ? t.resolve(r.IExpressionParser).parse(l, L) : l;
                } else {
                    throw createMappedError(821, h);
                }
            } else {
                throw createMappedError(776, n);
            }
        }
    }
    binding(t, e) {
        const s = this.Es.bindings;
        const n = s.length;
        let r = void 0;
        let l;
        let h = 0;
        for (;n > h; ++h) {
            r = s[h];
            if (r.target === this && r.targetProperty === "items") {
                l = this.forOf = r.ast;
                this.Ds = r;
                let t = l.iterable;
                while (t != null && us.includes(t.$kind)) {
                    t = t.expression;
                    this.Cs = true;
                }
                this.Bs = t;
                break;
            }
        }
        this.qs();
        const a = l.declaration;
        if (!(this.Rs = a.$kind === "ArrayDestructuring" || a.$kind === "ObjectDestructuring")) {
            this.local = i.astEvaluate(a, this.$controller.scope, r, null);
        }
        if (this.Ms !== void 0) {
            const t = i.astEvaluate(this.Ms, this.$controller.scope, r, null);
            this.contextual = t != null && t !== false;
        }
    }
    attaching(e, s) {
        this.Ps();
        this._s(void 0);
        return this.Is(e, this.As ?? t.emptyArray);
    }
    detaching(t, e) {
        this.qs();
        const s = this.Ts;
        this.Ts = false;
        const i = this.Vs(t, s);
        if (s) {
            this.views = [];
        }
        return i;
    }
    unbinding(t, e) {
        this.bs.clear();
    }
    itemsChanged() {
        if (!this.$controller.isActive) {
            return;
        }
        this.qs();
        this.Ps();
        this._s(void 0);
        this.Fs(void 0);
    }
    handleCollectionChange(t, e) {
        const s = this.$controller;
        if (!s.isActive) {
            return;
        }
        if (this.Cs) {
            if (this.Ss) {
                return;
            }
            this.Ss = true;
            this.items = i.astEvaluate(this.forOf.iterable, s.scope, this.Ds, null);
            this.Ss = false;
            return;
        }
        this.Ps();
        this._s(this.key === null ? e : void 0);
        this.Fs(e);
    }
    Fs(e) {
        const s = this.views;
        this.xs = s.slice();
        const n = s.length;
        const r = this.key !== null;
        const l = this.ws;
        const h = this.ys;
        if (r || e === void 0) {
            const t = this.local;
            const s = this.forOf.declaration;
            const r = this.Ds;
            const a = this.Rs;
            const c = h.length;
            e = i.createIndexMap(c);
            if (n === 0) {
                for (let t = 0; t < c; ++t) {
                    e[t] = -2;
                }
            } else if (c === 0) {
                for (let i = 0; i < n; ++i) {
                    e.deletedIndices.push(i);
                    e.deletedItems.push(getItem(a, s, l[i], r, t));
                }
            } else {
                const i = new Map;
                for (let t = 0; t < n; ++t) {
                    i.set(l[t], t);
                }
                const u = new Set;
                for (let t = 0; t < c; ++t) {
                    const s = i.get(h[t]);
                    if (s !== void 0) {
                        e[t] = s;
                        u.add(s);
                    } else {
                        e[t] = -2;
                    }
                }
                for (let i = 0; i < n; ++i) {
                    if (!u.has(i)) {
                        e.deletedIndices.push(i);
                        e.deletedItems.push(getItem(a, s, l[i], r, t));
                    }
                }
            }
        }
        if (e.deletedIndices.length > 0) {
            const s = t.onResolve(this.Hs(e), () => this.Os(e));
            if (t.isPromise(s)) {
                s.catch(rethrow);
            }
        } else {
            this.Os(e);
        }
    }
    qs() {
        const e = this.$controller.scope;
        let s = this.$s;
        let n = this.Cs;
        let r;
        if (n) {
            s = this.$s = i.astEvaluate(this.Bs, e, this.Ds, null) ?? null;
            n = this.Cs = !t.areEqual(this.items, s);
        }
        const l = this.ks;
        if (this.$controller.isActive) {
            const t = n ? s : this.items;
            r = this.ks = this.Ls.resolve(t).getObserver?.(t);
            if (l !== r) {
                l?.unsubscribe(this);
                r?.subscribe(this);
            }
        } else {
            l?.unsubscribe(this);
            this.ks = undefined;
        }
    }
    _s(t) {
        const e = this.ys;
        this.ws = e.slice();
        const s = this.As;
        const n = s.length;
        const r = this.ys = Array(n);
        const l = this.bs;
        const h = new Map;
        const a = this.$controller.scope;
        const c = this.Ds;
        const u = this.forOf;
        const f = this.local;
        const d = this.Rs;
        if (t === void 0) {
            const t = this.key;
            const e = t !== null;
            if (e) {
                const e = Array(n);
                if (typeof t === "string") {
                    for (let i = 0; i < n; ++i) {
                        e[i] = s[i][t];
                    }
                } else {
                    for (let r = 0; r < n; ++r) {
                        const n = createScope(s[r], u, a, c, f, d);
                        setItem(d, u.declaration, n, c, f, s[r]);
                        e[r] = i.astEvaluate(t, n, c, null);
                    }
                }
                for (let t = 0; t < n; ++t) {
                    r[t] = getScope(l, h, e[t], s[t], u, a, c, f, d);
                }
            } else {
                for (let t = 0; t < n; ++t) {
                    r[t] = getScope(l, h, s[t], s[t], u, a, c, f, d);
                }
            }
        } else {
            const i = e.length;
            for (let l = 0; l < n; ++l) {
                const n = t[l];
                if (n >= 0 && n < i) {
                    r[l] = e[n];
                } else {
                    r[l] = createScope(s[l], u, a, c, f, d);
                }
                setItem(d, u.declaration, r[l], c, f, s[l]);
            }
        }
        l.clear();
        this.bs = h;
    }
    Ps() {
        const e = this.items;
        if (t.isArray(e)) {
            this.As = e.slice(0);
            return;
        }
        const s = [];
        this.Ls.resolve(e).iterate(e, (t, e) => {
            s[e] = t;
        });
        this.As = s;
    }
    Is(t, e) {
        const s = this.$controller.ssrScope;
        if (s != null && isSSRTemplateController(s) && s.type === "repeat") {
            return this.Ns(t, e, s);
        }
        return this.Ws(t, e);
    }
    Ns(e, s, i) {
        const {$controller: n, f: r, l: l, ys: h, p: a} = this;
        const c = s.length;
        const {views: u} = adoptSSRViews(i, r, n, l, a);
        if (u.length === 0) {
            n.ssrScope = undefined;
            return this.Ws(e, s);
        }
        this.Ts = true;
        this.views = u;
        let f = void 0;
        for (let i = 0; i < c; ++i) {
            const r = u[i];
            const l = h[i];
            if (this.contextual) {
                setContextualProperties(l.overrideContext, i, c, s);
            }
            const a = r.activate(e ?? r, n, l);
            if (t.isPromise(a)) {
                (f ??= []).push(a);
            }
        }
        n.ssrScope = undefined;
        if (f !== void 0) {
            return f.length === 1 ? f[0] : Promise.all(f);
        }
    }
    Ws(e, s) {
        const {$controller: i, f: n, l: r, ys: l} = this;
        const h = s.length;
        const a = this.views = Array(h);
        let c = void 0;
        for (let u = 0; u < h; ++u) {
            const f = a[u] = n.create(i).setLocation(r);
            f.nodes.unlink();
            const d = l[u];
            if (this.contextual) {
                setContextualProperties(d.overrideContext, u, h, s);
            }
            const p = f.activate(e ?? f, i, d);
            if (t.isPromise(p)) {
                (c ??= []).push(p);
            }
        }
        if (c !== void 0) {
            return c.length === 1 ? c[0] : Promise.all(c);
        }
    }
    Vs(e, s = false) {
        let i = void 0;
        let n;
        let r;
        let l = 0;
        const {views: h, $controller: a} = this;
        const c = h.length;
        for (;c > l; ++l) {
            r = h[l];
            if (!s) {
                r.release();
            }
            n = r.deactivate(e ?? r, a);
            if (t.isPromise(n)) {
                (i ?? (i = [])).push(n);
            }
        }
        if (i !== void 0) {
            return i.length === 1 ? i[0] : Promise.all(i);
        }
    }
    Hs(e) {
        let s = void 0;
        let i;
        let n;
        const {$controller: r, views: l} = this;
        const h = e.deletedIndices.slice().sort(compareNumber);
        const a = h.length;
        let c = 0;
        for (;a > c; ++c) {
            n = l[h[c]];
            n.release();
            i = n.deactivate(n, r);
            if (t.isPromise(i)) {
                (s ?? (s = [])).push(i);
            }
        }
        c = 0;
        for (;a > c; ++c) {
            l.splice(h[c] - c, 1);
        }
        if (s !== void 0) {
            return s.length === 1 ? s[0] : Promise.all(s);
        }
    }
    Os(e) {
        let s = void 0;
        let i;
        let n;
        let r = 0;
        const {$controller: l, f: h, l: a, views: c, ys: u, xs: f} = this;
        const d = e.length;
        for (;d > r; ++r) {
            if (e[r] === -2) {
                n = h.create(l);
                c.splice(r, 0, n);
            }
        }
        if (c.length !== d) {
            throw createMappedError(814, [ c.length, d ]);
        }
        let p = 0;
        r = 0;
        for (;r < e.length; ++r) {
            if ((p = e[r]) !== -2) {
                c[r] = f[p];
            }
        }
        const m = longestIncreasingSubsequence(e);
        const g = m.length;
        let x;
        let v = g - 1;
        r = d - 1;
        for (;r >= 0; --r) {
            n = c[r];
            x = c[r + 1];
            if (this.contextual) {
                setContextualProperties(u[r].overrideContext, r, d, this.As);
            }
            if (e[r] === -2) {
                n.nodes.link(x?.nodes ?? a);
                n.setLocation(a);
                i = n.activate(n, l, u[r]);
                if (t.isPromise(i)) {
                    (s ?? (s = [])).push(i);
                }
            } else if (v < 0 || r !== m[v]) {
                n.nodes.link(x?.nodes ?? a);
                n.nodes.insertBefore(n.location);
            } else {
                --v;
            }
        }
        if (s !== void 0) {
            return s.length === 1 ? s[0] : Promise.all(s);
        }
    }
    dispose() {
        this.views.forEach(dispose);
        this.views = void 0;
    }
    accept(t) {
        const {views: e} = this;
        if (e !== void 0) {
            for (let s = 0, i = e.length; s < i; ++s) {
                const i = e[s].accept(t);
                if (i === true) {
                    return true;
                }
            }
        }
    }
}

Repeat.$au = {
    type: lt,
    name: "repeat",
    isTemplateController: true,
    defaultProperty: "items",
    bindables: [ "items" ]
};

let fs = 16;

let ds = new Int32Array(fs);

let ps = new Int32Array(fs);

function longestIncreasingSubsequence(t) {
    const e = t.length;
    if (e > fs) {
        fs = e;
        ds = new Int32Array(e);
        ps = new Int32Array(e);
    }
    let s = 0;
    let i = 0;
    let n = 0;
    let r = 0;
    let l = 0;
    let h = 0;
    let a = 0;
    let c = 0;
    for (;r < e; r++) {
        i = t[r];
        if (i !== -2) {
            l = ds[s];
            n = t[l];
            if (n !== -2 && n < i) {
                ps[r] = l;
                ds[++s] = r;
                continue;
            }
            h = 0;
            a = s;
            while (h < a) {
                c = h + a >> 1;
                n = t[ds[c]];
                if (n !== -2 && n < i) {
                    h = c + 1;
                } else {
                    a = c;
                }
            }
            n = t[ds[h]];
            if (i < n || n === -2) {
                if (h > 0) {
                    ps[r] = ds[h - 1];
                }
                ds[h] = r;
            }
        }
    }
    r = ++s;
    const u = new Int32Array(r);
    i = ds[s - 1];
    while (s-- > 0) {
        u[s] = i;
        i = ps[i];
    }
    while (r-- > 0) ds[r] = 0;
    return u;
}

class RepeatOverrideContext {
    get $odd() {
        return !this.$even;
    }
    get $even() {
        return this.$index % 2 === 0;
    }
    get $first() {
        return this.$index === 0;
    }
    get $middle() {
        return !this.$first && !this.$last;
    }
    get $last() {
        return this.$index === this.$length - 1;
    }
    get $previous() {
        return this.js?.[this.$index - 1];
    }
    constructor(t = 0, e = 1, s = undefined) {
        this.$index = t;
        this.$length = e;
        this.js = s;
    }
}

const setContextualProperties = (t, e, s, i) => {
    t.$index = e;
    t.$length = s;
    t.js = i;
};

const ms = /*@__PURE__*/ H("IRepeatableHandlerResolver", t => t.singleton(RepeatableHandlerResolver));

class RepeatableHandlerResolver {
    constructor() {
        this.zs = t.resolve(t.all(gs));
    }
    resolve(t) {
        if (xs.handles(t)) {
            return xs;
        }
        if (vs.handles(t)) {
            return vs;
        }
        if (ys.handles(t)) {
            return ys;
        }
        if (ws.handles(t)) {
            return ws;
        }
        if (bs.handles(t)) {
            return bs;
        }
        const e = this.zs.find(e => e.handles(t));
        if (e !== void 0) {
            return e;
        }
        return ks;
    }
}

class ArrayLikeHandler {
    static register(t) {
        t.register(O(gs, this));
    }
    handles(e) {
        return "length" in e && t.isNumber(e.length);
    }
    iterate(t, e) {
        for (let s = 0, i = t.length; s < i; ++s) {
            e(t[s], s, t);
        }
    }
}

const gs = /*@__PURE__*/ H("IRepeatableHandler");

const xs = {
    handles: t.isArray,
    getObserver: i.getCollectionObserver,
    iterate(t, e) {
        const s = t.length;
        let i = 0;
        for (;i < s; ++i) {
            e(t[i], i, t);
        }
    }
};

const vs = {
    handles: t.isSet,
    getObserver: i.getCollectionObserver,
    iterate(t, e) {
        let s = 0;
        let i;
        for (i of t.keys()) {
            e(i, s++, t);
        }
    }
};

const ys = {
    handles: t.isMap,
    getObserver: i.getCollectionObserver,
    iterate(t, e) {
        let s = 0;
        let i;
        for (i of t.entries()) {
            e(i, s++, t);
        }
    }
};

const ws = {
    handles: t.isNumber,
    iterate(t, e) {
        let s = 0;
        for (;s < t; ++s) {
            e(s, s, t);
        }
    }
};

const bs = {
    handles: t => t == null,
    iterate() {}
};

const ks = {
    handles(t) {
        return false;
    },
    iterate(t, e) {
        throw createMappedError(777, t);
    }
};

const setItem = (t, e, s, n, r, l) => {
    if (t) {
        i.astAssign(e, s, n, null, l);
    } else {
        s.bindingContext[r] = l;
    }
};

const getItem = (t, e, s, n, r) => t ? i.astEvaluate(e, s, n, null) : s.bindingContext[r];

const getScope = (t, e, s, n, r, l, h, a, c) => {
    let u = t.get(s);
    if (u === void 0) {
        u = createScope(n, r, l, h, a, c);
    } else if (u instanceof i.Scope) {
        t.delete(s);
    } else if (u.length === 1) {
        u = u[0];
        t.delete(s);
    } else {
        u = u.shift();
    }
    if (e.has(s)) {
        const t = e.get(s);
        if (t instanceof i.Scope) {
            e.set(s, [ t, u ]);
        } else {
            t.push(u);
        }
    } else {
        e.set(s, u);
    }
    setItem(c, r.declaration, u, h, a, n);
    return u;
};

const createScope = (t, e, s, n, r, l) => {
    if (l) {
        const r = i.Scope.fromParent(s, new i.BindingContext, new RepeatOverrideContext);
        i.astAssign(e.declaration, r, n, null, t);
        return r;
    }
    return i.Scope.fromParent(s, new i.BindingContext(r, t), new RepeatOverrideContext);
};

const compareNumber = (t, e) => t - e;

class With {
    constructor() {
        this.f = t.resolve(Bt);
        this.l = t.resolve($e);
        this.p = t.resolve(it);
    }
    valueChanged(t, e) {
        const s = this.$controller;
        const n = this.view.bindings;
        let r;
        let l = 0, h = 0;
        if (s.isActive && n != null) {
            r = i.Scope.fromParent(s.scope, t === void 0 ? {} : t);
            for (h = n.length; h > l; ++l) {
                n[l].bind(r);
            }
        }
    }
    attaching(t, e) {
        const {$controller: s, value: n} = this;
        const r = s.ssrScope;
        if (r != null && isSSRTemplateController(r) && r.type === "with") {
            return this.ps(r);
        }
        const l = this.view = this.f.create(s).setLocation(this.l);
        const h = i.Scope.fromParent(s.scope, n === void 0 ? {} : n);
        return l.activate(l, s, h);
    }
    ps(t) {
        const {$controller: e, value: s, f: n, l: r, p: l} = this;
        const h = adoptSSRView(t, n, e, r, l);
        if (h == null) {
            e.ssrScope = undefined;
            const t = this.view = n.create(e).setLocation(r);
            const l = i.Scope.fromParent(e.scope, s === void 0 ? {} : s);
            return t.activate(t, e, l);
        }
        this.view = h.view;
        e.ssrScope = undefined;
        const a = i.Scope.fromParent(e.scope, s === void 0 ? {} : s);
        return h.view.activate(h.view, e, a);
    }
    detaching(t, e) {
        return this.view.deactivate(t, this.$controller);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
    accept(t) {
        if (this.view?.accept(t) === true) {
            return true;
        }
    }
}

With.$au = {
    type: lt,
    name: "with",
    isTemplateController: true,
    bindables: [ "value" ]
};

class Switch {
    constructor() {
        this.cases = [];
        this.activeCases = [];
        this.promise = void 0;
        this.f = t.resolve(Bt);
        this.l = t.resolve($e);
        this.p = t.resolve(it);
    }
    link(t, e, s, i) {
        const n = this.$controller.ssrScope;
        if (n != null && isSSRTemplateController(n) && n.type === "switch") {
            return;
        }
        this.view = this.f.create(this.$controller).setLocation(this.l);
    }
    attaching(t, e) {
        let s = this.view;
        const i = this.$controller;
        const n = i.ssrScope;
        if (n != null && isSSRTemplateController(n) && n.type === "switch") {
            const t = adoptSSRView(n, this.f, i, this.l, this.p);
            if (t != null) {
                s?.dispose();
                s = this.view = t.view;
            }
            i.ssrScope = undefined;
        }
        if (s === void 0) {
            s = this.view = this.f.create(this.$controller).setLocation(this.l);
        }
        this.queue(() => s.activate(t, i, i.scope));
        this.queue(() => this.swap(t, this.value));
        return this.promise;
    }
    detaching(t, e) {
        this.queue(() => {
            const e = this.view;
            return e.deactivate(t, this.$controller);
        });
        return this.promise;
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
    valueChanged(t, e) {
        if (!this.$controller.isActive) {
            return;
        }
        this.queue(() => this.swap(null, this.value));
    }
    caseChanged(t) {
        this.queue(() => this.Us(t));
    }
    Us(e) {
        const s = e.isMatch(this.value);
        const i = this.activeCases;
        const n = i.length;
        if (!s) {
            if (n > 0 && i[0].id === e.id) {
                return this.Gs(null);
            }
            return;
        }
        if (n > 0 && i[0].id < e.id) {
            return;
        }
        const r = [];
        let l = e.fallThrough;
        if (!l) {
            r.push(e);
        } else {
            const t = this.cases;
            const s = t.indexOf(e);
            for (let e = s, i = t.length; e < i && l; e++) {
                const s = t[e];
                r.push(s);
                l = s.fallThrough;
            }
        }
        return t.onResolve(this.Gs(null, r), () => {
            this.activeCases = r;
            return this.Ks(null);
        });
    }
    swap(e, s) {
        const i = [];
        let n = false;
        for (const t of this.cases) {
            if (n || t.isMatch(s)) {
                i.push(t);
                n = t.fallThrough;
            }
            if (i.length > 0 && !n) {
                break;
            }
        }
        const r = this.defaultCase;
        if (i.length === 0 && r !== void 0) {
            i.push(r);
        }
        return t.onResolve(this.activeCases.length > 0 ? this.Gs(e, i) : void 0, () => {
            this.activeCases = i;
            if (i.length === 0) {
                return;
            }
            return this.Ks(e);
        });
    }
    Ks(e) {
        const s = this.$controller;
        if (!s.isActive) {
            return;
        }
        const i = this.activeCases;
        const n = i.length;
        if (n === 0) {
            return;
        }
        const r = s.scope;
        if (n === 1) {
            return i[0].activate(e, r);
        }
        return t.onResolveAll(...i.map(t => t.activate(e, r)));
    }
    Gs(e, s = []) {
        const i = this.activeCases;
        const n = i.length;
        if (n === 0) {
            return;
        }
        if (n === 1) {
            const t = i[0];
            if (!s.includes(t)) {
                i.length = 0;
                return t.deactivate(e);
            }
            return;
        }
        return t.onResolve(t.onResolveAll(...i.reduce((t, i) => {
            if (!s.includes(i)) {
                t.push(i.deactivate(e));
            }
            return t;
        }, [])), () => {
            i.length = 0;
        });
    }
    queue(e) {
        const s = this.promise;
        let i = void 0;
        i = this.promise = t.onResolve(t.onResolve(s, e), () => {
            if (this.promise === i) {
                this.promise = void 0;
            }
        });
    }
    accept(t) {
        if (this.$controller.accept(t) === true) {
            return true;
        }
        if (this.activeCases.some(e => e.accept(t))) {
            return true;
        }
    }
}

Switch.$au = {
    type: lt,
    name: "switch",
    isTemplateController: true,
    bindables: [ "value" ]
};

let Cs = 0;

const Ss = [ "value", {
    name: "fallThrough",
    mode: h,
    set(t) {
        switch (t) {
          case "true":
            return true;

          case "false":
            return false;

          default:
            return !!t;
        }
    }
} ];

class Case {
    constructor() {
        this.id = ++Cs;
        this.fallThrough = false;
        this.view = void 0;
        this.f = t.resolve(Bt);
        this.Ze = t.resolve(i.IObserverLocator);
        this.l = t.resolve($e);
        this.p = t.resolve(it);
        this.Xs = t.resolve(t.ILogger).scopeTo(`Case-#${this.id}`);
    }
    link(t, e, s, i) {
        const n = t.parent;
        const r = n?.viewModel;
        if (r instanceof Switch) {
            this.$switch = r;
            this.linkToSwitch(r);
        } else {
            throw createMappedError(815);
        }
    }
    detaching(t, e) {
        return this.deactivate(t);
    }
    isMatch(e) {
        const s = this.value;
        if (t.isArray(s)) {
            if (this.ks === void 0) {
                this.ks = this.Qs(s);
            }
            return s.includes(e);
        }
        return s === e;
    }
    valueChanged(e, s) {
        if (t.isArray(e)) {
            this.ks?.unsubscribe(this);
            this.ks = this.Qs(e);
        } else if (this.ks !== void 0) {
            this.ks.unsubscribe(this);
        }
        this.$switch.caseChanged(this);
    }
    handleCollectionChange() {
        this.$switch.caseChanged(this);
    }
    activate(t, e) {
        let s = this.view;
        if (s === void 0) {
            const t = this.$controller.ssrScope;
            if (t != null && isSSRTemplateController(t) && (t.type === "case" || t.type === "default-case")) {
                const e = adoptSSRView(t, this.f, this.$controller, this.l, this.p);
                if (e != null) {
                    s = this.view = e.view;
                }
                this.$controller.ssrScope = undefined;
            }
            if (s === void 0) {
                s = this.view = this.f.create(this.$controller).setLocation(this.l);
            }
        }
        if (s.isActive) {
            return;
        }
        const i = s.activate(t ?? s, this.$controller, e);
        if (i instanceof Promise) {
            return i.catch(() => s.deactivate(s, this.$controller));
        }
    }
    deactivate(t) {
        const e = this.view;
        if (e === void 0 || !e.isActive) {
            return;
        }
        return e.deactivate(t ?? e, this.$controller);
    }
    dispose() {
        this.ks?.unsubscribe(this);
        this.view?.dispose();
        this.view = void 0;
    }
    linkToSwitch(t) {
        t.cases.push(this);
    }
    Qs(t) {
        const e = this.Ze.getArrayObserver(t);
        e.subscribe(this);
        return e;
    }
    accept(t) {
        if (this.$controller.accept(t) === true) {
            return true;
        }
        return this.view?.accept(t);
    }
}

(() => {
    defineAttribute({
        name: "case",
        bindables: Ss,
        isTemplateController: true
    }, Case);
})();

class DefaultCase extends Case {
    linkToSwitch(t) {
        if (t.defaultCase !== void 0) {
            throw createMappedError(816);
        }
        t.defaultCase = this;
    }
}

(() => {
    defineAttribute({
        name: "default-case",
        bindables: Ss,
        isTemplateController: true
    }, DefaultCase);
})();

var Bs, As, Rs;

class PromiseTemplateController {
    constructor() {
        this.preSettledTask = null;
        this.postSettledTask = null;
        this.f = t.resolve(Bt);
        this.l = t.resolve($e);
        this.logger = t.resolve(t.ILogger).scopeTo("promise.resolve");
    }
    link(t, e, s, i) {
        this.view = this.f.create(this.$controller).setLocation(this.l);
    }
    attaching(e, s) {
        const n = this.view;
        const r = this.$controller;
        return t.onResolve(n.activate(e, r, this.viewScope = i.Scope.fromParent(r.scope, {})), () => this.swap(e));
    }
    valueChanged(t, e) {
        if (!this.$controller.isActive) {
            return;
        }
        this.swap(null);
    }
    swap(e) {
        const s = this.value;
        if (!t.isPromise(s)) {
            return;
        }
        const n = this.fulfilled;
        const r = this.rejected;
        const l = this.pending;
        const h = this.viewScope;
        let a;
        const $swap = () => {
            void t.onResolveAll(a = (this.preSettledTask = i.queueAsyncTask(() => t.onResolveAll(n?.deactivate(e), r?.deactivate(e), l?.activate(e, h)))).result.catch(t => {
                throw t;
            }), s.then(c => {
                if (this.value !== s) {
                    return;
                }
                const fulfill = () => {
                    this.postSettlePromise = (this.postSettledTask = i.queueAsyncTask(() => t.onResolveAll(l?.deactivate(e), r?.deactivate(e), n?.activate(e, h, c)))).result;
                };
                if (this.preSettledTask.status === D) {
                    void a.then(fulfill);
                } else {
                    this.preSettledTask.cancel();
                    fulfill();
                }
            }, c => {
                if (this.value !== s) {
                    return;
                }
                const reject = () => {
                    this.postSettlePromise = (this.postSettledTask = i.queueAsyncTask(() => t.onResolveAll(l?.deactivate(e), n?.deactivate(e), r?.activate(e, h, c)))).result;
                };
                if (this.preSettledTask.status === D) {
                    void a.then(reject);
                } else {
                    this.preSettledTask.cancel();
                    reject();
                }
            }));
        };
        if (this.postSettledTask?.status === D) {
            void this.postSettlePromise.then($swap);
        } else {
            this.postSettledTask?.cancel();
            $swap();
        }
    }
    detaching(t, e) {
        this.preSettledTask?.cancel();
        this.postSettledTask?.cancel();
        this.preSettledTask = this.postSettledTask = null;
        return this.view.deactivate(t, this.$controller);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
}

PromiseTemplateController.$au = {
    type: lt,
    name: "promise",
    isTemplateController: true,
    bindables: [ "value" ]
};

class PendingTemplateController {
    constructor() {
        this.view = void 0;
        this.f = t.resolve(Bt);
        this.l = t.resolve($e);
    }
    link(t, e, s, i) {
        getPromiseController(t).pending = this;
    }
    activate(t, e) {
        let s = this.view;
        if (s === void 0) {
            s = this.view = this.f.create(this.$controller).setLocation(this.l);
        }
        if (s.isActive) {
            return;
        }
        return s.activate(s, this.$controller, e);
    }
    deactivate(t) {
        const e = this.view;
        if (e === void 0 || !e.isActive) {
            return;
        }
        return e.deactivate(e, this.$controller);
    }
    detaching(t) {
        return this.deactivate(t);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
}

PendingTemplateController.$au = {
    type: lt,
    name: "pending",
    isTemplateController: true,
    bindables: {
        value: {
            mode: a
        }
    }
};

class FulfilledTemplateController {
    constructor() {
        this.view = void 0;
        this.f = t.resolve(Bt);
        this.l = t.resolve($e);
    }
    link(t, e, s, i) {
        getPromiseController(t).fulfilled = this;
    }
    activate(t, e, s) {
        this.value = s;
        let i = this.view;
        if (i === void 0) {
            i = this.view = this.f.create(this.$controller).setLocation(this.l);
        }
        if (i.isActive) {
            return;
        }
        return i.activate(i, this.$controller, e);
    }
    deactivate(t) {
        const e = this.view;
        if (e === void 0 || !e.isActive) {
            return;
        }
        return e.deactivate(e, this.$controller);
    }
    detaching(t, e) {
        return this.deactivate(t);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
}

FulfilledTemplateController.$au = {
    type: lt,
    name: "then",
    isTemplateController: true,
    bindables: {
        value: {
            mode: c
        }
    }
};

class RejectedTemplateController {
    constructor() {
        this.view = void 0;
        this.f = t.resolve(Bt);
        this.l = t.resolve($e);
    }
    link(t, e, s, i) {
        getPromiseController(t).rejected = this;
    }
    activate(t, e, s) {
        this.value = s;
        let i = this.view;
        if (i === void 0) {
            i = this.view = this.f.create(this.$controller).setLocation(this.l);
        }
        if (i.isActive) {
            return;
        }
        return i.activate(i, this.$controller, e);
    }
    deactivate(t) {
        const e = this.view;
        if (e === void 0 || !e.isActive) {
            return;
        }
        return e.deactivate(e, this.$controller);
    }
    detaching(t, e) {
        return this.deactivate(t);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
}

RejectedTemplateController.$au = {
    type: lt,
    name: "catch",
    isTemplateController: true,
    bindables: {
        value: {
            mode: c
        }
    }
};

function getPromiseController(t) {
    const e = t.parent;
    const s = e?.viewModel;
    if (s instanceof PromiseTemplateController) {
        return s;
    }
    throw createMappedError(813);
}

class PromiseAttributePattern {
    "promise.resolve"(t, s) {
        return new e.AttrSyntax(t, s, "promise", "bind");
    }
}

Bs = Symbol.metadata;

PromiseAttributePattern[Bs] = {
    [t.registrableMetadataKey]: e.AttributePattern.create([ {
        pattern: "promise.resolve",
        symbols: ""
    } ], PromiseAttributePattern)
};

class FulfilledAttributePattern {
    then(t, s) {
        return new e.AttrSyntax(t, s, "then", "from-view");
    }
}

As = Symbol.metadata;

FulfilledAttributePattern[As] = {
    [t.registrableMetadataKey]: e.AttributePattern.create([ {
        pattern: "then",
        symbols: ""
    } ], FulfilledAttributePattern)
};

class RejectedAttributePattern {
    catch(t, s) {
        return new e.AttrSyntax(t, s, "catch", "from-view");
    }
}

Rs = Symbol.metadata;

RejectedAttributePattern[Rs] = {
    [t.registrableMetadataKey]: e.AttributePattern.create([ {
        pattern: "catch",
        symbols: ""
    } ], RejectedAttributePattern)
};

class Focus {
    constructor() {
        this.Ys = false;
        this._e = t.resolve(ot);
        this.p = t.resolve(it);
    }
    binding() {
        this.valueChanged();
    }
    valueChanged() {
        if (this.$controller.isActive) {
            this.Zs();
        } else {
            this.Ys = true;
        }
    }
    attached() {
        if (this.Ys) {
            this.Ys = false;
            this.Zs();
        }
        this._e.addEventListener("focus", this);
        this._e.addEventListener("blur", this);
    }
    detaching() {
        const t = this._e;
        t.removeEventListener("focus", this);
        t.removeEventListener("blur", this);
    }
    handleEvent(t) {
        if (t.type === "focus") {
            this.value = true;
        } else if (!this.Js) {
            this.value = false;
        }
    }
    Zs() {
        const t = this._e;
        const e = this.Js;
        const s = this.value;
        if (s && !e) {
            t.focus();
        } else if (!s && e) {
            t.blur();
        }
    }
    get Js() {
        return this._e === this.p.document.activeElement;
    }
}

Focus.$au = {
    type: lt,
    name: "focus",
    bindables: {
        value: {
            mode: u
        }
    }
};

class Portal {
    constructor() {
        this.position = "beforeend";
        this.strict = false;
        const e = t.resolve(Bt);
        const s = t.resolve($e);
        const i = t.resolve(it);
        this.p = i;
        this.ti = i.document.createElement("div");
        (this.view = e.create()).setLocation(this.ei = At(i));
        setEffectiveParentNode(this.view.nodes, s);
    }
    attaching(t) {
        if (this.callbackContext == null) {
            this.callbackContext = this.$controller.scope.bindingContext;
        }
        const e = this.ti = this.si();
        this.ii(e, this.position);
        return this.ni(t, e);
    }
    detaching(t) {
        return this.oi(t, this.ti);
    }
    targetChanged() {
        const {$controller: e} = this;
        if (!e.isActive) {
            return;
        }
        const s = this.si();
        if (this.ti === s) {
            return;
        }
        this.ti = s;
        const i = t.onResolve(this.oi(null, s), () => {
            this.ii(s, this.position);
            return this.ni(null, s);
        });
        if (t.isPromise(i)) {
            i.catch(rethrow);
        }
    }
    positionChanged() {
        const {$controller: e, ti: s} = this;
        if (!e.isActive) {
            return;
        }
        const i = t.onResolve(this.oi(null, s), () => {
            this.ii(s, this.position);
            return this.ni(null, s);
        });
        if (t.isPromise(i)) {
            i.catch(rethrow);
        }
    }
    ni(e, s) {
        const {activating: i, callbackContext: n, view: r} = this;
        return t.onResolve(i?.call(n, s, r), () => this.ri(e, s));
    }
    ri(e, s) {
        const {$controller: i, view: n} = this;
        if (e === null) {
            n.nodes.insertBefore(this.ei);
        } else {
            return t.onResolve(n.activate(e ?? n, i, i.scope), () => this.li(s));
        }
        return this.li(s);
    }
    li(t) {
        const {activated: e, callbackContext: s, view: i} = this;
        return e?.call(s, t, i);
    }
    oi(e, s) {
        const {deactivating: i, callbackContext: n, view: r} = this;
        return t.onResolve(i?.call(n, s, r), () => this.hi(e, s));
    }
    hi(e, s) {
        const {$controller: i, view: n} = this;
        if (e === null) {
            n.nodes.remove();
        } else {
            return t.onResolve(n.deactivate(e, i), () => this.ai(s));
        }
        return this.ai(s);
    }
    ai(e) {
        const {deactivated: s, callbackContext: i, view: n} = this;
        return t.onResolve(s?.call(i, e, n), () => this.ci());
    }
    si() {
        const e = this.p;
        const s = e.document;
        let i = this.target;
        let n = this.renderContext;
        if (i === "") {
            if (this.strict) {
                throw createMappedError(811);
            }
            return s.body;
        }
        if (t.isString(i)) {
            let r = s;
            if (t.isString(n)) {
                n = s.querySelector(n);
            }
            if (n instanceof e.Node) {
                r = n;
            }
            i = r.querySelector(i);
        }
        if (i instanceof e.Node) {
            return i;
        }
        if (i == null) {
            if (this.strict) {
                throw createMappedError(812);
            }
            return s.body;
        }
        return i;
    }
    ci() {
        this.ei.remove();
        this.ei.$start.remove();
    }
    ii(t, e) {
        const s = this.ei;
        const i = s.$start;
        const n = t.parentNode;
        const r = [ i, s ];
        switch (e) {
          case "beforeend":
            insertManyBefore(t, null, r);
            break;

          case "afterbegin":
            insertManyBefore(t, t.firstChild, r);
            break;

          case "beforebegin":
            insertManyBefore(n, t, r);
            break;

          case "afterend":
            insertManyBefore(n, t.nextSibling, r);
            break;

          default:
            throw createMappedError(779, e);
        }
    }
    dispose() {
        this.view.dispose();
        this.view = void 0;
        this.callbackContext = null;
    }
    accept(t) {
        if (this.view?.accept(t) === true) {
            return true;
        }
    }
}

Portal.$au = {
    type: lt,
    name: "portal",
    isTemplateController: true,
    defaultProperty: "target",
    bindables: [ "target", "position", "activated", "activating", "callbackContext", {
        name: "renderContext",
        callback: "targetChanged"
    }, "strict", "deactivated", "deactivating" ]
};

let Ts;

class AuSlot {
    constructor() {
        this.ui = null;
        this.fi = null;
        this.ie = false;
        this.expose = null;
        this.slotchange = null;
        this.di = new Set;
        this.ks = null;
        const s = t.resolve(_e);
        const i = t.resolve($e);
        const n = t.resolve(e.IInstruction);
        const r = t.resolve(oe);
        const l = this.name = n.data.name;
        const h = n.projections?.[Rt];
        const a = s.instruction?.projections?.[l];
        const c = s.controller.container;
        let u;
        let f;
        if (a == null) {
            f = c.createChild({
                inheritParentResources: true
            });
            u = r.getViewFactory(h ?? (Ts ??= CustomElementDefinition.create({
                name: "au-slot-empty-template",
                template: "",
                needsCompile: false
            })), f);
            this.pi = false;
        } else {
            f = c.createChild();
            f.useResources(s.parent.controller.container);
            registerResolver(f, _e, new t.InstanceProvider(void 0, s.parent));
            u = r.getViewFactory(a, f);
            this.pi = true;
            this.mi = c.getAll(Lt, false)?.filter(t => t.slotName === "*" || t.slotName === l) ?? t.emptyArray;
        }
        this.gi = (this.mi ??= t.emptyArray).length > 0;
        this.xi = s;
        this.view = u.create().setLocation(this.l = i);
    }
    get nodes() {
        const t = [];
        const e = this.l;
        let s = e.$start.nextSibling;
        while (s != null && s !== e) {
            if (s.nodeType !== 8) {
                t.push(s);
            }
            s = s.nextSibling;
        }
        return t;
    }
    subscribe(t) {
        this.di.add(t);
    }
    unsubscribe(t) {
        this.di.delete(t);
    }
    binding(t, e) {
        this.ui = e.scope;
        while (e.vmKind === "synthetic" && e.parent?.viewModel instanceof AuSlot) {
            e = e.parent.parent;
        }
        const s = e.scope.bindingContext;
        let n;
        if (this.pi) {
            n = this.xi.controller.scope.parent;
            (this.fi = i.Scope.fromParent(n, n.bindingContext)).overrideContext.$host = this.expose ?? s;
        }
    }
    attaching(e, s) {
        return t.onResolve(this.view.activate(e, this.$controller, this.pi ? this.fi : this.ui), () => {
            if (this.gi || t.isFunction(this.slotchange)) {
                this.mi.forEach(t => t.watch(this));
                this.ss();
                this.yi();
                this.ie = true;
            }
        });
    }
    detaching(t, e) {
        this.ie = false;
        this.wi();
        this.mi.forEach(t => t.unwatch(this));
        return this.view.deactivate(t, this.$controller);
    }
    exposeChanged(t) {
        if (this.pi && this.fi != null) {
            this.fi.overrideContext.$host = t;
        }
    }
    dispose() {
        this.view.dispose();
        this.view = void 0;
    }
    accept(t) {
        if (this.view?.accept(t) === true) {
            return true;
        }
    }
    ss() {
        if (this.ks != null) {
            return;
        }
        const t = this.l;
        const e = t.parentElement;
        if (e == null) {
            return;
        }
        (this.ks = createMutationObserver(e, e => {
            if (isMutationWithinLocation(t, e)) {
                this.yi();
            }
        })).observe(e, {
            childList: true
        });
    }
    wi() {
        this.ks?.disconnect();
        this.ks = null;
    }
    yi() {
        const t = this.nodes;
        const e = new Set(this.di);
        let s;
        if (this.ie) {
            this.slotchange?.call(void 0, this.name, t);
        }
        for (s of e) {
            s.handleSlotChange(this, t);
        }
    }
}

AuSlot.$au = {
    type: Xe,
    name: "au-slot",
    template: null,
    containerless: true,
    processContent(t, e, s) {
        s.name = t.getAttribute("name") ?? Rt;
        let i = t.firstChild;
        let n = null;
        while (i !== null) {
            n = i.nextSibling;
            if (isElement(i) && i.hasAttribute(Tt)) {
                t.removeChild(i);
            }
            i = n;
        }
    },
    bindables: [ "expose", "slotchange" ]
};

const comparePosition = (t, e) => t.compareDocumentPosition(e);

const isMutationWithinLocation = (t, e) => {
    for (const {addedNodes: s, removedNodes: i, nextSibling: n} of e) {
        let e = 0;
        let r = s.length;
        let l;
        for (;e < r; ++e) {
            l = s[e];
            if (comparePosition(t.$start, l) === 4 && comparePosition(t, l) === 2) {
                return true;
            }
        }
        if (i.length > 0) {
            if (n != null && comparePosition(t.$start, n) === 4 && comparePosition(t, n) === 2) {
                return true;
            }
        }
    }
};

class AuCompose {
    constructor() {
        this.scopeBehavior = "auto";
        this.bi = void 0;
        this.tag = null;
        this.flushMode = "sync";
        this.c = t.resolve(t.IContainer);
        this.parent = t.resolve(Pe);
        this.ki = t.resolve(ot);
        this.l = t.resolve($e);
        this.p = t.resolve(it);
        this.r = t.resolve(oe);
        this.Ci = t.resolve(e.IInstruction);
        this.Si = t.resolve(t.transient(CompositionContextFactory, null));
        this.vt = t.resolve(e.ITemplateCompiler);
        this.et = t.resolve(_e);
        this.ep = t.resolve(r.IExpressionParser);
        this.oL = t.resolve(i.IObserverLocator);
    }
    get composing() {
        return this.Bi;
    }
    get composition() {
        return this.bi;
    }
    attaching(e, s) {
        return this.Bi = t.onResolve(this.queue(new ChangeInfo(this.template, this.component, this.model, void 0), e), t => {
            if (this.Si.Ai(t)) {
                this.Bi = void 0;
            }
        });
    }
    detaching(e) {
        const s = this.bi;
        const i = this.Bi;
        this.Si.invalidate();
        this.bi = this.Bi = void 0;
        return t.onResolve(i, () => s?.deactivate(e));
    }
    propertyChanged(t) {
        if (t === "composing" || t === "composition") {
            return;
        }
        if (this.flushMode === "sync") {
            if (t === "model" && this.bi != null) {
                this.bi.update(this.model);
                return;
            }
            if (t === "tag" && this.bi?.controller.vmKind === Ce) {
                return;
            }
            this.Ri(new ChangeInfo(this.template, this.component, this.model, t));
        }
    }
    propertiesChanged(t) {
        if (this.flushMode === "async") {
            if ("model" in t && this.bi != null && Object.keys(t).length === 1) {
                this.bi.update(this.model);
                return;
            }
            if ("tag" in t && this.bi?.controller.vmKind === Ce) {
                return;
            }
            this.Ri(new ChangeInfo(this.template, this.component, this.model, "model" in t ? "model" : undefined));
        }
    }
    Ri(e) {
        this.Bi = t.onResolve(this.Bi, () => t.onResolve(this.queue(e, void 0), t => {
            if (this.Si.Ai(t)) {
                this.Bi = void 0;
            }
        }));
    }
    queue(e, s) {
        const i = this.Si;
        const n = this.bi;
        return t.onResolve(i.create(e), e => {
            if (i.Ai(e)) {
                return t.onResolve(this.compose(e), r => {
                    if (i.Ai(e)) {
                        return t.onResolve(r.activate(s), () => {
                            if (i.Ai(e)) {
                                this.bi = r;
                                return t.onResolve(n?.deactivate(s), () => e);
                            } else {
                                return t.onResolve(r.controller.deactivate(r.controller, this.$controller), () => {
                                    r.controller.dispose();
                                    return e;
                                });
                            }
                        });
                    }
                    r.controller.dispose();
                    return e;
                });
            }
            return e;
        });
    }
    compose(e) {
        const {Ti: s, Ei: n, Li: r} = e.change;
        const {c: l, $controller: h, l: a, Ci: c} = this;
        const u = this.Mi(this.et.controller.container, n);
        const f = l.createChild();
        const d = this.p.document.createElement(u == null ? this.tag ?? "div" : u.name);
        a.parentNode.insertBefore(d, a);
        let p;
        if (u == null) {
            p = this.tag == null ? convertToRenderLocation(d) : null;
        } else {
            p = u.containerless ? convertToRenderLocation(d) : null;
        }
        const removeCompositionHost = () => {
            d.remove();
            if (p != null) {
                let t = p.$start.nextSibling;
                let e = null;
                while (t !== null && t !== p) {
                    e = t.nextSibling;
                    t.remove();
                    t = e;
                }
                p.$start?.remove();
                p.remove();
            }
        };
        const m = this.Di(f, typeof n === "string" ? u.Type : n, d, p);
        const compose = () => {
            const n = c.captures ?? t.emptyArray;
            if (u !== null) {
                const s = u.capture;
                const [i, r] = n.reduce((e, i) => {
                    const n = !(i.target in u.bindables) && (s === true || t.isFunction(s) && !!s(i.target));
                    e[n ? 0 : 1].push(i);
                    return e;
                }, [ [], [] ]);
                const l = Controller.$el(f, m, d, {
                    projections: c.projections,
                    captures: i
                }, u, p);
                this.qi(d, u, r).forEach(t => l.addBinding(t));
                return new CompositionController(l, t => l.activate(t ?? l, h, h.scope.parent), e => t.onResolve(l.deactivate(e ?? l, h), removeCompositionHost), t => m.activate?.(t), e);
            } else {
                const r = CustomElementDefinition.create({
                    name: Je.generateName(),
                    template: s
                });
                const l = this.r.compile(r, f);
                const a = this.r.getViewFactory(l, f);
                const c = Controller.$view(a, h, d);
                const u = this.scopeBehavior === "auto" ? i.Scope.fromParent(this.parent.scope, m) : i.Scope.create(m);
                c.setHost(d);
                if (p == null) {
                    this.qi(d, a.def, n).forEach(t => c.addBinding(t));
                } else {
                    c.setLocation(p);
                }
                return new CompositionController(c, t => c.activate(t ?? c, h, u), e => t.onResolve(c.deactivate(e ?? c, h), removeCompositionHost), t => m.activate?.(t), e);
            }
        };
        if ("activate" in m) {
            return t.onResolve(m.activate(r), () => compose());
        } else {
            return compose();
        }
    }
    Di(e, s, i, n) {
        if (s == null) {
            return new EmptyComponent;
        }
        if (typeof s === "object") {
            return s;
        }
        const r = this.p;
        registerHostNode(e, i, r);
        registerResolver(e, $e, new t.InstanceProvider("IRenderLocation", n));
        const l = e.invoke(s);
        registerResolver(e, s, new t.InstanceProvider("au-compose.component", l));
        return l;
    }
    Mi(e, s) {
        if (typeof s === "string") {
            const t = Je.find(e, s);
            if (t == null) {
                throw createMappedError(806, s);
            }
            return t;
        }
        const i = t.isFunction(s) ? s : s?.constructor;
        return Je.isType(i, void 0) ? Je.getDefinition(i, null) : null;
    }
    qi(t, e, s) {
        const i = new HydrationContext(this.$controller, {
            projections: null,
            captures: s
        }, this.et.parent);
        return SpreadBinding.create(i, t, e, this.r, this.vt, this.p, this.ep, this.oL);
    }
}

AuCompose.$au = {
    type: Xe,
    name: "au-compose",
    capture: true,
    containerless: true,
    bindables: [ "template", "component", "model", {
        name: "scopeBehavior",
        set: t => {
            if (t === "scoped" || t === "auto") {
                return t;
            }
            throw createMappedError(805, t);
        }
    }, {
        name: "composing",
        mode: c
    }, {
        name: "composition",
        mode: c
    }, "tag", {
        name: "flushMode",
        set: t => {
            if (t === "sync" || t === "async") {
                return t;
            }
            throw createMappedError(809, t);
        }
    } ]
};

class EmptyComponent {}

class CompositionContextFactory {
    constructor() {
        this.id = 0;
    }
    Ai(t) {
        return t.id === this.id;
    }
    create(e) {
        return t.onResolve(e.load(), t => new CompositionContext(++this.id, t));
    }
    invalidate() {
        this.id++;
    }
}

class ChangeInfo {
    constructor(t, e, s, i) {
        this.Ti = t;
        this.Ei = e;
        this.Li = s;
        this.Pi = i;
    }
    load() {
        if (t.isPromise(this.Ti) || t.isPromise(this.Ei)) {
            return Promise.all([ this.Ti, this.Ei ]).then(([t, e]) => new LoadedChangeInfo(t, e, this.Li, this.Pi));
        } else {
            return new LoadedChangeInfo(this.Ti, this.Ei, this.Li, this.Pi);
        }
    }
}

class LoadedChangeInfo {
    constructor(t, e, s, i) {
        this.Ti = t;
        this.Ei = e;
        this.Li = s;
        this.Pi = i;
    }
}

class CompositionContext {
    constructor(t, e) {
        this.id = t;
        this.change = e;
    }
}

class CompositionController {
    constructor(t, e, s, i, n) {
        this.controller = t;
        this.start = e;
        this.stop = s;
        this.update = i;
        this.context = n;
        this.state = 0;
    }
    activate(t) {
        if (this.state !== 0) {
            throw createMappedError(807, this);
        }
        this.state = 1;
        return this.start(t);
    }
    deactivate(t) {
        switch (this.state) {
          case 1:
            this.state = -1;
            return this.stop(t);

          case -1:
            throw createMappedError(808);

          default:
            this.state = -1;
        }
    }
}

const Es = /*@__PURE__*/ H("ISanitizer", t => t.singleton(class {
    sanitize() {
        throw createMappedError(99, "sanitize");
    }
}));

class SanitizeValueConverter {
    constructor() {
        this._i = t.resolve(Es);
    }
    toView(t) {
        if (t == null) {
            return null;
        }
        return this._i.sanitize(t);
    }
}

SanitizeValueConverter.$au = {
    type: dt,
    name: "sanitize"
};

class Show {
    constructor() {
        this.el = t.resolve(ot);
        this.p = t.resolve(it);
        this.Ii = false;
        this.L = false;
        this.$val = "";
        this.$prio = "";
        this.update = () => {
            this.L = false;
            if (Boolean(this.value) !== this.Vi) {
                if (this.Vi === this.Fi) {
                    this.Vi = !this.Fi;
                    this.$val = this.el.style.getPropertyValue("display");
                    this.$prio = this.el.style.getPropertyPriority("display");
                    this.el.style.setProperty("display", "none", "important");
                } else {
                    this.Vi = this.Fi;
                    this.el.style.setProperty("display", this.$val, this.$prio);
                    if (this.el.getAttribute("style") === "") {
                        this.el.removeAttribute("style");
                    }
                }
            }
        };
        const s = t.resolve(e.IInstruction);
        this.Vi = this.Fi = s.alias !== "hide";
    }
    binding() {
        this.Ii = true;
        this.update();
    }
    detaching() {
        this.Ii = false;
        this.L = false;
    }
    valueChanged() {
        if (this.Ii && !this.L) {
            this.L = true;
            i.queueTask(this.update);
        }
    }
}

Show.$au = {
    type: lt,
    name: "show",
    bindables: [ "value" ],
    aliases: [ "hide" ]
};

const Ls = [ ns, i.DirtyChecker, NodeObserverLocator ];

const Ms = [ e.RefAttributePattern, e.DotSeparatedAttributePattern, e.EventAttributePattern, St ];

const Ds = [ e.AtPrefixedTriggerAttributePattern, e.ColonPrefixedBindAttributePattern ];

const qs = [ e.DefaultBindingCommand, e.OneTimeBindingCommand, e.FromViewBindingCommand, e.ToViewBindingCommand, e.TwoWayBindingCommand, e.ForBindingCommand, e.RefBindingCommand, e.TriggerBindingCommand, e.CaptureBindingCommand, e.ClassBindingCommand, e.StyleBindingCommand, e.AttrBindingCommand, e.SpreadValueBindingCommand ];

const Ps = [ DebounceBindingBehavior, OneTimeBindingBehavior, ToViewBindingBehavior, FromViewBindingBehavior, SignalBindingBehavior, ThrottleBindingBehavior, TwoWayBindingBehavior, SanitizeValueConverter, If, Else, Repeat, With, Switch, Case, DefaultCase, PromiseTemplateController, PendingTemplateController, FulfilledTemplateController, RejectedTemplateController, PromiseAttributePattern, FulfilledAttributePattern, RejectedAttributePattern, AttrBindingBehavior, SelfBindingBehavior, UpdateTriggerBindingBehavior, AuCompose, Portal, Focus, Show, AuSlot ];

const _s = [ Ot, $t, Ft, Ht, qt, Pt, _t, It, Vt, jt, Xt, zt, Ut, Gt, Kt, Nt, Qt, Yt ];

const Is = /*@__PURE__*/ createConfiguration(t.noop);

function createConfiguration(t) {
    return {
        optionsProvider: t,
        register(e) {
            const s = {
                coercingOptions: {
                    enableCoercion: false,
                    coerceNullish: false
                }
            };
            t(s);
            return e.register(N(i.ICoercionConfiguration, s.coercingOptions), r.ExpressionParser, ...Ls, ...Ps, ...Ms, ...qs, ..._s);
        },
        customize(e) {
            return createConfiguration(e ?? t);
        }
    };
}

function children(e, s) {
    if (!children.mixed) {
        children.mixed = true;
        i.subscriberCollection(ChildrenBinding, null);
        lifecycleHooks()(ChildrenLifecycleHooks, null);
    }
    let n;
    const r = g("dependencies");
    function decorator(t, e) {
        switch (e.kind) {
          case "field":
            n.name = e.name;
            break;
        }
        const s = e.metadata[r] ??= [];
        s.push(new ChildrenLifecycleHooks(n ?? {}));
    }
    if (arguments.length > 1) {
        n = {};
        decorator(e, s);
        return;
    } else if (t.isString(e)) {
        n = {
            query: e
        };
        return decorator;
    }
    n = e === void 0 ? {} : e;
    return decorator;
}

children.mixed = false;

class ChildrenBinding {
    constructor(t, e, s, i, n, r) {
        this.Hi = void 0;
        this.isBound = false;
        this.obj = e;
        this.cb = s;
        this.Z = i;
        this.Oi = n;
        this.$i = r;
        this.ks = createMutationObserver(this.ki = t, () => {
            this.Ni();
        });
    }
    getValue() {
        return this.isBound ? this.Hi : this.Wi();
    }
    setValue(t) {}
    bind() {
        if (this.isBound) {
            return;
        }
        this.isBound = true;
        this.ks.observe(this.ki, {
            childList: true
        });
        this.Hi = this.Wi();
    }
    unbind() {
        if (!this.isBound) {
            return;
        }
        this.isBound = false;
        this.ks.takeRecords();
        this.ks.disconnect();
        this.Hi = t.emptyArray;
    }
    Ni() {
        this.Hi = this.Wi();
        this.cb?.call(this.obj);
        this.subs.notify(this.Hi, undefined);
    }
    get() {
        throw createMappedError(99, "get");
    }
    Wi() {
        const t = this.Z;
        const e = this.Oi;
        const s = this.$i;
        const i = t === "$all" ? this.ki.childNodes : this.ki.querySelectorAll(`:scope > ${t}`);
        const n = i.length;
        const r = [];
        const l = {
            optional: true
        };
        let h;
        let a;
        let c = 0;
        let u;
        while (n > c) {
            u = i[c];
            h = findElementControllerFor(u, l);
            a = h?.viewModel ?? null;
            if (e == null ? true : e(u, a)) {
                r.push(s == null ? a ?? u : s(u, a));
            }
            ++c;
        }
        return r;
    }
}

class ChildrenLifecycleHooks {
    constructor(t) {
        this.J = t;
    }
    register(t) {
        N(ct, this).register(t);
    }
    hydrating(t, e) {
        const s = this.J;
        const i = s.query ?? "*";
        const n = new ChildrenBinding(e.host, t, t[s.callback ?? `${v(s.name)}Changed`], i, s.filter, s.map);
        if (/[\s>]/.test(i)) {
            throw createMappedError(9989, i);
        }
        A(t, s.name, {
            enumerable: true,
            configurable: true,
            get: k(() => n.getValue(), {
                getObserver: () => n
            }),
            set: () => {}
        });
        e.addBinding(n);
    }
}

exports.BindingMode = e.BindingMode;

exports.AdoptedStyleSheetsStyles = AdoptedStyleSheetsStyles;

exports.AppRoot = AppRoot;

exports.AppTask = st;

exports.ArrayLikeHandler = ArrayLikeHandler;

exports.AttrBindingBehavior = AttrBindingBehavior;

exports.AttrMapper = AttrMapper;

exports.AttributeBinding = AttributeBinding;

exports.AttributeBindingRenderer = Xt;

exports.AttributeNSAccessor = AttributeNSAccessor;

exports.AuCompose = AuCompose;

exports.AuSlot = AuSlot;

exports.AuSlotsInfo = AuSlotsInfo;

exports.Aurelia = Aurelia;

exports.Bindable = V;

exports.BindableDefinition = BindableDefinition;

exports.BindingBehavior = K;

exports.BindingBehaviorDefinition = BindingBehaviorDefinition;

exports.BindingModeBehavior = BindingModeBehavior;

exports.BindingTargetSubscriber = BindingTargetSubscriber;

exports.CSSModulesProcessorRegistry = CSSModulesProcessorRegistry;

exports.Case = Case;

exports.CheckedObserver = CheckedObserver;

exports.ChildrenBinding = ChildrenBinding;

exports.ClassAttributeAccessor = ClassAttributeAccessor;

exports.ComputedWatcher = ComputedWatcher;

exports.ContentBinding = ContentBinding;

exports.Controller = Controller;

exports.CustomAttribute = at;

exports.CustomAttributeDefinition = CustomAttributeDefinition;

exports.CustomAttributeRenderer = _t;

exports.CustomElement = Je;

exports.CustomElementDefinition = CustomElementDefinition;

exports.CustomElementRenderer = Pt;

exports.DataAttributeAccessor = DataAttributeAccessor;

exports.DebounceBindingBehavior = DebounceBindingBehavior;

exports.DefaultBindingLanguage = qs;

exports.DefaultBindingSyntax = Ms;

exports.DefaultCase = DefaultCase;

exports.DefaultComponents = Ls;

exports.DefaultRenderers = _s;

exports.DefaultResources = Ps;

exports.Else = Else;

exports.EventModifier = EventModifier;

exports.EventModifierRegistration = St;

exports.ExpressionWatcher = ExpressionWatcher;

exports.FlushQueue = FlushQueue;

exports.Focus = Focus;

exports.FragmentNodeSequence = FragmentNodeSequence;

exports.FromViewBindingBehavior = FromViewBindingBehavior;

exports.FulfilledTemplateController = FulfilledTemplateController;

exports.IAppRoot = es;

exports.IAppTask = et;

exports.IAuSlotWatcher = Lt;

exports.IAuSlotsInfo = Et;

exports.IAurelia = ss;

exports.IController = Pe;

exports.IEventModifier = Ct;

exports.IEventTarget = Oe;

exports.IFlushQueue = vt;

exports.IHistory = Ue;

exports.IHydrationContext = _e;

exports.IKeyMapping = kt;

exports.ILifecycleHooks = ct;

exports.IListenerBindingOptions = Wt;

exports.ILocation = ze;

exports.IModifiedEventHandlerCreator = bt;

exports.INode = ot;

exports.IPlatform = it;

exports.IRenderLocation = $e;

exports.IRenderer = Dt;

exports.IRendering = oe;

exports.IRepeatableHandler = gs;

exports.IRepeatableHandlerResolver = ms;

exports.ISSRContext = ft;

exports.ISVGAnalyzer = is;

exports.ISanitizer = Es;

exports.IShadowDOMGlobalStyles = he;

exports.IShadowDOMStyleFactory = re;

exports.IShadowDOMStyles = le;

exports.ISignaler = Z;

exports.IViewFactory = Bt;

exports.IWindow = je;

exports.If = If;

exports.InterpolationBinding = InterpolationBinding;

exports.InterpolationBindingRenderer = Ht;

exports.InterpolationPartBinding = InterpolationPartBinding;

exports.IteratorBindingRenderer = $t;

exports.LetBinding = LetBinding;

exports.LetElementRenderer = Vt;

exports.LifecycleHooks = ut;

exports.LifecycleHooksDefinition = LifecycleHooksDefinition;

exports.LifecycleHooksEntry = LifecycleHooksEntry;

exports.ListenerBinding = ListenerBinding;

exports.ListenerBindingOptions = ListenerBindingOptions;

exports.ListenerBindingRenderer = jt;

exports.MountTarget = ye;

exports.NodeObserverLocator = NodeObserverLocator;

exports.NoopSVGAnalyzer = NoopSVGAnalyzer;

exports.OneTimeBindingBehavior = OneTimeBindingBehavior;

exports.PendingTemplateController = PendingTemplateController;

exports.Portal = Portal;

exports.PromiseTemplateController = PromiseTemplateController;

exports.PropertyBinding = PropertyBinding;

exports.PropertyBindingRenderer = Ot;

exports.RefBinding = RefBinding;

exports.RefBindingRenderer = Ft;

exports.RejectedTemplateController = RejectedTemplateController;

exports.Rendering = Rendering;

exports.Repeat = Repeat;

exports.RuntimeTemplateCompilerImplementation = ns;

exports.SVGAnalyzer = SVGAnalyzer;

exports.SanitizeValueConverter = SanitizeValueConverter;

exports.SelectValueObserver = SelectValueObserver;

exports.SelfBindingBehavior = SelfBindingBehavior;

exports.SetAttributeRenderer = zt;

exports.SetClassAttributeRenderer = Ut;

exports.SetPropertyRenderer = qt;

exports.SetStyleAttributeRenderer = Gt;

exports.ShadowDOMRegistry = ShadowDOMRegistry;

exports.ShortHandBindingSyntax = Ds;

exports.SignalBindingBehavior = SignalBindingBehavior;

exports.SpreadRenderer = Qt;

exports.StandardConfiguration = Is;

exports.State = qe;

exports.StyleAttributeAccessor = StyleAttributeAccessor;

exports.StyleConfiguration = ae;

exports.StyleElementStyles = StyleElementStyles;

exports.StylePropertyBindingRenderer = Kt;

exports.Switch = Switch;

exports.TemplateControllerRenderer = It;

exports.TextBindingRenderer = Nt;

exports.ThrottleBindingBehavior = ThrottleBindingBehavior;

exports.ToViewBindingBehavior = ToViewBindingBehavior;

exports.TwoWayBindingBehavior = TwoWayBindingBehavior;

exports.UpdateTriggerBindingBehavior = UpdateTriggerBindingBehavior;

exports.ValueAttributeObserver = ValueAttributeObserver;

exports.ValueConverter = mt;

exports.ValueConverterDefinition = ValueConverterDefinition;

exports.ViewFactory = ViewFactory;

exports.Watch = rt;

exports.With = With;

exports.adoptSSRView = adoptSSRView;

exports.adoptSSRViews = adoptSSRViews;

exports.alias = alias;

exports.bindable = bindable;

exports.bindingBehavior = bindingBehavior;

exports.capture = capture;

exports.children = children;

exports.coercer = coercer;

exports.containerless = containerless;

exports.convertToRenderLocation = convertToRenderLocation;

exports.cssModules = cssModules;

exports.customAttribute = customAttribute;

exports.customElement = customElement;

exports.findMatchingEndMarker = findMatchingEndMarker;

exports.getEffectiveParentNode = getEffectiveParentNode;

exports.hydrateSSRDefinition = hydrateSSRDefinition;

exports.isCustomElementController = isCustomElementController;

exports.isCustomElementViewModel = isCustomElementViewModel;

exports.isRenderLocation = isRenderLocation;

exports.isSSRScope = isSSRScope;

exports.isSSRTemplateController = isSSRTemplateController;

exports.lifecycleHooks = lifecycleHooks;

exports.mixinAstEvaluator = xt;

exports.mixinUseScope = gt;

exports.mixingBindingLimited = yt;

exports.partitionSiblingNodes = partitionSiblingNodes;

exports.processContent = processContent;

exports.refs = nt;

exports.registerAliases = registerAliases;

exports.registerHostNode = registerHostNode;

exports.renderer = renderer;

exports.setEffectiveParentNode = setEffectiveParentNode;

exports.shadowCSS = shadowCSS;

exports.slotted = slotted;

exports.templateController = templateController;

exports.useShadowDOM = useShadowDOM;

exports.valueConverter = valueConverter;

exports.watch = watch;
//# sourceMappingURL=index.cjs.map
