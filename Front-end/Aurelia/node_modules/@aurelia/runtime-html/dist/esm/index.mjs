import { Protocol as t, isString as e, createLookup as i, getPrototypeChain as s, noop as n, DI as r, Registration as l, firstDefined as h, mergeArrays as a, isFunction as c, resourceBaseName as u, resource as f, getResourceKeyFor as d, resolve as p, IPlatform as m, emptyArray as g, registrableMetadataKey as v, isArray as x, all as y, isObject as w, own as b, InstanceProvider as k, IContainer as C, areEqual as S, optionalResource as B, optional as A, onResolveAll as R, isPromise as T, onResolve as E, fromDefinitionOrDefault as L, pascalCase as M, fromAnnotationOrDefinitionOrTypeOrDefault as D, fromAnnotationOrTypeOrDefault as P, isSymbol as _, createImplementationRegister as I, kebabCase as q, IServiceLocator as V, emptyObject as F, isNumber as H, isSet as O, isMap as $, ILogger as N, transient as W } from "@aurelia/kernel";

import { BindingMode as j, itIteratorBinding as z, itMultiAttr as U, itHydrateLetElement as G, itLetBinding as K, itHydrateTemplateController as X, itHydrateAttribute as Q, itHydrateElement as Y, itSetAttribute as Z, itSetProperty as J, itRefBinding as tt, itListenerBinding as et, itInterpolation as it, itTextBinding as st, itPropertyBinding as nt, itSpreadElementProp as ot, itSpreadTransferedBinding as rt, itSetClassAttribute as lt, itSetStyleAttribute as ht, itStylePropertyBinding as at, itAttributeBinding as ct, ITemplateCompiler as ut, IInstruction as ft, itSpreadValueBinding as dt, TemplateCompilerHooks as pt, IAttrMapper as mt, IResourceResolver as gt, TemplateCompiler as vt, AttributePattern as xt, AttrSyntax as yt, RefAttributePattern as wt, DotSeparatedAttributePattern as bt, EventAttributePattern as kt, AtPrefixedTriggerAttributePattern as Ct, ColonPrefixedBindAttributePattern as St, DefaultBindingCommand as Bt, OneTimeBindingCommand as At, FromViewBindingCommand as Rt, ToViewBindingCommand as Tt, TwoWayBindingCommand as Et, ForBindingCommand as Lt, RefBindingCommand as Mt, TriggerBindingCommand as Dt, CaptureBindingCommand as Pt, ClassBindingCommand as _t, StyleBindingCommand as It, AttrBindingCommand as qt, SpreadValueBindingCommand as Vt } from "@aurelia/template-compiler";

export { BindingMode } from "@aurelia/template-compiler";

import { Metadata as Ft } from "@aurelia/metadata";

import { AccessorType as Ht, astEvaluate as Ot, queueAsyncTask as $t, queueTask as Nt, astBind as Wt, astUnbind as jt, connectable as zt, astAssign as Ut, subscriberCollection as Gt, Scope as Kt, IObserverLocator as Xt, ConnectableSwitcher as Qt, ProxyObservable as Yt, ICoercionConfiguration as Zt, tasksSettled as Jt, PropertyAccessor as te, INodeObserverLocator as ee, IDirtyChecker as ie, getObserverLookup as se, SetterObserver as ne, createIndexMap as oe, getCollectionObserver as re, BindingContext as le, DirtyChecker as he } from "@aurelia/runtime";

import { BrowserPlatform as ae } from "@aurelia/platform-browser";

import { createInterpolation as ce, createAccessScopeExpression as ue, IExpressionParser as fe, ExpressionParser as de } from "@aurelia/expression-parser";

typeof SuppressedError === "function" ? SuppressedError : function(t, e, i) {
    var s = new Error(i);
    return s.name = "SuppressedError", s.error = t, s.suppressed = e, s;
};

const {default: pe, oneTime: me, toView: ge, fromView: ve, twoWay: xe} = j;

const ye = Ft.get;

const we = Ft.has;

const be = Ft.define;

const clearMetadata = (t, ...e) => {
    for (const i of e) {
        Ft.delete(i, t);
    }
};

const {annotation: ke} = t;

const Ce = ke.keyFor;

const Se = Object;

const Be = String;

const Ae = Se.prototype;

const Re = Ae.hasOwnProperty;

const Te = Se.freeze;

const Ee = Se.assign;

const Le = Se.getOwnPropertyNames;

const Me = Se.keys;

const De = /*@__PURE__*/ i();

const isDataAttribute = (t, i, s) => {
    if (De[i] === true) {
        return true;
    }
    if (!e(i)) {
        return false;
    }
    const n = i.slice(0, 5);
    return De[i] = n === "aria-" || n === "data-" || s.isStandardSvgAttribute(t, i);
};

const rethrow = t => {
    throw t;
};

const Pe = Reflect.defineProperty;

const defineHiddenProp = (t, e, i) => {
    Pe(t, e, {
        enumerable: false,
        configurable: true,
        writable: true,
        value: i
    });
    return i;
};

const addSignalListener = (t, e, i) => t.addSignalListener(e, i);

const removeSignalListener = (t, e, i) => t.removeSignalListener(e, i);

const _e = "Interpolation";

const Ie = "IsIterator";

const qe = "IsFunction";

const Ve = "IsProperty";

const Fe = "pending";

const He = "running";

const Oe = Ht.Observer;

const $e = Ht.Node;

const Ne = Ht.Layout;

const createMappedError = (t, ...e) => {
    const i = Be(t).padStart(4, "0");
    return new Error(`AUR${i}:${e.map(Be)}`);
};

function bindable(t, s) {
    let n = void 0;
    function decorator(t, e) {
        let s;
        switch (e.kind) {
          case "getter":
          case "field":
            {
                const t = e.name;
                if (typeof t !== "string") throw createMappedError(227);
                s = t;
                break;
            }

          case "class":
            if (n == null) throw createMappedError(228);
            if (typeof n == "string") {
                s = n;
            } else {
                const t = n.name;
                if (!t) throw createMappedError(229);
                if (typeof t !== "string") throw createMappedError(227);
                s = t;
            }
            break;
        }
        const r = n == null || typeof n === "string" ? {
            name: s
        } : n;
        const l = e.metadata[We] ??= i();
        l[s] = BindableDefinition.create(s, r);
    }
    if (arguments.length > 1) {
        n = {};
        decorator(t, s);
        return;
    } else if (e(t)) {
        n = t;
        return decorator;
    }
    n = t === void 0 ? {} : t;
    return decorator;
}

const We = /*@__PURE__*/ Ce("bindables");

const je = Te({
    name: We,
    keyFrom: t => `${We}:${t}`,
    from(...t) {
        const i = {};
        const s = Array.isArray;
        function addName(t) {
            i[t] = BindableDefinition.create(t);
        }
        function addDescription(t, e) {
            i[t] = e instanceof BindableDefinition ? e : BindableDefinition.create(t, e === true ? {} : e);
        }
        function addList(t) {
            if (s(t)) {
                t.forEach(t => e(t) ? addName(t) : addDescription(t.name, t));
            } else if (t instanceof BindableDefinition) {
                i[t.name] = t;
            } else if (t !== void 0) {
                Me(t).forEach(e => addDescription(e, t[e]));
            }
        }
        t.forEach(addList);
        return i;
    },
    getAll(t) {
        const e = [];
        const i = s(t);
        let n = i.length;
        let r;
        while (--n >= 0) {
            r = i[n];
            const t = ye(We, r);
            if (t == null) continue;
            e.push(...Object.values(t));
        }
        return e;
    },
    i(t, e) {
        let s = ye(We, e);
        if (s == null) {
            be(s = i(), e, We);
        }
        s[t.name] = t;
    }
});

class BindableDefinition {
    constructor(t, e, i, s, n) {
        this.attribute = t;
        this.callback = e;
        this.mode = i;
        this.name = s;
        this.set = n;
    }
    static toAttr(t) {
        return this.h[t] ??= t.replace(/([A-Z])/g, (t, e) => `-${e.toLowerCase()}`);
    }
    static create(t, i = {}) {
        const s = i.mode ?? ge;
        return new BindableDefinition(i.attribute ?? BindableDefinition.toAttr(t), i.callback ?? `${t}Changed`, e(s) ? j[s] ?? pe : s, i.name ?? t, i.set ?? getInterceptor(i));
    }
}

BindableDefinition.h = {};

function coercer(t, e) {
    e.addInitializer(function() {
        ze.define(this, e.name);
    });
}

const ze = {
    key: /*@__PURE__*/ Ce("coercer"),
    define(t, e) {
        be(t[e].bind(t), t, ze.key);
    },
    for(t) {
        return ye(ze.key, t);
    }
};

function getInterceptor(t = {}) {
    const e = t.type ?? null;
    if (e == null) {
        return n;
    }
    let i;
    switch (e) {
      case Number:
      case Boolean:
      case String:
      case BigInt:
        i = e;
        break;

      default:
        {
            const t = e.coerce;
            i = typeof t === "function" ? t.bind(e) : ze.for(e) ?? n;
            break;
        }
    }
    return i === n ? i : createCoercer(i, t.nullable);
}

function createCoercer(t, e) {
    return function(i, s) {
        if (!s?.enableCoercion) return i;
        return (e ?? (s?.coerceNullish ?? false ? false : true)) && i == null ? i : t(i, s);
    };
}

const Ue = r.createInterface;

const Ge = l.singleton;

const Ke = l.aliasTo;

const Xe = l.instance;

l.callback;

l.transient;

const registerResolver = (t, e, i) => t.registerResolver(e, i);

function alias(...t) {
    return function(e, i) {
        i.addInitializer(function() {
            const e = Ce("aliases");
            const i = ye(e, this);
            if (i === void 0) {
                be(t, this, e);
            } else {
                i.push(...t);
            }
        });
    };
}

function registerAliases(t, e, i, s) {
    for (let n = 0, r = t.length; n < r; ++n) {
        Ke(i, e.keyFrom(t[n])).register(s);
    }
}

const Qe = "custom-element";

const Ye = "custom-attribute";

const Ze = "__au_static_resource__";

const getDefinitionFromStaticAu = (t, e, i, s = Ze) => {
    let n = ye(s, t);
    if (n == null) {
        if (t.$au?.type === e) {
            n = i(t.$au, t);
            be(n, t, s);
        }
    }
    return n;
};

const clearStaticAuDefinition = t => {
    clearMetadata(t, Ze);
};

function bindingBehavior(t) {
    return function(e, i) {
        i.addInitializer(function() {
            ei.define(t, this);
        });
        return e;
    };
}

class BindingBehaviorDefinition {
    constructor(t, e, i, s) {
        this.Type = t;
        this.name = e;
        this.aliases = i;
        this.key = s;
    }
    static create(t, i) {
        let s;
        let n;
        if (e(t)) {
            s = t;
            n = {
                name: s
            };
        } else {
            s = t.name;
            n = t;
        }
        return new BindingBehaviorDefinition(i, h(getBehaviorAnnotation(i, "name"), s), a(getBehaviorAnnotation(i, "aliases"), n.aliases, i.aliases), ei.keyFrom(s));
    }
    register(t, e) {
        const i = this.Type;
        const s = typeof e === "string" ? getBindingBehaviorKeyFrom(e) : this.key;
        const n = this.aliases;
        if (!t.has(s, false)) {
            t.register(t.has(i, false) ? null : Ge(i, i), Ke(i, s), ...n.map(t => Ke(i, getBindingBehaviorKeyFrom(t))));
        }
    }
}

const Je = "binding-behavior";

const ti = /*@__PURE__*/ d(Je);

const getBehaviorAnnotation = (t, e) => ye(Ce(e), t);

const getBindingBehaviorKeyFrom = t => `${ti}:${t}`;

const ei = /*@__PURE__*/ Te({
    name: ti,
    keyFrom: getBindingBehaviorKeyFrom,
    isType(t) {
        return c(t) && (we(ti, t) || t.$au?.type === Je);
    },
    define(t, e) {
        const i = BindingBehaviorDefinition.create(t, e);
        const s = i.Type;
        be(i, s, ti, u);
        return s;
    },
    getDefinition(t) {
        const e = ye(ti, t) ?? getDefinitionFromStaticAu(t, Je, BindingBehaviorDefinition.create);
        if (e === void 0) {
            throw createMappedError(151, t);
        }
        return e;
    },
    find(t, e) {
        const i = t.find(Je, e);
        return i == null ? null : ye(ti, i) ?? getDefinitionFromStaticAu(i, Je, BindingBehaviorDefinition.create) ?? null;
    },
    get(t, e) {
        return t.get(f(getBindingBehaviorKeyFrom(e)));
    }
});

const ii = new Map;

const createConfig = t => ({
    type: Je,
    name: t
});

class BindingModeBehavior {
    bind(t, e) {
        ii.set(e, e.mode);
        e.mode = this.mode;
    }
    unbind(t, e) {
        e.mode = ii.get(e);
        ii.delete(e);
    }
}

class OneTimeBindingBehavior extends BindingModeBehavior {
    get mode() {
        return me;
    }
}

OneTimeBindingBehavior.$au = createConfig("oneTime");

class ToViewBindingBehavior extends BindingModeBehavior {
    get mode() {
        return ge;
    }
}

ToViewBindingBehavior.$au = createConfig("toView");

class FromViewBindingBehavior extends BindingModeBehavior {
    get mode() {
        return ve;
    }
}

FromViewBindingBehavior.$au = createConfig("fromView");

class TwoWayBindingBehavior extends BindingModeBehavior {
    get mode() {
        return xe;
    }
}

TwoWayBindingBehavior.$au = createConfig("twoWay");

const si = new WeakMap;

const ni = 200;

class DebounceBindingBehavior {
    constructor() {
        this.p = p(m);
    }
    bind(t, i, s, n) {
        const r = {
            type: "debounce",
            delay: s ?? ni,
            now: this.p.performanceNow,
            signals: e(n) ? [ n ] : n ?? g
        };
        const l = i.limit?.(r);
        if (l == null) ; else {
            si.set(i, l);
        }
    }
    unbind(t, e) {
        si.get(e)?.dispose();
        si.delete(e);
    }
}

DebounceBindingBehavior.$au = {
    type: Je,
    name: "debounce"
};

const oi = /*@__PURE__*/ Ue("ISignaler", t => t.singleton(Signaler));

class Signaler {
    constructor() {
        this.signals = i();
    }
    dispatchSignal(t) {
        const e = this.signals[t];
        if (e === undefined) {
            return;
        }
        let i;
        for (i of e.keys()) {
            i.handleChange(undefined, undefined);
        }
    }
    addSignalListener(t, e) {
        (this.signals[t] ??= new Set).add(e);
    }
    removeSignalListener(t, e) {
        this.signals[t]?.delete(e);
    }
}

class SignalBindingBehavior {
    constructor() {
        this.u = new Map;
        this.C = p(oi);
    }
    bind(t, e, ...i) {
        if (!("handleChange" in e)) {
            throw createMappedError(817);
        }
        if (i.length === 0) {
            throw createMappedError(818);
        }
        this.u.set(e, i);
        let s;
        for (s of i) {
            addSignalListener(this.C, s, e);
        }
    }
    unbind(t, e) {
        const i = this.u.get(e);
        this.u.delete(e);
        let s;
        for (s of i) {
            removeSignalListener(this.C, s, e);
        }
    }
}

SignalBindingBehavior.$au = {
    type: Je,
    name: "signal"
};

const ri = new WeakMap;

const li = 200;

class ThrottleBindingBehavior {
    constructor() {
        ({performanceNow: this.B} = p(m));
    }
    bind(t, i, s, n) {
        const r = {
            type: "throttle",
            delay: s ?? li,
            now: this.B,
            signals: e(n) ? [ n ] : n ?? g
        };
        const l = i.limit?.(r);
        if (l == null) ; else {
            ri.set(i, l);
        }
    }
    unbind(t, e) {
        ri.get(e)?.dispose();
        ri.delete(e);
    }
}

ThrottleBindingBehavior.$au = {
    type: Je,
    name: "throttle"
};

const hi = /*@__PURE__*/ Ue("IAppTask");

class $AppTask {
    constructor(t, e, i) {
        this.c = void 0;
        this.slot = t;
        this.k = e;
        this.cb = i;
    }
    register(t) {
        return this.c = t.register(Xe(hi, this));
    }
    run() {
        const t = this.k;
        const e = this.cb;
        return t === null ? e() : e(this.c.get(t));
    }
}

const ai = Te({
    creating: createAppTaskSlotHook("creating"),
    hydrating: createAppTaskSlotHook("hydrating"),
    hydrated: createAppTaskSlotHook("hydrated"),
    activating: createAppTaskSlotHook("activating"),
    activated: createAppTaskSlotHook("activated"),
    deactivating: createAppTaskSlotHook("deactivating"),
    deactivated: createAppTaskSlotHook("deactivated")
});

function createAppTaskSlotHook(t) {
    function appTaskFactory(e, i) {
        if (c(i)) {
            return new $AppTask(t, e, i);
        }
        return new $AppTask(t, null, e);
    }
    return appTaskFactory;
}

const ci = m;

class Refs {}

const ui = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    let e = false;
    return new class {
        get hideProp() {
            return e;
        }
        set hideProp(t) {
            e = t;
        }
        get(e, i) {
            return t.get(e)?.[i] ?? null;
        }
        set(i, s, n) {
            const r = t.get(i) ?? (t.set(i, new Refs), t.get(i));
            if (s in r) {
                throw new Error(`Node already associated with a controller, remove the ref "${s}" first before associating with another controller`);
            }
            if (!e) {
                i.$au ??= r;
            }
            return r[s] = n;
        }
        clear(e) {
            const i = t.get(e);
            if (i == null) return;
            t.delete(e);
            if (e.$au != null) {
                delete e.$au;
            }
        }
    };
})();

const fi = /*@__PURE__*/ Ue("INode");

function watch(t, e, i) {
    if (t == null) {
        throw createMappedError(772);
    }
    return function decorator(s, n) {
        const r = n.kind === "class";
        let l;
        let h;
        if (r) {
            if (!c(e) && (e == null || !(e in s.prototype))) {
                throw createMappedError(773, `${Be(e)}@${s.name}}`);
            }
            h = e;
            l = i ?? {};
        } else {
            if (!c(s) || n.static) {
                throw createMappedError(774, n.name);
            }
            h = s;
            l = e ?? {};
        }
        const a = new WatchDefinition(t, h, l.flush);
        if (r) {
            addDefinition(s);
        } else {
            let t = false;
            n.addInitializer(function() {
                if (!t) {
                    t = true;
                    addDefinition(this.constructor);
                }
            });
        }
        function addDefinition(t) {
            di.add(t, a);
            if (isAttributeType(t)) {
                getAttributeDefinition(t).watches.push(a);
            }
            if (isElementType(t)) {
                getElementDefinition(t).watches.push(a);
            }
        }
    };
}

class WatchDefinition {
    constructor(t, e, i = "async") {
        this.expression = t;
        this.callback = e;
        this.flush = i;
    }
}

const di = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    return Te({
        add(e, i) {
            let s = t.get(e);
            if (s == null) {
                t.set(e, s = []);
            }
            s.push(i);
        },
        getDefinitions(e) {
            return t.get(e) ?? g;
        }
    });
})();

function customAttribute(t) {
    return function(e, i) {
        i.addInitializer(function() {
            defineAttribute(t, this);
        });
        return e;
    };
}

function templateController(t) {
    return function(i, s) {
        s.addInitializer(function() {
            defineAttribute(e(t) ? {
                isTemplateController: true,
                name: t
            } : {
                isTemplateController: true,
                ...t
            }, this);
        });
        return i;
    };
}

class CustomAttributeDefinition {
    get type() {
        return Ye;
    }
    constructor(t, e, i, s, n, r, l, h, a, c, u) {
        this.Type = t;
        this.name = e;
        this.aliases = i;
        this.key = s;
        this.isTemplateController = n;
        this.bindables = r;
        this.noMultiBindings = l;
        this.watches = h;
        this.dependencies = a;
        this.containerStrategy = c;
        this.defaultProperty = u;
    }
    static create(t, i) {
        let s;
        let n;
        if (e(t)) {
            s = t;
            n = {
                name: s
            };
        } else {
            s = t.name;
            n = t;
        }
        for (const t of Object.values(je.from(n.bindables))) {
            je.i(t, i);
        }
        return new CustomAttributeDefinition(i, h(getAttributeAnnotation(i, "name"), s), a(getAttributeAnnotation(i, "aliases"), n.aliases, i.aliases), getAttributeKeyFrom(s), h(getAttributeAnnotation(i, "isTemplateController"), n.isTemplateController, i.isTemplateController, false), je.from(...je.getAll(i), getAttributeAnnotation(i, "bindables"), i.bindables, n.bindables), h(getAttributeAnnotation(i, "noMultiBindings"), n.noMultiBindings, i.noMultiBindings, false), a(di.getDefinitions(i), i.watches), a(getAttributeAnnotation(i, "dependencies"), n.dependencies, i.dependencies), h(getAttributeAnnotation(i, "containerStrategy"), n.containerStrategy, i.containerStrategy, "reuse"), h(getAttributeAnnotation(i, "defaultProperty"), n.defaultProperty, i.defaultProperty, "value"));
    }
    register(t, e) {
        const i = this.Type;
        const s = typeof e === "string" ? getAttributeKeyFrom(e) : this.key;
        const n = this.aliases;
        if (t.has(s, false)) {
            console.warn(createMappedError(154, this.name));
        }
        t.register(Ge(s, i), ...n.map(t => Ke(s, getAttributeKeyFrom(t))));
    }
    toString() {
        return `au:ca:${this.name}`;
    }
}

const pi = "custom-attribute";

const mi = /*@__PURE__*/ d(pi);

const getAttributeKeyFrom = t => `${mi}:${t}`;

const getAttributeAnnotation = (t, e) => ye(Ce(e), t);

const isAttributeType = t => c(t) && (we(mi, t) || t.$au?.type === pi);

const findAttributeControllerFor = (t, e) => ui.get(t, getAttributeKeyFrom(e)) ?? void 0;

const defineAttribute = (t, e) => {
    const i = CustomAttributeDefinition.create(t, e);
    const s = i.Type;
    be(i, s, mi, u);
    return s;
};

const getAttributeDefinition = t => {
    const e = ye(mi, t) ?? getDefinitionFromStaticAu(t, pi, CustomAttributeDefinition.create);
    if (e === void 0) {
        throw createMappedError(759, t);
    }
    return e;
};

const findClosestControllerByName = (t, i) => {
    let s = "";
    let n = "";
    if (e(i)) {
        s = getAttributeKeyFrom(i);
        n = i;
    } else {
        const t = getAttributeDefinition(i);
        s = t.key;
        n = t.name;
    }
    let r = t;
    while (r !== null) {
        const t = ui.get(r, s);
        if (t?.is(n)) {
            return t;
        }
        r = getEffectiveParentNode(r);
    }
    return null;
};

const gi = /*@__PURE__*/ Te({
    name: mi,
    keyFrom: getAttributeKeyFrom,
    isType: isAttributeType,
    for: findAttributeControllerFor,
    closest: findClosestControllerByName,
    define: defineAttribute,
    getDefinition: getAttributeDefinition,
    annotate(t, e, i) {
        be(i, t, Ce(e));
    },
    getAnnotation: getAttributeAnnotation,
    find(t, e) {
        const i = t.find(pi, e);
        return i === null ? null : ye(mi, i) ?? getDefinitionFromStaticAu(i, pi, CustomAttributeDefinition.create) ?? null;
    }
});

const vi = /*@__PURE__*/ Ue("ILifecycleHooks");

class LifecycleHooksEntry {
    constructor(t, e) {
        this.definition = t;
        this.instance = e;
    }
}

class LifecycleHooksDefinition {
    constructor(t, e) {
        this.Type = t;
        this.propertyNames = e;
    }
    static create(t, e) {
        const i = new Set;
        let s = e.prototype;
        while (s !== Ae) {
            for (const t of Le(s)) {
                if (t !== "constructor" && !t.startsWith("_")) {
                    i.add(t);
                }
            }
            s = Object.getPrototypeOf(s);
        }
        return new LifecycleHooksDefinition(e, i);
    }
}

const xi = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    const e = new WeakMap;
    return Te({
        define(t, i) {
            const s = LifecycleHooksDefinition.create(t, i);
            const n = s.Type;
            e.set(n, s);
            return {
                register(t) {
                    Ge(vi, n).register(t);
                }
            };
        },
        resolve(i) {
            let s = t.get(i);
            if (s === void 0) {
                t.set(i, s = new LifecycleHooksLookupImpl);
                const n = i.root;
                const r = n === i ? i.getAll(vi) : i.has(vi, false) ? n.getAll(vi).concat(i.getAll(vi)) : n.getAll(vi);
                let l;
                let h;
                let a;
                let c;
                let u;
                for (l of r) {
                    h = e.get(l.constructor);
                    a = new LifecycleHooksEntry(h, l);
                    for (c of h.propertyNames) {
                        u = s[c];
                        if (u === void 0) {
                            s[c] = [ a ];
                        } else {
                            u.push(a);
                        }
                    }
                }
            }
            return s;
        }
    });
})();

class LifecycleHooksLookupImpl {}

function lifecycleHooks(t, e) {
    function decorator(t, e) {
        const i = e?.metadata ?? (t[Symbol.metadata] ??= Object.create(null));
        i[v] = xi.define({}, t);
        return t;
    }
    return t == null ? decorator : decorator(t, e);
}

function isSSRTemplateController(t) {
    return "type" in t;
}

function isSSRScope(t) {
    return !("type" in t);
}

const yi = /*@__PURE__*/ Ue("ISSRContext");

function adoptSSRView(t, e, i, s, n) {
    const r = t.views[0];
    if (r == null) {
        return null;
    }
    const l = r.nodeCount ?? 1;
    const h = partitionSiblingNodes(s, [ l ]);
    if (h.length === 0 || h[0].length === 0) {
        return null;
    }
    const a = FragmentNodeSequence.adoptSiblings(n, h[0]);
    const c = e.createAdopted(i, a, r);
    c.setLocation(s);
    return {
        view: c,
        viewScope: r
    };
}

function adoptSSRViews(t, e, i, s, n) {
    const r = t.views;
    const l = r.length;
    const h = Array(l);
    for (let t = 0; t < l; ++t) {
        h[t] = r[t]?.nodeCount ?? 1;
    }
    const a = partitionSiblingNodes(s, h);
    const c = Array(l);
    for (let t = 0; t < l; ++t) {
        const l = a[t] ?? [];
        const h = FragmentNodeSequence.adoptSiblings(n, l);
        const u = e.createAdopted(i, h, r[t]);
        u.setLocation(s);
        c[t] = u;
    }
    return {
        views: c,
        viewScopes: r
    };
}

function hydrateSSRDefinition(t) {
    const e = new Map;
    for (const i of t.expressions) {
        e.set(i.id, i.ast);
    }
    const {instructions: i} = translateDefinition(t.definition, t.nestedHtmlTree, e);
    return {
        template: t.template,
        instructions: i,
        needsCompile: false
    };
}

function translateDefinition(t, e, i) {
    const s = [];
    for (let n = 0; n < t.nestedTemplates.length; n++) {
        const r = t.nestedTemplates[n];
        const l = e[n];
        if (r && l) {
            const t = translateDefinition(r, l.nested, i);
            s.push({
                template: l.html,
                instructions: t.instructions,
                name: r.name,
                needsCompile: false
            });
        }
    }
    const n = {
        exprMap: i,
        nestedDefs: s
    };
    const r = t.instructions.map(t => t.map(t => translateInstruction(t, n)));
    return {
        instructions: r,
        nestedDefs: s
    };
}

function translateInstruction(t, e) {
    switch (t.type) {
      case nt:
        {
            const i = getExpr(e.exprMap, t.exprId);
            const s = {
                type: nt,
                from: i,
                to: t.to,
                mode: t.mode
            };
            return s;
        }

      case st:
        {
            const i = t.exprIds.map(t => getExpr(e.exprMap, t));
            const s = ce(t.parts, i);
            const n = {
                type: st,
                from: s
            };
            return n;
        }

      case it:
        {
            const i = t.exprIds.map(t => getExpr(e.exprMap, t));
            const s = ce(t.parts, i);
            const n = {
                type: it,
                from: s,
                to: t.to
            };
            return n;
        }

      case et:
        {
            const i = getExpr(e.exprMap, t.exprId);
            const s = {
                type: et,
                from: i,
                to: t.to,
                capture: t.capture,
                modifier: t.modifier ?? null
            };
            return s;
        }

      case tt:
        {
            const i = getExpr(e.exprMap, t.exprId);
            const s = {
                type: tt,
                from: i,
                to: t.to
            };
            return s;
        }

      case J:
        {
            const e = {
                type: J,
                value: t.value,
                to: t.to
            };
            return e;
        }

      case Z:
        {
            const e = {
                type: Z,
                value: t.value ?? "",
                to: t.to
            };
            return e;
        }

      case Y:
        {
            const i = t.instructions.map(t => translateInstruction(t, e));
            const s = {
                type: Y,
                res: t.res,
                props: i,
                projections: null,
                containerless: t.containerless ?? false,
                captures: void 0,
                data: {}
            };
            return s;
        }

      case Q:
        {
            const i = t.instructions.map(t => translateInstruction(t, e));
            const s = {
                type: Q,
                res: t.res,
                alias: t.alias,
                props: i
            };
            return s;
        }

      case X:
        {
            const i = e.nestedDefs[t.templateIndex];
            if (!i) {
                throw new Error(`Missing nested template at index ${t.templateIndex}`);
            }
            const s = t.instructions.map(t => translateInstruction(t, e));
            const n = {
                name: i.name,
                type: "custom-element",
                template: i.template,
                instructions: i.instructions,
                needsCompile: false
            };
            const r = {
                type: X,
                def: n,
                res: t.res,
                alias: void 0,
                props: s
            };
            return r;
        }

      case G:
        {
            const i = t.bindings.map(t => {
                const i = getExpr(e.exprMap, t.exprId);
                const s = {
                    type: K,
                    from: i,
                    to: t.to
                };
                return s;
            });
            const s = {
                type: G,
                instructions: i,
                toBindingContext: t.toBindingContext
            };
            return s;
        }

      case z:
        {
            const i = getExpr(e.exprMap, t.exprId);
            const s = [];
            if (t.aux) {
                for (const i of t.aux) {
                    const t = getExpr(e.exprMap, i.exprId);
                    const n = {
                        type: U,
                        value: t,
                        to: i.name,
                        command: "bind"
                    };
                    s.push(n);
                }
            }
            const n = {
                type: z,
                forOf: i,
                to: t.to,
                props: s
            };
            return n;
        }

      default:
        throw new Error(`Unknown instruction type: ${t.type}`);
    }
}

function getExpr(t, e) {
    const i = t.get(e);
    if (!i) {
        throw new Error(`Expression not found: ${e}`);
    }
    return i;
}

function valueConverter(t) {
    return function(e, i) {
        i.addInitializer(function() {
            ki.define(t, this);
        });
        return e;
    };
}

class ValueConverterDefinition {
    constructor(t, e, i, s) {
        this.Type = t;
        this.name = e;
        this.aliases = i;
        this.key = s;
    }
    static create(t, i) {
        let s;
        let n;
        if (e(t)) {
            s = t;
            n = {
                name: s
            };
        } else {
            s = t.name;
            n = t;
        }
        return new ValueConverterDefinition(i, h(getConverterAnnotation(i, "name"), s), a(getConverterAnnotation(i, "aliases"), n.aliases, i.aliases), ki.keyFrom(s));
    }
    register(t, e) {
        const i = this.Type;
        const s = typeof e === "string" ? getValueConverterKeyFrom(e) : this.key;
        const n = this.aliases;
        if (!t.has(s, false)) {
            t.register(t.has(i, false) ? null : Ge(i, i), Ke(i, s), ...n.map(t => Ke(i, getValueConverterKeyFrom(t))));
        }
    }
}

const wi = "value-converter";

const bi = /*@__PURE__*/ d(wi);

const getConverterAnnotation = (t, e) => ye(Ce(e), t);

const getValueConverterKeyFrom = t => `${bi}:${t}`;

const ki = Te({
    name: bi,
    keyFrom: getValueConverterKeyFrom,
    isType(t) {
        return c(t) && (we(bi, t) || t.$au?.type === wi);
    },
    define(t, e) {
        const i = ValueConverterDefinition.create(t, e);
        const s = i.Type;
        be(i, s, bi, u);
        return s;
    },
    getDefinition(t) {
        const e = ye(bi, t) ?? getDefinitionFromStaticAu(t, wi, ValueConverterDefinition.create);
        if (e === void 0) {
            throw createMappedError(152, t);
        }
        return e;
    },
    annotate(t, e, i) {
        be(i, t, Ce(e));
    },
    getAnnotation: getConverterAnnotation,
    find(t, e) {
        const i = t.find(wi, e);
        return i == null ? null : ye(bi, i) ?? getDefinitionFromStaticAu(i, wi, ValueConverterDefinition.create) ?? null;
    },
    get(t, e) {
        return t.get(f(getValueConverterKeyFrom(e)));
    }
});

class BindingTargetSubscriber {
    constructor(t, e) {
        this.v = void 0;
        this.b = t;
        this.A = e;
    }
    flush() {
        if (this.b.isBound) {
            this.b.updateSource(this.v);
        }
    }
    handleChange(t, e) {
        const i = this.b;
        if (t !== Ot(i.ast, i.s, i, null)) {
            this.v = t;
            this.A.add(this);
        }
    }
}

const Ci = /*@__PURE__*/ (() => {
    function useScope(t) {
        this.s = t;
    }
    return t => {
        defineHiddenProp(t.prototype, "useScope", useScope);
    };
})();

const Si = /*@__PURE__*/ (() => {
    class ResourceLookup {}
    const t = new WeakMap;
    const e = new WeakMap;
    const i = new WeakMap;
    function evaluatorGet(t) {
        return this.l.get(t);
    }
    function evaluatorGetBehavior(t, i) {
        let s = e.get(t);
        if (s == null) {
            e.set(t, s = new ResourceLookup);
        }
        return s[i] ??= ei.get(t.l, i);
    }
    function evaluatorBindBehavior(t, e, s) {
        const n = evaluatorGetBehavior(this, t);
        if (n == null) {
            throw createMappedError(101, t);
        }
        let r = i.get(this);
        if (r == null) {
            i.set(this, r = {});
        }
        if (r[t]) {
            throw createMappedError(102, t);
        }
        n.bind?.(e, this, ...s);
    }
    function evaluatorUnbindBehavior(t, e) {
        const s = evaluatorGetBehavior(this, t);
        const n = i.get(this);
        s?.unbind?.(e, this);
        if (n != null) {
            n[t] = false;
        }
    }
    function evaluatorGetConverter(e, i) {
        let s = t.get(e);
        if (s == null) {
            t.set(e, s = new ResourceLookup);
        }
        return s[i] ??= ki.get(e.l, i);
    }
    function evaluatorBindConverter(t) {
        const e = evaluatorGetConverter(this, t);
        if (e == null) {
            throw createMappedError(103, t);
        }
        const i = e.signals;
        if (i != null) {
            const t = this.l.get(oi);
            const e = i.length;
            let s = 0;
            for (;s < e; ++s) {
                t.addSignalListener(i[s], this);
            }
        }
    }
    function evaluatorUnbindConverter(t) {
        const e = evaluatorGetConverter(this, t);
        if (e?.signals === void 0) {
            return;
        }
        const i = this.l.get(oi);
        let s = 0;
        for (;s < e.signals.length; ++s) {
            i.removeSignalListener(e.signals[s], this);
        }
    }
    function evaluatorUseConverter(t, e, i, s) {
        const n = evaluatorGetConverter(this, t);
        if (n == null) {
            throw createMappedError(103, t);
        }
        const r = n.withContext === true;
        let l = null;
        if (r) {
            const t = this.l.get(Ns);
            const e = t.controller;
            const i = e.viewModel;
            l = {
                source: i,
                binding: this
            };
        }
        switch (e) {
          case "toView":
            {
                if ("toView" in n) {
                    return r ? n.toView(i, l, ...s) : n.toView(i, ...s);
                }
                return i;
            }

          case "fromView":
            {
                if ("fromView" in n) {
                    return r ? n.fromView?.(i, l, ...s) : n.fromView?.(i, ...s);
                }
                return i;
            }
        }
    }
    return t => {
        const e = t.prototype;
        defineHiddenProp(e, "get", evaluatorGet);
        defineHiddenProp(e, "bindBehavior", evaluatorBindBehavior);
        defineHiddenProp(e, "unbindBehavior", evaluatorUnbindBehavior);
        defineHiddenProp(e, "bindConverter", evaluatorBindConverter);
        defineHiddenProp(e, "unbindConverter", evaluatorUnbindConverter);
        defineHiddenProp(e, "useConverter", evaluatorUseConverter);
    };
})();

const Bi = /*@__PURE__*/ Ue("IFlushQueue", t => t.singleton(FlushQueue));

class FlushQueue {
    constructor() {
        this.R = false;
        this.T = new Set;
    }
    get count() {
        return this.T.size;
    }
    add(t) {
        this.T.add(t);
        if (this.R) {
            return;
        }
        this.R = true;
        try {
            this.T.forEach(flushItem);
        } finally {
            this.R = false;
        }
    }
    clear() {
        this.T.clear();
        this.R = false;
    }
}

const flushItem = function(t, e, i) {
    i.delete(t);
    t.flush();
};

const Ai = /*@__PURE__*/ (() => {
    const t = new WeakSet;
    const debounced = (t, e, i) => {
        let s;
        let n;
        let r;
        let l = false;
        const callOriginalCallback = () => e(r);
        const fn = e => {
            r = e;
            if (i.isBound) {
                n = s;
                s = $t(callOriginalCallback, {
                    delay: t.delay
                });
                n?.cancel();
            } else {
                callOriginalCallback();
            }
        };
        const h = fn.dispose = () => {
            n?.cancel();
            s?.cancel();
            n = s = void 0;
        };
        fn.flush = () => {
            l = s?.status === Fe;
            h();
            if (l) {
                callOriginalCallback();
            }
        };
        return fn;
    };
    const throttled = (t, e, i) => {
        let s;
        let n;
        let r = 0;
        let l = 0;
        let h;
        let a = false;
        const now = () => t.now();
        const callOriginalCallback = () => e(h);
        const fn = e => {
            h = e;
            if (i.isBound) {
                l = now() - r;
                n = s;
                if (l > t.delay) {
                    r = now();
                    callOriginalCallback();
                } else {
                    s = $t(() => {
                        r = now();
                        callOriginalCallback();
                    }, {
                        delay: t.delay - l
                    });
                }
                n?.cancel();
            } else {
                callOriginalCallback();
            }
        };
        const c = fn.dispose = () => {
            n?.cancel();
            s?.cancel();
            n = s = void 0;
        };
        fn.flush = () => {
            a = s?.status === Fe;
            c();
            if (a) {
                callOriginalCallback();
            }
        };
        return fn;
    };
    return (e, i) => {
        defineHiddenProp(e.prototype, "limit", function(e) {
            if (t.has(this)) {
                throw createMappedError(9996);
            }
            t.add(this);
            const s = i(this, e);
            const n = e.signals;
            const r = n.length > 0 ? this.get(oi) : null;
            const l = this[s];
            const callOriginal = (...t) => l.call(this, ...t);
            const h = e.type === "debounce" ? debounced(e, callOriginal, this) : throttled(e, callOriginal, this);
            const a = r ? {
                handleChange: h.flush
            } : null;
            this[s] = h;
            if (r) {
                n.forEach(t => addSignalListener(r, t, a));
            }
            return {
                dispose: () => {
                    if (r) {
                        n.forEach(t => removeSignalListener(r, t, a));
                    }
                    t.delete(this);
                    h.dispose();
                    delete this[s];
                }
            };
        });
    };
})();

const Ri = ((t = new WeakSet) => e => function() {
    if (!t.has(this)) {
        t.add(this);
        e.call(this);
    }
})();

class AttributeBinding {
    constructor(t, e, i, s, n, r, l, h, a) {
        this.targetAttribute = r;
        this.targetProperty = l;
        this.mode = h;
        this.strict = a;
        this.isBound = false;
        this.s = void 0;
        this.L = false;
        this.v = void 0;
        this.boundFn = false;
        this.M = false;
        this.l = e;
        this.ast = s;
        this.P = t;
        this.target = n;
        this.oL = i;
        if ((this.M = l.indexOf(" ") > -1) && !AttributeBinding._.has(l)) {
            AttributeBinding._.set(l, l.split(" "));
        }
    }
    updateTarget(t) {
        const i = this.target;
        const s = this.targetAttribute;
        const n = this.targetProperty;
        switch (s) {
          case "class":
            if (this.M) {
                const e = !!t;
                for (const t of AttributeBinding._.get(n)) {
                    i.classList.toggle(t, e);
                }
            } else {
                i.classList.toggle(n, !!t);
            }
            break;

          case "style":
            {
                let s = "";
                let r = Be(t);
                if (e(r) && r.includes("!important")) {
                    s = "important";
                    r = r.replace("!important", "");
                }
                i.style.setProperty(n, r, s);
                break;
            }

          default:
            {
                if (t == null) {
                    i.removeAttribute(s);
                } else {
                    i.setAttribute(s, Be(t));
                }
            }
        }
    }
    handleChange() {
        if (!this.isBound) return;
        if (this.L) return;
        this.L = true;
        Nt(() => {
            this.L = false;
            if (!this.isBound) return;
            this.obs.version++;
            const t = Ot(this.ast, this.s, this, (this.mode & ge) > 0 ? this : null);
            this.obs.clear();
            if (t !== this.v) {
                this.v = t;
                this.updateTarget(t);
            }
        });
    }
    handleCollectionChange() {
        this.handleChange();
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        Wt(this.ast, t, this);
        if (this.mode & (ge | me)) {
            this.updateTarget(this.v = Ot(this.ast, t, this, (this.mode & ge) > 0 ? this : null));
        }
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        jt(this.ast, this.s, this);
        this.s = void 0;
        this.v = void 0;
        this.obs.clearAll();
    }
}

AttributeBinding.mix = Ri(() => {
    Ci(AttributeBinding);
    Ai(AttributeBinding, () => "updateTarget");
    zt(AttributeBinding, null);
    Si(AttributeBinding);
});

AttributeBinding._ = new Map;

class InterpolationBinding {
    constructor(t, e, i, s, n, r, l, h) {
        this.ast = s;
        this.target = n;
        this.targetProperty = r;
        this.mode = l;
        this.strict = h;
        this.isBound = false;
        this.s = void 0;
        this.L = false;
        this.P = t;
        this.oL = i;
        this.I = i.getAccessor(n, r);
        const a = s.expressions;
        const c = this.partBindings = Array(a.length);
        const u = a.length;
        let f = 0;
        for (;u > f; ++f) {
            c[f] = new InterpolationPartBinding(a[f], n, r, e, i, h, this);
        }
    }
    q() {
        if (!this.isBound || this.P.state > Is) return;
        const t = this.P.state !== _s && (this.I.type & Ne) > 0;
        if (t) {
            if (this.L) return;
            this.L = true;
            Nt(() => {
                this.L = false;
                if (!this.isBound || this.P.state > Is) return;
                this.updateTarget();
            });
        } else {
            this.updateTarget();
        }
    }
    updateTarget() {
        const t = this.partBindings;
        const e = this.ast;
        const i = this.target;
        const s = this.targetProperty;
        const n = e.parts;
        const r = t.length;
        let l = "";
        let h = 0;
        if (r === 1) {
            l = n[0] + t[0].V() + n[1];
        } else {
            l = n[0];
            for (;r > h; ++h) {
                l += t[h].V() + n[h + 1];
            }
        }
        this.I.setValue(l, i, s);
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        const e = this.partBindings;
        const i = e.length;
        let s = 0;
        for (;i > s; ++s) {
            e[s].bind(t);
        }
        this.updateTarget();
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.s = void 0;
        const t = this.partBindings;
        const e = t.length;
        let i = 0;
        for (;e > i; ++i) {
            t[i].unbind();
        }
    }
    useAccessor(t) {
        this.I = t;
    }
}

class InterpolationPartBinding {
    constructor(t, e, i, s, n, r, l) {
        this.ast = t;
        this.target = e;
        this.targetProperty = i;
        this.strict = r;
        this.owner = l;
        this.mode = ge;
        this.isBound = false;
        this.v = "";
        this.boundFn = false;
        this.D = false;
        this.l = s;
        this.oL = n;
    }
    updateTarget() {
        this.owner.q();
    }
    handleChange() {
        if (!this.isBound) return;
        this.D = true;
        this.updateTarget();
    }
    handleCollectionChange() {
        if (!this.isBound) return;
        this.D = true;
        this.updateTarget();
    }
    V() {
        if (!this.D) return this.v;
        this.obs.version++;
        const t = Ot(this.ast, this.s, this, (this.mode & ge) > 0 ? this : null);
        this.obs.clear();
        this.v = t;
        if (x(t)) {
            this.observeCollection(t);
        }
        this.D = false;
        return this.v;
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        Wt(this.ast, t, this);
        this.v = Ot(this.ast, this.s, this, (this.mode & ge) > 0 ? this : null);
        if (x(this.v)) {
            this.observeCollection(this.v);
        }
        this.D = false;
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.v = void 0;
        this.D = false;
        jt(this.ast, this.s, this);
        this.s = void 0;
        this.obs.clearAll();
    }
}

InterpolationPartBinding.mix = Ri(() => {
    Ci(InterpolationPartBinding);
    Ai(InterpolationPartBinding, () => "updateTarget");
    zt(InterpolationPartBinding, null);
    Si(InterpolationPartBinding);
});

class ContentBinding {
    constructor(t, e, i, s, n, r, l) {
        this.p = s;
        this.ast = n;
        this.target = r;
        this.strict = l;
        this.isBound = false;
        this.mode = ge;
        this.L = false;
        this.v = "";
        this.F = false;
        this.boundFn = false;
        this.l = e;
        this.P = t;
        this.oL = i;
    }
    updateTarget(t) {
        const e = this.target;
        const i = this.v;
        this.v = t;
        if (this.F) {
            i.parentNode?.removeChild(i);
            this.F = false;
        }
        if (t instanceof this.p.Node) {
            e.parentNode?.insertBefore(t, e);
            t = "";
            this.F = true;
        }
        e.textContent = Be(t ?? "");
    }
    handleChange() {
        if (!this.isBound || this.P.state > Is) return;
        if (this.L) return;
        this.L = true;
        Nt(() => {
            this.L = false;
            if (!this.isBound || this.P.state > Is) return;
            this.obs.version++;
            const t = Ot(this.ast, this.s, this, (this.mode & ge) > 0 ? this : null);
            this.obs.clear();
            if (t !== this.v) {
                if (x(t)) {
                    this.observeCollection(t);
                }
                this.updateTarget(t);
            }
        });
    }
    handleCollectionChange() {
        if (!this.isBound || this.P.state > Is) return;
        if (this.L) return;
        this.L = true;
        Nt(() => {
            this.L = false;
            if (!this.isBound || this.P.state > Is) return;
            this.obs.version++;
            const t = this.v = Ot(this.ast, this.s, this, (this.mode & ge) > 0 ? this : null);
            this.obs.clear();
            if (x(t)) {
                this.observeCollection(t);
            }
            this.updateTarget(t);
        });
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        Wt(this.ast, t, this);
        const e = this.v = Ot(this.ast, this.s, this, (this.mode & ge) > 0 ? this : null);
        if (x(e)) {
            this.observeCollection(e);
        }
        this.updateTarget(e);
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        jt(this.ast, this.s, this);
        if (this.F) {
            this.v.parentNode?.removeChild(this.v);
        }
        this.s = void 0;
        this.obs.clearAll();
    }
}

ContentBinding.mix = Ri(() => {
    Ci(ContentBinding);
    Ai(ContentBinding, () => "updateTarget");
    zt(ContentBinding, null);
    Si(ContentBinding);
});

class LetBinding {
    constructor(t, e, i, s, n, r, l) {
        this.ast = s;
        this.targetProperty = n;
        this.isBound = false;
        this.s = void 0;
        this.target = null;
        this.boundFn = false;
        this.l = e;
        this.oL = i;
        this.strict = l;
        this.H = r;
        this.P = t;
    }
    updateTarget() {
        this.target[this.targetProperty] = this.v;
    }
    handleChange() {
        if (!this.isBound || this.P.state > Is) return;
        this.obs.version++;
        this.v = Ot(this.ast, this.s, this, this);
        this.obs.clear();
        this.updateTarget();
    }
    handleCollectionChange() {
        this.handleChange();
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        this.target = this.H ? t.bindingContext : t.overrideContext;
        Wt(this.ast, t, this);
        this.v = Ot(this.ast, this.s, this, this);
        this.updateTarget();
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        jt(this.ast, this.s, this);
        this.s = void 0;
        this.obs.clearAll();
    }
}

LetBinding.mix = Ri(() => {
    Ci(LetBinding);
    Ai(LetBinding, () => "updateTarget");
    zt(LetBinding, null);
    Si(LetBinding);
});

class PropertyBinding {
    constructor(t, e, i, s, n, r, l, h) {
        this.ast = s;
        this.target = n;
        this.targetProperty = r;
        this.mode = l;
        this.strict = h;
        this.isBound = false;
        this.s = void 0;
        this.I = void 0;
        this.L = false;
        this.O = null;
        this.boundFn = false;
        this.l = e;
        this.P = t;
        this.oL = i;
    }
    updateTarget(t) {
        this.I.setValue(t, this.target, this.targetProperty);
    }
    updateSource(t) {
        Ut(this.ast, this.s, this, null, t);
    }
    handleChange() {
        if (!this.isBound || this.P.state > Is) return;
        const t = this.P.state !== _s && (this.I.type & Ne) > 0;
        if (t) {
            if (this.L) return;
            this.L = true;
            Nt(() => {
                this.L = false;
                if (!this.isBound) return;
                this.$();
            });
        } else {
            this.$();
        }
    }
    $() {
        this.obs.version++;
        const t = Ot(this.ast, this.s, this, (this.mode & ge) > 0 ? this : null);
        this.obs.clear();
        this.updateTarget(t);
    }
    handleCollectionChange() {
        this.handleChange();
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        Wt(this.ast, t, this);
        const e = this.oL;
        const i = this.mode;
        let s = this.I;
        if (!s) {
            if (i & ve) {
                s = e.getObserver(this.target, this.targetProperty);
            } else {
                s = e.getAccessor(this.target, this.targetProperty);
            }
            this.I = s;
        }
        const n = (i & ge) > 0;
        if (i & (ge | me)) {
            this.updateTarget(Ot(this.ast, this.s, this, n ? this : null));
        }
        if (i & ve) {
            s.subscribe(this.O ??= new BindingTargetSubscriber(this, this.l.get(Bi)));
            if (!n) {
                this.updateSource(s.getValue(this.target, this.targetProperty));
            }
        }
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        if (this.O) {
            this.I.unsubscribe(this.O);
            this.O = null;
        }
        jt(this.ast, this.s, this);
        this.s = void 0;
        this.obs.clearAll();
    }
    useTargetObserver(t) {
        this.I?.unsubscribe(this);
        (this.I = t).subscribe(this);
    }
    useTargetSubscriber(t) {
        if (this.O != null) {
            throw createMappedError(9995);
        }
        this.O = t;
    }
}

PropertyBinding.mix = Ri(() => {
    Ci(PropertyBinding);
    Ai(PropertyBinding, t => t.mode & ve ? "updateSource" : "updateTarget");
    zt(PropertyBinding, null);
    Si(PropertyBinding);
});

class RefBinding {
    constructor(t, e, i, s, n) {
        this.oL = e;
        this.ast = i;
        this.target = s;
        this.strict = n;
        this.isBound = false;
        this.s = void 0;
        this.l = t;
    }
    updateSource() {
        if (this.isBound) {
            this.obs.version++;
            Ut(this.ast, this.s, this, this, this.target);
            this.obs.clear();
        } else {
            Ut(this.ast, this.s, this, null, null);
        }
    }
    handleChange() {
        if (this.isBound) {
            this.updateSource();
        }
    }
    handleCollectionChange() {
        if (this.isBound) {
            this.updateSource();
        }
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        Wt(this.ast, t, this);
        this.isBound = true;
        this.updateSource();
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.obs.clearAll();
        if (Ot(this.ast, this.s, this, null) === this.target) {
            this.updateSource();
        }
        jt(this.ast, this.s, this);
        this.s = void 0;
    }
}

RefBinding.mix = Ri(() => {
    zt(RefBinding, null);
    Ai(RefBinding, () => "updateSource");
    Ci(RefBinding);
    Si(RefBinding);
});

class ListenerBindingOptions {
    constructor(t, e = false, i) {
        this.prevent = t;
        this.capture = e;
        this.onError = i;
    }
}

class ListenerBinding {
    constructor(t, e, i, s, n, r, l) {
        this.ast = e;
        this.target = i;
        this.targetEvent = s;
        this.strict = l;
        this.isBound = false;
        this.self = false;
        this.boundFn = true;
        this.N = null;
        this.l = t;
        this.W = n;
        this.N = r;
    }
    callSource(t) {
        const e = this.s.overrideContext;
        e.$event = t;
        let i = Ot(this.ast, this.s, this, null);
        delete e.$event;
        if (c(i)) {
            i = i(t);
        }
        if (i !== true && this.W.prevent) {
            t.preventDefault();
        }
    }
    handleEvent(t) {
        if (this.self) {
            if (this.target !== t.composedPath()[0]) {
                return;
            }
        }
        if (this.N?.(t) !== false) {
            try {
                this.callSource(t);
            } catch (e) {
                this.W.onError(t, e);
            }
        }
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.s = t;
        Wt(this.ast, t, this);
        this.target.addEventListener(this.targetEvent, this, this.W);
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        jt(this.ast, this.s, this);
        this.s = void 0;
        this.target.removeEventListener(this.targetEvent, this, this.W);
    }
}

ListenerBinding.mix = Ri(function() {
    Ci(ListenerBinding);
    Ai(ListenerBinding, () => "callSource");
    Si(ListenerBinding);
});

const Ti = /*@__PURE__*/ Ue("IEventModifier");

const Ei = /*@__PURE__*/ Ue("IKeyMapping", t => t.instance({
    meta: Te([ "ctrl", "alt", "shift", "meta" ]),
    keys: {
        escape: "Escape",
        enter: "Enter",
        space: "Space",
        tab: "tab",
        ...Array.from({
            length: 25
        }).reduce((t, e, i) => {
            let s = String.fromCharCode(i + 65);
            t[i + 65] = s;
            s = String.fromCharCode(i + 97);
            t[i + 97] = t[s] = s;
            return t;
        }, {})
    }
}));

class ModifiedMouseEventHandler {
    constructor() {
        this.type = [ "click", "mousedown", "mousemove", "mouseup", "dblclick", "contextmenu" ];
        this.j = p(Ei);
        this.U = [ "left", "middle", "right" ];
    }
    static register(t) {
        t.register(Ge(Ti, ModifiedMouseEventHandler));
    }
    getHandler(t) {
        const e = t.split(/[:+.]/);
        return t => {
            let i = false;
            let s = false;
            let n;
            for (n of e) {
                switch (n) {
                  case "prevent":
                    i = true;
                    continue;

                  case "stop":
                    s = true;
                    continue;

                  case "left":
                  case "middle":
                  case "right":
                    if (t.button !== this.U.indexOf(n)) return false;
                    continue;
                }
                if (this.j.meta.includes(n) && t[`${n}Key`] !== true) {
                    return false;
                }
            }
            if (i) t.preventDefault();
            if (s) t.stopPropagation();
            return true;
        };
    }
}

class ModifiedKeyboardEventHandler {
    constructor() {
        this.j = p(Ei);
        this.type = [ "keydown", "keyup" ];
    }
    static register(t) {
        t.register(Ge(Ti, ModifiedKeyboardEventHandler));
    }
    getHandler(t) {
        const e = t.split(/[:+.]/);
        return t => {
            let i = false;
            let s = false;
            let n;
            for (n of e) {
                switch (n) {
                  case "prevent":
                    i = true;
                    continue;

                  case "stop":
                    s = true;
                    continue;
                }
                if (this.j.meta.includes(n)) {
                    if (t[`${n}Key`] !== true) {
                        return false;
                    }
                    continue;
                }
                const e = this.j.keys[n];
                if (e !== t.key) {
                    return false;
                }
            }
            if (i) t.preventDefault();
            if (s) t.stopPropagation();
            return true;
        };
    }
}

class ModifiedEventHandler {
    constructor() {
        this.type = [ "$ALL" ];
    }
    static register(t) {
        t.register(Ge(Ti, ModifiedEventHandler));
    }
    getHandler(t) {
        const e = t.split(/[:+.]/);
        return t => {
            let i = false;
            let s = false;
            let n;
            for (n of e) {
                switch (n) {
                  case "prevent":
                    i = true;
                    continue;

                  case "stop":
                    s = true;
                    continue;
                }
            }
            if (i) t.preventDefault();
            if (s) t.stopPropagation();
            return true;
        };
    }
}

const Li = /*@__PURE__*/ Ue("IEventModifierHandler", t => t.instance({
    getHandler: () => null
}));

class EventModifier {
    constructor() {
        this.G = p(y(Ti)).reduce((t, e) => {
            const i = x(e.type) ? e.type : [ e.type ];
            i.forEach(i => t[i] = e);
            return t;
        }, {});
    }
    static register(t) {
        t.register(Ge(Li, EventModifier));
    }
    getHandler(t, i) {
        return e(i) ? (this.G[t] ?? this.G.$ALL)?.getHandler(i) ?? null : null;
    }
}

const Mi = {
    register(t) {
        t.register(EventModifier, ModifiedMouseEventHandler, ModifiedKeyboardEventHandler, ModifiedEventHandler);
    }
};

const Di = /*@__PURE__*/ Ue("IViewFactory");

class ViewFactory {
    constructor(t, e) {
        this.isCaching = false;
        this.h = null;
        this.K = -1;
        this.name = e.name;
        this.container = t;
        this.def = e;
    }
    setCacheSize(t, i) {
        if (t) {
            if (t === "*") {
                t = ViewFactory.maxCacheSize;
            } else if (e(t)) {
                t = parseInt(t, 10);
            }
            if (this.K === -1 || !i) {
                this.K = t;
            }
        }
        if (this.K > 0) {
            this.h = [];
        } else {
            this.h = null;
        }
        this.isCaching = this.K > 0;
    }
    canReturnToCache(t) {
        return this.h != null && this.h.length < this.K;
    }
    tryReturnToCache(t) {
        if (this.canReturnToCache(t)) {
            this.h.push(t);
            return true;
        }
        return false;
    }
    create(t) {
        const e = this.h;
        let i;
        if (e != null && e.length > 0) {
            i = e.pop();
            return i;
        }
        i = Controller.$view(this, t);
        return i;
    }
    createAdopted(t, e, i) {
        return Controller.$viewAdopted(this, t, e, i);
    }
}

ViewFactory.maxCacheSize = 65535;

const Pi = /*@__PURE__*/ (() => {
    const createComment = (t, e) => t.document.createComment(e);
    return t => {
        const e = createComment(t, "au-end");
        e.$start = createComment(t, "au-start");
        return e;
    };
})();

const insertManyBefore = (t, e, i) => {
    if (t === null) {
        return;
    }
    const s = i.length;
    let n = 0;
    while (s > n) {
        t.insertBefore(i[n], e);
        ++n;
    }
};

const createMutationObserver = (t, e) => new t.ownerDocument.defaultView.MutationObserver(e);

const isElement = t => t.nodeType === 1;

const _i = "default";

const Ii = "au-slot";

const qi = /*@__PURE__*/ Ue("IAuSlotsInfo");

class AuSlotsInfo {
    constructor(t) {
        this.projectedSlots = t;
    }
}

const Vi = /*@__PURE__*/ Ue("IAuSlotWatcher");

class AuSlotWatcherBinding {
    constructor(t, e, i, s) {
        this.X = new Set;
        this.Y = g;
        this.isBound = false;
        this.cb = (this.o = t)[e];
        this.slotName = i;
        this.Z = s;
    }
    bind() {
        this.isBound = true;
    }
    unbind() {
        this.isBound = false;
    }
    getValue() {
        return this.Y;
    }
    watch(t) {
        if (!this.X.has(t)) {
            this.X.add(t);
            t.subscribe(this);
        }
    }
    unwatch(t) {
        if (this.X.delete(t)) {
            t.unsubscribe(this);
        }
    }
    handleSlotChange(t, e) {
        if (!this.isBound) {
            return;
        }
        const i = this.Y;
        const s = [];
        const n = this.Z;
        let r;
        let l;
        for (r of this.X) {
            for (l of r === t ? e : r.nodes) {
                if (n === "$all" || isElement(l) && (n === "*" || l.matches(n))) {
                    s[s.length] = l;
                }
            }
        }
        if (s.length !== i.length || s.some((t, e) => t !== i[e])) {
            this.Y = s;
            this.cb?.call(this.o, s);
            this.subs.notify(s, i);
        }
    }
    get() {
        throw createMappedError(99, "get");
    }
}

class SlottedLifecycleHooks {
    constructor(t) {
        this.J = t;
    }
    register(t) {
        Xe(vi, this).register(t);
    }
    hydrating(t, e) {
        const i = this.J;
        const s = new AuSlotWatcherBinding(t, i.callback ?? `${Be(i.name)}Changed`, i.slotName ?? "default", i.query ?? "*");
        Pe(t, i.name, {
            enumerable: true,
            configurable: true,
            get: Ee(() => s.getValue(), {
                getObserver: () => s
            }),
            set: () => {}
        });
        Xe(Vi, s).register(e.container);
        e.addBinding(s);
    }
}

function slotted(t, e) {
    if (!Fi) {
        Fi = true;
        Gt(AuSlotWatcherBinding, null);
        lifecycleHooks()(SlottedLifecycleHooks, null);
    }
    const i = Ce("dependencies");
    function decorator(s, n) {
        if (n.kind !== "field") throw createMappedError(9990);
        const r = typeof t === "object" ? t : {
            query: t,
            slotName: e,
            name: ""
        };
        r.name = n.name;
        const l = n.metadata[i] ??= [];
        l.push(new SlottedLifecycleHooks(r));
    }
    return decorator;
}

let Fi = false;

class SpreadBinding {
    static create(t, e, i, s, n, r, l, h) {
        const a = [];
        const c = s.renderers;
        const getHydrationContext = e => {
            let i = e;
            let s = t;
            while (s != null && i > 0) {
                s = s.parent;
                --i;
            }
            if (s == null) {
                throw createMappedError(9999);
            }
            return s;
        };
        const renderSpreadInstruction = t => {
            const s = getHydrationContext(t);
            const u = new SpreadBinding(s);
            const f = n.compileSpread(s.controller.definition, s.instruction?.captures ?? g, s.controller.container, e, i);
            let d;
            for (d of f) {
                switch (d.type) {
                  case rt:
                    renderSpreadInstruction(t + 1);
                    break;

                  case ot:
                    c[d.instruction.type].render(u, findElementControllerFor(e), d.instruction, r, l, h);
                    break;

                  default:
                    c[d.type].render(u, e, d, r, l, h);
                }
            }
            a.push(u);
        };
        renderSpreadInstruction(0);
        return a;
    }
    get container() {
        return this.locator;
    }
    get definition() {
        return this.$controller.definition;
    }
    get state() {
        return this.$controller.state;
    }
    constructor(t) {
        this.isBound = false;
        this.tt = [];
        this.locator = (this.$controller = (this.et = t).controller).container;
    }
    get(t) {
        return this.locator.get(t);
    }
    bind(t) {
        if (this.isBound) return;
        this.isBound = true;
        const e = this.scope = this.et.controller.scope.parent ?? void 0;
        if (e == null) {
            throw createMappedError(9999);
        }
        this.tt.forEach(t => t.bind(e));
    }
    unbind() {
        this.tt.forEach(t => t.unbind());
        this.isBound = false;
    }
    addBinding(t) {
        this.tt.push(t);
    }
    addChild(t) {
        if (t.vmKind !== Ms) {
            throw createMappedError(9998);
        }
        this.$controller.addChild(t);
    }
}

class SpreadValueBinding {
    constructor(t, e, i, s, n, r, l) {
        this.target = e;
        this.targetKeys = i;
        this.ast = s;
        this.strict = l;
        this.isBound = false;
        this.s = void 0;
        this.boundFn = false;
        this.it = {};
        this.st = new WeakMap;
        this.P = t;
        this.oL = n;
        this.l = r;
    }
    updateTarget() {
        this.obs.version++;
        const t = Ot(this.ast, this.s, this, this);
        this.obs.clear();
        this.nt(t, true);
    }
    handleChange() {
        if (!this.isBound) return;
        this.updateTarget();
    }
    handleCollectionChange() {
        if (!this.isBound) return;
        this.updateTarget();
    }
    bind(t) {
        if (this.isBound) {
            if (this.s === t) return;
            this.unbind();
        }
        this.isBound = true;
        this.s = t;
        Wt(this.ast, t, this);
        const e = Ot(this.ast, t, this, this);
        this.nt(e, false);
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        jt(this.ast, this.s, this);
        this.s = void 0;
        let t;
        for (t in this.it) {
            this.it[t].unbind();
        }
    }
    nt(t, e) {
        let i;
        if (!w(t)) {
            for (i in this.it) {
                this.it[i]?.unbind();
            }
            return;
        }
        let s;
        let n = this.st.get(t);
        if (n == null) {
            this.st.set(t, n = Kt.fromParent(this.s, t));
        }
        for (i of this.targetKeys) {
            s = this.it[i];
            if (i in t) {
                if (s == null) {
                    s = this.it[i] = new PropertyBinding(this.P, this.l, this.oL, SpreadValueBinding.ot[i] ??= ue(i, 0), this.target, i, j.toView, this.strict);
                }
                s.bind(n);
            } else if (e) {
                s?.unbind();
            }
        }
    }
}

SpreadValueBinding.mix = Ri(() => {
    Ci(SpreadValueBinding);
    Ai(SpreadValueBinding, () => "updateTarget");
    zt(SpreadValueBinding, null);
    Si(SpreadValueBinding);
});

SpreadValueBinding.ot = {};

const addListener = (t, e, i, s) => {
    t.addEventListener(e, i, s);
};

const removeListener = (t, e, i, s) => {
    t.removeEventListener(e, i, s);
};

const mixinNodeObserverUseConfig = t => {
    let e;
    const i = t.prototype;
    defineHiddenProp(i, "subscribe", function(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            for (e of this.cf.events) {
                addListener(this.rt, e, this);
            }
            this.lt = true;
            this.ht?.();
        }
    });
    defineHiddenProp(i, "unsubscribe", function(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            for (e of this.cf.events) {
                removeListener(this.rt, e, this);
            }
            this.lt = false;
            this.ct?.();
        }
    });
    defineHiddenProp(i, "useConfig", function(t) {
        this.cf = t;
        if (this.lt) {
            for (e of this.cf.events) {
                removeListener(this.rt, e, this);
            }
            for (e of this.cf.events) {
                addListener(this.rt, e, this);
            }
        }
    });
};

const mixinNoopSubscribable = t => {
    defineHiddenProp(t.prototype, "subscribe", n);
    defineHiddenProp(t.prototype, "unsubscribe", n);
};

class ClassAttributeAccessor {
    get doNotCache() {
        return true;
    }
    constructor(t, e = {}) {
        this.obj = t;
        this.mapping = e;
        this.type = $e | Ne;
        this.v = "";
        this.ut = {};
        this.ft = 0;
    }
    getValue() {
        return this.v;
    }
    setValue(t) {
        if (t !== this.v) {
            this.v = t;
            this.dt();
        }
    }
    dt() {
        const t = this.ut;
        const e = ++this.ft;
        const i = this.obj.classList;
        const s = getClassesToAdd(this.v);
        const n = s.length;
        let r = 0;
        let l;
        if (n > 0) {
            for (;r < n; r++) {
                l = s[r];
                l = this.mapping[l] || l;
                if (l.length === 0) {
                    continue;
                }
                t[l] = this.ft;
                i.add(l);
            }
        }
        if (e === 1) {
            return;
        }
        for (l in t) {
            l = this.mapping[l] || l;
            if (t[l] === e) {
                continue;
            }
            i.remove(l);
        }
    }
}

(() => {
    mixinNoopSubscribable(ClassAttributeAccessor);
})();

function getClassesToAdd(t) {
    if (e(t)) {
        return splitClassString(t);
    }
    if (typeof t !== "object") {
        return g;
    }
    if (x(t)) {
        const e = t.length;
        if (e > 0) {
            const i = [];
            let s = 0;
            for (;e > s; ++s) {
                i.push(...getClassesToAdd(t[s]));
            }
            return i;
        } else {
            return g;
        }
    }
    const i = [];
    let s;
    for (s in t) {
        if (Boolean(t[s])) {
            if (s.includes(" ")) {
                i.push(...splitClassString(s));
            } else {
                i.push(s);
            }
        }
    }
    return i;
}

function splitClassString(t) {
    const e = t.match(/\S+/g);
    if (e === null) {
        return g;
    }
    return e;
}

const fromHydrationContext = t => ({
    $isResolver: true,
    resolve(e, i) {
        return i.get(Ns).controller.container.get(b(t));
    }
});

const Hi = /*@__PURE__*/ Ue("IRenderer");

function renderer(t, e) {
    const i = e?.metadata ?? (t[Symbol.metadata] ??= Object.create(null));
    i[v] = {
        register(e) {
            Ge(Hi, t).register(e);
        }
    };
    return t;
}

function ensureExpression(t, i, s) {
    if (e(i)) {
        return t.parse(i, s);
    }
    return i;
}

function getTarget(t) {
    if (t.viewModel != null) {
        return t.viewModel;
    }
    return t;
}

function getRefTarget(t, e) {
    if (e === "element") {
        return t;
    }
    switch (e) {
      case "controller":
        return findElementControllerFor(t);

      case "view":
        throw createMappedError(750);

      case "component":
        return findElementControllerFor(t).viewModel;

      default:
        {
            const i = findAttributeControllerFor(t, e);
            if (i !== void 0) {
                return i.viewModel;
            }
            const s = findElementControllerFor(t, {
                name: e
            });
            if (s === void 0) {
                throw createMappedError(751, e);
            }
            return s.viewModel;
        }
    }
}

const Oi = /*@__PURE__*/ renderer(class SetPropertyRenderer {
    constructor() {
        this.target = J;
    }
    render(t, e, i) {
        const s = getTarget(e);
        if (s.$observers?.[i.to] !== void 0) {
            s.$observers[i.to].setValue(i.value);
        } else {
            s[i.to] = i.value;
        }
    }
}, null);

const $i = /*@__PURE__*/ renderer(class CustomElementRenderer {
    constructor() {
        this.r = p(fs);
        this.target = Y;
    }
    render(t, e, i, s, n, r, l) {
        let h;
        let a;
        let c;
        const u = i.res;
        const f = i.projections;
        const d = t.container;
        switch (typeof u) {
          case "string":
            h = ln.find(d, u);
            if (h == null) {
                throw createMappedError(752, i, t);
            }
            break;

          default:
            h = u;
        }
        const p = i.containerless || h.containerless;
        const m = p ? convertToRenderLocation(e) : null;
        const g = createElementContainer(s, t, e, i, m, f == null ? void 0 : new AuSlotsInfo(Me(f)));
        a = g.invoke(h.Type);
        c = Controller.$el(g, a, e, i, h, m, l);
        const v = this.r.renderers;
        const x = i.props;
        const y = x.length;
        let w = 0;
        let b;
        while (y > w) {
            b = x[w];
            v[b.type].render(t, c, b, s, n, r);
            ++w;
        }
        t.addChild(c);
    }
}, null);

const Ni = /*@__PURE__*/ renderer(class CustomAttributeRenderer {
    constructor() {
        this.r = p(fs);
        this.target = Q;
    }
    render(t, e, i, s, n, r) {
        let l = t.container;
        let h;
        switch (typeof i.res) {
          case "string":
            h = gi.find(l, i.res);
            if (h == null) {
                throw createMappedError(753, i, t);
            }
            break;

          default:
            h = i.res;
        }
        const a = invokeAttribute(s, h, t, e, i, void 0, void 0);
        const c = Controller.$attr(a.ctn, a.vm, e, h);
        ui.set(e, h.key, c);
        const u = this.r.renderers;
        const f = i.props;
        const d = f.length;
        let p = 0;
        let m;
        while (d > p) {
            m = f[p];
            u[m.type].render(t, c, m, s, n, r);
            ++p;
        }
        t.addChild(c);
    }
}, null);

const Wi = /*@__PURE__*/ renderer(class TemplateControllerRenderer {
    constructor() {
        this.r = p(fs);
        this.target = X;
    }
    render(t, e, i, s, n, r, l) {
        let h = t.container;
        let a;
        switch (typeof i.res) {
          case "string":
            a = gi.find(h, i.res);
            if (a == null) {
                throw createMappedError(754, i, t);
            }
            break;

          default:
            a = i.res;
        }
        const c = this.r.getViewFactory(i.def, a.containerStrategy === "new" ? h.createChild({
            inheritParentResources: true
        }) : h);
        const u = convertToRenderLocation(e);
        const f = invokeAttribute(s, a, t, e, i, c, u);
        const d = Controller.$attr(f.ctn, f.vm, e, a, l);
        ui.set(u, a.key, d);
        f.vm.link?.(t, d, e, i);
        const p = this.r.renderers;
        const m = i.props;
        const g = m.length;
        let v = 0;
        let x;
        while (g > v) {
            x = m[v];
            p[x.type].render(t, d, x, s, n, r);
            ++v;
        }
        t.addChild(d);
    }
}, null);

const ji = /*@__PURE__*/ renderer(class LetElementRenderer {
    constructor() {
        this.target = G;
        LetBinding.mix();
    }
    render(t, e, i, s, n, r) {
        e.remove();
        const l = i.instructions;
        const h = i.toBindingContext;
        const a = t.container;
        const c = l.length;
        let u;
        let f;
        let d = 0;
        while (c > d) {
            u = l[d];
            f = ensureExpression(n, u.from, Ve);
            t.addBinding(new LetBinding(t, a, r, f, u.to, h, t.strict ?? false));
            ++d;
        }
    }
}, null);

const zi = /*@__PURE__*/ renderer(class RefBindingRenderer {
    constructor() {
        this.target = tt;
        RefBinding.mix();
    }
    render(t, e, i, s, n, r) {
        t.addBinding(new RefBinding(t.container, r, ensureExpression(n, i.from, Ve), getRefTarget(e, i.to), t.strict ?? false));
    }
}, null);

const Ui = /*@__PURE__*/ renderer(class InterpolationBindingRenderer {
    constructor() {
        this.target = it;
        InterpolationPartBinding.mix();
    }
    render(t, e, i, s, n, r) {
        const l = t.container;
        const h = new InterpolationBinding(t, l, r, ensureExpression(n, i.from, _e), getTarget(e), i.to, ge, t.strict ?? false);
        if (i.to === "class" && h.target.nodeType > 0) {
            const t = l.get(fromHydrationContext(Xs));
            h.useAccessor(new ClassAttributeAccessor(h.target, t));
        }
        t.addBinding(h);
    }
}, null);

const Gi = /*@__PURE__*/ renderer(class PropertyBindingRenderer {
    constructor() {
        this.target = nt;
        PropertyBinding.mix();
    }
    render(t, e, i, s, n, r) {
        const l = t.container;
        const h = new PropertyBinding(t, l, r, ensureExpression(n, i.from, Ve), getTarget(e), i.to, i.mode, t.strict ?? false);
        if (i.to === "class" && h.target.nodeType > 0) {
            const t = l.get(fromHydrationContext(Xs));
            h.useTargetObserver(new ClassAttributeAccessor(h.target, t));
        }
        t.addBinding(h);
    }
}, null);

const Ki = /*@__PURE__*/ renderer(class IteratorBindingRenderer {
    constructor() {
        this.target = z;
        PropertyBinding.mix();
    }
    render(t, e, i, s, n, r) {
        t.addBinding(new PropertyBinding(t, t.container, r, ensureExpression(n, i.forOf, Ie), getTarget(e), i.to, ge, t.strict ?? false));
    }
}, null);

const Xi = /*@__PURE__*/ renderer(class TextBindingRenderer {
    constructor() {
        this.target = st;
        ContentBinding.mix();
    }
    render(t, e, i, s, n, r) {
        t.addBinding(new ContentBinding(t, t.container, r, s, ensureExpression(n, i.from, Ve), e, t.strict ?? false));
    }
}, null);

const Qi = Ue("IListenerBindingOptions", t => t.singleton(class {
    constructor() {
        this.p = p(ci);
        this.prevent = false;
        this.onError = (t, e) => {
            const i = new this.p.CustomEvent("au-event-error", {
                cancelable: true,
                detail: {
                    event: t,
                    error: e
                }
            });
            this.p.window.dispatchEvent(i);
            if (i.defaultPrevented) {
                return;
            }
            throw e;
        };
    }
}));

const Yi = /*@__PURE__*/ renderer(class ListenerBindingRenderer {
    constructor() {
        this.target = et;
        this.gt = p(Li);
        this.vt = p(Qi);
        ListenerBinding.mix();
    }
    render(t, e, i, s, n) {
        t.addBinding(new ListenerBinding(t.container, ensureExpression(n, i.from, qe), e, i.to, new ListenerBindingOptions(this.vt.prevent, i.capture, this.vt.onError), this.gt.getHandler(i.to, i.modifier), t.strict ?? false));
    }
}, null);

const Zi = /*@__PURE__*/ renderer(class SetAttributeRenderer {
    constructor() {
        this.target = Z;
    }
    render(t, e, i) {
        e.setAttribute(i.to, i.value);
    }
}, null);

const Ji = /*@__PURE__*/ renderer(class SetClassAttributeRenderer {
    constructor() {
        this.target = lt;
    }
    render(t, e, i) {
        addClasses(e.classList, i.value);
    }
}, null);

const ts = /*@__PURE__*/ renderer(class SetStyleAttributeRenderer {
    constructor() {
        this.target = ht;
    }
    render(t, e, i) {
        e.style.cssText += i.value;
    }
}, null);

const es = /*@__PURE__*/ renderer(class StylePropertyBindingRenderer {
    constructor() {
        this.target = at;
        PropertyBinding.mix();
    }
    render(t, e, i, s, n, r) {
        t.addBinding(new PropertyBinding(t, t.container, r, ensureExpression(n, i.from, Ve), e.style, i.to, ge, t.strict ?? false));
    }
}, null);

const is = /*@__PURE__*/ renderer(class AttributeBindingRenderer {
    constructor() {
        this.target = ct;
        AttributeBinding.mix();
    }
    render(t, e, i, s, n, r) {
        const l = t.container;
        const h = l.has(Xs, false) ? l.get(Xs) : null;
        t.addBinding(new AttributeBinding(t, l, r, ensureExpression(n, i.from, Ve), e, i.attr, h == null ? i.to : i.to.split(/\s/g).map(t => h[t] ?? t).join(" "), ge, t.strict ?? false));
    }
}, null);

const ss = /*@__PURE__*/ renderer(class SpreadRenderer {
    constructor() {
        this.xt = p(ut);
        this.r = p(fs);
        this.target = rt;
    }
    render(t, e, i, s, n, r) {
        SpreadBinding.create(t.container.get(Ns), e, void 0, this.r, this.xt, s, n, r).forEach(e => t.addBinding(e));
    }
}, null);

const ns = /*@__PURE__*/ renderer(class SpreadValueRenderer {
    constructor() {
        this.target = dt;
        SpreadValueBinding.mix();
    }
    render(t, e, i, s, n, r) {
        const l = i.target;
        if (l === "$bindables") {
            t.addBinding(new SpreadValueBinding(t, e.viewModel, Me(e.definition.bindables), n.parse(i.from, Ve), r, t.container, t.strict ?? false));
        } else {
            throw createMappedError(820, l);
        }
    }
}, null);

function addClasses(t, e) {
    const i = e.length;
    let s = 0;
    for (let n = 0; n < i; ++n) {
        if (e.charCodeAt(n) === 32) {
            if (n !== s) {
                t.add(e.slice(s, n));
            }
            s = n + 1;
        } else if (n + 1 === i) {
            t.add(e.slice(s));
        }
    }
}

const os = "IController";

const rs = "IInstruction";

const ls = "IRenderLocation";

const hs = "ISlotsInfo";

function createElementContainer(t, e, i, s, n, r) {
    const l = e.container.createChild();
    registerHostNode(l, i, t);
    registerResolver(l, $s, new k(os, e));
    registerResolver(l, ft, new k(rs, s));
    registerResolver(l, Ks, n == null ? as : new RenderLocationProvider(n));
    registerResolver(l, Di, cs);
    registerResolver(l, qi, r == null ? us : new k(hs, r));
    return l;
}

class ViewFactoryProvider {
    get $isResolver() {
        return true;
    }
    constructor(t) {
        this.f = t;
    }
    resolve() {
        const t = this.f;
        if (t === null) {
            throw createMappedError(755);
        }
        if (!e(t.name) || t.name.length === 0) {
            throw createMappedError(756);
        }
        return t;
    }
}

function invokeAttribute(t, e, i, s, n, r, l, h) {
    const a = i instanceof Controller ? i : i.$controller;
    const c = a.container.createChild();
    registerHostNode(c, s, t);
    registerResolver(c, $s, new k(os, a));
    registerResolver(c, ft, new k(rs, n));
    registerResolver(c, Ks, l == null ? as : new k(ls, l));
    registerResolver(c, Di, r == null ? cs : new ViewFactoryProvider(r));
    registerResolver(c, qi, us);
    return {
        vm: c.invoke(e.Type),
        ctn: c
    };
}

class RenderLocationProvider {
    get name() {
        return "IRenderLocation";
    }
    get $isResolver() {
        return true;
    }
    constructor(t) {
        this.l = t;
    }
    resolve() {
        return this.l;
    }
}

const as = new RenderLocationProvider(null);

const cs = new ViewFactoryProvider(null);

const us = new k(hs, new AuSlotsInfo(g));

const fs = /*@__PURE__*/ Ue("IRendering", t => t.singleton(Rendering));

class Rendering {
    get renderers() {
        return this.yt ??= this.wt.getAll(Hi, false).reduce((t, e) => {
            t[e.target] ??= e;
            return t;
        }, i());
    }
    constructor() {
        this.bt = new WeakMap;
        this.kt = new WeakMap;
        const t = this.wt = p(C).root;
        const e = this.p = t.get(ci);
        this.ep = t.get(fe);
        this.oL = t.get(Xt);
        this.Ct = new FragmentNodeSequence(e, e.document.createDocumentFragment());
        this.St = t.has(yi, true) ? t.get(yi).preserveMarkers : false;
    }
    compile(t, e) {
        const i = e.get(ut);
        const s = this.bt;
        let n = s.get(t);
        if (n == null) {
            s.set(t, n = CustomElementDefinition.create(t.needsCompile ? i.compile(t, e) : t));
        }
        return n;
    }
    getViewFactory(t, e) {
        return new ViewFactory(e, CustomElementDefinition.getOrCreate(t));
    }
    createNodes(t) {
        if (t.enhance === true) {
            return new FragmentNodeSequence(this.p, t.template);
        }
        let i;
        let s = false;
        const n = this.kt;
        const r = this.p;
        const l = r.document;
        if (n.has(t)) {
            i = n.get(t);
        } else {
            const h = t.template;
            let a;
            if (h == null) {
                i = null;
            } else if (h instanceof r.Node) {
                if (h.nodeName === "TEMPLATE") {
                    i = h.content;
                    s = true;
                } else {
                    (i = l.createDocumentFragment()).appendChild(h.cloneNode(true));
                }
            } else {
                a = l.createElement("template");
                if (e(h)) {
                    a.innerHTML = h;
                }
                i = a.content;
                s = true;
            }
            n.set(t, i);
        }
        if (i == null) {
            return this.Ct;
        }
        const h = s ? l.importNode(i, true) : l.adoptNode(i.cloneNode(true));
        return new FragmentNodeSequence(this.p, h, this.St);
    }
    adoptNodes(t) {
        return FragmentNodeSequence.adoptChildren(this.p, t);
    }
    render(t, e, i, s) {
        const n = i.instructions;
        const r = this.renderers;
        const l = e.length;
        const h = n.length;
        const a = t.ssrScope;
        const c = a != null;
        const u = a?.children;
        let f = 0;
        let d = 0;
        let p = 0;
        let m = h;
        let g;
        let v;
        let x;
        if (!c && l !== h) {
            throw createMappedError(757, l, h);
        }
        if (s != null) {
            g = i.surrogates;
            if ((m = g.length) > 0) {
                p = 0;
                while (m > p) {
                    v = g[p];
                    r[v.type].render(t, s, v, this.p, this.ep, this.oL);
                    ++p;
                }
            }
        }
        if (h > 0) {
            while (h > d) {
                g = n[d];
                x = e[d];
                p = 0;
                m = g.length;
                while (m > p) {
                    v = g[p];
                    const e = v.type;
                    const i = e === X || e === Y;
                    const s = i && u != null ? u[f++] : undefined;
                    r[e].render(t, x, v, this.p, this.ep, this.oL, s);
                    ++p;
                }
                ++d;
            }
        }
    }
}

function cssModules(...t) {
    return new CSSModulesProcessorRegistry(t);
}

class CSSModulesProcessorRegistry {
    constructor(t) {
        this.modules = t;
    }
    register(t) {
        let e = t.get(b(Xs));
        if (e == null) {
            t.register(Xe(Xs, e = i()));
        }
        {
            Ee(e, ...this.modules);
        }
        class CompilingHook {
            compiling(t) {
                const processElement = t => {
                    const i = t.getAttributeNode("class");
                    if (!i?.value) return;
                    const s = i.value.split(/\s+/g).map(t => e[t] || t).join(" ");
                    i.value = s;
                };
                const processContainer = t => {
                    t.querySelectorAll("[class]").forEach(processElement);
                    t.querySelectorAll("template").forEach(t => processContainer(t.content));
                };
                processElement(t);
                processContainer(t.tagName === "TEMPLATE" ? t.content : t);
            }
        }
        t.register(pt.define(CompilingHook));
    }
}

function shadowCSS(...t) {
    return new ShadowDOMRegistry(t);
}

const ds = /*@__PURE__*/ Ue("IShadowDOMStyleFactory", t => t.cachedCallback(t => {
    if (AdoptedStyleSheetsStyles.supported(t.get(ci))) {
        return t.get(AdoptedStyleSheetsStylesFactory);
    }
    return t.get(StyleElementStylesFactory);
}));

class ShadowDOMRegistry {
    constructor(t) {
        this.css = t;
    }
    register(t) {
        const e = t.get(ms);
        const i = t.get(ds);
        t.register(Xe(ps, i.createStyles(this.css, e)));
    }
}

class AdoptedStyleSheetsStylesFactory {
    constructor() {
        this.p = p(ci);
        this.cache = new Map;
    }
    createStyles(t, e) {
        return new AdoptedStyleSheetsStyles(this.p, t, this.cache, e);
    }
}

class StyleElementStylesFactory {
    constructor() {
        this.p = p(ci);
    }
    createStyles(t, e) {
        return new StyleElementStyles(this.p, t, e);
    }
}

const ps = /*@__PURE__*/ Ue("IShadowDOMStyles");

const ms = /*@__PURE__*/ Ue("IShadowDOMGlobalStyles", t => t.instance({
    applyTo: n
}));

class AdoptedStyleSheetsStyles {
    constructor(t, e, i, s = null) {
        this.sharedStyles = s;
        this.styleSheets = e.map(e => {
            let s;
            if (e instanceof t.CSSStyleSheet) {
                s = e;
            } else {
                s = i.get(e);
                if (s === void 0) {
                    s = new t.CSSStyleSheet;
                    s.replaceSync(e);
                    i.set(e, s);
                }
            }
            return s;
        });
    }
    static supported(t) {
        return "adoptedStyleSheets" in t.ShadowRoot.prototype;
    }
    applyTo(t) {
        if (this.sharedStyles !== null) {
            this.sharedStyles.applyTo(t);
        }
        t.adoptedStyleSheets = [ ...t.adoptedStyleSheets, ...this.styleSheets ];
    }
}

class StyleElementStyles {
    constructor(t, e, i = null) {
        this.p = t;
        this.localStyles = e;
        this.sharedStyles = i;
    }
    applyTo(t) {
        const e = this.localStyles;
        const i = this.p;
        for (let s = e.length - 1; s > -1; --s) {
            const n = i.document.createElement("style");
            n.innerHTML = e[s];
            t.prepend(n);
        }
        if (this.sharedStyles !== null) {
            this.sharedStyles.applyTo(t);
        }
    }
}

const gs = {
    shadowDOM(t) {
        return ai.creating(C, e => {
            if (t.sharedStyles != null) {
                const i = e.get(ds);
                e.register(Xe(ms, i.createStyles(t.sharedStyles, null)));
            }
        });
    }
};

const {enter: vs, exit: xs} = Qt;

const {wrap: ys, unwrap: ws} = Yt;

class ComputedWatcher {
    get value() {
        return this.v;
    }
    constructor(t, e, i, s, n = "async") {
        this.obj = t;
        this.$get = i;
        this.isBound = false;
        this.L = false;
        this.Bt = 0;
        this.v = void 0;
        this.cb = s;
        this.oL = e;
        this.At = n;
    }
    handleChange() {
        this.run();
    }
    handleCollectionChange() {
        this.run();
    }
    bind() {
        if (this.isBound) return;
        this.compute();
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.obs.clearAll();
    }
    run() {
        if (!this.isBound) return;
        if (this.At === "sync") {
            this.Rt();
            return;
        }
        if (this.L) return;
        this.L = true;
        Nt(() => {
            this.L = false;
            this.Rt();
        });
    }
    Rt() {
        if (!this.isBound) return;
        const t = this.obj;
        const e = this.v;
        if (++this.Bt > 100) {
            throw createMappedError(655);
        }
        const i = this.compute();
        if (!S(i, e)) {
            this.cb.call(t, i, e, t);
        }
        if (!this.L) {
            this.Bt = 0;
        }
    }
    compute() {
        this.obs.version++;
        try {
            vs(this);
            return this.v = ws(this.$get.call(void 0, ys(this.obj), this));
        } finally {
            this.obs.clear();
            xs(this);
        }
    }
}

(() => {
    zt(ComputedWatcher, null);
})();

class ExpressionWatcher {
    get value() {
        return this.v;
    }
    constructor(t, e, i, s, n, r = "async") {
        this.scope = t;
        this.l = e;
        this.oL = i;
        this.isBound = false;
        this.L = false;
        this.boundFn = false;
        this.obj = t.bindingContext;
        this.Tt = s;
        this.cb = n;
        this.At = r;
    }
    handleChange() {
        this.run();
    }
    handleCollectionChange() {
        this.run();
    }
    run() {
        if (!this.isBound) return;
        if (this.At === "sync") {
            this.Rt();
            return;
        }
        if (this.L) return;
        this.L = true;
        Nt(() => {
            this.L = false;
            this.Rt();
        });
    }
    Rt() {
        if (!this.isBound) return;
        const t = this.Tt;
        const e = this.obj;
        const i = this.v;
        this.obs.version++;
        const s = Ot(t, this.scope, this, this);
        this.obs.clear();
        if (!S(s, i)) {
            this.v = s;
            this.cb.call(e, s, i, e);
        }
    }
    bind() {
        if (this.isBound) return;
        this.obs.version++;
        this.v = Ot(this.Tt, this.scope, this, this);
        this.obs.clear();
        this.isBound = true;
    }
    unbind() {
        if (!this.isBound) return;
        this.isBound = false;
        this.obs.clearAll();
        this.v = void 0;
    }
}

(() => {
    zt(ExpressionWatcher, null);
    Si(ExpressionWatcher);
})();

class Controller {
    get lifecycleHooks() {
        return this.Et;
    }
    get isActive() {
        return (this.state & (_s | Is)) > 0 && (this.state & qs) === 0;
    }
    get name() {
        if (this.parent === null) {
            switch (this.vmKind) {
              case Ms:
                return `[${this.definition.name}]`;

              case Ls:
                return this.definition.name;

              case Ds:
                return this.viewFactory.name;
            }
        }
        switch (this.vmKind) {
          case Ms:
            return `${this.parent.name}>[${this.definition.name}]`;

          case Ls:
            return `${this.parent.name}>${this.definition.name}`;

          case Ds:
            return this.viewFactory.name === this.parent.definition?.name ? `${this.parent.name}[view]` : `${this.parent.name}[view:${this.viewFactory.name}]`;
        }
    }
    get viewModel() {
        return this.Lt;
    }
    set viewModel(t) {
        this.Lt = t;
        this.Mt = t == null || this.vmKind === Ds ? HooksDefinition.none : new HooksDefinition(t);
    }
    get strict() {
        return this.definition?.strict;
    }
    constructor(t, e, i, s, n, r, l) {
        this.container = t;
        this.vmKind = e;
        this.definition = i;
        this.viewFactory = s;
        this.host = r;
        this.head = null;
        this.tail = null;
        this.next = null;
        this.parent = null;
        this.bindings = null;
        this.children = null;
        this.scope = null;
        this.isBound = false;
        this.Dt = false;
        this.mountTarget = ks;
        this.shadowRoot = null;
        this.nodes = null;
        this.location = null;
        this.Et = null;
        this.state = Ps;
        this.Pt = false;
        this.$initiator = null;
        this.$resolve = void 0;
        this.$reject = void 0;
        this.$promise = void 0;
        this._t = 0;
        this.It = 0;
        this.qt = 0;
        this.Lt = n;
        this.Mt = e === Ds ? HooksDefinition.none : new HooksDefinition(n);
        this.location = l;
        this.r = t.root.get(fs);
    }
    static getCached(t) {
        return bs.get(t);
    }
    static getCachedOrThrow(t) {
        const e = Controller.getCached(t);
        if (e === void 0) {
            throw createMappedError(500, t);
        }
        return e;
    }
    static $el(t, e, i, s, n = void 0, r = null, l = null) {
        if (bs.has(e)) {
            return bs.get(e);
        }
        {
            n = n ?? getElementDefinition(e.constructor);
        }
        registerResolver(t, n.Type, new k(n.key, e, n.Type));
        const h = new Controller(t, Ls, n, null, e, i, r);
        const a = t.get(A(Ns));
        if (n.dependencies.length > 0) {
            t.register(...n.dependencies);
        }
        registerResolver(t, Ns, new k("IHydrationContext", new HydrationContext(h, s, a)));
        bs.set(e, h);
        if (l != null) {
            h.ssrScope = l;
        }
        if (s == null || s.hydrate !== false) {
            h.hE(s);
        }
        return h;
    }
    static $attr(t, e, i, s, n) {
        if (bs.has(e)) {
            return bs.get(e);
        }
        s = s ?? getAttributeDefinition(e.constructor);
        registerResolver(t, s.Type, new k(s.key, e, s.Type));
        const r = new Controller(t, Ms, s, null, e, i, null);
        if (s.dependencies.length > 0) {
            t.register(...s.dependencies);
        }
        bs.set(e, r);
        if (n != null) {
            r.ssrScope = n;
        }
        r.Vt();
        return r;
    }
    static $view(t, e = void 0, i = null) {
        const s = new Controller(t.container, Ds, null, t, null, i, null);
        s.parent = e ?? null;
        s.Ft();
        return s;
    }
    static $viewAdopted(t, e, i, s) {
        const n = new Controller(t.container, Ds, null, t, null, null, null);
        n.parent = e ?? null;
        if (s != null) {
            n.ssrScope = s;
        }
        n.Ht(i);
        return n;
    }
    hE(t) {
        const e = this.container;
        const i = this.Lt;
        const s = this.definition;
        this.scope = Kt.create(i, null, true);
        if (s.watches.length > 0) {
            createWatchers(this, e, s, i);
        }
        createObservers(this, s, i);
        this.Et = xi.resolve(e);
        e.register(s.Type);
        if (s.injectable !== null) {
            registerResolver(e, s.injectable, new k("definition.injectable", i));
        }
        if (t == null || t.hydrate !== false) {
            this.hS();
            this.hC();
        }
    }
    hS() {
        if (this.Et.hydrating != null) {
            this.Et.hydrating.forEach(callHydratingHook, this);
        }
        if (this.Mt.Ot) {
            this.Lt.hydrating(this);
        }
        const t = this.definition;
        const e = this.$t = this.r.compile(t, this.container);
        const i = e.shadowOptions;
        const s = e.hasSlots;
        const n = e.containerless;
        const r = this.host;
        let l = this.location;
        if (n && l == null) {
            l = this.location = convertToRenderLocation(r);
        }
        Us(r, nn, this);
        Us(r, t.key, this);
        if (i !== null || s) {
            if (l != null) {
                throw createMappedError(501);
            }
            Us(this.shadowRoot = r.attachShadow(i ?? Es), nn, this);
            Us(this.shadowRoot, t.key, this);
            this.mountTarget = Ss;
        } else if (l != null) {
            if (r !== l) {
                Us(l, nn, this);
                Us(l, t.key, this);
            }
            this.mountTarget = Bs;
        } else {
            this.mountTarget = Cs;
        }
        this.Lt.$controller = this;
        if (this.ssrScope != null) {
            this.nodes = this.r.adoptNodes(r);
        } else {
            this.nodes = this.r.createNodes(e);
        }
        if (this.Et.hydrated !== void 0) {
            this.Et.hydrated.forEach(callHydratedHook, this);
        }
        if (this.Mt.Nt) {
            this.Lt.hydrated(this);
        }
    }
    hC() {
        const t = this.nodes.findTargets();
        this.r.render(this, t, this.$t, this.host);
        if (this.Et.created !== void 0) {
            this.Et.created.forEach(callCreatedHook, this);
        }
        if (this.Mt.Wt) {
            this.Lt.created(this);
        }
    }
    Vt() {
        const t = this.definition;
        const e = this.Lt;
        if (t.watches.length > 0) {
            createWatchers(this, this.container, t, e);
        }
        createObservers(this, t, e);
        e.$controller = this;
        this.Et = xi.resolve(this.container);
        if (this.Et.created !== void 0) {
            this.Et.created.forEach(callCreatedHook, this);
        }
        if (this.Mt.Wt) {
            this.Lt.created(this);
        }
    }
    Ft() {
        this.$t = this.r.compile(this.viewFactory.def, this.container);
        this.r.render(this, (this.nodes = this.r.createNodes(this.$t)).findTargets(), this.$t, this.host);
    }
    Ht(t) {
        this.$t = this.r.compile(this.viewFactory.def, this.container);
        this.nodes = t;
        const e = t.findTargets();
        this.r.render(this, e, this.$t, this.host);
    }
    activate(t, e, i) {
        switch (this.state) {
          case Ps:
          case Vs:
            if (!(e === null || e.isActive)) {
                return;
            }
            this.state = _s;
            break;

          case qs:
            if (!(e === null || e.isActive)) {
                return;
            }
            this.Dt = false;
            this.isBound = false;
            this.head = this.tail = this.next = null;
            this._t = 0;
            this.It = 0;
            this.qt = 0;
            this.state = _s;
            break;

          case Is:
            return;

          case Hs:
            throw createMappedError(502, this.name);

          default:
            throw createMappedError(503, this.name, stringifyState(this.state));
        }
        this.parent = e;
        switch (this.vmKind) {
          case Ls:
            this.scope.parent = i ?? null;
            break;

          case Ms:
            this.scope = i ?? null;
            break;

          case Ds:
            if (i === void 0 || i === null) {
                throw createMappedError(504, this.name);
            }
            this.scope = i;
            break;
        }
        this.$initiator = t;
        this.jt();
        let s = void 0;
        if (this.vmKind !== Ds && this.Et.binding != null) {
            s = R(...this.Et.binding.map(callBindingHook, this));
        }
        if (this.Mt.zt) {
            s = R(s, this.Lt.binding(this.$initiator, this.parent));
        }
        if (T(s)) {
            this.Ut();
            s.then(() => {
                this.Dt = true;
                if (this.state !== _s) {
                    this.Gt();
                } else {
                    this.bind();
                }
            }).catch(t => {
                this.Kt(t);
            });
            return this.$promise;
        }
        this.Dt = true;
        this.bind();
        return this.$promise;
    }
    bind() {
        let t = 0;
        let e = 0;
        let i = void 0;
        if (this.bindings !== null) {
            t = 0;
            e = this.bindings.length;
            while (e > t) {
                this.bindings[t].bind(this.scope);
                ++t;
            }
        }
        if (this.vmKind !== Ds && this.Et.bound != null) {
            i = R(...this.Et.bound.map(callBoundHook, this));
        }
        if (this.Mt.Xt) {
            i = R(i, this.Lt.bound(this.$initiator, this.parent));
        }
        if (T(i)) {
            this.Ut();
            i.then(() => {
                this.isBound = true;
                if (this.state !== _s) {
                    this.Gt();
                } else {
                    this.Qt();
                }
            }).catch(t => {
                this.Kt(t);
            });
            return;
        }
        this.isBound = true;
        this.Qt();
    }
    Yt(...t) {
        switch (this.mountTarget) {
          case Cs:
            this.host.append(...t);
            break;

          case Ss:
            this.shadowRoot.append(...t);
            break;

          case Bs:
            {
                let e = 0;
                for (;e < t.length; ++e) {
                    this.location.parentNode.insertBefore(t[e], this.location);
                }
                break;
            }
        }
    }
    Qt() {
        switch (this.mountTarget) {
          case Cs:
            this.nodes.appendTo(this.host, this.definition != null && this.definition.enhance);
            break;

          case Ss:
            {
                const t = this.container;
                const e = t.has(ps, false) ? t.get(ps) : t.get(ms);
                e.applyTo(this.shadowRoot);
                this.nodes.appendTo(this.shadowRoot);
                break;
            }

          case Bs:
            this.nodes.insertBefore(this.location);
            break;
        }
        let t = 0;
        let e = void 0;
        if (this.vmKind !== Ds && this.Et.attaching != null) {
            e = R(...this.Et.attaching.map(callAttachingHook, this));
        }
        if (this.Mt.Zt) {
            e = R(e, this.Lt.attaching(this.$initiator, this.parent));
        }
        if (T(e)) {
            this.Ut();
            this.jt();
            e.then(() => {
                this.Gt();
            }).catch(t => {
                this.Kt(t);
            });
        }
        if (this.children !== null) {
            for (;t < this.children.length; ++t) {
                void this.children[t].activate(this.$initiator, this, this.scope);
            }
        }
        this.Gt();
    }
    deactivate(t, e) {
        let i = void 0;
        switch (this.state & ~Fs) {
          case Is:
            this.state = qs;
            break;

          case _s:
            this.state = qs;
            i = this.$promise?.catch(n);
            break;

          case qs:
            return this.$promise;

          case Ps:
          case Vs:
          case Hs:
          case Vs | Hs:
            return;

          default:
            throw createMappedError(505, this.name, this.state);
        }
        this.$initiator = t;
        if (t === this) {
            this.Jt();
        }
        const s = T(i) && t !== this;
        if (s) {
            t.Ut();
            t.Jt();
        }
        let r = 0;
        let l;
        if (this.children !== null) {
            for (r = 0; r < this.children.length; ++r) {
                void this.children[r].deactivate(t, this);
            }
        }
        return E(i, () => {
            if (this.isBound) {
                if (this.vmKind !== Ds && this.Et.detaching != null) {
                    l = R(...this.Et.detaching.map(callDetachingHook, this));
                }
                if (this.Mt.te) {
                    l = R(l, this.Lt.detaching(this.$initiator, this.parent));
                }
            }
            if (T(l)) {
                this.Ut();
                t.Jt();
                l.then(() => {
                    t.ee();
                }).catch(e => {
                    t.Kt(e);
                });
            }
            if (t.head === null) {
                t.head = this;
            } else {
                t.tail.next = this;
            }
            t.tail = this;
            if (t !== this) {
                if (s) {
                    t.ee();
                }
                return;
            }
            this.ee();
            return this.$promise;
        });
    }
    removeNodes() {
        switch (this.vmKind) {
          case Ls:
          case Ds:
            this.nodes.remove();
            this.nodes.unlink();
        }
    }
    unbind() {
        let t = 0;
        if (this.bindings !== null) {
            for (;t < this.bindings.length; ++t) {
                this.bindings[t].unbind();
            }
        }
        this.parent = null;
        switch (this.vmKind) {
          case Ms:
            this.scope = null;
            break;

          case Ds:
            this.scope = null;
            if ((this.state & Fs) === Fs && !this.viewFactory.tryReturnToCache(this) && this.$initiator === this) {
                this.dispose();
            }
            break;

          case Ls:
            this.scope.parent = null;
            break;
        }
        this.state = Vs;
        this.$initiator = null;
        this.ie();
    }
    Ut() {
        if (this.$promise === void 0) {
            this.$promise = new Promise((t, e) => {
                this.$resolve = t;
                this.$reject = e;
            });
            if (this.$initiator !== this) {
                this.parent.Ut();
            }
        }
    }
    ie() {
        if (this.$promise !== void 0) {
            Ws = this.$resolve;
            this.$resolve = this.$reject = this.$promise = void 0;
            Ws();
            Ws = void 0;
        }
    }
    Kt(t) {
        if (this.$promise !== void 0) {
            js = this.$reject;
            this.$resolve = this.$reject = this.$promise = void 0;
            js(t);
            js = void 0;
        }
        if (this.$initiator !== this) {
            this.parent.Kt(t);
        }
    }
    jt() {
        ++this._t;
        if (this.$initiator !== this) {
            this.parent.jt();
        }
    }
    Gt() {
        if (this.state !== _s) {
            --this._t;
            this.ie();
            if (this.$initiator !== this) {
                this.parent.Gt();
            }
            return;
        }
        if (--this._t === 0) {
            if (this.vmKind !== Ds && this.Et.attached != null) {
                zs = R(...this.Et.attached.map(callAttachedHook, this));
            }
            if (this.Mt.se) {
                zs = R(zs, this.Lt.attached(this.$initiator));
            }
            if (T(zs)) {
                this.Ut();
                zs.then(() => {
                    this.state = Is;
                    this.ie();
                    if (this.$initiator !== this) {
                        this.parent.Gt();
                    }
                }).catch(t => {
                    this.Kt(t);
                });
                zs = void 0;
                return;
            }
            zs = void 0;
            this.state = Is;
            this.ie();
        }
        if (this.$initiator !== this) {
            this.parent.Gt();
        }
    }
    Jt() {
        ++this.It;
    }
    ee() {
        if (--this.It === 0) {
            this.ne();
            this.removeNodes();
            let t = this.$initiator.head;
            let e = void 0;
            while (t !== null) {
                if (t !== this) {
                    if (t.debug) {
                        t.logger.trace(`detach()`);
                    }
                    t.removeNodes();
                }
                if (t.Dt) {
                    if (t.vmKind !== Ds && t.Et.unbinding != null) {
                        e = R(...t.Et.unbinding.map(callUnbindingHook, t));
                    }
                    if (t.Mt.oe) {
                        if (t.debug) {
                            t.logger.trace("unbinding()");
                        }
                        e = R(e, t.viewModel.unbinding(t.$initiator, t.parent));
                    }
                }
                if (T(e)) {
                    this.Ut();
                    this.ne();
                    e.then(() => {
                        this.re();
                    }).catch(t => {
                        this.Kt(t);
                    });
                }
                e = void 0;
                t = t.next;
            }
            this.re();
        }
    }
    ne() {
        ++this.qt;
    }
    re() {
        if (--this.qt === 0) {
            let t = this.$initiator.head;
            let e = null;
            while (t !== null) {
                if (t !== this) {
                    t.Dt = false;
                    t.isBound = false;
                    t.unbind();
                }
                e = t.next;
                t.next = null;
                t = e;
            }
            this.head = this.tail = null;
            this.Dt = false;
            this.isBound = false;
            this.unbind();
        }
    }
    addBinding(t) {
        if (this.bindings === null) {
            this.bindings = [ t ];
        } else {
            this.bindings[this.bindings.length] = t;
        }
    }
    addChild(t) {
        if (this.children === null) {
            this.children = [ t ];
        } else {
            this.children[this.children.length] = t;
        }
    }
    is(t) {
        switch (this.vmKind) {
          case Ms:
          case Ls:
            {
                return this.definition.name === t;
            }

          case Ds:
            return this.viewFactory.name === t;
        }
    }
    setHost(t) {
        if (this.vmKind === Ls) {
            Us(t, nn, this);
            Us(t, this.definition.key, this);
        }
        this.host = t;
        this.mountTarget = Cs;
        return this;
    }
    setShadowRoot(t) {
        if (this.vmKind === Ls) {
            Us(t, nn, this);
            Us(t, this.definition.key, this);
        }
        this.shadowRoot = t;
        this.mountTarget = Ss;
        return this;
    }
    setLocation(t) {
        if (this.vmKind === Ls) {
            Us(t, nn, this);
            Us(t, this.definition.key, this);
        }
        this.location = t;
        this.mountTarget = Bs;
        return this;
    }
    release() {
        this.state |= Fs;
    }
    dispose() {
        if ((this.state & Hs) === Hs) {
            return;
        }
        this.state |= Hs;
        if (this.Mt.le) {
            this.Lt.dispose();
        }
        if (this.children !== null) {
            this.children.forEach(callDispose);
            this.children = null;
        }
        this.scope = null;
        this.nodes = null;
        this.location = null;
        this.viewFactory = null;
        if (this.Lt !== null) {
            bs.delete(this.Lt);
            this.Lt = null;
        }
        this.Lt = null;
        this.host = null;
        this.shadowRoot = null;
        this.container.disposeResolvers();
    }
    accept(t) {
        if (t(this) === true) {
            return true;
        }
        if (this.Mt.he && this.Lt.accept(t) === true) {
            return true;
        }
        if (this.children !== null) {
            const {children: e} = this;
            for (let i = 0, s = e.length; i < s; ++i) {
                if (e[i].accept(t) === true) {
                    return true;
                }
            }
        }
    }
}

const bs = new WeakMap;

const ks = 0;

const Cs = 1;

const Ss = 2;

const Bs = 3;

const As = Te({
    none: ks,
    host: Cs,
    shadowRoot: Ss,
    location: Bs
});

const Rs = B(Zt);

function createObservers(t, e, i) {
    const s = e.bindables;
    const r = Le(s);
    const l = r.length;
    if (l === 0) return;
    const h = t.container.get(Xt);
    const a = "propertiesChanged" in i;
    const c = t.vmKind === Ds ? void 0 : t.container.get(Rs);
    const u = a ? (() => {
        let e = {};
        let s = false;
        let n = 0;
        const callPropertiesChanged = () => {
            if (!s) {
                s = true;
                Nt(() => {
                    s = false;
                    const r = e;
                    e = {};
                    n = 0;
                    if (t.isBound) {
                        i.propertiesChanged?.(r);
                        if (n > 0) {
                            callPropertiesChanged();
                        }
                    }
                });
            }
        };
        return (t, i, s) => {
            e[t] = {
                newValue: i,
                oldValue: s
            };
            n++;
            callPropertiesChanged();
        };
    })() : n;
    for (let e = 0; e < l; ++e) {
        const l = r[e];
        const f = s[l];
        const d = f.callback;
        const p = h.getObserver(i, l);
        if (f.set !== n) {
            if (p.useCoercer?.(f.set, c) !== true) {
                throw createMappedError(507, l);
            }
        }
        if (d in i || i.propertyChanged != null || a) {
            const callback = (e, s) => {
                if (t.isBound) {
                    i[d]?.(e, s);
                    i.propertyChanged?.(l, e, s);
                    u(l, e, s);
                }
            };
            if (p.useCallback?.(callback) !== true) {
                throw createMappedError(508, l);
            }
        }
    }
}

const Ts = new Map;

const getAccessScopeAst = t => {
    let e = Ts.get(t);
    if (e == null) {
        e = ue(t, 0);
        Ts.set(t, e);
    }
    return e;
};

function createWatchers(t, i, s, n) {
    const r = i.get(Xt);
    const l = i.get(fe);
    const h = s.watches;
    const a = t.vmKind === Ls ? t.scope : Kt.create(n, null, true);
    const u = h.length;
    let f;
    let d;
    let p;
    let m;
    let g = 0;
    for (;u > g; ++g) {
        ({expression: f, callback: d, flush: m} = h[g]);
        d = c(d) ? d : Reflect.get(n, d);
        if (!c(d)) {
            throw createMappedError(506, d);
        }
        if (c(f)) {
            t.addBinding(new ComputedWatcher(n, r, f, d, m));
        } else {
            p = e(f) ? l.parse(f, Ve) : getAccessScopeAst(f);
            t.addBinding(new ExpressionWatcher(a, i, r, p, d, m));
        }
    }
}

function isCustomElementController(t) {
    return t instanceof Controller && t.vmKind === Ls;
}

function isCustomElementViewModel(t) {
    return isElementType(t?.constructor);
}

class HooksDefinition {
    constructor(t) {
        this.ae = "define" in t;
        this.Ot = "hydrating" in t;
        this.Nt = "hydrated" in t;
        this.Wt = "created" in t;
        this.zt = "binding" in t;
        this.Xt = "bound" in t;
        this.Zt = "attaching" in t;
        this.se = "attached" in t;
        this.te = "detaching" in t;
        this.oe = "unbinding" in t;
        this.le = "dispose" in t;
        this.he = "accept" in t;
    }
}

HooksDefinition.none = new HooksDefinition({});

const Es = {
    mode: "open"
};

const Ls = "customElement";

const Ms = "customAttribute";

const Ds = "synthetic";

const Ps = 0;

const _s = 1;

const Is = 2;

const qs = 4;

const Vs = 8;

const Fs = 16;

const Hs = 32;

const Os = /*@__PURE__*/ Te({
    none: Ps,
    activating: _s,
    activated: Is,
    deactivating: qs,
    deactivated: Vs,
    released: Fs,
    disposed: Hs
});

function stringifyState(t) {
    const e = [];
    if ((t & _s) === _s) {
        e.push("activating");
    }
    if ((t & Is) === Is) {
        e.push("activated");
    }
    if ((t & qs) === qs) {
        e.push("deactivating");
    }
    if ((t & Vs) === Vs) {
        e.push("deactivated");
    }
    if ((t & Fs) === Fs) {
        e.push("released");
    }
    if ((t & Hs) === Hs) {
        e.push("disposed");
    }
    return e.length === 0 ? "none" : e.join("|");
}

const $s = /*@__PURE__*/ Ue("IController");

const Ns = /*@__PURE__*/ Ue("IHydrationContext");

class HydrationContext {
    constructor(t, e, i) {
        this.instruction = e;
        this.parent = i;
        this.controller = t;
    }
}

function callDispose(t) {
    t.dispose();
}

function callCreatedHook(t) {
    t.instance.created(this.Lt, this);
}

function callHydratingHook(t) {
    t.instance.hydrating(this.Lt, this);
}

function callHydratedHook(t) {
    t.instance.hydrated(this.Lt, this);
}

function callBindingHook(t) {
    return t.instance.binding(this.Lt, this["$initiator"], this.parent);
}

function callBoundHook(t) {
    return t.instance.bound(this.Lt, this["$initiator"], this.parent);
}

function callAttachingHook(t) {
    return t.instance.attaching(this.Lt, this["$initiator"], this.parent);
}

function callAttachedHook(t) {
    return t.instance.attached(this.Lt, this["$initiator"]);
}

function callDetachingHook(t) {
    return t.instance.detaching(this.Lt, this["$initiator"], this.parent);
}

function callUnbindingHook(t) {
    return t.instance.unbinding(this.Lt, this["$initiator"], this.parent);
}

let Ws;

let js;

let zs;

const Us = ui.set;

const Gs = /*@__PURE__*/ Ue("IEventTarget", t => t.cachedCallback(t => {
    if (t.has(an, true)) {
        return t.get(an).host;
    }
    return t.get(ci).document;
}));

const Ks = /*@__PURE__*/ Ue("IRenderLocation");

const Xs = /*@__PURE__*/ Ue("ICssClassMapping");

const Qs = new WeakMap;

function getEffectiveParentNode(t) {
    if (Qs.has(t)) {
        return Qs.get(t);
    }
    let e = 0;
    let i = t.nextSibling;
    while (i !== null) {
        if (i.nodeType === 8) {
            switch (i.textContent) {
              case "au-start":
                ++e;
                break;

              case "au-end":
                if (e-- === 0) {
                    return i;
                }
            }
        }
        i = i.nextSibling;
    }
    if (t.parentNode === null && t.nodeType === 11) {
        const e = findElementControllerFor(t, {
            optional: true
        });
        if (e == null) {
            return null;
        }
        if (e.mountTarget === As.shadowRoot) {
            return getEffectiveParentNode(e.host);
        }
    }
    return t.parentNode;
}

function setEffectiveParentNode(t, e) {
    if (t.platform !== void 0 && !(t instanceof t.platform.Node)) {
        const i = t.childNodes;
        for (let t = 0, s = i.length; t < s; ++t) {
            Qs.set(i[t], e);
        }
    } else {
        Qs.set(t, e);
    }
}

function convertToRenderLocation(t) {
    if (isRenderLocation(t)) {
        return t;
    }
    const e = t.ownerDocument.createComment("au-end");
    const i = e.$start = t.ownerDocument.createComment("au-start");
    const s = t.parentNode;
    if (s !== null) {
        s.replaceChild(e, t);
        s.insertBefore(i, e);
    }
    return e;
}

function isRenderLocation(t) {
    return t.textContent === "au-end";
}

function findMatchingEndMarker(t) {
    let e = 1;
    let i = t.nextSibling;
    while (i !== null) {
        if (i.nodeType === 8) {
            const t = i.textContent;
            if (t === "au-start") {
                e++;
            } else if (t === "au-end") {
                if (--e === 0) {
                    return i;
                }
            }
        }
        i = i.nextSibling;
    }
    return null;
}

function partitionSiblingNodes(t, e) {
    const i = t;
    const s = t.$start;
    if (s == null) {
        return [];
    }
    const n = [];
    let r = s.nextSibling;
    for (let t = 0, s = e.length; s > t; ++t) {
        const s = e[t];
        const l = [];
        for (let t = 0; t < s && r != null && r !== i; ++t) {
            l.push(r);
            r = r.nextSibling;
        }
        n.push(l);
    }
    return n;
}

function collectMarkerComments(t) {
    const e = [];
    const i = t.ownerDocument.createTreeWalker(t, 128);
    let s;
    while ((s = i.nextNode()) !== null) {
        if (s.textContent === "au") {
            e.push(s);
        }
    }
    return e;
}

class FragmentNodeSequence {
    get firstChild() {
        return this.ce;
    }
    get lastChild() {
        return this.ue;
    }
    constructor(t, e, i) {
        this.platform = t;
        this.next = void 0;
        this.fe = false;
        this.de = false;
        this.ref = null;
        const s = collectMarkerComments(this.f = e);
        let n = 0;
        let r = s.length;
        let l = this.t = Array(r);
        let h;
        let a;
        while (r > n) {
            a = s[n];
            h = a.nextSibling;
            if (!i) {
                a.remove();
            }
            if (h.nodeType === 8) {
                const t = h;
                h = h.nextSibling;
                h.$start = t;
            }
            l[n] = h;
            ++n;
        }
        const c = e.childNodes;
        const u = this.childNodes = Array(r = c.length);
        n = 0;
        while (r > n) {
            u[n] = c[n];
            ++n;
        }
        this.ce = e.firstChild;
        this.ue = e.lastChild;
    }
    static adoptChildren(t, e) {
        const i = t.document.createDocumentFragment();
        const s = new FragmentNodeSequence(t, i);
        const n = e.childNodes;
        const r = Array(n.length);
        for (let t = 0, e = n.length; e > t; ++t) {
            r[t] = n[t];
        }
        s.childNodes = r;
        s.ce = r[0] ?? null;
        s.ue = r[r.length - 1] ?? null;
        s.fe = true;
        s.t = s.pe(e);
        return s;
    }
    static adoptSiblings(t, e) {
        const i = t.document.createDocumentFragment();
        const s = new FragmentNodeSequence(t, i);
        s.childNodes = e;
        s.ce = e[0] ?? null;
        s.ue = e[e.length - 1] ?? null;
        s.fe = true;
        s.t = s.me(e);
        return s;
    }
    me(t) {
        const e = [];
        const i = [];
        const collectFromChildren = t => {
            let e = 0;
            for (let s = 0, n = t.length; n > s; ++s) {
                const n = t[s];
                if (n.nodeType === 8) {
                    const t = n.textContent;
                    if (t === "au") {
                        if (e === 0) {
                            i.push(n);
                        }
                        continue;
                    } else if (t === "au-start") {
                        e++;
                        continue;
                    } else if (t === "au-end") {
                        e--;
                        continue;
                    }
                }
                if (e > 0) continue;
                if (n.nodeType === 1) {
                    const t = n;
                    if (t.tagName.includes("-")) {
                        continue;
                    }
                    collectFromChildren(t.childNodes);
                }
            }
        };
        for (let e = 0, s = t.length; s > e; ++e) {
            const s = t[e];
            if (s.nodeType === 8 && s.textContent === "au") {
                i.push(s);
            } else if (s.nodeType === 1) {
                const t = s;
                if (!t.tagName.includes("-")) {
                    collectFromChildren(t.childNodes);
                }
            }
        }
        for (let t = 0, s = i.length; s > t; ++t) {
            const s = i[t];
            let n = s.nextSibling;
            if (n.nodeType === 8 && n.textContent === "au-start") {
                const t = n;
                const e = findMatchingEndMarker(n);
                if (e !== null) {
                    n = e;
                    n.$start = t;
                }
            }
            e[t] = n;
        }
        return e;
    }
    pe(t) {
        const e = [];
        const i = [];
        const collectSiblings = t => {
            let e = 0;
            for (let s = 0, n = t.length; n > s; ++s) {
                const n = t[s];
                if (n.nodeType === 8) {
                    const t = n.textContent;
                    if (t === "au") {
                        if (e === 0) {
                            i.push(n);
                        }
                        continue;
                    } else if (t === "au-start") {
                        e++;
                        continue;
                    } else if (t === "au-end") {
                        e--;
                        continue;
                    }
                }
                if (e > 0) continue;
                if (n.nodeType === 1) {
                    const t = n;
                    if (t.tagName.includes("-")) {
                        continue;
                    }
                    collectSiblings(t.childNodes);
                }
            }
        };
        collectSiblings(t.childNodes);
        for (let t = 0, s = i.length; s > t; ++t) {
            const s = i[t];
            let n = s.nextSibling;
            if (n.nodeType === 8 && n.textContent === "au-start") {
                const t = n;
                const e = findMatchingEndMarker(n);
                if (e !== null) {
                    n = e;
                    n.$start = t;
                }
            }
            e[t] = n;
        }
        return e;
    }
    findTargets() {
        return this.t;
    }
    insertBefore(t) {
        if (this.de && !!this.ref) {
            this.addToLinked();
        } else {
            const e = t.parentNode;
            if (this.fe) {
                let i = this.ce;
                let s;
                const n = this.ue;
                while (i != null) {
                    s = i.nextSibling;
                    e.insertBefore(i, t);
                    if (i === n) {
                        break;
                    }
                    i = s;
                }
            } else {
                this.fe = true;
                t.parentNode.insertBefore(this.f, t);
            }
        }
    }
    appendTo(t, e = false) {
        if (this.fe) {
            let e = this.ce;
            let i;
            const s = this.ue;
            while (e != null) {
                i = e.nextSibling;
                t.appendChild(e);
                if (e === s) {
                    break;
                }
                e = i;
            }
        } else {
            this.fe = true;
            if (!e) {
                t.appendChild(this.f);
            }
        }
    }
    remove() {
        if (this.fe) {
            this.fe = false;
            const t = this.f;
            const e = this.ue;
            let i;
            let s = this.ce;
            while (s !== null) {
                i = s.nextSibling;
                t.appendChild(s);
                if (s === e) {
                    break;
                }
                s = i;
            }
        }
    }
    addToLinked() {
        const t = this.ref;
        const e = t.parentNode;
        if (this.fe) {
            let i = this.ce;
            let s;
            const n = this.ue;
            while (i != null) {
                s = i.nextSibling;
                e.insertBefore(i, t);
                if (i === n) {
                    break;
                }
                i = s;
            }
        } else {
            this.fe = true;
            e.insertBefore(this.f, t);
        }
    }
    unlink() {
        this.de = false;
        this.next = void 0;
        this.ref = void 0;
    }
    link(t) {
        this.de = true;
        if (isRenderLocation(t)) {
            this.ref = t;
        } else {
            this.next = t;
            this.ge();
        }
    }
    ge() {
        if (this.next !== void 0) {
            this.ref = this.next.firstChild;
        } else {
            this.ref = void 0;
        }
    }
}

const Ys = /*@__PURE__*/ Ue("IWindow", t => t.callback(t => t.get(ci).window));

const Zs = /*@__PURE__*/ Ue("ILocation", t => t.callback(t => t.get(Ys).location));

const Js = /*@__PURE__*/ Ue("IHistory", t => t.callback(t => t.get(Ys).history));

const registerHostNode = (t, e, i = t.get(ci)) => {
    registerResolver(t, i.HTMLElement, registerResolver(t, i.Element, registerResolver(t, fi, new k("ElementResolver", e))));
    return t;
};

function customElement(t) {
    return function(e, i) {
        i.addInitializer(function() {
            defineElement(t, this);
        });
        return e;
    };
}

function useShadowDOM(t, e) {
    if (t === void 0) {
        return function(t, e) {
            e.addInitializer(function() {
                annotateElementMetadata(this, "shadowOptions", {
                    mode: "open"
                });
            });
        };
    }
    if (!c(t)) {
        return function(e, i) {
            i.addInitializer(function() {
                annotateElementMetadata(this, "shadowOptions", t);
            });
        };
    }
    e.addInitializer(function() {
        annotateElementMetadata(this, "shadowOptions", {
            mode: "open"
        });
    });
}

function containerless(t, e) {
    if (t === void 0) {
        return function(t, e) {
            e.addInitializer(function() {
                markContainerless(t);
            });
        };
    }
    e.addInitializer(function() {
        markContainerless(t);
    });
}

function markContainerless(t) {
    const e = ye(nn, t);
    if (e === void 0) {
        annotateElementMetadata(t, "containerless", true);
        return;
    }
    e.containerless = true;
}

const tn = new WeakMap;

class CustomElementDefinition {
    get type() {
        return Qe;
    }
    constructor(t, e, i, s, n, r, l, h, a, c, u, f, d, p, m, g, v, x, y) {
        this.Type = t;
        this.name = e;
        this.aliases = i;
        this.key = s;
        this.capture = n;
        this.template = r;
        this.instructions = l;
        this.dependencies = h;
        this.injectable = a;
        this.needsCompile = c;
        this.surrogates = u;
        this.bindables = f;
        this.containerless = d;
        this.shadowOptions = p;
        this.hasSlots = m;
        this.enhance = g;
        this.watches = v;
        this.strict = x;
        this.processContent = y;
    }
    static create(t, i = null) {
        if (i === null) {
            const s = t;
            if (e(s)) {
                throw createMappedError(761, t);
            }
            const n = L("name", s, on);
            if (c(s.Type)) {
                i = s.Type;
            } else {
                i = rn(M(n));
            }
            for (const t of Object.values(je.from(s.bindables))) {
                je.i(t, i);
            }
            return new CustomElementDefinition(i, n, a(s.aliases), L("key", s, () => getElementKeyFrom(n)), D("capture", s, i, returnFalse), D("template", s, i, returnNull), a(s.instructions), a(getElementAnnotation(i, "dependencies"), s.dependencies), L("injectable", s, returnNull), L("needsCompile", s, returnTrue), a(s.surrogates), je.from(getElementAnnotation(i, "bindables"), s.bindables), D("containerless", s, i, returnFalse), L("shadowOptions", s, returnNull), L("hasSlots", s, returnFalse), L("enhance", s, returnFalse), L("watches", s, returnEmptyArray), L("strict", s, returnUndefined), P("processContent", i, returnNull));
        }
        if (e(t)) {
            return new CustomElementDefinition(i, t, a(getElementAnnotation(i, "aliases"), i.aliases), getElementKeyFrom(t), P("capture", i, returnFalse), P("template", i, returnNull), a(getElementAnnotation(i, "instructions"), i.instructions), a(getElementAnnotation(i, "dependencies"), i.dependencies), P("injectable", i, returnNull), P("needsCompile", i, returnTrue), a(getElementAnnotation(i, "surrogates"), i.surrogates), je.from(...je.getAll(i), getElementAnnotation(i, "bindables"), i.bindables), P("containerless", i, returnFalse), P("shadowOptions", i, returnNull), P("hasSlots", i, returnFalse), P("enhance", i, returnFalse), a(di.getDefinitions(i), i.watches), P("strict", i, returnUndefined), P("processContent", i, returnNull));
        }
        const s = L("name", t, on);
        for (const e of Object.values(je.from(t.bindables))) {
            je.i(e, i);
        }
        return new CustomElementDefinition(i, s, a(getElementAnnotation(i, "aliases"), t.aliases, i.aliases), getElementKeyFrom(s), D("capture", t, i, returnFalse), D("template", t, i, returnNull), a(getElementAnnotation(i, "instructions"), t.instructions, i.instructions), a(getElementAnnotation(i, "dependencies"), t.dependencies, i.dependencies), D("injectable", t, i, returnNull), D("needsCompile", t, i, returnTrue), a(getElementAnnotation(i, "surrogates"), t.surrogates, i.surrogates), je.from(...je.getAll(i), getElementAnnotation(i, "bindables"), i.bindables, t.bindables), D("containerless", t, i, returnFalse), D("shadowOptions", t, i, returnNull), D("hasSlots", t, i, returnFalse), D("enhance", t, i, returnFalse), a(t.watches, di.getDefinitions(i), i.watches), D("strict", t, i, returnUndefined), D("processContent", t, i, returnNull));
    }
    static getOrCreate(t) {
        if (t instanceof CustomElementDefinition) {
            return t;
        }
        if (tn.has(t)) {
            return tn.get(t);
        }
        const e = CustomElementDefinition.create(t);
        tn.set(t, e);
        be(e, e.Type, nn);
        return e;
    }
    register(t, e) {
        const i = this.Type;
        const s = typeof e === "string" ? getElementKeyFrom(e) : this.key;
        const n = this.aliases;
        if (t.has(s, false)) {
            console.warn(createMappedError(153, this.name));
            return;
        }
        t.register(Ge(s, i), ...n.map(t => Ke(s, getElementKeyFrom(t))));
    }
    toString() {
        return `au:ce:${this.name}`;
    }
}

const en = {
    name: undefined,
    searchParents: false,
    optional: false
};

const returnNull = () => null;

const returnUndefined = () => void 0;

const returnFalse = () => false;

const returnTrue = () => true;

const returnEmptyArray = () => g;

const sn = "custom-element";

const nn = /*@__PURE__*/ d(sn);

const getElementKeyFrom = t => `${nn}:${t}`;

const on = /*@__PURE__*/ (t => () => `unnamed-${++t}`)(0);

const annotateElementMetadata = (t, e, i) => {
    be(i, t, Ce(e));
};

const defineElement = (t, e) => {
    const i = CustomElementDefinition.create(t, e);
    const s = i.Type;
    be(i, s, nn, u);
    return s;
};

const isElementType = t => c(t) && (we(nn, t) || t.$au?.type === sn);

const findElementControllerFor = (t, e = en) => {
    if (e.name === void 0 && e.searchParents !== true) {
        const i = ui.get(t, nn);
        if (i === null) {
            if (e.optional === true) {
                return null;
            }
            throw createMappedError(762, t);
        }
        return i;
    }
    if (e.name !== void 0) {
        if (e.searchParents !== true) {
            const i = ui.get(t, nn);
            if (i === null) {
                throw createMappedError(763, t);
            }
            if (i.is(e.name)) {
                return i;
            }
            return void 0;
        }
        let i = t;
        let s = false;
        while (i !== null) {
            const t = ui.get(i, nn);
            if (t !== null) {
                s = true;
                if (t.is(e.name)) {
                    return t;
                }
            }
            i = getEffectiveParentNode(i);
        }
        if (s) {
            return void 0;
        }
        throw createMappedError(764, t);
    }
    let i = t;
    while (i !== null) {
        const t = ui.get(i, nn);
        if (t !== null) {
            return t;
        }
        i = getEffectiveParentNode(i);
    }
    throw createMappedError(765, t);
};

const getElementAnnotation = (t, e) => ye(Ce(e), t);

const getElementDefinition = t => {
    const e = ye(nn, t) ?? getDefinitionFromStaticAu(t, sn, CustomElementDefinition.create);
    if (e == null) {
        throw createMappedError(760, t);
    }
    return e;
};

const createElementInjectable = () => {
    const t = {
        $isInterface: false,
        register() {
            return {
                $isResolver: true,
                resolve(e, i) {
                    if (i.has(t, true)) {
                        return i.get(t);
                    } else {
                        return null;
                    }
                }
            };
        }
    };
    return t;
};

const rn = /*@__PURE__*/ function() {
    const t = {
        value: "",
        writable: false,
        enumerable: false,
        configurable: true
    };
    const e = {};
    return function(i, s = e) {
        const n = class Anonymous {};
        t.value = i;
        Pe(n, "name", t);
        if (s !== e) {
            Ee(n.prototype, s);
        }
        return n;
    };
}();

const clearElementDefinition = t => {
    clearMetadata(t, nn);
    clearStaticAuDefinition(t);
};

const ln = /*@__PURE__*/ Te({
    name: nn,
    keyFrom: getElementKeyFrom,
    isType: isElementType,
    for: findElementControllerFor,
    define: defineElement,
    getDefinition: getElementDefinition,
    annotate: annotateElementMetadata,
    getAnnotation: getElementAnnotation,
    generateName: on,
    createInjectable: createElementInjectable,
    generateType: rn,
    find(t, e) {
        const i = t.find(sn, e);
        return i == null ? null : ye(nn, i) ?? getDefinitionFromStaticAu(i, sn, CustomElementDefinition.create) ?? null;
    },
    clearDefinition: clearElementDefinition
});

const hn = /*@__PURE__*/ Ce("processContent");

function processContent(t) {
    return t === void 0 ? function(t, e) {
        if (!e.static || e.kind !== "method") throw createMappedError(766, t);
        e.addInitializer(function() {
            be(t, this, hn);
        });
    } : function(i, s) {
        s.addInitializer(function() {
            if (e(t) || _(t)) {
                t = this[t];
            }
            if (!c(t)) throw createMappedError(766, t);
            const i = ye(nn, this);
            if (i !== void 0) {
                i.processContent = t;
            } else {
                be(t, this, hn);
            }
        });
        return i;
    };
}

function capture(t) {
    return function(e, i) {
        const s = c(t) ? t : true;
        i.addInitializer(function() {
            annotateElementMetadata(this, "capture", s);
            if (isElementType(this)) {
                getElementDefinition(this).capture = s;
            }
        });
    };
}

const an = /*@__PURE__*/ Ue("IAppRoot");

class AppRoot {
    get controller() {
        return this.P;
    }
    constructor(t, e, i, s = false) {
        this.config = t;
        this.container = e;
        this.ve = void 0;
        this.xe = s;
        const n = this.host = t.host;
        i.prepare(this);
        registerResolver(e, Gs, new k("IEventTarget", n));
        registerHostNode(e, n, this.platform = this.ye(e, n));
        this.ve = E(this.we("creating"), () => {
            if (!t.allowActionlessForm !== false) {
                n.addEventListener("submit", t => {
                    const e = t.target;
                    const i = !e.getAttribute("action");
                    if (e.tagName === "FORM" && i) {
                        t.preventDefault();
                    }
                }, false);
            }
            const i = s ? e : e.createChild();
            const r = t.component;
            let l;
            if (c(r)) {
                l = i.invoke(r);
                Xe(r, l);
            } else {
                l = t.component;
            }
            const h = {
                hydrate: false,
                projections: null
            };
            const a = s ? CustomElementDefinition.create({
                name: on(),
                template: this.host,
                enhance: true,
                strict: t.strictBinding
            }) : void 0;
            const u = this.P = Controller.$el(i, l, n, h, a, null, t.ssrScope);
            u.hE(h);
            return E(this.we("hydrating"), () => {
                u.hS();
                return E(this.we("hydrated"), () => {
                    u.hC();
                    this.ve = void 0;
                });
            });
        });
    }
    activate() {
        return E(this.ve, () => E(this.we("activating"), () => E(this.P.activate(this.P, null, void 0), () => this.we("activated"))));
    }
    deactivate() {
        return E(this.we("deactivating"), () => E(this.P.deactivate(this.P, null), () => this.we("deactivated")));
    }
    we(t) {
        const e = this.container;
        const i = this.xe && !e.has(hi, false) ? [] : e.getAll(hi);
        return R(...i.reduce((e, i) => {
            if (i.slot === t) {
                e.push(i.run());
            }
            return e;
        }, []));
    }
    ye(t, e) {
        let i;
        if (!t.has(ci, false)) {
            if (e.ownerDocument.defaultView === null) {
                throw createMappedError(769);
            }
            i = new ae(e.ownerDocument.defaultView);
            t.register(Xe(ci, i));
        } else {
            i = t.get(ci);
        }
        return i;
    }
    dispose() {
        this.P?.dispose();
    }
}

const cn = /*@__PURE__*/ Ue("IAurelia");

class Aurelia {
    get isRunning() {
        return this.ir;
    }
    get isStarting() {
        return this.be;
    }
    get isStopping() {
        return this.ke;
    }
    get root() {
        if (this.Ce == null) {
            if (this.next == null) {
                throw createMappedError(767);
            }
            return this.next;
        }
        return this.Ce;
    }
    constructor(t = r.createContainer()) {
        this.container = t;
        this.ir = false;
        this.be = false;
        this.ke = false;
        this.Ce = void 0;
        this.next = void 0;
        this.Se = void 0;
        this.Be = void 0;
        if (t.has(cn, true) || t.has(Aurelia, true)) {
            throw createMappedError(768);
        }
        registerResolver(t, cn, new k("IAurelia", this));
        registerResolver(t, Aurelia, new k("Aurelia", this));
        registerResolver(t, an, this.Ae = new k("IAppRoot"));
    }
    register(...t) {
        this.container.register(...t);
        return this;
    }
    app(t) {
        this.next = new AppRoot(t, this.container, this.Ae);
        return this;
    }
    enhance(t) {
        const e = t.container ?? this.container.createChild();
        const i = registerResolver(e, an, new k("IAppRoot"));
        const s = new AppRoot({
            host: t.host,
            component: t.component
        }, e, i, true);
        return E(s.activate(), () => s);
    }
    hydrate(t) {
        const e = t.container ?? this.container.createChild();
        const i = new AppRoot({
            host: t.host,
            component: t.component,
            ssrScope: t.ssrScope
        }, e, this.Ae, false);
        return E(i.activate(), () => i);
    }
    start(t = this.next) {
        if (t == null) {
            throw createMappedError(770);
        }
        if (T(this.Se)) {
            return this.Se;
        }
        return this.Se = E(this.stop(), () => {
            if (!ui.hideProp) {
                Reflect.set(t.host, "$aurelia", this);
            }
            this.Ae.prepare(this.Ce = t);
            this.be = true;
            return E(t.activate(), () => {
                this.ir = true;
                this.be = false;
                this.Se = void 0;
                this.Re(t, "au-started", t.host);
            });
        });
    }
    stop(t = false) {
        if (T(this.Be)) {
            return this.Be;
        }
        if (this.ir === true) {
            const e = this.Ce;
            this.ir = false;
            this.ke = true;
            return this.Be = E(e.deactivate(), () => E(Jt(), () => {
                Reflect.deleteProperty(e.host, "$aurelia");
                if (t) {
                    e.dispose();
                }
                this.Ce = void 0;
                this.Ae.dispose();
                this.ke = false;
                this.Be = void 0;
                this.Re(e, "au-stopped", e.host);
            }));
        }
    }
    dispose() {
        if (this.ir || this.ke) {
            throw createMappedError(771);
        }
        this.container.dispose();
    }
    Re(t, e, i) {
        const s = new t.platform.window.CustomEvent(e, {
            detail: this,
            bubbles: true,
            cancelable: true
        });
        i.dispatchEvent(s);
    }
}

const un = /*@__PURE__*/ Ue("ISVGAnalyzer", t => t.singleton(NoopSVGAnalyzer));

const o = t => {
    const s = i();
    t = e(t) ? t.split(" ") : t;
    let n;
    for (n of t) {
        s[n] = true;
    }
    return s;
};

class NoopSVGAnalyzer {
    isStandardSvgAttribute(t, e) {
        return false;
    }
}

class SVGAnalyzer {
    static register(t) {
        t.register(Ge(this, this), Ke(this, un));
    }
    constructor() {
        this.Te = Ee(i(), {
            a: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage target transform xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            altGlyph: o("class dx dy externalResourcesRequired format glyphRef id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rotate style systemLanguage x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            altglyph: i(),
            altGlyphDef: o("id xml:base xml:lang xml:space"),
            altglyphdef: i(),
            altGlyphItem: o("id xml:base xml:lang xml:space"),
            altglyphitem: i(),
            animate: o("accumulate additive attributeName attributeType begin by calcMode dur end externalResourcesRequired fill from id keySplines keyTimes max min onbegin onend onload onrepeat repeatCount repeatDur requiredExtensions requiredFeatures restart systemLanguage to values xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            animateColor: o("accumulate additive attributeName attributeType begin by calcMode dur end externalResourcesRequired fill from id keySplines keyTimes max min onbegin onend onload onrepeat repeatCount repeatDur requiredExtensions requiredFeatures restart systemLanguage to values xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            animateMotion: o("accumulate additive begin by calcMode dur end externalResourcesRequired fill from id keyPoints keySplines keyTimes max min onbegin onend onload onrepeat origin path repeatCount repeatDur requiredExtensions requiredFeatures restart rotate systemLanguage to values xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            animateTransform: o("accumulate additive attributeName attributeType begin by calcMode dur end externalResourcesRequired fill from id keySplines keyTimes max min onbegin onend onload onrepeat repeatCount repeatDur requiredExtensions requiredFeatures restart systemLanguage to type values xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            circle: o("class cx cy externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup r requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            clipPath: o("class clipPathUnits externalResourcesRequired id requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            "color-profile": o("id local name rendering-intent xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            cursor: o("externalResourcesRequired id requiredExtensions requiredFeatures systemLanguage x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            defs: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            desc: o("class id style xml:base xml:lang xml:space"),
            ellipse: o("class cx cy externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rx ry style systemLanguage transform xml:base xml:lang xml:space"),
            feBlend: o("class height id in in2 mode result style width x xml:base xml:lang xml:space y"),
            feColorMatrix: o("class height id in result style type values width x xml:base xml:lang xml:space y"),
            feComponentTransfer: o("class height id in result style width x xml:base xml:lang xml:space y"),
            feComposite: o("class height id in in2 k1 k2 k3 k4 operator result style width x xml:base xml:lang xml:space y"),
            feConvolveMatrix: o("bias class divisor edgeMode height id in kernelMatrix kernelUnitLength order preserveAlpha result style targetX targetY width x xml:base xml:lang xml:space y"),
            feDiffuseLighting: o("class diffuseConstant height id in kernelUnitLength result style surfaceScale width x xml:base xml:lang xml:space y"),
            feDisplacementMap: o("class height id in in2 result scale style width x xChannelSelector xml:base xml:lang xml:space y yChannelSelector"),
            feDistantLight: o("azimuth elevation id xml:base xml:lang xml:space"),
            feFlood: o("class height id result style width x xml:base xml:lang xml:space y"),
            feFuncA: o("amplitude exponent id intercept offset slope tableValues type xml:base xml:lang xml:space"),
            feFuncB: o("amplitude exponent id intercept offset slope tableValues type xml:base xml:lang xml:space"),
            feFuncG: o("amplitude exponent id intercept offset slope tableValues type xml:base xml:lang xml:space"),
            feFuncR: o("amplitude exponent id intercept offset slope tableValues type xml:base xml:lang xml:space"),
            feGaussianBlur: o("class height id in result stdDeviation style width x xml:base xml:lang xml:space y"),
            feImage: o("class externalResourcesRequired height id preserveAspectRatio result style width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            feMerge: o("class height id result style width x xml:base xml:lang xml:space y"),
            feMergeNode: o("id xml:base xml:lang xml:space"),
            feMorphology: o("class height id in operator radius result style width x xml:base xml:lang xml:space y"),
            feOffset: o("class dx dy height id in result style width x xml:base xml:lang xml:space y"),
            fePointLight: o("id x xml:base xml:lang xml:space y z"),
            feSpecularLighting: o("class height id in kernelUnitLength result specularConstant specularExponent style surfaceScale width x xml:base xml:lang xml:space y"),
            feSpotLight: o("id limitingConeAngle pointsAtX pointsAtY pointsAtZ specularExponent x xml:base xml:lang xml:space y z"),
            feTile: o("class height id in result style width x xml:base xml:lang xml:space y"),
            feTurbulence: o("baseFrequency class height id numOctaves result seed stitchTiles style type width x xml:base xml:lang xml:space y"),
            filter: o("class externalResourcesRequired filterRes filterUnits height id primitiveUnits style width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            font: o("class externalResourcesRequired horiz-adv-x horiz-origin-x horiz-origin-y id style vert-adv-y vert-origin-x vert-origin-y xml:base xml:lang xml:space"),
            "font-face": o("accent-height alphabetic ascent bbox cap-height descent font-family font-size font-stretch font-style font-variant font-weight hanging id ideographic mathematical overline-position overline-thickness panose-1 slope stemh stemv strikethrough-position strikethrough-thickness underline-position underline-thickness unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical widths x-height xml:base xml:lang xml:space"),
            "font-face-format": o("id string xml:base xml:lang xml:space"),
            "font-face-name": o("id name xml:base xml:lang xml:space"),
            "font-face-src": o("id xml:base xml:lang xml:space"),
            "font-face-uri": o("id xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            foreignObject: o("class externalResourcesRequired height id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform width x xml:base xml:lang xml:space y"),
            g: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            glyph: o("arabic-form class d glyph-name horiz-adv-x id lang orientation style unicode vert-adv-y vert-origin-x vert-origin-y xml:base xml:lang xml:space"),
            glyphRef: o("class dx dy format glyphRef id style x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            glyphref: i(),
            hkern: o("g1 g2 id k u1 u2 xml:base xml:lang xml:space"),
            image: o("class externalResourcesRequired height id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup preserveAspectRatio requiredExtensions requiredFeatures style systemLanguage transform width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            line: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform x1 x2 xml:base xml:lang xml:space y1 y2"),
            linearGradient: o("class externalResourcesRequired gradientTransform gradientUnits id spreadMethod style x1 x2 xlink:arcrole xlink:href xlink:role xlink:title xlink:type xml:base xml:lang xml:space y1 y2"),
            marker: o("class externalResourcesRequired id markerHeight markerUnits markerWidth orient preserveAspectRatio refX refY style viewBox xml:base xml:lang xml:space"),
            mask: o("class externalResourcesRequired height id maskContentUnits maskUnits requiredExtensions requiredFeatures style systemLanguage width x xml:base xml:lang xml:space y"),
            metadata: o("id xml:base xml:lang xml:space"),
            "missing-glyph": o("class d horiz-adv-x id style vert-adv-y vert-origin-x vert-origin-y xml:base xml:lang xml:space"),
            mpath: o("externalResourcesRequired id xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            path: o("class d externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup pathLength requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            pattern: o("class externalResourcesRequired height id patternContentUnits patternTransform patternUnits preserveAspectRatio requiredExtensions requiredFeatures style systemLanguage viewBox width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            polygon: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup points requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            polyline: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup points requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            radialGradient: o("class cx cy externalResourcesRequired fx fy gradientTransform gradientUnits id r spreadMethod style xlink:arcrole xlink:href xlink:role xlink:title xlink:type xml:base xml:lang xml:space"),
            rect: o("class externalResourcesRequired height id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rx ry style systemLanguage transform width x xml:base xml:lang xml:space y"),
            script: o("externalResourcesRequired id type xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            set: o("attributeName attributeType begin dur end externalResourcesRequired fill id max min onbegin onend onload onrepeat repeatCount repeatDur requiredExtensions requiredFeatures restart systemLanguage to xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space"),
            stop: o("class id offset style xml:base xml:lang xml:space"),
            style: o("id media title type xml:base xml:lang xml:space"),
            svg: o("baseProfile class contentScriptType contentStyleType externalResourcesRequired height id onabort onactivate onclick onerror onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup onresize onscroll onunload onzoom preserveAspectRatio requiredExtensions requiredFeatures style systemLanguage version viewBox width x xml:base xml:lang xml:space y zoomAndPan"),
            switch: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform xml:base xml:lang xml:space"),
            symbol: o("class externalResourcesRequired id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup preserveAspectRatio style viewBox xml:base xml:lang xml:space"),
            text: o("class dx dy externalResourcesRequired id lengthAdjust onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rotate style systemLanguage textLength transform x xml:base xml:lang xml:space y"),
            textPath: o("class externalResourcesRequired id lengthAdjust method onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures spacing startOffset style systemLanguage textLength xlink:arcrole xlink:href xlink:role xlink:title xlink:type xml:base xml:lang xml:space"),
            title: o("class id style xml:base xml:lang xml:space"),
            tref: o("class dx dy externalResourcesRequired id lengthAdjust onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rotate style systemLanguage textLength x xlink:arcrole xlink:href xlink:role xlink:title xlink:type xml:base xml:lang xml:space y"),
            tspan: o("class dx dy externalResourcesRequired id lengthAdjust onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures rotate style systemLanguage textLength x xml:base xml:lang xml:space y"),
            use: o("class externalResourcesRequired height id onactivate onclick onfocusin onfocusout onload onmousedown onmousemove onmouseout onmouseover onmouseup requiredExtensions requiredFeatures style systemLanguage transform width x xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xml:lang xml:space y"),
            view: o("externalResourcesRequired id preserveAspectRatio viewBox viewTarget xml:base xml:lang xml:space zoomAndPan"),
            vkern: o("g1 g2 id k u1 u2 xml:base xml:lang xml:space")
        });
        this.Ee = o("a altGlyph animate animateColor circle clipPath defs ellipse feBlend feColorMatrix feComponentTransfer feComposite feConvolveMatrix feDiffuseLighting feDisplacementMap feFlood feGaussianBlur feImage feMerge feMorphology feOffset feSpecularLighting feTile feTurbulence filter font foreignObject g glyph glyphRef image line linearGradient marker mask missing-glyph path pattern polygon polyline radialGradient rect stop svg switch symbol text textPath tref tspan use");
        this.Le = o("alignment-baseline baseline-shift clip-path clip-rule clip color-interpolation-filters color-interpolation color-profile color-rendering color cursor direction display dominant-baseline enable-background fill-opacity fill-rule fill filter flood-color flood-opacity font-family font-size-adjust font-size font-stretch font-style font-variant font-weight glyph-orientation-horizontal glyph-orientation-vertical image-rendering kerning letter-spacing lighting-color marker-end marker-mid marker-start mask opacity overflow pointer-events shape-rendering stop-color stop-opacity stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width stroke text-anchor text-decoration text-rendering unicode-bidi visibility word-spacing writing-mode");
        const t = p(ci);
        this.SVGElement = t.globalThis.SVGElement;
        const e = t.document.createElement("div");
        e.innerHTML = "<svg><altGlyph /></svg>";
        if (e.firstElementChild.nodeName === "altglyph") {
            const t = this.Te;
            let e = t.altGlyph;
            t.altGlyph = t.altglyph;
            t.altglyph = e;
            e = t.altGlyphDef;
            t.altGlyphDef = t.altglyphdef;
            t.altglyphdef = e;
            e = t.altGlyphItem;
            t.altGlyphItem = t.altglyphitem;
            t.altglyphitem = e;
            e = t.glyphRef;
            t.glyphRef = t.glyphref;
            t.glyphref = e;
        }
    }
    isStandardSvgAttribute(t, e) {
        if (!(t instanceof this.SVGElement)) {
            return false;
        }
        return this.Ee[t.nodeName] === true && this.Le[e] === true || this.Te[t.nodeName]?.[e] === true;
    }
}

class AttrMapper {
    constructor() {
        this.fns = [];
        this.Me = i();
        this.De = i();
        this.svg = p(un);
        this.useMapping({
            LABEL: {
                for: "htmlFor"
            },
            IMG: {
                usemap: "useMap"
            },
            INPUT: {
                maxlength: "maxLength",
                minlength: "minLength",
                formaction: "formAction",
                formenctype: "formEncType",
                formmethod: "formMethod",
                formnovalidate: "formNoValidate",
                formtarget: "formTarget",
                inputmode: "inputMode"
            },
            TEXTAREA: {
                maxlength: "maxLength"
            },
            TD: {
                rowspan: "rowSpan",
                colspan: "colSpan"
            },
            TH: {
                rowspan: "rowSpan",
                colspan: "colSpan"
            }
        });
        this.useGlobalMapping({
            accesskey: "accessKey",
            contenteditable: "contentEditable",
            tabindex: "tabIndex",
            textcontent: "textContent",
            innerhtml: "innerHTML",
            scrolltop: "scrollTop",
            scrollleft: "scrollLeft",
            readonly: "readOnly"
        });
    }
    useMapping(t) {
        let e;
        let s;
        let n;
        let r;
        for (n in t) {
            e = t[n];
            s = this.Me[n] ??= i();
            for (r in e) {
                if (s[r] !== void 0) {
                    throw createError(r, n);
                }
                s[r] = e[r];
            }
        }
    }
    useGlobalMapping(t) {
        const e = this.De;
        for (const i in t) {
            if (e[i] !== void 0) {
                throw createError(i, "*");
            }
            e[i] = t[i];
        }
    }
    useTwoWay(t) {
        this.fns.push(t);
    }
    isTwoWay(t, e) {
        return shouldDefaultToTwoWay(t, e) || this.fns.length > 0 && this.fns.some(i => i(t, e));
    }
    map(t, e) {
        return this.Me[t.nodeName]?.[e] ?? this.De[e] ?? (isDataAttribute(t, e, this.svg) ? e : null);
    }
}

AttrMapper.register = I(mt);

function shouldDefaultToTwoWay(t, e) {
    switch (t.nodeName) {
      case "INPUT":
        switch (t.type) {
          case "checkbox":
          case "radio":
            return e === "checked";

          default:
            return e === "value" || e === "files" || e === "value-as-number" || e === "value-as-date";
        }

      case "TEXTAREA":
      case "SELECT":
        return e === "value";

      default:
        switch (e) {
          case "textcontent":
          case "innerhtml":
            return t.hasAttribute("contenteditable");

          case "scrolltop":
          case "scrollleft":
            return true;

          default:
            return false;
        }
    }
}

function createError(t, e) {
    return createMappedError(719, t, e);
}

const dn = {
    register(t) {
        t.register(vt, AttrMapper, ResourceResolver);
    }
};

class BindablesInfo {
    constructor(t, e, i) {
        this.attrs = t;
        this.bindables = e;
        this.primary = i;
    }
}

class ResourceResolver {
    constructor() {
        this.Pe = new WeakMap;
        this._e = new WeakMap;
    }
    el(t, e) {
        let i = this.Pe.get(t);
        if (i == null) {
            this.Pe.set(t, i = new RecordCache);
        }
        return e in i.Ie ? i.Ie[e] : i.Ie[e] = ln.find(t, e);
    }
    attr(t, e) {
        let i = this.Pe.get(t);
        if (i == null) {
            this.Pe.set(t, i = new RecordCache);
        }
        return e in i.qe ? i.qe[e] : i.qe[e] = gi.find(t, e);
    }
    bindables(t) {
        let e = this._e.get(t);
        if (e == null) {
            const s = t.bindables;
            const n = i();
            let r;
            let l;
            let h;
            let a;
            const c = t.type === "custom-attribute";
            const u = c ? t.defaultProperty : null;
            for (l in s) {
                r = s[l];
                a = r.attribute;
                if (u != null && l === u) {
                    h = r;
                }
                n[a] = BindableDefinition.create(l, r);
            }
            if (c && h == null) {
                const t = u;
                h = n[t] = BindableDefinition.create(t);
            }
            this._e.set(t, e = new BindablesInfo(n, s, h ?? null));
        }
        return e;
    }
}

ResourceResolver.register = I(gt);

class RecordCache {
    constructor() {
        this.Ie = i();
        this.qe = i();
    }
}

const pn = i();

class AttributeNSAccessor {
    static forNs(t) {
        return pn[t] ??= new AttributeNSAccessor(t);
    }
    constructor(t) {
        this.ns = t;
        this.type = $e | Ne;
    }
    getValue(t, e) {
        return t.getAttributeNS(this.ns, e);
    }
    setValue(t, e, i) {
        if (t == null) {
            e.removeAttributeNS(this.ns, i);
        } else {
            e.setAttributeNS(this.ns, i, t);
        }
    }
}

(() => {
    mixinNoopSubscribable(AttributeNSAccessor);
})();

class DataAttributeAccessor {
    constructor() {
        this.type = $e | Ne;
    }
    getValue(t, e) {
        return t.getAttribute(e);
    }
    setValue(t, e, i) {
        if (t == null) {
            e.removeAttribute(i);
        } else {
            e.setAttribute(i, t);
        }
    }
}

(() => {
    mixinNoopSubscribable(DataAttributeAccessor);
})();

const mn = /*@__PURE__*/ new DataAttributeAccessor;

class SelectValueObserver {
    static Ve(t) {
        const e = [];
        if (t.length === 0) {
            return e;
        }
        const i = t.length;
        let s = 0;
        let n;
        while (i > s) {
            n = t[s];
            if (n.selected) {
                e[e.length] = Re.call(n, "model") ? n.model : n.value;
            }
            ++s;
        }
        return e;
    }
    static Fe(t, e) {
        return t === e;
    }
    constructor(t, e, i, s) {
        this.type = $e | Oe | Ne;
        this.v = void 0;
        this.ov = void 0;
        this.He = false;
        this.Oe = void 0;
        this.$e = void 0;
        this.iO = false;
        this.lt = false;
        this.rt = t;
        this.oL = s;
        this.cf = i;
    }
    getValue() {
        return this.iO ? this.v : this.rt.multiple ? SelectValueObserver.Ve(this.rt.options) : this.rt.value;
    }
    setValue(t) {
        this.ov = this.v;
        this.v = t;
        this.He = t !== this.ov;
        this.Ne(t instanceof Array ? t : null);
        this.dt();
    }
    dt() {
        if (this.He) {
            this.He = false;
            this.syncOptions();
        }
    }
    handleCollectionChange() {
        this.syncOptions();
    }
    syncOptions() {
        const t = this.v;
        const e = this.rt;
        const i = x(t);
        const s = e.matcher ?? SelectValueObserver.Fe;
        const n = e.options;
        let r = n.length;
        while (r-- > 0) {
            const e = n[r];
            const l = Re.call(e, "model") ? e.model : e.value;
            if (i) {
                e.selected = t.findIndex(t => !!s(l, t)) !== -1;
                continue;
            }
            e.selected = !!s(l, t);
        }
    }
    syncValue() {
        const t = this.rt;
        const e = t.options;
        const i = e.length;
        const s = this.v;
        let n = 0;
        if (t.multiple) {
            if (!(s instanceof Array)) {
                return true;
            }
            let r;
            const l = t.matcher || SelectValueObserver.Fe;
            const h = [];
            while (n < i) {
                r = e[n];
                if (r.selected) {
                    h.push(Re.call(r, "model") ? r.model : r.value);
                }
                ++n;
            }
            let a;
            n = 0;
            while (n < s.length) {
                a = s[n];
                if (h.findIndex(t => !!l(a, t)) === -1) {
                    s.splice(n, 1);
                } else {
                    ++n;
                }
            }
            n = 0;
            while (n < h.length) {
                a = h[n];
                if (s.findIndex(t => !!l(a, t)) === -1) {
                    s.push(a);
                }
                ++n;
            }
            return false;
        }
        let r = null;
        let l;
        while (n < i) {
            l = e[n];
            if (l.selected) {
                r = Re.call(l, "model") ? l.model : l.value;
                break;
            }
            ++n;
        }
        this.ov = this.v;
        this.v = r;
        return true;
    }
    ht() {
        (this.$e = createMutationObserver(this.rt, this.We.bind(this))).observe(this.rt, {
            childList: true,
            subtree: true,
            characterData: true
        });
        this.Ne(this.v instanceof Array ? this.v : null);
        this.iO = true;
    }
    ct() {
        this.$e.disconnect();
        this.Oe?.unsubscribe(this);
        this.$e = this.Oe = void 0;
        this.iO = false;
    }
    Ne(t) {
        this.Oe?.unsubscribe(this);
        this.Oe = void 0;
        if (t != null) {
            if (!this.rt.multiple) {
                throw createMappedError(654);
            }
            (this.Oe = this.oL.getArrayObserver(t)).subscribe(this);
        }
    }
    handleEvent() {
        const t = this.syncValue();
        if (t) {
            this.At();
        }
    }
    We(t) {
        this.syncOptions();
        const e = this.syncValue();
        if (e) {
            this.At();
        }
    }
    At() {
        const t = this.ov;
        this.ov = this.v;
        this.subs.notify(this.v, t);
    }
}

(() => {
    mixinNodeObserverUseConfig(SelectValueObserver);
    Gt(SelectValueObserver, null);
})();

const gn = "--";

class StyleAttributeAccessor {
    constructor(t) {
        this.obj = t;
        this.type = $e | Ne;
        this.v = "";
        this.ov = "";
        this.styles = {};
        this.version = 0;
        this.He = false;
    }
    getValue() {
        return this.obj.style.cssText;
    }
    setValue(t) {
        this.v = t;
        this.He = t !== this.ov;
        this.dt();
    }
    je(t) {
        const e = [];
        const i = /url\([^)]+$/;
        let s = 0;
        let n = "";
        let r;
        let l;
        let h;
        let a;
        while (s < t.length) {
            r = t.indexOf(";", s);
            if (r === -1) {
                r = t.length;
            }
            n += t.substring(s, r);
            s = r + 1;
            if (i.test(n)) {
                n += ";";
                continue;
            }
            l = n.indexOf(":");
            h = n.substring(0, l).trim();
            a = n.substring(l + 1).trim();
            e.push([ h, a ]);
            n = "";
        }
        return e;
    }
    ze(t) {
        let i;
        let s;
        const n = [];
        for (s in t) {
            i = t[s];
            if (i == null) {
                continue;
            }
            if (e(i)) {
                if (s.startsWith(gn)) {
                    n.push([ s, i ]);
                    continue;
                }
                n.push([ q(s), i ]);
                continue;
            }
            n.push(...this.Ue(i));
        }
        return n;
    }
    Ge(t) {
        const e = t.length;
        if (e > 0) {
            const i = [];
            let s = 0;
            for (;e > s; ++s) {
                i.push(...this.Ue(t[s]));
            }
            return i;
        }
        return g;
    }
    Ue(t) {
        if (e(t)) {
            return this.je(t);
        }
        if (t instanceof Array) {
            return this.Ge(t);
        }
        if (t instanceof Object) {
            return this.ze(t);
        }
        return g;
    }
    dt() {
        if (this.He) {
            this.He = false;
            const t = this.v;
            const e = this.styles;
            const i = this.Ue(t);
            let s;
            let n = this.version;
            this.ov = t;
            let r;
            let l;
            let h;
            let a = 0;
            const c = i.length;
            for (;a < c; ++a) {
                r = i[a];
                l = r[0];
                h = r[1];
                this.setProperty(l, h);
                e[l] = n;
            }
            this.styles = e;
            this.version += 1;
            if (n === 0) {
                return;
            }
            n -= 1;
            for (s in e) {
                if (!Re.call(e, s) || e[s] !== n) {
                    continue;
                }
                this.obj.style.removeProperty(s);
            }
        }
    }
    setProperty(t, e) {
        let i = "";
        if (e != null && c(e.indexOf) && e.includes("!important")) {
            i = "important";
            e = e.replace("!important", "");
        }
        this.obj.style.setProperty(t, e, i);
    }
    bind() {
        this.v = this.ov = this.obj.style.cssText;
    }
}

(() => {
    mixinNoopSubscribable(StyleAttributeAccessor);
})();

class ValueAttributeObserver {
    constructor(t, e, i) {
        this.type = $e | Oe | Ne;
        this.v = "";
        this.ov = "";
        this.He = false;
        this.lt = false;
        this.rt = t;
        this.k = e;
        this.cf = i;
    }
    getValue() {
        return this.v;
    }
    setValue(t) {
        if (S(t, this.v)) {
            return;
        }
        this.ov = this.v;
        this.v = t;
        this.He = true;
        if (!this.cf.readonly) {
            this.dt();
        }
    }
    dt() {
        if (this.He) {
            this.He = false;
            this.rt[this.k] = this.v ?? this.cf.default;
            this.At();
        }
    }
    handleEvent() {
        this.ov = this.v;
        this.v = this.rt[this.k];
        if (this.ov !== this.v) {
            this.He = false;
            this.At();
        }
    }
    ht() {
        this.v = this.ov = this.rt[this.k];
    }
    At() {
        const t = this.ov;
        this.ov = this.v;
        this.subs.notify(this.v, t);
    }
}

(() => {
    mixinNodeObserverUseConfig(ValueAttributeObserver);
    Gt(ValueAttributeObserver, null);
})();

const vn = (() => {
    const t = "http://www.w3.org/1999/xlink";
    const e = "http://www.w3.org/XML/1998/namespace";
    const s = "http://www.w3.org/2000/xmlns/";
    return Ee(i(), {
        "xlink:actuate": [ "actuate", t ],
        "xlink:arcrole": [ "arcrole", t ],
        "xlink:href": [ "href", t ],
        "xlink:role": [ "role", t ],
        "xlink:show": [ "show", t ],
        "xlink:title": [ "title", t ],
        "xlink:type": [ "type", t ],
        "xml:lang": [ "lang", e ],
        "xml:space": [ "space", e ],
        xmlns: [ "xmlns", s ],
        "xmlns:xlink": [ "xlink", s ]
    });
})();

const xn = new te;

xn.type = $e | Ne;

class NodeObserverLocator {
    constructor() {
        this.allowDirtyCheck = true;
        this.Ke = i();
        this.Xe = i();
        this.Qe = i();
        this.Ye = i();
        this.Ze = p(V);
        this.p = p(ci);
        this.Je = p(ie);
        this.svg = p(un);
        const t = [ "change", "input" ];
        const e = {
            events: t,
            default: ""
        };
        this.useConfig({
            INPUT: {
                value: e,
                valueAsNumber: {
                    events: t,
                    default: 0
                },
                checked: {
                    type: CheckedObserver,
                    events: t
                },
                files: {
                    events: t,
                    readonly: true
                }
            },
            SELECT: {
                value: {
                    type: SelectValueObserver,
                    events: [ "change" ],
                    default: ""
                }
            },
            TEXTAREA: {
                value: e
            }
        });
        const s = {
            events: [ "change", "input", "blur", "keyup", "paste" ],
            default: ""
        };
        const n = {
            events: [ "scroll" ],
            default: 0
        };
        this.useConfigGlobal({
            scrollTop: n,
            scrollLeft: n,
            textContent: s,
            innerHTML: s
        });
        this.overrideAccessorGlobal("css", "style", "class");
        this.overrideAccessor({
            INPUT: [ "value", "checked", "model" ],
            SELECT: [ "value" ],
            TEXTAREA: [ "value" ]
        });
    }
    handles(t, e) {
        return t instanceof this.p.Node;
    }
    useConfig(t, s, n) {
        const r = this.Ke;
        let l;
        if (e(t)) {
            l = r[t] ??= i();
            if (l[s] == null) {
                l[s] = n;
            } else {
                throwMappingExisted(t, s);
            }
        } else {
            for (const e in t) {
                l = r[e] ??= i();
                const n = t[e];
                for (s in n) {
                    if (l[s] == null) {
                        l[s] = n[s];
                    } else {
                        throwMappingExisted(e, s);
                    }
                }
            }
        }
    }
    useConfigGlobal(t, e) {
        const i = this.Xe;
        if (typeof t === "object") {
            for (const e in t) {
                if (i[e] == null) {
                    i[e] = t[e];
                } else {
                    throwMappingExisted("*", e);
                }
            }
        } else {
            if (i[t] == null) {
                i[t] = e;
            } else {
                throwMappingExisted("*", t);
            }
        }
    }
    getAccessor(t, e, i) {
        if (e in this.Ye || e in (this.Qe[t.tagName] ?? F)) {
            return this.getObserver(t, e, i);
        }
        switch (e) {
          case "src":
          case "href":
          case "role":
          case "minLength":
          case "maxLength":
          case "placeholder":
          case "size":
          case "pattern":
          case "title":
          case "popovertarget":
          case "popovertargetaction":
            return mn;

          default:
            {
                const i = vn[e];
                if (i !== undefined) {
                    return AttributeNSAccessor.forNs(i[1]);
                }
                if (isDataAttribute(t, e, this.svg)) {
                    return mn;
                }
                return xn;
            }
        }
    }
    overrideAccessor(t, s) {
        let n;
        if (e(t)) {
            n = this.Qe[t] ??= i();
            n[s] = true;
        } else {
            for (const e in t) {
                for (const s of t[e]) {
                    n = this.Qe[e] ??= i();
                    n[s] = true;
                }
            }
        }
    }
    overrideAccessorGlobal(...t) {
        for (const e of t) {
            this.Ye[e] = true;
        }
    }
    getNodeObserverConfig(t, e) {
        return this.Ke[t.tagName]?.[e] ?? this.Xe[e];
    }
    getNodeObserver(t, e, i) {
        const s = this.Ke[t.tagName]?.[e] ?? this.Xe[e];
        let n;
        if (s != null) {
            n = new (s.type ?? ValueAttributeObserver)(t, e, s, i, this.Ze);
            if (!n.doNotCache) {
                se(t)[e] = n;
            }
            return n;
        }
        return null;
    }
    getObserver(t, e, i) {
        switch (e) {
          case "class":
            return new ClassAttributeAccessor(t);

          case "css":
          case "style":
            return new StyleAttributeAccessor(t);
        }
        const s = this.getNodeObserver(t, e, i);
        if (s != null) {
            return s;
        }
        const n = vn[e];
        if (n !== undefined) {
            return AttributeNSAccessor.forNs(n[1]);
        }
        if (isDataAttribute(t, e, this.svg)) {
            return mn;
        }
        if (e in t.constructor.prototype) {
            if (this.allowDirtyCheck) {
                return this.Je.createProperty(t, e);
            }
            throw createMappedError(652, e);
        } else {
            return new ne(t, e);
        }
    }
}

NodeObserverLocator.register = I(ee);

function getCollectionObserver(t, e) {
    if (t instanceof Array) {
        return e.getArrayObserver(t);
    }
    if (t instanceof Map) {
        return e.getMapObserver(t);
    }
    if (t instanceof Set) {
        return e.getSetObserver(t);
    }
}

function throwMappingExisted(t, e) {
    throw createMappedError(653, t, e);
}

function defaultMatcher(t, e) {
    return t === e;
}

class CheckedObserver {
    constructor(t, e, i, s) {
        this.type = $e | Oe | Ne;
        this.v = void 0;
        this.ov = void 0;
        this.ti = void 0;
        this.ei = void 0;
        this.lt = false;
        this.rt = t;
        this.oL = s;
        this.cf = i;
    }
    getValue() {
        return this.v;
    }
    setValue(t) {
        const e = this.v;
        if (t === e) {
            return;
        }
        this.v = t;
        this.ov = e;
        this.ii();
        this.si();
        this.At();
    }
    handleCollectionChange() {
        this.si();
    }
    handleChange(t, e) {
        this.si();
    }
    si() {
        const t = this.v;
        const e = this.rt;
        const i = Re.call(e, "model") ? e.model : e.value;
        const s = e.type === "radio";
        const n = e.matcher !== void 0 ? e.matcher : defaultMatcher;
        if (s) {
            e.checked = !!n(t, i);
        } else if (t === true) {
            e.checked = true;
        } else {
            let s = false;
            if (x(t)) {
                s = t.findIndex(t => !!n(t, i)) !== -1;
            } else if (t instanceof Set) {
                for (const e of t) {
                    if (n(e, i)) {
                        s = true;
                        break;
                    }
                }
            } else if (t instanceof Map) {
                for (const e of t) {
                    const t = e[0];
                    const r = e[1];
                    if (n(t, i) && r === true) {
                        s = true;
                        break;
                    }
                }
            }
            e.checked = s;
        }
    }
    handleEvent() {
        let t = this.ov = this.v;
        const e = this.rt;
        const i = Re.call(e, "model") ? e.model : e.value;
        const s = e.checked;
        const n = e.matcher !== void 0 ? e.matcher : defaultMatcher;
        if (e.type === "checkbox") {
            if (x(t)) {
                const e = t.findIndex(t => !!n(t, i));
                if (s && e === -1) {
                    t.push(i);
                } else if (!s && e !== -1) {
                    t.splice(e, 1);
                }
                return;
            } else if (t instanceof Set) {
                const e = {};
                let r = e;
                for (const e of t) {
                    if (n(e, i) === true) {
                        r = e;
                        break;
                    }
                }
                if (s && r === e) {
                    t.add(i);
                } else if (!s && r !== e) {
                    t.delete(r);
                }
                return;
            } else if (t instanceof Map) {
                let e;
                for (const s of t) {
                    const t = s[0];
                    if (n(t, i) === true) {
                        e = t;
                        break;
                    }
                }
                t.set(e, s);
                return;
            }
            t = s;
        } else if (s) {
            t = i;
        } else {
            return;
        }
        this.v = t;
        this.At();
    }
    ht() {
        this.ii();
    }
    ct() {
        this.v = this.ov = void 0;
        this.ti?.unsubscribe(this);
        this.ei?.unsubscribe(this);
        this.ti = this.ei = void 0;
    }
    At() {
        yn = this.ov;
        this.ov = this.v;
        this.subs.notify(this.v, yn);
        yn = void 0;
    }
    ii() {
        const t = this.rt;
        (this.ei ??= t.$observers?.model ?? t.$observers?.value)?.subscribe(this);
        this.ti?.unsubscribe(this);
        this.ti = void 0;
        if (t.type === "checkbox") {
            (this.ti = getCollectionObserver(this.v, this.oL))?.subscribe(this);
        }
    }
}

(() => {
    mixinNodeObserverUseConfig(CheckedObserver);
    Gt(CheckedObserver, null);
})();

let yn = void 0;

class AttrBindingBehavior {
    bind(t, e) {
        if (!(e instanceof PropertyBinding)) {
            throw createMappedError(9994, e);
        }
        e.useTargetObserver(mn);
    }
}

AttrBindingBehavior.$au = {
    type: Je,
    name: "attr"
};

class SelfBindingBehavior {
    bind(t, e) {
        if (!("handleEvent" in e)) {
            throw createMappedError(801);
        }
        e.self = true;
    }
    unbind(t, e) {
        e.self = false;
    }
}

SelfBindingBehavior.$au = {
    type: Je,
    name: "self"
};

class UpdateTriggerBindingBehavior {
    constructor() {
        this.oL = p(Xt);
        this.ni = p(ee);
    }
    bind(t, e, ...i) {
        if (!(this.ni instanceof NodeObserverLocator)) {
            throw createMappedError(9993);
        }
        if (i.length === 0) {
            throw createMappedError(802);
        }
        if (!(e instanceof PropertyBinding) || !(e.mode & ve)) {
            throw createMappedError(803);
        }
        const s = this.ni.getNodeObserverConfig(e.target, e.targetProperty);
        if (s == null) {
            throw createMappedError(9992, e);
        }
        const n = this.ni.getNodeObserver(e.target, e.targetProperty, this.oL);
        n.useConfig({
            readonly: s.readonly,
            default: s.default,
            events: i
        });
        e.useTargetObserver(n);
    }
}

UpdateTriggerBindingBehavior.$au = {
    type: Je,
    name: "updateTrigger"
};

class If {
    constructor() {
        this.elseFactory = void 0;
        this.elseView = void 0;
        this.ifView = void 0;
        this.view = void 0;
        this.value = false;
        this.cache = true;
        this.pending = void 0;
        this.oi = false;
        this.ri = 0;
        this.li = p(Di);
        this.l = p(Ks);
        this.p = p(ci);
    }
    attaching(t, e) {
        const i = this.$controller.ssrScope;
        if (i != null && isSSRTemplateController(i) && i.type === "if") {
            return this.hi(i);
        }
        return this.ai(this.value);
    }
    detaching(t, e) {
        this.oi = true;
        return E(this.pending, () => {
            this.oi = false;
            this.pending = void 0;
            void this.view?.deactivate(t, this.$controller);
        });
    }
    valueChanged(t, e) {
        if (!this.$controller.isActive) return;
        t = !!t;
        e = !!e;
        if (t !== e) return this.ai(t);
    }
    ai(t) {
        const e = this.view;
        const i = this.$controller;
        const s = this.ri++;
        const isCurrent = () => !this.oi && this.ri === s + 1;
        let n;
        return E(this.pending, () => this.pending = E(e?.isActive ? e.deactivate(e, i) : void 0, () => {
            if (!isCurrent()) {
                return;
            }
            if (t) {
                n = this.view = this.ifView = this.cache && this.ifView != null ? this.ifView : this.li.create(i);
            } else {
                n = this.view = this.elseView = this.cache && this.elseView != null ? this.elseView : this.elseFactory?.create(i);
            }
            if (n == null) {
                return;
            }
            n.setLocation(this.l);
            const e = n.activate(n, i, i.scope);
            if (e instanceof Promise) {
                return e.then(() => {
                    if (isCurrent()) {
                        this.pending = void 0;
                    }
                }, () => {
                    if (isCurrent()) {
                        this.pending = void 0;
                    }
                    void n.deactivate(n, i);
                });
            }
            if (isCurrent()) {
                this.pending = void 0;
            }
        }));
    }
    hi(t) {
        const e = this.$controller;
        const i = t.state?.value === true;
        const s = i ? this.li : this.elseFactory;
        if (s == null || t.views.length === 0) {
            e.ssrScope = void 0;
            return;
        }
        const n = adoptSSRView(t, s, e, this.l, this.p);
        if (n == null) {
            e.ssrScope = void 0;
            return;
        }
        const {view: r} = n;
        if (i) {
            this.view = this.ifView = r;
        } else {
            this.view = this.elseView = r;
        }
        e.ssrScope = void 0;
        return r.activate(r, e, e.scope);
    }
    dispose() {
        this.ifView?.dispose();
        this.elseView?.dispose();
        this.ifView = this.elseView = this.view = void 0;
    }
    accept(t) {
        if (this.view?.accept(t) === true) {
            return true;
        }
    }
}

If.$au = {
    type: pi,
    name: "if",
    isTemplateController: true,
    bindables: {
        value: true,
        cache: {
            set: t => t === "" || !!t && t !== "false"
        }
    }
};

class Else {
    constructor() {
        this.f = p(Di);
    }
    link(t, e, i, s) {
        const n = t.children;
        const r = n[n.length - 1];
        if (r instanceof If) {
            r.elseFactory = this.f;
        } else if (r.viewModel instanceof If) {
            r.viewModel.elseFactory = this.f;
        } else {
            throw createMappedError(810);
        }
    }
}

Else.$au = {
    type: "custom-attribute",
    name: "else",
    isTemplateController: true
};

function dispose(t) {
    t.dispose();
}

const wn = [ "BindingBehavior", "ValueConverter" ];

class Repeat {
    constructor() {
        this.views = [];
        this.key = null;
        this.contextual = true;
        this.ci = [];
        this.ui = [];
        this.fi = [];
        this.di = new Map;
        this.pi = void 0;
        this.mi = false;
        this.gi = false;
        this.xi = null;
        this.yi = void 0;
        this.wi = false;
        this.bi = false;
        this.l = p(Ks);
        this.ki = p($s);
        this.f = p(Di);
        this.Ci = p(Sn);
        this.p = p(ci);
        const t = p(ft);
        const e = t.props[0].props;
        for (let t = 0, i = e.length; t < i; ++t) {
            const i = e[t];
            const {to: s, value: n, command: r} = i;
            if (s === "key") {
                if (r === null) {
                    this.key = n;
                } else if (r === "bind") {
                    this.key = typeof n === "string" ? p(fe).parse(n, Ve) : n;
                } else {
                    throw createMappedError(775, r);
                }
            } else if (s === "contextual") {
                if (r === null) {
                    this.contextual = n === "false" ? false : !!n;
                } else if (r === "bind") {
                    this.Si = typeof n === "string" ? p(fe).parse(n, Ve) : n;
                } else {
                    throw createMappedError(821, r);
                }
            } else {
                throw createMappedError(776, s);
            }
        }
    }
    binding(t, e) {
        const i = this.ki.bindings;
        const s = i.length;
        let n = void 0;
        let r;
        let l = 0;
        for (;s > l; ++l) {
            n = i[l];
            if (n.target === this && n.targetProperty === "items") {
                r = this.forOf = n.ast;
                this.Bi = n;
                let t = r.iterable;
                while (t != null && wn.includes(t.$kind)) {
                    t = t.expression;
                    this.mi = true;
                }
                this.xi = t;
                break;
            }
        }
        this.Ai();
        const h = r.declaration;
        if (!(this.wi = h.$kind === "ArrayDestructuring" || h.$kind === "ObjectDestructuring")) {
            this.local = Ot(h, this.$controller.scope, n, null);
        }
        if (this.Si !== void 0) {
            const t = Ot(this.Si, this.$controller.scope, n, null);
            this.contextual = t != null && t !== false;
        }
    }
    attaching(t, e) {
        this.Ri();
        this.Ti(void 0);
        return this.Ei(t, this.yi ?? g);
    }
    detaching(t, e) {
        this.Ai();
        const i = this.bi;
        this.bi = false;
        const s = this.Li(t, i);
        if (i) {
            this.views = [];
        }
        return s;
    }
    unbinding(t, e) {
        this.di.clear();
    }
    itemsChanged() {
        if (!this.$controller.isActive) {
            return;
        }
        this.Ai();
        this.Ri();
        this.Ti(void 0);
        this.Mi(void 0);
    }
    handleCollectionChange(t, e) {
        const i = this.$controller;
        if (!i.isActive) {
            return;
        }
        if (this.mi) {
            if (this.gi) {
                return;
            }
            this.gi = true;
            this.items = Ot(this.forOf.iterable, i.scope, this.Bi, null);
            this.gi = false;
            return;
        }
        this.Ri();
        this.Ti(this.key === null ? e : void 0);
        this.Mi(e);
    }
    Mi(t) {
        const e = this.views;
        this.ci = e.slice();
        const i = e.length;
        const s = this.key !== null;
        const n = this.fi;
        const r = this.ui;
        if (s || t === void 0) {
            const e = this.local;
            const s = this.forOf.declaration;
            const l = this.Bi;
            const h = this.wi;
            const a = r.length;
            t = oe(a);
            if (i === 0) {
                for (let e = 0; e < a; ++e) {
                    t[e] = -2;
                }
            } else if (a === 0) {
                for (let r = 0; r < i; ++r) {
                    t.deletedIndices.push(r);
                    t.deletedItems.push(getItem(h, s, n[r], l, e));
                }
            } else {
                const c = new Map;
                for (let t = 0; t < i; ++t) {
                    c.set(n[t], t);
                }
                const u = new Set;
                for (let e = 0; e < a; ++e) {
                    const i = c.get(r[e]);
                    if (i !== void 0) {
                        t[e] = i;
                        u.add(i);
                    } else {
                        t[e] = -2;
                    }
                }
                for (let r = 0; r < i; ++r) {
                    if (!u.has(r)) {
                        t.deletedIndices.push(r);
                        t.deletedItems.push(getItem(h, s, n[r], l, e));
                    }
                }
            }
        }
        if (t.deletedIndices.length > 0) {
            const e = E(this.Di(t), () => this.Pi(t));
            if (T(e)) {
                e.catch(rethrow);
            }
        } else {
            this.Pi(t);
        }
    }
    Ai() {
        const t = this.$controller.scope;
        let e = this._i;
        let i = this.mi;
        let s;
        if (i) {
            e = this._i = Ot(this.xi, t, this.Bi, null) ?? null;
            i = this.mi = !S(this.items, e);
        }
        const n = this.pi;
        if (this.$controller.isActive) {
            const t = i ? e : this.items;
            s = this.pi = this.Ci.resolve(t).getObserver?.(t);
            if (n !== s) {
                n?.unsubscribe(this);
                s?.subscribe(this);
            }
        } else {
            n?.unsubscribe(this);
            this.pi = undefined;
        }
    }
    Ti(t) {
        const e = this.ui;
        this.fi = e.slice();
        const i = this.yi;
        const s = i.length;
        const n = this.ui = Array(s);
        const r = this.di;
        const l = new Map;
        const h = this.$controller.scope;
        const a = this.Bi;
        const c = this.forOf;
        const u = this.local;
        const f = this.wi;
        if (t === void 0) {
            const t = this.key;
            const e = t !== null;
            if (e) {
                const e = Array(s);
                if (typeof t === "string") {
                    for (let n = 0; n < s; ++n) {
                        e[n] = i[n][t];
                    }
                } else {
                    for (let n = 0; n < s; ++n) {
                        const s = createScope(i[n], c, h, a, u, f);
                        setItem(f, c.declaration, s, a, u, i[n]);
                        e[n] = Ot(t, s, a, null);
                    }
                }
                for (let t = 0; t < s; ++t) {
                    n[t] = getScope(r, l, e[t], i[t], c, h, a, u, f);
                }
            } else {
                for (let t = 0; t < s; ++t) {
                    n[t] = getScope(r, l, i[t], i[t], c, h, a, u, f);
                }
            }
        } else {
            const r = e.length;
            for (let l = 0; l < s; ++l) {
                const s = t[l];
                if (s >= 0 && s < r) {
                    n[l] = e[s];
                } else {
                    n[l] = createScope(i[l], c, h, a, u, f);
                }
                setItem(f, c.declaration, n[l], a, u, i[l]);
            }
        }
        r.clear();
        this.di = l;
    }
    Ri() {
        const t = this.items;
        if (x(t)) {
            this.yi = t.slice(0);
            return;
        }
        const e = [];
        this.Ci.resolve(t).iterate(t, (t, i) => {
            e[i] = t;
        });
        this.yi = e;
    }
    Ei(t, e) {
        const i = this.$controller.ssrScope;
        if (i != null && isSSRTemplateController(i) && i.type === "repeat") {
            return this.Ii(t, e, i);
        }
        return this.qi(t, e);
    }
    Ii(t, e, i) {
        const {$controller: s, f: n, l: r, ui: l, p: h} = this;
        const a = e.length;
        const {views: c} = adoptSSRViews(i, n, s, r, h);
        if (c.length === 0) {
            s.ssrScope = undefined;
            return this.qi(t, e);
        }
        this.bi = true;
        this.views = c;
        let u = void 0;
        for (let i = 0; i < a; ++i) {
            const n = c[i];
            const r = l[i];
            if (this.contextual) {
                setContextualProperties(r.overrideContext, i, a, e);
            }
            const h = n.activate(t ?? n, s, r);
            if (T(h)) {
                (u ??= []).push(h);
            }
        }
        s.ssrScope = undefined;
        if (u !== void 0) {
            return u.length === 1 ? u[0] : Promise.all(u);
        }
    }
    qi(t, e) {
        const {$controller: i, f: s, l: n, ui: r} = this;
        const l = e.length;
        const h = this.views = Array(l);
        let a = void 0;
        for (let c = 0; c < l; ++c) {
            const u = h[c] = s.create(i).setLocation(n);
            u.nodes.unlink();
            const f = r[c];
            if (this.contextual) {
                setContextualProperties(f.overrideContext, c, l, e);
            }
            const d = u.activate(t ?? u, i, f);
            if (T(d)) {
                (a ??= []).push(d);
            }
        }
        if (a !== void 0) {
            return a.length === 1 ? a[0] : Promise.all(a);
        }
    }
    Li(t, e = false) {
        let i = void 0;
        let s;
        let n;
        let r = 0;
        const {views: l, $controller: h} = this;
        const a = l.length;
        for (;a > r; ++r) {
            n = l[r];
            if (!e) {
                n.release();
            }
            s = n.deactivate(t ?? n, h);
            if (T(s)) {
                (i ?? (i = [])).push(s);
            }
        }
        if (i !== void 0) {
            return i.length === 1 ? i[0] : Promise.all(i);
        }
    }
    Di(t) {
        let e = void 0;
        let i;
        let s;
        const {$controller: n, views: r} = this;
        const l = t.deletedIndices.slice().sort(compareNumber);
        const h = l.length;
        let a = 0;
        for (;h > a; ++a) {
            s = r[l[a]];
            s.release();
            i = s.deactivate(s, n);
            if (T(i)) {
                (e ?? (e = [])).push(i);
            }
        }
        a = 0;
        for (;h > a; ++a) {
            r.splice(l[a] - a, 1);
        }
        if (e !== void 0) {
            return e.length === 1 ? e[0] : Promise.all(e);
        }
    }
    Pi(t) {
        let e = void 0;
        let i;
        let s;
        let n = 0;
        const {$controller: r, f: l, l: h, views: a, ui: c, ci: u} = this;
        const f = t.length;
        for (;f > n; ++n) {
            if (t[n] === -2) {
                s = l.create(r);
                a.splice(n, 0, s);
            }
        }
        if (a.length !== f) {
            throw createMappedError(814, [ a.length, f ]);
        }
        let d = 0;
        n = 0;
        for (;n < t.length; ++n) {
            if ((d = t[n]) !== -2) {
                a[n] = u[d];
            }
        }
        const p = longestIncreasingSubsequence(t);
        const m = p.length;
        let g;
        let v = m - 1;
        n = f - 1;
        for (;n >= 0; --n) {
            s = a[n];
            g = a[n + 1];
            if (this.contextual) {
                setContextualProperties(c[n].overrideContext, n, f, this.yi);
            }
            if (t[n] === -2) {
                s.nodes.link(g?.nodes ?? h);
                s.setLocation(h);
                i = s.activate(s, r, c[n]);
                if (T(i)) {
                    (e ?? (e = [])).push(i);
                }
            } else if (v < 0 || n !== p[v]) {
                s.nodes.link(g?.nodes ?? h);
                s.nodes.insertBefore(s.location);
            } else {
                --v;
            }
        }
        if (e !== void 0) {
            return e.length === 1 ? e[0] : Promise.all(e);
        }
    }
    dispose() {
        this.views.forEach(dispose);
        this.views = void 0;
    }
    accept(t) {
        const {views: e} = this;
        if (e !== void 0) {
            for (let i = 0, s = e.length; i < s; ++i) {
                const s = e[i].accept(t);
                if (s === true) {
                    return true;
                }
            }
        }
    }
}

Repeat.$au = {
    type: pi,
    name: "repeat",
    isTemplateController: true,
    defaultProperty: "items",
    bindables: [ "items" ]
};

let bn = 16;

let kn = new Int32Array(bn);

let Cn = new Int32Array(bn);

function longestIncreasingSubsequence(t) {
    const e = t.length;
    if (e > bn) {
        bn = e;
        kn = new Int32Array(e);
        Cn = new Int32Array(e);
    }
    let i = 0;
    let s = 0;
    let n = 0;
    let r = 0;
    let l = 0;
    let h = 0;
    let a = 0;
    let c = 0;
    for (;r < e; r++) {
        s = t[r];
        if (s !== -2) {
            l = kn[i];
            n = t[l];
            if (n !== -2 && n < s) {
                Cn[r] = l;
                kn[++i] = r;
                continue;
            }
            h = 0;
            a = i;
            while (h < a) {
                c = h + a >> 1;
                n = t[kn[c]];
                if (n !== -2 && n < s) {
                    h = c + 1;
                } else {
                    a = c;
                }
            }
            n = t[kn[h]];
            if (s < n || n === -2) {
                if (h > 0) {
                    Cn[r] = kn[h - 1];
                }
                kn[h] = r;
            }
        }
    }
    r = ++i;
    const u = new Int32Array(r);
    s = kn[i - 1];
    while (i-- > 0) {
        u[i] = s;
        s = Cn[s];
    }
    while (r-- > 0) kn[r] = 0;
    return u;
}

class RepeatOverrideContext {
    get $odd() {
        return !this.$even;
    }
    get $even() {
        return this.$index % 2 === 0;
    }
    get $first() {
        return this.$index === 0;
    }
    get $middle() {
        return !this.$first && !this.$last;
    }
    get $last() {
        return this.$index === this.$length - 1;
    }
    get $previous() {
        return this.Vi?.[this.$index - 1];
    }
    constructor(t = 0, e = 1, i = undefined) {
        this.$index = t;
        this.$length = e;
        this.Vi = i;
    }
}

const setContextualProperties = (t, e, i, s) => {
    t.$index = e;
    t.$length = i;
    t.Vi = s;
};

const Sn = /*@__PURE__*/ Ue("IRepeatableHandlerResolver", t => t.singleton(RepeatableHandlerResolver));

class RepeatableHandlerResolver {
    constructor() {
        this.Fi = p(y(Bn));
    }
    resolve(t) {
        if (An.handles(t)) {
            return An;
        }
        if (Rn.handles(t)) {
            return Rn;
        }
        if (Tn.handles(t)) {
            return Tn;
        }
        if (En.handles(t)) {
            return En;
        }
        if (Ln.handles(t)) {
            return Ln;
        }
        const e = this.Fi.find(e => e.handles(t));
        if (e !== void 0) {
            return e;
        }
        return Mn;
    }
}

class ArrayLikeHandler {
    static register(t) {
        t.register(Ge(Bn, this));
    }
    handles(t) {
        return "length" in t && H(t.length);
    }
    iterate(t, e) {
        for (let i = 0, s = t.length; i < s; ++i) {
            e(t[i], i, t);
        }
    }
}

const Bn = /*@__PURE__*/ Ue("IRepeatableHandler");

const An = {
    handles: x,
    getObserver: re,
    iterate(t, e) {
        const i = t.length;
        let s = 0;
        for (;s < i; ++s) {
            e(t[s], s, t);
        }
    }
};

const Rn = {
    handles: O,
    getObserver: re,
    iterate(t, e) {
        let i = 0;
        let s;
        for (s of t.keys()) {
            e(s, i++, t);
        }
    }
};

const Tn = {
    handles: $,
    getObserver: re,
    iterate(t, e) {
        let i = 0;
        let s;
        for (s of t.entries()) {
            e(s, i++, t);
        }
    }
};

const En = {
    handles: H,
    iterate(t, e) {
        let i = 0;
        for (;i < t; ++i) {
            e(i, i, t);
        }
    }
};

const Ln = {
    handles: t => t == null,
    iterate() {}
};

const Mn = {
    handles(t) {
        return false;
    },
    iterate(t, e) {
        throw createMappedError(777, t);
    }
};

const setItem = (t, e, i, s, n, r) => {
    if (t) {
        Ut(e, i, s, null, r);
    } else {
        i.bindingContext[n] = r;
    }
};

const getItem = (t, e, i, s, n) => t ? Ot(e, i, s, null) : i.bindingContext[n];

const getScope = (t, e, i, s, n, r, l, h, a) => {
    let c = t.get(i);
    if (c === void 0) {
        c = createScope(s, n, r, l, h, a);
    } else if (c instanceof Kt) {
        t.delete(i);
    } else if (c.length === 1) {
        c = c[0];
        t.delete(i);
    } else {
        c = c.shift();
    }
    if (e.has(i)) {
        const t = e.get(i);
        if (t instanceof Kt) {
            e.set(i, [ t, c ]);
        } else {
            t.push(c);
        }
    } else {
        e.set(i, c);
    }
    setItem(a, n.declaration, c, l, h, s);
    return c;
};

const createScope = (t, e, i, s, n, r) => {
    if (r) {
        const n = Kt.fromParent(i, new le, new RepeatOverrideContext);
        Ut(e.declaration, n, s, null, t);
        return n;
    }
    return Kt.fromParent(i, new le(n, t), new RepeatOverrideContext);
};

const compareNumber = (t, e) => t - e;

class With {
    constructor() {
        this.f = p(Di);
        this.l = p(Ks);
        this.p = p(ci);
    }
    valueChanged(t, e) {
        const i = this.$controller;
        const s = this.view.bindings;
        let n;
        let r = 0, l = 0;
        if (i.isActive && s != null) {
            n = Kt.fromParent(i.scope, t === void 0 ? {} : t);
            for (l = s.length; l > r; ++r) {
                s[r].bind(n);
            }
        }
    }
    attaching(t, e) {
        const {$controller: i, value: s} = this;
        const n = i.ssrScope;
        if (n != null && isSSRTemplateController(n) && n.type === "with") {
            return this.hi(n);
        }
        const r = this.view = this.f.create(i).setLocation(this.l);
        const l = Kt.fromParent(i.scope, s === void 0 ? {} : s);
        return r.activate(r, i, l);
    }
    hi(t) {
        const {$controller: e, value: i, f: s, l: n, p: r} = this;
        const l = adoptSSRView(t, s, e, n, r);
        if (l == null) {
            e.ssrScope = undefined;
            const t = this.view = s.create(e).setLocation(n);
            const r = Kt.fromParent(e.scope, i === void 0 ? {} : i);
            return t.activate(t, e, r);
        }
        this.view = l.view;
        e.ssrScope = undefined;
        const h = Kt.fromParent(e.scope, i === void 0 ? {} : i);
        return l.view.activate(l.view, e, h);
    }
    detaching(t, e) {
        return this.view.deactivate(t, this.$controller);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
    accept(t) {
        if (this.view?.accept(t) === true) {
            return true;
        }
    }
}

With.$au = {
    type: pi,
    name: "with",
    isTemplateController: true,
    bindables: [ "value" ]
};

class Switch {
    constructor() {
        this.cases = [];
        this.activeCases = [];
        this.promise = void 0;
        this.f = p(Di);
        this.l = p(Ks);
        this.p = p(ci);
    }
    link(t, e, i, s) {
        const n = this.$controller.ssrScope;
        if (n != null && isSSRTemplateController(n) && n.type === "switch") {
            return;
        }
        this.view = this.f.create(this.$controller).setLocation(this.l);
    }
    attaching(t, e) {
        let i = this.view;
        const s = this.$controller;
        const n = s.ssrScope;
        if (n != null && isSSRTemplateController(n) && n.type === "switch") {
            const t = adoptSSRView(n, this.f, s, this.l, this.p);
            if (t != null) {
                i?.dispose();
                i = this.view = t.view;
            }
            s.ssrScope = undefined;
        }
        if (i === void 0) {
            i = this.view = this.f.create(this.$controller).setLocation(this.l);
        }
        this.queue(() => i.activate(t, s, s.scope));
        this.queue(() => this.swap(t, this.value));
        return this.promise;
    }
    detaching(t, e) {
        this.queue(() => {
            const e = this.view;
            return e.deactivate(t, this.$controller);
        });
        return this.promise;
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
    valueChanged(t, e) {
        if (!this.$controller.isActive) {
            return;
        }
        this.queue(() => this.swap(null, this.value));
    }
    caseChanged(t) {
        this.queue(() => this.Hi(t));
    }
    Hi(t) {
        const e = t.isMatch(this.value);
        const i = this.activeCases;
        const s = i.length;
        if (!e) {
            if (s > 0 && i[0].id === t.id) {
                return this.Oi(null);
            }
            return;
        }
        if (s > 0 && i[0].id < t.id) {
            return;
        }
        const n = [];
        let r = t.fallThrough;
        if (!r) {
            n.push(t);
        } else {
            const e = this.cases;
            const i = e.indexOf(t);
            for (let t = i, s = e.length; t < s && r; t++) {
                const i = e[t];
                n.push(i);
                r = i.fallThrough;
            }
        }
        return E(this.Oi(null, n), () => {
            this.activeCases = n;
            return this.$i(null);
        });
    }
    swap(t, e) {
        const i = [];
        let s = false;
        for (const t of this.cases) {
            if (s || t.isMatch(e)) {
                i.push(t);
                s = t.fallThrough;
            }
            if (i.length > 0 && !s) {
                break;
            }
        }
        const n = this.defaultCase;
        if (i.length === 0 && n !== void 0) {
            i.push(n);
        }
        return E(this.activeCases.length > 0 ? this.Oi(t, i) : void 0, () => {
            this.activeCases = i;
            if (i.length === 0) {
                return;
            }
            return this.$i(t);
        });
    }
    $i(t) {
        const e = this.$controller;
        if (!e.isActive) {
            return;
        }
        const i = this.activeCases;
        const s = i.length;
        if (s === 0) {
            return;
        }
        const n = e.scope;
        if (s === 1) {
            return i[0].activate(t, n);
        }
        return R(...i.map(e => e.activate(t, n)));
    }
    Oi(t, e = []) {
        const i = this.activeCases;
        const s = i.length;
        if (s === 0) {
            return;
        }
        if (s === 1) {
            const s = i[0];
            if (!e.includes(s)) {
                i.length = 0;
                return s.deactivate(t);
            }
            return;
        }
        return E(R(...i.reduce((i, s) => {
            if (!e.includes(s)) {
                i.push(s.deactivate(t));
            }
            return i;
        }, [])), () => {
            i.length = 0;
        });
    }
    queue(t) {
        const e = this.promise;
        let i = void 0;
        i = this.promise = E(E(e, t), () => {
            if (this.promise === i) {
                this.promise = void 0;
            }
        });
    }
    accept(t) {
        if (this.$controller.accept(t) === true) {
            return true;
        }
        if (this.activeCases.some(e => e.accept(t))) {
            return true;
        }
    }
}

Switch.$au = {
    type: pi,
    name: "switch",
    isTemplateController: true,
    bindables: [ "value" ]
};

let Dn = 0;

const Pn = [ "value", {
    name: "fallThrough",
    mode: me,
    set(t) {
        switch (t) {
          case "true":
            return true;

          case "false":
            return false;

          default:
            return !!t;
        }
    }
} ];

class Case {
    constructor() {
        this.id = ++Dn;
        this.fallThrough = false;
        this.view = void 0;
        this.f = p(Di);
        this.Ze = p(Xt);
        this.l = p(Ks);
        this.p = p(ci);
        this.Ni = p(N).scopeTo(`Case-#${this.id}`);
    }
    link(t, e, i, s) {
        const n = t.parent;
        const r = n?.viewModel;
        if (r instanceof Switch) {
            this.$switch = r;
            this.linkToSwitch(r);
        } else {
            throw createMappedError(815);
        }
    }
    detaching(t, e) {
        return this.deactivate(t);
    }
    isMatch(t) {
        const e = this.value;
        if (x(e)) {
            if (this.pi === void 0) {
                this.pi = this.Wi(e);
            }
            return e.includes(t);
        }
        return e === t;
    }
    valueChanged(t, e) {
        if (x(t)) {
            this.pi?.unsubscribe(this);
            this.pi = this.Wi(t);
        } else if (this.pi !== void 0) {
            this.pi.unsubscribe(this);
        }
        this.$switch.caseChanged(this);
    }
    handleCollectionChange() {
        this.$switch.caseChanged(this);
    }
    activate(t, e) {
        let i = this.view;
        if (i === void 0) {
            const t = this.$controller.ssrScope;
            if (t != null && isSSRTemplateController(t) && (t.type === "case" || t.type === "default-case")) {
                const e = adoptSSRView(t, this.f, this.$controller, this.l, this.p);
                if (e != null) {
                    i = this.view = e.view;
                }
                this.$controller.ssrScope = undefined;
            }
            if (i === void 0) {
                i = this.view = this.f.create(this.$controller).setLocation(this.l);
            }
        }
        if (i.isActive) {
            return;
        }
        const s = i.activate(t ?? i, this.$controller, e);
        if (s instanceof Promise) {
            return s.catch(() => i.deactivate(i, this.$controller));
        }
    }
    deactivate(t) {
        const e = this.view;
        if (e === void 0 || !e.isActive) {
            return;
        }
        return e.deactivate(t ?? e, this.$controller);
    }
    dispose() {
        this.pi?.unsubscribe(this);
        this.view?.dispose();
        this.view = void 0;
    }
    linkToSwitch(t) {
        t.cases.push(this);
    }
    Wi(t) {
        const e = this.Ze.getArrayObserver(t);
        e.subscribe(this);
        return e;
    }
    accept(t) {
        if (this.$controller.accept(t) === true) {
            return true;
        }
        return this.view?.accept(t);
    }
}

(() => {
    defineAttribute({
        name: "case",
        bindables: Pn,
        isTemplateController: true
    }, Case);
})();

class DefaultCase extends Case {
    linkToSwitch(t) {
        if (t.defaultCase !== void 0) {
            throw createMappedError(816);
        }
        t.defaultCase = this;
    }
}

(() => {
    defineAttribute({
        name: "default-case",
        bindables: Pn,
        isTemplateController: true
    }, DefaultCase);
})();

var _n, In, qn;

class PromiseTemplateController {
    constructor() {
        this.preSettledTask = null;
        this.postSettledTask = null;
        this.f = p(Di);
        this.l = p(Ks);
        this.logger = p(N).scopeTo("promise.resolve");
    }
    link(t, e, i, s) {
        this.view = this.f.create(this.$controller).setLocation(this.l);
    }
    attaching(t, e) {
        const i = this.view;
        const s = this.$controller;
        return E(i.activate(t, s, this.viewScope = Kt.fromParent(s.scope, {})), () => this.swap(t));
    }
    valueChanged(t, e) {
        if (!this.$controller.isActive) {
            return;
        }
        this.swap(null);
    }
    swap(t) {
        const e = this.value;
        if (!T(e)) {
            return;
        }
        const i = this.fulfilled;
        const s = this.rejected;
        const n = this.pending;
        const r = this.viewScope;
        let l;
        const $swap = () => {
            void R(l = (this.preSettledTask = $t(() => R(i?.deactivate(t), s?.deactivate(t), n?.activate(t, r)))).result.catch(t => {
                throw t;
            }), e.then(h => {
                if (this.value !== e) {
                    return;
                }
                const fulfill = () => {
                    this.postSettlePromise = (this.postSettledTask = $t(() => R(n?.deactivate(t), s?.deactivate(t), i?.activate(t, r, h)))).result;
                };
                if (this.preSettledTask.status === He) {
                    void l.then(fulfill);
                } else {
                    this.preSettledTask.cancel();
                    fulfill();
                }
            }, h => {
                if (this.value !== e) {
                    return;
                }
                const reject = () => {
                    this.postSettlePromise = (this.postSettledTask = $t(() => R(n?.deactivate(t), i?.deactivate(t), s?.activate(t, r, h)))).result;
                };
                if (this.preSettledTask.status === He) {
                    void l.then(reject);
                } else {
                    this.preSettledTask.cancel();
                    reject();
                }
            }));
        };
        if (this.postSettledTask?.status === He) {
            void this.postSettlePromise.then($swap);
        } else {
            this.postSettledTask?.cancel();
            $swap();
        }
    }
    detaching(t, e) {
        this.preSettledTask?.cancel();
        this.postSettledTask?.cancel();
        this.preSettledTask = this.postSettledTask = null;
        return this.view.deactivate(t, this.$controller);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
}

PromiseTemplateController.$au = {
    type: pi,
    name: "promise",
    isTemplateController: true,
    bindables: [ "value" ]
};

class PendingTemplateController {
    constructor() {
        this.view = void 0;
        this.f = p(Di);
        this.l = p(Ks);
    }
    link(t, e, i, s) {
        getPromiseController(t).pending = this;
    }
    activate(t, e) {
        let i = this.view;
        if (i === void 0) {
            i = this.view = this.f.create(this.$controller).setLocation(this.l);
        }
        if (i.isActive) {
            return;
        }
        return i.activate(i, this.$controller, e);
    }
    deactivate(t) {
        const e = this.view;
        if (e === void 0 || !e.isActive) {
            return;
        }
        return e.deactivate(e, this.$controller);
    }
    detaching(t) {
        return this.deactivate(t);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
}

PendingTemplateController.$au = {
    type: pi,
    name: "pending",
    isTemplateController: true,
    bindables: {
        value: {
            mode: ge
        }
    }
};

class FulfilledTemplateController {
    constructor() {
        this.view = void 0;
        this.f = p(Di);
        this.l = p(Ks);
    }
    link(t, e, i, s) {
        getPromiseController(t).fulfilled = this;
    }
    activate(t, e, i) {
        this.value = i;
        let s = this.view;
        if (s === void 0) {
            s = this.view = this.f.create(this.$controller).setLocation(this.l);
        }
        if (s.isActive) {
            return;
        }
        return s.activate(s, this.$controller, e);
    }
    deactivate(t) {
        const e = this.view;
        if (e === void 0 || !e.isActive) {
            return;
        }
        return e.deactivate(e, this.$controller);
    }
    detaching(t, e) {
        return this.deactivate(t);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
}

FulfilledTemplateController.$au = {
    type: pi,
    name: "then",
    isTemplateController: true,
    bindables: {
        value: {
            mode: ve
        }
    }
};

class RejectedTemplateController {
    constructor() {
        this.view = void 0;
        this.f = p(Di);
        this.l = p(Ks);
    }
    link(t, e, i, s) {
        getPromiseController(t).rejected = this;
    }
    activate(t, e, i) {
        this.value = i;
        let s = this.view;
        if (s === void 0) {
            s = this.view = this.f.create(this.$controller).setLocation(this.l);
        }
        if (s.isActive) {
            return;
        }
        return s.activate(s, this.$controller, e);
    }
    deactivate(t) {
        const e = this.view;
        if (e === void 0 || !e.isActive) {
            return;
        }
        return e.deactivate(e, this.$controller);
    }
    detaching(t, e) {
        return this.deactivate(t);
    }
    dispose() {
        this.view?.dispose();
        this.view = void 0;
    }
}

RejectedTemplateController.$au = {
    type: pi,
    name: "catch",
    isTemplateController: true,
    bindables: {
        value: {
            mode: ve
        }
    }
};

function getPromiseController(t) {
    const e = t.parent;
    const i = e?.viewModel;
    if (i instanceof PromiseTemplateController) {
        return i;
    }
    throw createMappedError(813);
}

class PromiseAttributePattern {
    "promise.resolve"(t, e) {
        return new yt(t, e, "promise", "bind");
    }
}

_n = Symbol.metadata;

PromiseAttributePattern[_n] = {
    [v]: xt.create([ {
        pattern: "promise.resolve",
        symbols: ""
    } ], PromiseAttributePattern)
};

class FulfilledAttributePattern {
    then(t, e) {
        return new yt(t, e, "then", "from-view");
    }
}

In = Symbol.metadata;

FulfilledAttributePattern[In] = {
    [v]: xt.create([ {
        pattern: "then",
        symbols: ""
    } ], FulfilledAttributePattern)
};

class RejectedAttributePattern {
    catch(t, e) {
        return new yt(t, e, "catch", "from-view");
    }
}

qn = Symbol.metadata;

RejectedAttributePattern[qn] = {
    [v]: xt.create([ {
        pattern: "catch",
        symbols: ""
    } ], RejectedAttributePattern)
};

class Focus {
    constructor() {
        this.ji = false;
        this.Ie = p(fi);
        this.p = p(ci);
    }
    binding() {
        this.valueChanged();
    }
    valueChanged() {
        if (this.$controller.isActive) {
            this.zi();
        } else {
            this.ji = true;
        }
    }
    attached() {
        if (this.ji) {
            this.ji = false;
            this.zi();
        }
        this.Ie.addEventListener("focus", this);
        this.Ie.addEventListener("blur", this);
    }
    detaching() {
        const t = this.Ie;
        t.removeEventListener("focus", this);
        t.removeEventListener("blur", this);
    }
    handleEvent(t) {
        if (t.type === "focus") {
            this.value = true;
        } else if (!this.Ui) {
            this.value = false;
        }
    }
    zi() {
        const t = this.Ie;
        const e = this.Ui;
        const i = this.value;
        if (i && !e) {
            t.focus();
        } else if (!i && e) {
            t.blur();
        }
    }
    get Ui() {
        return this.Ie === this.p.document.activeElement;
    }
}

Focus.$au = {
    type: pi,
    name: "focus",
    bindables: {
        value: {
            mode: xe
        }
    }
};

class Portal {
    constructor() {
        this.position = "beforeend";
        this.strict = false;
        const t = p(Di);
        const e = p(Ks);
        const i = p(ci);
        this.p = i;
        this.Gi = i.document.createElement("div");
        (this.view = t.create()).setLocation(this.Ki = Pi(i));
        setEffectiveParentNode(this.view.nodes, e);
    }
    attaching(t) {
        if (this.callbackContext == null) {
            this.callbackContext = this.$controller.scope.bindingContext;
        }
        const e = this.Gi = this.Xi();
        this.Qi(e, this.position);
        return this.Yi(t, e);
    }
    detaching(t) {
        return this.Zi(t, this.Gi);
    }
    targetChanged() {
        const {$controller: t} = this;
        if (!t.isActive) {
            return;
        }
        const e = this.Xi();
        if (this.Gi === e) {
            return;
        }
        this.Gi = e;
        const i = E(this.Zi(null, e), () => {
            this.Qi(e, this.position);
            return this.Yi(null, e);
        });
        if (T(i)) {
            i.catch(rethrow);
        }
    }
    positionChanged() {
        const {$controller: t, Gi: e} = this;
        if (!t.isActive) {
            return;
        }
        const i = E(this.Zi(null, e), () => {
            this.Qi(e, this.position);
            return this.Yi(null, e);
        });
        if (T(i)) {
            i.catch(rethrow);
        }
    }
    Yi(t, e) {
        const {activating: i, callbackContext: s, view: n} = this;
        return E(i?.call(s, e, n), () => this.Ji(t, e));
    }
    Ji(t, e) {
        const {$controller: i, view: s} = this;
        if (t === null) {
            s.nodes.insertBefore(this.Ki);
        } else {
            return E(s.activate(t ?? s, i, i.scope), () => this.ts(e));
        }
        return this.ts(e);
    }
    ts(t) {
        const {activated: e, callbackContext: i, view: s} = this;
        return e?.call(i, t, s);
    }
    Zi(t, e) {
        const {deactivating: i, callbackContext: s, view: n} = this;
        return E(i?.call(s, e, n), () => this.es(t, e));
    }
    es(t, e) {
        const {$controller: i, view: s} = this;
        if (t === null) {
            s.nodes.remove();
        } else {
            return E(s.deactivate(t, i), () => this.ss(e));
        }
        return this.ss(e);
    }
    ss(t) {
        const {deactivated: e, callbackContext: i, view: s} = this;
        return E(e?.call(i, t, s), () => this.rs());
    }
    Xi() {
        const t = this.p;
        const i = t.document;
        let s = this.target;
        let n = this.renderContext;
        if (s === "") {
            if (this.strict) {
                throw createMappedError(811);
            }
            return i.body;
        }
        if (e(s)) {
            let r = i;
            if (e(n)) {
                n = i.querySelector(n);
            }
            if (n instanceof t.Node) {
                r = n;
            }
            s = r.querySelector(s);
        }
        if (s instanceof t.Node) {
            return s;
        }
        if (s == null) {
            if (this.strict) {
                throw createMappedError(812);
            }
            return i.body;
        }
        return s;
    }
    rs() {
        this.Ki.remove();
        this.Ki.$start.remove();
    }
    Qi(t, e) {
        const i = this.Ki;
        const s = i.$start;
        const n = t.parentNode;
        const r = [ s, i ];
        switch (e) {
          case "beforeend":
            insertManyBefore(t, null, r);
            break;

          case "afterbegin":
            insertManyBefore(t, t.firstChild, r);
            break;

          case "beforebegin":
            insertManyBefore(n, t, r);
            break;

          case "afterend":
            insertManyBefore(n, t.nextSibling, r);
            break;

          default:
            throw createMappedError(779, e);
        }
    }
    dispose() {
        this.view.dispose();
        this.view = void 0;
        this.callbackContext = null;
    }
    accept(t) {
        if (this.view?.accept(t) === true) {
            return true;
        }
    }
}

Portal.$au = {
    type: pi,
    name: "portal",
    isTemplateController: true,
    defaultProperty: "target",
    bindables: [ "target", "position", "activated", "activating", "callbackContext", {
        name: "renderContext",
        callback: "targetChanged"
    }, "strict", "deactivated", "deactivating" ]
};

let Vn;

class AuSlot {
    constructor() {
        this.ls = null;
        this.cs = null;
        this.se = false;
        this.expose = null;
        this.slotchange = null;
        this.us = new Set;
        this.pi = null;
        const t = p(Ns);
        const e = p(Ks);
        const i = p(ft);
        const s = p(fs);
        const n = this.name = i.data.name;
        const r = i.projections?.[_i];
        const l = t.instruction?.projections?.[n];
        const h = t.controller.container;
        let a;
        let c;
        if (l == null) {
            c = h.createChild({
                inheritParentResources: true
            });
            a = s.getViewFactory(r ?? (Vn ??= CustomElementDefinition.create({
                name: "au-slot-empty-template",
                template: "",
                needsCompile: false
            })), c);
            this.ds = false;
        } else {
            c = h.createChild();
            c.useResources(t.parent.controller.container);
            registerResolver(c, Ns, new k(void 0, t.parent));
            a = s.getViewFactory(l, c);
            this.ds = true;
            this.ps = h.getAll(Vi, false)?.filter(t => t.slotName === "*" || t.slotName === n) ?? g;
        }
        this.gs = (this.ps ??= g).length > 0;
        this.xs = t;
        this.view = a.create().setLocation(this.l = e);
    }
    get nodes() {
        const t = [];
        const e = this.l;
        let i = e.$start.nextSibling;
        while (i != null && i !== e) {
            if (i.nodeType !== 8) {
                t.push(i);
            }
            i = i.nextSibling;
        }
        return t;
    }
    subscribe(t) {
        this.us.add(t);
    }
    unsubscribe(t) {
        this.us.delete(t);
    }
    binding(t, e) {
        this.ls = e.scope;
        while (e.vmKind === "synthetic" && e.parent?.viewModel instanceof AuSlot) {
            e = e.parent.parent;
        }
        const i = e.scope.bindingContext;
        let s;
        if (this.ds) {
            s = this.xs.controller.scope.parent;
            (this.cs = Kt.fromParent(s, s.bindingContext)).overrideContext.$host = this.expose ?? i;
        }
    }
    attaching(t, e) {
        return E(this.view.activate(t, this.$controller, this.ds ? this.cs : this.ls), () => {
            if (this.gs || c(this.slotchange)) {
                this.ps.forEach(t => t.watch(this));
                this.ii();
                this.ys();
                this.se = true;
            }
        });
    }
    detaching(t, e) {
        this.se = false;
        this.ws();
        this.ps.forEach(t => t.unwatch(this));
        return this.view.deactivate(t, this.$controller);
    }
    exposeChanged(t) {
        if (this.ds && this.cs != null) {
            this.cs.overrideContext.$host = t;
        }
    }
    dispose() {
        this.view.dispose();
        this.view = void 0;
    }
    accept(t) {
        if (this.view?.accept(t) === true) {
            return true;
        }
    }
    ii() {
        if (this.pi != null) {
            return;
        }
        const t = this.l;
        const e = t.parentElement;
        if (e == null) {
            return;
        }
        (this.pi = createMutationObserver(e, e => {
            if (isMutationWithinLocation(t, e)) {
                this.ys();
            }
        })).observe(e, {
            childList: true
        });
    }
    ws() {
        this.pi?.disconnect();
        this.pi = null;
    }
    ys() {
        const t = this.nodes;
        const e = new Set(this.us);
        let i;
        if (this.se) {
            this.slotchange?.call(void 0, this.name, t);
        }
        for (i of e) {
            i.handleSlotChange(this, t);
        }
    }
}

AuSlot.$au = {
    type: sn,
    name: "au-slot",
    template: null,
    containerless: true,
    processContent(t, e, i) {
        i.name = t.getAttribute("name") ?? _i;
        let s = t.firstChild;
        let n = null;
        while (s !== null) {
            n = s.nextSibling;
            if (isElement(s) && s.hasAttribute(Ii)) {
                t.removeChild(s);
            }
            s = n;
        }
    },
    bindables: [ "expose", "slotchange" ]
};

const comparePosition = (t, e) => t.compareDocumentPosition(e);

const isMutationWithinLocation = (t, e) => {
    for (const {addedNodes: i, removedNodes: s, nextSibling: n} of e) {
        let e = 0;
        let r = i.length;
        let l;
        for (;e < r; ++e) {
            l = i[e];
            if (comparePosition(t.$start, l) === 4 && comparePosition(t, l) === 2) {
                return true;
            }
        }
        if (s.length > 0) {
            if (n != null && comparePosition(t.$start, n) === 4 && comparePosition(t, n) === 2) {
                return true;
            }
        }
    }
};

class AuCompose {
    constructor() {
        this.scopeBehavior = "auto";
        this.bs = void 0;
        this.tag = null;
        this.flushMode = "sync";
        this.c = p(C);
        this.parent = p($s);
        this.ks = p(fi);
        this.l = p(Ks);
        this.p = p(ci);
        this.r = p(fs);
        this.Cs = p(ft);
        this.Ss = p(W(CompositionContextFactory, null));
        this.xt = p(ut);
        this.et = p(Ns);
        this.ep = p(fe);
        this.oL = p(Xt);
    }
    get composing() {
        return this.Bs;
    }
    get composition() {
        return this.bs;
    }
    attaching(t, e) {
        return this.Bs = E(this.queue(new ChangeInfo(this.template, this.component, this.model, void 0), t), t => {
            if (this.Ss.As(t)) {
                this.Bs = void 0;
            }
        });
    }
    detaching(t) {
        const e = this.bs;
        const i = this.Bs;
        this.Ss.invalidate();
        this.bs = this.Bs = void 0;
        return E(i, () => e?.deactivate(t));
    }
    propertyChanged(t) {
        if (t === "composing" || t === "composition") {
            return;
        }
        if (this.flushMode === "sync") {
            if (t === "model" && this.bs != null) {
                this.bs.update(this.model);
                return;
            }
            if (t === "tag" && this.bs?.controller.vmKind === Ls) {
                return;
            }
            this.Rs(new ChangeInfo(this.template, this.component, this.model, t));
        }
    }
    propertiesChanged(t) {
        if (this.flushMode === "async") {
            if ("model" in t && this.bs != null && Object.keys(t).length === 1) {
                this.bs.update(this.model);
                return;
            }
            if ("tag" in t && this.bs?.controller.vmKind === Ls) {
                return;
            }
            this.Rs(new ChangeInfo(this.template, this.component, this.model, "model" in t ? "model" : undefined));
        }
    }
    Rs(t) {
        this.Bs = E(this.Bs, () => E(this.queue(t, void 0), t => {
            if (this.Ss.As(t)) {
                this.Bs = void 0;
            }
        }));
    }
    queue(t, e) {
        const i = this.Ss;
        const s = this.bs;
        return E(i.create(t), t => {
            if (i.As(t)) {
                return E(this.compose(t), n => {
                    if (i.As(t)) {
                        return E(n.activate(e), () => {
                            if (i.As(t)) {
                                this.bs = n;
                                return E(s?.deactivate(e), () => t);
                            } else {
                                return E(n.controller.deactivate(n.controller, this.$controller), () => {
                                    n.controller.dispose();
                                    return t;
                                });
                            }
                        });
                    }
                    n.controller.dispose();
                    return t;
                });
            }
            return t;
        });
    }
    compose(t) {
        const {Ts: e, Es: i, Ls: s} = t.change;
        const {c: n, $controller: r, l: l, Cs: h} = this;
        const a = this.Ms(this.et.controller.container, i);
        const u = n.createChild();
        const f = this.p.document.createElement(a == null ? this.tag ?? "div" : a.name);
        l.parentNode.insertBefore(f, l);
        let d;
        if (a == null) {
            d = this.tag == null ? convertToRenderLocation(f) : null;
        } else {
            d = a.containerless ? convertToRenderLocation(f) : null;
        }
        const removeCompositionHost = () => {
            f.remove();
            if (d != null) {
                let t = d.$start.nextSibling;
                let e = null;
                while (t !== null && t !== d) {
                    e = t.nextSibling;
                    t.remove();
                    t = e;
                }
                d.$start?.remove();
                d.remove();
            }
        };
        const p = this.Ds(u, typeof i === "string" ? a.Type : i, f, d);
        const compose = () => {
            const i = h.captures ?? g;
            if (a !== null) {
                const e = a.capture;
                const [s, n] = i.reduce((t, i) => {
                    const s = !(i.target in a.bindables) && (e === true || c(e) && !!e(i.target));
                    t[s ? 0 : 1].push(i);
                    return t;
                }, [ [], [] ]);
                const l = Controller.$el(u, p, f, {
                    projections: h.projections,
                    captures: s
                }, a, d);
                this.Ps(f, a, n).forEach(t => l.addBinding(t));
                return new CompositionController(l, t => l.activate(t ?? l, r, r.scope.parent), t => E(l.deactivate(t ?? l, r), removeCompositionHost), t => p.activate?.(t), t);
            } else {
                const s = CustomElementDefinition.create({
                    name: ln.generateName(),
                    template: e
                });
                const n = this.r.compile(s, u);
                const l = this.r.getViewFactory(n, u);
                const h = Controller.$view(l, r, f);
                const a = this.scopeBehavior === "auto" ? Kt.fromParent(this.parent.scope, p) : Kt.create(p);
                h.setHost(f);
                if (d == null) {
                    this.Ps(f, l.def, i).forEach(t => h.addBinding(t));
                } else {
                    h.setLocation(d);
                }
                return new CompositionController(h, t => h.activate(t ?? h, r, a), t => E(h.deactivate(t ?? h, r), removeCompositionHost), t => p.activate?.(t), t);
            }
        };
        if ("activate" in p) {
            return E(p.activate(s), () => compose());
        } else {
            return compose();
        }
    }
    Ds(t, e, i, s) {
        if (e == null) {
            return new EmptyComponent;
        }
        if (typeof e === "object") {
            return e;
        }
        const n = this.p;
        registerHostNode(t, i, n);
        registerResolver(t, Ks, new k("IRenderLocation", s));
        const r = t.invoke(e);
        registerResolver(t, e, new k("au-compose.component", r));
        return r;
    }
    Ms(t, e) {
        if (typeof e === "string") {
            const i = ln.find(t, e);
            if (i == null) {
                throw createMappedError(806, e);
            }
            return i;
        }
        const i = c(e) ? e : e?.constructor;
        return ln.isType(i, void 0) ? ln.getDefinition(i, null) : null;
    }
    Ps(t, e, i) {
        const s = new HydrationContext(this.$controller, {
            projections: null,
            captures: i
        }, this.et.parent);
        return SpreadBinding.create(s, t, e, this.r, this.xt, this.p, this.ep, this.oL);
    }
}

AuCompose.$au = {
    type: sn,
    name: "au-compose",
    capture: true,
    containerless: true,
    bindables: [ "template", "component", "model", {
        name: "scopeBehavior",
        set: t => {
            if (t === "scoped" || t === "auto") {
                return t;
            }
            throw createMappedError(805, t);
        }
    }, {
        name: "composing",
        mode: ve
    }, {
        name: "composition",
        mode: ve
    }, "tag", {
        name: "flushMode",
        set: t => {
            if (t === "sync" || t === "async") {
                return t;
            }
            throw createMappedError(809, t);
        }
    } ]
};

class EmptyComponent {}

class CompositionContextFactory {
    constructor() {
        this.id = 0;
    }
    As(t) {
        return t.id === this.id;
    }
    create(t) {
        return E(t.load(), t => new CompositionContext(++this.id, t));
    }
    invalidate() {
        this.id++;
    }
}

class ChangeInfo {
    constructor(t, e, i, s) {
        this.Ts = t;
        this.Es = e;
        this.Ls = i;
        this._s = s;
    }
    load() {
        if (T(this.Ts) || T(this.Es)) {
            return Promise.all([ this.Ts, this.Es ]).then(([t, e]) => new LoadedChangeInfo(t, e, this.Ls, this._s));
        } else {
            return new LoadedChangeInfo(this.Ts, this.Es, this.Ls, this._s);
        }
    }
}

class LoadedChangeInfo {
    constructor(t, e, i, s) {
        this.Ts = t;
        this.Es = e;
        this.Ls = i;
        this._s = s;
    }
}

class CompositionContext {
    constructor(t, e) {
        this.id = t;
        this.change = e;
    }
}

class CompositionController {
    constructor(t, e, i, s, n) {
        this.controller = t;
        this.start = e;
        this.stop = i;
        this.update = s;
        this.context = n;
        this.state = 0;
    }
    activate(t) {
        if (this.state !== 0) {
            throw createMappedError(807, this);
        }
        this.state = 1;
        return this.start(t);
    }
    deactivate(t) {
        switch (this.state) {
          case 1:
            this.state = -1;
            return this.stop(t);

          case -1:
            throw createMappedError(808);

          default:
            this.state = -1;
        }
    }
}

const Fn = /*@__PURE__*/ Ue("ISanitizer", t => t.singleton(class {
    sanitize() {
        throw createMappedError(99, "sanitize");
    }
}));

class SanitizeValueConverter {
    constructor() {
        this.Is = p(Fn);
    }
    toView(t) {
        if (t == null) {
            return null;
        }
        return this.Is.sanitize(t);
    }
}

SanitizeValueConverter.$au = {
    type: wi,
    name: "sanitize"
};

class Show {
    constructor() {
        this.el = p(fi);
        this.p = p(ci);
        this.qs = false;
        this.L = false;
        this.$val = "";
        this.$prio = "";
        this.update = () => {
            this.L = false;
            if (Boolean(this.value) !== this.Vs) {
                if (this.Vs === this.Fs) {
                    this.Vs = !this.Fs;
                    this.$val = this.el.style.getPropertyValue("display");
                    this.$prio = this.el.style.getPropertyPriority("display");
                    this.el.style.setProperty("display", "none", "important");
                } else {
                    this.Vs = this.Fs;
                    this.el.style.setProperty("display", this.$val, this.$prio);
                    if (this.el.getAttribute("style") === "") {
                        this.el.removeAttribute("style");
                    }
                }
            }
        };
        const t = p(ft);
        this.Vs = this.Fs = t.alias !== "hide";
    }
    binding() {
        this.qs = true;
        this.update();
    }
    detaching() {
        this.qs = false;
        this.L = false;
    }
    valueChanged() {
        if (this.qs && !this.L) {
            this.L = true;
            Nt(this.update);
        }
    }
}

Show.$au = {
    type: pi,
    name: "show",
    bindables: [ "value" ],
    aliases: [ "hide" ]
};

const Hn = [ dn, he, NodeObserverLocator ];

const On = [ wt, bt, kt, Mi ];

const $n = [ Ct, St ];

const Nn = [ Bt, At, Rt, Tt, Et, Lt, Mt, Dt, Pt, _t, It, qt, Vt ];

const Wn = [ DebounceBindingBehavior, OneTimeBindingBehavior, ToViewBindingBehavior, FromViewBindingBehavior, SignalBindingBehavior, ThrottleBindingBehavior, TwoWayBindingBehavior, SanitizeValueConverter, If, Else, Repeat, With, Switch, Case, DefaultCase, PromiseTemplateController, PendingTemplateController, FulfilledTemplateController, RejectedTemplateController, PromiseAttributePattern, FulfilledAttributePattern, RejectedAttributePattern, AttrBindingBehavior, SelfBindingBehavior, UpdateTriggerBindingBehavior, AuCompose, Portal, Focus, Show, AuSlot ];

const jn = [ Gi, Ki, zi, Ui, Oi, $i, Ni, Wi, ji, Yi, is, Zi, Ji, ts, es, Xi, ss, ns ];

const zn = /*@__PURE__*/ createConfiguration(n);

function createConfiguration(t) {
    return {
        optionsProvider: t,
        register(e) {
            const i = {
                coercingOptions: {
                    enableCoercion: false,
                    coerceNullish: false
                }
            };
            t(i);
            return e.register(Xe(Zt, i.coercingOptions), de, ...Hn, ...Wn, ...On, ...Nn, ...jn);
        },
        customize(e) {
            return createConfiguration(e ?? t);
        }
    };
}

function children(t, i) {
    if (!children.mixed) {
        children.mixed = true;
        Gt(ChildrenBinding, null);
        lifecycleHooks()(ChildrenLifecycleHooks, null);
    }
    let s;
    const n = Ce("dependencies");
    function decorator(t, e) {
        switch (e.kind) {
          case "field":
            s.name = e.name;
            break;
        }
        const i = e.metadata[n] ??= [];
        i.push(new ChildrenLifecycleHooks(s ?? {}));
    }
    if (arguments.length > 1) {
        s = {};
        decorator(t, i);
        return;
    } else if (e(t)) {
        s = {
            query: t
        };
        return decorator;
    }
    s = t === void 0 ? {} : t;
    return decorator;
}

children.mixed = false;

class ChildrenBinding {
    constructor(t, e, i, s, n, r) {
        this.Hs = void 0;
        this.isBound = false;
        this.obj = e;
        this.cb = i;
        this.Z = s;
        this.Os = n;
        this.$s = r;
        this.pi = createMutationObserver(this.ks = t, () => {
            this.Ns();
        });
    }
    getValue() {
        return this.isBound ? this.Hs : this.Ws();
    }
    setValue(t) {}
    bind() {
        if (this.isBound) {
            return;
        }
        this.isBound = true;
        this.pi.observe(this.ks, {
            childList: true
        });
        this.Hs = this.Ws();
    }
    unbind() {
        if (!this.isBound) {
            return;
        }
        this.isBound = false;
        this.pi.takeRecords();
        this.pi.disconnect();
        this.Hs = g;
    }
    Ns() {
        this.Hs = this.Ws();
        this.cb?.call(this.obj);
        this.subs.notify(this.Hs, undefined);
    }
    get() {
        throw createMappedError(99, "get");
    }
    Ws() {
        const t = this.Z;
        const e = this.Os;
        const i = this.$s;
        const s = t === "$all" ? this.ks.childNodes : this.ks.querySelectorAll(`:scope > ${t}`);
        const n = s.length;
        const r = [];
        const l = {
            optional: true
        };
        let h;
        let a;
        let c = 0;
        let u;
        while (n > c) {
            u = s[c];
            h = findElementControllerFor(u, l);
            a = h?.viewModel ?? null;
            if (e == null ? true : e(u, a)) {
                r.push(i == null ? a ?? u : i(u, a));
            }
            ++c;
        }
        return r;
    }
}

class ChildrenLifecycleHooks {
    constructor(t) {
        this.J = t;
    }
    register(t) {
        Xe(vi, this).register(t);
    }
    hydrating(t, e) {
        const i = this.J;
        const s = i.query ?? "*";
        const n = new ChildrenBinding(e.host, t, t[i.callback ?? `${Be(i.name)}Changed`], s, i.filter, i.map);
        if (/[\s>]/.test(s)) {
            throw createMappedError(9989, s);
        }
        Pe(t, i.name, {
            enumerable: true,
            configurable: true,
            get: Ee(() => n.getValue(), {
                getObserver: () => n
            }),
            set: () => {}
        });
        e.addBinding(n);
    }
}

export { AdoptedStyleSheetsStyles, AppRoot, ai as AppTask, ArrayLikeHandler, AttrBindingBehavior, AttrMapper, AttributeBinding, is as AttributeBindingRenderer, AttributeNSAccessor, AuCompose, AuSlot, AuSlotsInfo, Aurelia, je as Bindable, BindableDefinition, ei as BindingBehavior, BindingBehaviorDefinition, BindingModeBehavior, BindingTargetSubscriber, CSSModulesProcessorRegistry, Case, CheckedObserver, ChildrenBinding, ClassAttributeAccessor, ComputedWatcher, ContentBinding, Controller, gi as CustomAttribute, CustomAttributeDefinition, Ni as CustomAttributeRenderer, ln as CustomElement, CustomElementDefinition, $i as CustomElementRenderer, DataAttributeAccessor, DebounceBindingBehavior, Nn as DefaultBindingLanguage, On as DefaultBindingSyntax, DefaultCase, Hn as DefaultComponents, jn as DefaultRenderers, Wn as DefaultResources, Else, EventModifier, Mi as EventModifierRegistration, ExpressionWatcher, FlushQueue, Focus, FragmentNodeSequence, FromViewBindingBehavior, FulfilledTemplateController, an as IAppRoot, hi as IAppTask, Vi as IAuSlotWatcher, qi as IAuSlotsInfo, cn as IAurelia, $s as IController, Li as IEventModifier, Gs as IEventTarget, Bi as IFlushQueue, Js as IHistory, Ns as IHydrationContext, Ei as IKeyMapping, vi as ILifecycleHooks, Qi as IListenerBindingOptions, Zs as ILocation, Ti as IModifiedEventHandlerCreator, fi as INode, ci as IPlatform, Ks as IRenderLocation, Hi as IRenderer, fs as IRendering, Bn as IRepeatableHandler, Sn as IRepeatableHandlerResolver, yi as ISSRContext, un as ISVGAnalyzer, Fn as ISanitizer, ms as IShadowDOMGlobalStyles, ds as IShadowDOMStyleFactory, ps as IShadowDOMStyles, oi as ISignaler, Di as IViewFactory, Ys as IWindow, If, InterpolationBinding, Ui as InterpolationBindingRenderer, InterpolationPartBinding, Ki as IteratorBindingRenderer, LetBinding, ji as LetElementRenderer, xi as LifecycleHooks, LifecycleHooksDefinition, LifecycleHooksEntry, ListenerBinding, ListenerBindingOptions, Yi as ListenerBindingRenderer, As as MountTarget, NodeObserverLocator, NoopSVGAnalyzer, OneTimeBindingBehavior, PendingTemplateController, Portal, PromiseTemplateController, PropertyBinding, Gi as PropertyBindingRenderer, RefBinding, zi as RefBindingRenderer, RejectedTemplateController, Rendering, Repeat, dn as RuntimeTemplateCompilerImplementation, SVGAnalyzer, SanitizeValueConverter, SelectValueObserver, SelfBindingBehavior, Zi as SetAttributeRenderer, Ji as SetClassAttributeRenderer, Oi as SetPropertyRenderer, ts as SetStyleAttributeRenderer, ShadowDOMRegistry, $n as ShortHandBindingSyntax, SignalBindingBehavior, ss as SpreadRenderer, zn as StandardConfiguration, Os as State, StyleAttributeAccessor, gs as StyleConfiguration, StyleElementStyles, es as StylePropertyBindingRenderer, Switch, Wi as TemplateControllerRenderer, Xi as TextBindingRenderer, ThrottleBindingBehavior, ToViewBindingBehavior, TwoWayBindingBehavior, UpdateTriggerBindingBehavior, ValueAttributeObserver, ki as ValueConverter, ValueConverterDefinition, ViewFactory, di as Watch, With, adoptSSRView, adoptSSRViews, alias, bindable, bindingBehavior, capture, children, coercer, containerless, convertToRenderLocation, cssModules, customAttribute, customElement, findMatchingEndMarker, getEffectiveParentNode, hydrateSSRDefinition, isCustomElementController, isCustomElementViewModel, isRenderLocation, isSSRScope, isSSRTemplateController, lifecycleHooks, Si as mixinAstEvaluator, Ci as mixinUseScope, Ai as mixingBindingLimited, partitionSiblingNodes, processContent, ui as refs, registerAliases, registerHostNode, renderer, setEffectiveParentNode, shadowCSS, slotted, templateController, useShadowDOM, valueConverter, watch };
//# sourceMappingURL=index.mjs.map
