import { DI as t, Registration as e, getResourceKeyFor as n, registrableMetadataKey as r, emptyArray as i, resolve as s, IContainer as o, isString as l, firstDefined as a, mergeArrays as u, Protocol as c, resourceBaseName as f, resource as m, camelCase as d, IPlatform as p, createImplementationRegister as h, noop as g, toArray as y, pascalCase as b, ILogger as A, allResources as w } from "@aurelia/kernel";

import { Metadata as C } from "@aurelia/metadata";

import { createPrimitiveLiteralExpression as P, IExpressionParser as T } from "@aurelia/expression-parser";

const B = t.createInterface;

const S = Object.freeze;

const {aliasTo: v, singleton: R} = e;

const x = "Interpolation";

const I = "IsFunction";

const _ = "IsProperty";

const E = "custom-element";

const k = /*@__PURE__*/ S({
    default: 0,
    oneTime: 1,
    toView: 2,
    fromView: 4,
    twoWay: 6
});

const D = /*@__PURE__*/ B("ITemplateCompiler");

const $ = /*@__PURE__*/ B("IAttrMapper");

const createMappedError = (t, ...e) => {
    const n = String(t).padStart(4, "0");
    return new Error(`AUR${n}:${e.map(String)}`);
};

var L, F, V, M, W;

class AttrSyntax {
    constructor(t, e, n, r, i = null) {
        this.rawName = t;
        this.rawValue = e;
        this.target = n;
        this.command = r;
        this.parts = i;
    }
}

const O = /*@__PURE__*/ B("IAttributePattern");

const j = 0;

const N = 1;

function createSymbolSet(t) {
    const e = new Set;
    for (let n = 0; n < t.length; n++) {
        e.add(t[n]);
    }
    return e;
}

function compilePattern(t, e) {
    const n = [];
    const r = createSymbolSet(e);
    let i = 0;
    let s = 0;
    let o = 0;
    let l = 0;
    while (l < t.length) {
        if (t.startsWith("PART", l)) {
            n.push({
                kind: j
            });
            s++;
            l += 4;
            continue;
        }
        const e = l;
        while (l < t.length && !t.startsWith("PART", l)) l++;
        const a = t.slice(e, l);
        let u = 0;
        while (u < a.length) {
            const t = r.has(a[u]);
            let e = u + 1;
            while (e < a.length && r.has(a[e]) === t) e++;
            n.push({
                kind: N,
                value: a.slice(u, e)
            });
            if (t) o++; else i++;
            u = e;
        }
    }
    return {
        tokens: n,
        score: {
            statics: i,
            dynamics: s,
            symbols: o
        }
    };
}

class CompiledPattern {
    constructor(t) {
        this.def = t;
        const {tokens: e, score: n} = compilePattern(t.pattern, t.symbols);
        this.i = e;
        this.u = n;
        this.A = createSymbolSet(t.symbols);
    }
    C(t) {
        const e = [];
        const n = this.i;
        const r = this.A;
        let i = 0;
        let s = "";
        for (let o = 0; o < n.length; o++) {
            const l = n[o];
            if (l.kind === N) {
                const {value: n} = l;
                if (!t.startsWith(n, i)) return null;
                for (let t = 0; t < n.length; t++) {
                    const i = n[t];
                    if (r.has(i)) {
                        if (s.length > 0) {
                            e.push(s);
                            s = "";
                        }
                    } else {
                        s += i;
                    }
                }
                i += n.length;
            } else {
                const e = i;
                while (i < t.length && !r.has(t[i])) i++;
                if (i === e) return null;
                s += t.slice(e, i);
            }
        }
        if (s.length > 0) e.push(s);
        return i === t.length ? e : null;
    }
}

function isBetterScore(t, e) {
    if (t.statics !== e.statics) return t.statics > e.statics;
    if (t.dynamics !== e.dynamics) return t.dynamics > e.dynamics;
    return t.symbols > e.symbols;
}

class Interpretation {
    constructor() {
        this.parts = i;
        this.P = null;
    }
    get pattern() {
        return this.P;
    }
    set pattern(t) {
        this.P = t;
    }
    T(t, e) {
        this.P = t;
        this.parts = e;
    }
}

const U = /*@__PURE__*/ B("ISyntaxInterpreter", t => t.singleton(SyntaxInterpreter));

class SyntaxInterpreter {
    constructor() {
        this.B = [];
        this.R = new Map;
    }
    add(t) {
        for (const e of t) {
            this.B.push(new CompiledPattern(e));
        }
    }
    interpret(t) {
        const e = new Interpretation;
        const n = this.R.get(t);
        if (n !== void 0) {
            e.T(n.pattern, n.parts);
            return e;
        }
        let r = null;
        let s = null;
        for (let e = 0; e < this.B.length; e++) {
            const n = this.B[e];
            const i = n.C(t);
            if (i !== null) {
                if (r === null || isBetterScore(n.u, r.u)) {
                    r = n;
                    s = i;
                }
            }
        }
        if (r !== null) {
            const n = {
                pattern: r.def.pattern,
                parts: s
            };
            this.R.set(t, n);
            e.T(n.pattern, n.parts);
        } else {
            this.R.set(t, {
                pattern: null,
                parts: i
            });
        }
        return e;
    }
}

const H = /*@__PURE__*/ B("IAttributeParser", t => t.singleton(AttributeParser));

class AttributeParser {
    constructor() {
        this.I = s(U);
        this.c = s(o);
        this._ = {};
        this.$ = [];
        this.L = false;
    }
    registerPattern(t, e) {
        if (this.L) throw createMappedError(88);
        const n = this._;
        for (const r of t) {
            if (n[r.pattern] != null) throw createMappedError(89, r.pattern);
            n[r.pattern] = {
                type: e
            };
        }
        this.$.push(...t);
    }
    parse(t, e) {
        if (!this.L) {
            this.I.add(this.$);
            this.L = true;
        }
        const n = this.I.interpret(t);
        const r = n.pattern;
        if (r === null) {
            return new AttrSyntax(t, e, t, null, null);
        }
        const i = this._[r];
        if (i.instance === void 0) {
            i.instance = this.c.get(i.type);
        }
        return i.instance[r](t, e, n.parts);
    }
}

function attributePattern(...t) {
    return function decorator(e, n) {
        n.metadata[r] = q.create(t, e);
        return e;
    };
}

const q = /*@__PURE__*/ S({
    name: n("attribute-pattern"),
    create(t, e) {
        return {
            register(n) {
                n.get(H).registerPattern(t, e);
                R(O, e).register(n);
            }
        };
    }
});

class DotSeparatedAttributePattern {
    "PART.PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], n[1]);
    }
    "PART.PART.PART"(t, e, n) {
        return new AttrSyntax(t, e, `${n[0]}.${n[1]}`, n[2]);
    }
}

L = Symbol.metadata;

DotSeparatedAttributePattern[L] = {
    [r]: /*@__PURE__*/ q.create([ {
        pattern: "PART.PART",
        symbols: "."
    }, {
        pattern: "PART.PART.PART",
        symbols: "."
    } ], DotSeparatedAttributePattern)
};

class RefAttributePattern {
    ref(t, e, n) {
        return new AttrSyntax(t, e, "element", "ref");
    }
    "PART.ref"(t, e, n) {
        let r = n[0];
        if (r === "view-model") {
            r = "component";
        }
        return new AttrSyntax(t, e, r, "ref");
    }
}

F = Symbol.metadata;

RefAttributePattern[F] = {
    [r]: /*@__PURE__*/ q.create([ {
        pattern: "ref",
        symbols: ""
    }, {
        pattern: "PART.ref",
        symbols: "."
    } ], RefAttributePattern)
};

class EventAttributePattern {
    "PART.trigger:PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "trigger", n);
    }
    "PART.capture:PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "capture", n);
    }
}

V = Symbol.metadata;

EventAttributePattern[V] = {
    [r]: /*@__PURE__*/ q.create([ {
        pattern: "PART.trigger:PART",
        symbols: ".:"
    }, {
        pattern: "PART.capture:PART",
        symbols: ".:"
    } ], EventAttributePattern)
};

class ColonPrefixedBindAttributePattern {
    ":PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "bind");
    }
}

M = Symbol.metadata;

ColonPrefixedBindAttributePattern[M] = {
    [r]: /*@__PURE__*/ q.create([ {
        pattern: ":PART",
        symbols: ":"
    } ], ColonPrefixedBindAttributePattern)
};

class AtPrefixedTriggerAttributePattern {
    "@PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "trigger");
    }
    "@PART:PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "trigger", [ n[0], "trigger", ...n.slice(1) ]);
    }
}

W = Symbol.metadata;

AtPrefixedTriggerAttributePattern[W] = {
    [r]: /*@__PURE__*/ q.create([ {
        pattern: "@PART",
        symbols: "@"
    }, {
        pattern: "@PART:PART",
        symbols: "@:"
    } ], AtPrefixedTriggerAttributePattern)
};

const z = C.get;

C.has;

const G = C.define;

const J = 0;

const K = 1;

const Q = 2;

const X = 3;

const Y = 10;

const Z = 11;

const tt = 12;

const et = 13;

const nt = 14;

const rt = 15;

const it = 16;

const st = 30;

const ot = 31;

const lt = 32;

const at = 33;

const ut = 34;

const ct = 35;

const ft = 36;

const mt = 50;

const dt = 51;

const pt = 52;

const ht = /*@__PURE__*/ B("Instruction");

function isInstruction(t) {
    const e = t.type;
    return typeof e === "number" && e >= 0 && e < 100;
}

function bindingCommand(t) {
    return function(e, n) {
        n.addInitializer(function() {
            bt.define(t, e);
        });
        return e;
    };
}

class BindingCommandDefinition {
    constructor(t, e, n, r) {
        this.Type = t;
        this.name = e;
        this.aliases = n;
        this.key = r;
    }
    static create(t, e) {
        let n;
        let r;
        if (l(t)) {
            n = t;
            r = {
                name: n
            };
        } else {
            n = t.name;
            r = t;
        }
        return new BindingCommandDefinition(e, a(getCommandAnnotation(e, "name"), n), u(getCommandAnnotation(e, "aliases"), r.aliases, e.aliases), getCommandKeyFrom(n));
    }
    register(t, e) {
        const n = this.Type;
        const r = typeof e === "string" ? getCommandKeyFrom(e) : this.key;
        const i = this.aliases;
        if (!t.has(r, false)) {
            t.register(t.has(n, false) ? null : R(n, n), v(n, r), ...i.map(t => v(n, getCommandKeyFrom(t))));
        }
    }
}

const gt = "binding-command";

const yt = /*@__PURE__*/ n(gt);

const getCommandKeyFrom = t => `${yt}:${t}`;

const getCommandAnnotation = (t, e) => z(c.annotation.keyFor(e), t);

const bt = /*@__PURE__*/ (() => {
    const t = "__au_static_resource__";
    const getDefinitionFromStaticAu = (e, n, r) => {
        let i = z(t, e);
        if (i == null) {
            if (e.$au?.type === n) {
                i = r(e.$au, e);
                G(i, e, t);
            }
        }
        return i;
    };
    return S({
        name: yt,
        keyFrom: getCommandKeyFrom,
        define(t, e) {
            const n = BindingCommandDefinition.create(t, e);
            const r = n.Type;
            G(n, r, yt, f);
            return r;
        },
        getAnnotation: getCommandAnnotation,
        find(t, e) {
            const n = t.find(gt, e);
            return n == null ? null : z(yt, n) ?? getDefinitionFromStaticAu(n, gt, BindingCommandDefinition.create) ?? null;
        },
        get(t, e) {
            return t.get(m(getCommandKeyFrom(e)));
        }
    });
})();

class OneTimeBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(t, e, n) {
        const r = t.attr;
        let i = r.target;
        let s = t.attr.rawValue;
        s = s === "" ? d(i) : s;
        if (t.bindable == null) {
            i = n.map(t.node, i) ?? d(i);
        } else {
            i = t.bindable.name;
        }
        return {
            type: tt,
            from: e.parse(s, _),
            to: i,
            mode: 1
        };
    }
}

OneTimeBindingCommand.$au = {
    type: gt,
    name: "one-time"
};

class ToViewBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(t, e, n) {
        const r = t.attr;
        let i = r.target;
        let s = t.attr.rawValue;
        s = s === "" ? d(i) : s;
        if (t.bindable == null) {
            i = n.map(t.node, i) ?? d(i);
        } else {
            i = t.bindable.name;
        }
        return {
            type: tt,
            from: e.parse(s, _),
            to: i,
            mode: 2
        };
    }
}

ToViewBindingCommand.$au = {
    type: gt,
    name: "to-view"
};

class FromViewBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(t, e, n) {
        const r = t.attr;
        let i = r.target;
        let s = r.rawValue;
        s = s === "" ? d(i) : s;
        if (t.bindable == null) {
            i = n.map(t.node, i) ?? d(i);
        } else {
            i = t.bindable.name;
        }
        return {
            type: tt,
            from: e.parse(s, _),
            to: i,
            mode: 4
        };
    }
}

FromViewBindingCommand.$au = {
    type: gt,
    name: "from-view"
};

class TwoWayBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(t, e, n) {
        const r = t.attr;
        let i = r.target;
        let s = r.rawValue;
        s = s === "" ? d(i) : s;
        if (t.bindable == null) {
            i = n.map(t.node, i) ?? d(i);
        } else {
            i = t.bindable.name;
        }
        return {
            type: tt,
            from: e.parse(s, _),
            to: i,
            mode: 6
        };
    }
}

TwoWayBindingCommand.$au = {
    type: gt,
    name: "two-way"
};

class DefaultBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(t, e, n) {
        const r = t.attr;
        const i = t.bindable;
        let s = r.rawValue;
        let o = r.target;
        let a;
        s = s === "" ? d(o) : s;
        if (i == null) {
            a = n.isTwoWay(t.node, o) ? 6 : 2;
            o = n.map(t.node, o) ?? d(o);
        } else {
            a = i.mode === 0 || i.mode == null ? 2 : i.mode;
            o = i.name;
        }
        return {
            type: tt,
            from: e.parse(s, _),
            to: o,
            mode: l(a) ? k[a] ?? 0 : a
        };
    }
}

DefaultBindingCommand.$au = {
    type: gt,
    name: "bind"
};

class ForBindingCommand {
    constructor() {
        this.F = s(H);
    }
    get ignoreAttr() {
        return false;
    }
    build(t, e) {
        const n = t.bindable === null ? d(t.attr.target) : t.bindable.name;
        const r = e.parse(t.attr.rawValue, "IsIterator");
        let s = i;
        if (r.semiIdx > -1) {
            const e = t.attr.rawValue.slice(r.semiIdx + 1);
            const n = e.split(";");
            const i = [];
            for (let t = 0, e = n.length; t < e; t++) {
                const e = n[t];
                const r = e.indexOf(":");
                if (r > -1) {
                    const t = e.slice(0, r).trim();
                    const n = e.slice(r + 1).trim();
                    const s = this.F.parse(t, n);
                    i.push({
                        type: it,
                        value: n,
                        to: s.target,
                        command: s.command
                    });
                }
            }
            s = i;
        }
        return {
            type: rt,
            forOf: r,
            to: n,
            props: s
        };
    }
}

ForBindingCommand.$au = {
    type: gt,
    name: "for"
};

class TriggerBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        return {
            type: ot,
            from: e.parse(t.attr.rawValue, I),
            to: t.attr.target,
            capture: false,
            modifier: t.attr.parts?.[2] ?? null
        };
    }
}

TriggerBindingCommand.$au = {
    type: gt,
    name: "trigger"
};

class CaptureBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        return {
            type: ot,
            from: e.parse(t.attr.rawValue, I),
            to: t.attr.target,
            capture: true,
            modifier: t.attr.parts?.[2] ?? null
        };
    }
}

CaptureBindingCommand.$au = {
    type: gt,
    name: "capture"
};

class AttrBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        const n = t.attr;
        const r = n.target;
        let i = n.rawValue;
        i = i === "" ? d(r) : i;
        return {
            type: lt,
            attr: r,
            from: e.parse(i, _),
            to: r
        };
    }
}

AttrBindingCommand.$au = {
    type: gt,
    name: "attr"
};

class StyleBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        return {
            type: lt,
            attr: "style",
            from: e.parse(t.attr.rawValue, _),
            to: t.attr.target
        };
    }
}

StyleBindingCommand.$au = {
    type: gt,
    name: "style"
};

class ClassBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        let n = t.attr.target;
        if (n.includes(",")) {
            const t = n.split(",").filter(t => t.length > 0);
            if (t.length === 0) {
                throw createMappedError(723);
            }
            n = t.join(" ");
        }
        return {
            type: lt,
            attr: "class",
            from: e.parse(t.attr.rawValue, _),
            to: n
        };
    }
}

ClassBindingCommand.$au = {
    type: gt,
    name: "class"
};

class RefBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        return {
            type: nt,
            from: e.parse(t.attr.rawValue, _),
            to: t.attr.target
        };
    }
}

RefBindingCommand.$au = {
    type: gt,
    name: "ref"
};

class SpreadValueBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(t) {
        return {
            type: pt,
            target: t.attr.target,
            from: t.attr.rawValue
        };
    }
}

SpreadValueBindingCommand.$au = {
    type: gt,
    name: "spread"
};

typeof SuppressedError === "function" ? SuppressedError : function(t, e, n) {
    var r = new Error(n);
    return r.name = "SuppressedError", r.error = t, r.suppressed = e, r;
};

const At = /*@__PURE__*/ B("ITemplateElementFactory", t => t.singleton(TemplateElementFactory));

const wt = {};

class TemplateElementFactory {
    constructor() {
        this.p = s(p);
        this.V = this.t();
    }
    t() {
        return this.p.document.createElement("template");
    }
    createTemplate(t) {
        if (l(t)) {
            let e = wt[t];
            if (e === void 0) {
                const n = this.V;
                n.innerHTML = t;
                const r = n.content.firstElementChild;
                if (needsWrapping(r)) {
                    this.V = this.t();
                    e = n;
                } else {
                    n.content.removeChild(r);
                    e = r;
                }
                wt[t] = e;
            }
            return e.cloneNode(true);
        }
        if (t.nodeName !== "TEMPLATE") {
            const e = this.t();
            e.content.appendChild(t);
            return e;
        }
        t.parentNode?.removeChild(t);
        return t.cloneNode(true);
        function needsWrapping(t) {
            if (t == null) return true;
            if (t.nodeName !== "TEMPLATE") return true;
            const e = t.nextElementSibling;
            if (e != null) return true;
            const n = t.previousSibling;
            if (n != null) {
                switch (n.nodeType) {
                  case 3:
                    return n.textContent.trim().length > 0;
                }
            }
            const r = t.nextSibling;
            if (r != null) {
                switch (r.nodeType) {
                  case 3:
                    return r.textContent.trim().length > 0;
                }
            }
            return false;
        }
    }
}

const Ct = "au-start";

const Pt = "au-end";

const insertBefore = (t, e, n) => t.insertBefore(e, n);

const insertManyBefore = (t, e, n) => {
    if (t === null) {
        return;
    }
    const r = n.length;
    let i = 0;
    while (r > i) {
        t.insertBefore(n[i], e);
        ++i;
    }
};

const appendToTemplate = (t, e) => t.content.appendChild(e);

const appendManyToTemplate = (t, e) => {
    const n = e.length;
    let r = 0;
    while (n > r) {
        t.content.appendChild(e[r]);
        ++r;
    }
};

const isElement = t => t.nodeType === 1;

const isTextNode = t => t.nodeType === 3;

const Tt = "au-slot";

const Bt = "default";

const St = (t => () => `anonymous-${++t}`)(0);

class TemplateCompiler {
    constructor() {
        this.debug = false;
        this.resolveResources = true;
    }
    compile(t, e) {
        if (t.template == null || t.needsCompile === false) {
            return t;
        }
        const n = new CompilationContext(t, e, null, null, void 0);
        const r = l(t.template) || !t.enhance ? n.M.createTemplate(t.template) : t.template;
        const s = r.nodeName === vt && r.content != null;
        const o = s ? r.content : r;
        const a = Lt.findAll(e);
        const u = a.length;
        let c = 0;
        if (u > 0) {
            while (u > c) {
                a[c].compiling?.(r);
                ++c;
            }
        }
        if (r.hasAttribute(Dt)) {
            throw createMappedError(701, t);
        }
        this.W(o, n);
        this.O(o, n);
        const f = {
            ...t,
            name: t.name || St(),
            dependencies: (t.dependencies ?? i).concat(n.deps ?? i),
            instructions: n.rows,
            surrogates: s ? this.j(r, n) : i,
            template: r,
            hasSlots: n.hasSlot,
            needsCompile: false
        };
        return f;
    }
    compileSpread(t, e, n, r, i) {
        const s = new CompilationContext(t, n, null, null, void 0);
        const o = [];
        const l = i ?? s.N(r.nodeName.toLowerCase());
        const a = l !== null;
        const u = s.ep;
        const c = e.length;
        let f = 0;
        let m;
        let p = null;
        let h;
        let g;
        let y;
        let b;
        let A = null;
        let w;
        let C;
        let P;
        for (;c > f; ++f) {
            m = e[f];
            C = m.target;
            P = m.rawValue;
            if (C === "...$attrs") {
                o.push({
                    type: mt
                });
                continue;
            }
            A = s.U(m);
            if (A !== null && A.ignoreAttr) {
                o.push(A.build({
                    node: r,
                    attr: m,
                    bindable: null,
                    def: null
                }, s.ep, s.m));
                continue;
            }
            if (a) {
                y = s.H(l);
                b = y.attrs[C];
                if (b !== void 0) {
                    if (A == null) {
                        w = u.parse(P, x);
                        o.push({
                            type: dt,
                            instruction: w == null ? {
                                type: Y,
                                value: P,
                                to: b.name
                            } : {
                                type: Z,
                                from: w,
                                to: b.name
                            }
                        });
                    } else {
                        o.push({
                            type: dt,
                            instruction: A.build({
                                node: r,
                                attr: m,
                                bindable: b,
                                def: l
                            }, s.ep, s.m)
                        });
                    }
                    continue;
                }
            }
            p = s.q(C);
            if (p !== null) {
                if (p.isTemplateController) {
                    throw createMappedError(9998, C);
                }
                g = this.G(r, p, m, P, A, s, false);
                (h ??= []).push({
                    type: K,
                    res: this.resolveResources ? p : p.name,
                    alias: p.aliases != null && p.aliases.includes(C) ? C : void 0,
                    props: g
                });
                continue;
            }
            if (A == null) {
                w = u.parse(P, x);
                if (w != null) {
                    o.push({
                        type: Z,
                        from: w,
                        to: s.m.map(r, C) ?? d(C)
                    });
                } else {
                    switch (C) {
                      case "class":
                        o.push({
                            type: ct,
                            value: P
                        });
                        break;

                      case "style":
                        o.push({
                            type: ft,
                            value: P
                        });
                        break;

                      default:
                        o.push({
                            type: ut,
                            value: P,
                            to: C
                        });
                    }
                }
            } else {
                o.push(A.build({
                    node: r,
                    attr: m,
                    bindable: null,
                    def: null
                }, s.ep, s.m));
            }
        }
        if (h != null) {
            return h.concat(o);
        }
        return o;
    }
    j(t, e) {
        const n = t.attributes;
        for (let t = 0, r = n.length; t < r; ++t) {
            const r = e.F.parse(n[t].name, n[t].value);
            if (xt[r.target]) {
                throw createMappedError(702, n[t].name);
            }
        }
        const {tcInstructions: r, attrInstructions: s, plainAttrInstructions: o} = this.J(t, null, i, e, true);
        if (r != null && r.length > 0) {
            const t = r[0].res;
            const e = typeof t === "string" ? t : t.name;
            throw createMappedError(703, e);
        }
        if (s != null) {
            return s.concat(o ?? i);
        }
        return o ?? i;
    }
    O(t, e) {
        switch (t.nodeType) {
          case 1:
            switch (t.nodeName) {
              case "LET":
                return this.K(t, e);

              default:
                return this.X(t, e);
            }

          case 3:
            return this.Y(t, e);

          case 11:
            {
                let n = t.firstChild;
                while (n !== null) {
                    n = this.O(n, e);
                }
                break;
            }
        }
        return t.nextSibling;
    }
    K(t, e) {
        const n = t.attributes;
        const r = n.length;
        const i = [];
        const s = e.ep;
        let o = false;
        let l = 0;
        let a;
        let u;
        let c;
        let f;
        let m;
        let p;
        let h;
        let g;
        for (;r > l; ++l) {
            a = n[l];
            c = a.name;
            f = a.value;
            if (c === "to-binding-context") {
                o = true;
                continue;
            }
            u = e.F.parse(c, f);
            p = u.target;
            h = u.rawValue;
            m = e.U(u);
            if (m !== null) {
                if (u.command === "bind") {
                    i.push({
                        type: et,
                        from: s.parse(h, _),
                        to: d(p)
                    });
                } else {
                    throw createMappedError(704, u);
                }
                continue;
            }
            g = s.parse(h, x);
            i.push({
                type: et,
                from: g === null ? P(h) : g,
                to: d(p)
            });
        }
        e.rows.push([ {
            type: X,
            instructions: i,
            toBindingContext: o
        } ]);
        return this.Z(t, e).nextSibling;
    }
    X(t, e) {
        const n = t.nextSibling;
        const r = (t.getAttribute("as-element") ?? t.nodeName).toLowerCase();
        const s = e.N(r);
        const o = s !== null;
        const l = o && s.shadowOptions != null;
        const a = s?.capture ? [] : i;
        if (r === "slot") {
            if (e.root.def.shadowOptions == null) {
                throw createMappedError(717, e.root.def.name);
            }
            e.root.hasSlot = true;
        }
        let u = true;
        const c = {};
        if (o) {
            u = s.processContent?.call(s.Type, t, e.p, c);
        }
        const {tcInstructions: f, attrInstructions: m, elBindableInstructions: d, plainAttrInstructions: p, hasContainerless: h} = this.J(t, s, a, e);
        if (this.tt(t, p) && p != null && p.length > 1) {
            this.et(t, p);
        }
        let g;
        if (o) {
            g = {
                type: J,
                res: this.resolveResources ? s : s.name,
                props: d ?? i,
                projections: null,
                containerless: h,
                captures: a,
                data: c
            };
        }
        let y;
        let b = false;
        if (p != null || g != null || m != null) {
            y = i.concat(g ?? i, m ?? i, p ?? i);
            b = true;
        }
        if (f != null) {
            const n = f.length;
            let i = n - 1;
            let a = f[i];
            this.nt(t, e);
            let c;
            if (t.nodeName === vt) {
                c = t;
            } else {
                c = e.t();
                appendToTemplate(c, t);
            }
            const m = c;
            const d = e.rt(y == null ? [] : [ y ]);
            if (u !== false) {
                const n = this.it(t, o, l, r, e);
                if (n != null) {
                    g.projections = n;
                }
            }
            if (b) {
                if (o && (h || s.containerless)) {
                    this.nt(t, d);
                } else {
                    this.Z(t, d);
                }
            }
            const p = !o || !s.containerless && !h && u !== false;
            if (p) {
                if (t.nodeName === vt) {
                    this.O(t.content, d);
                } else {
                    let e = t.firstChild;
                    while (e !== null) {
                        e = this.O(e, d);
                    }
                }
            }
            a.def = {
                name: St(),
                type: E,
                template: m,
                instructions: d.rows,
                needsCompile: false
            };
            while (i-- > 0) {
                a = f[i];
                c = e.t();
                appendManyToTemplate(c, [ e.st(), e.ot(Ct), e.ot(Pt) ]);
                a.def = {
                    name: St(),
                    type: E,
                    template: c,
                    needsCompile: false,
                    instructions: [ [ f[i + 1] ] ]
                };
            }
            e.rows.push([ a ]);
        } else {
            if (y != null) {
                e.rows.push(y);
            }
            if (u !== false) {
                const n = this.it(t, o, l, r, e);
                if (n != null) {
                    g.projections = n;
                }
            }
            if (b) {
                if (o && (h || s.containerless)) {
                    this.nt(t, e);
                } else {
                    this.Z(t, e);
                }
            }
            const n = !o || !s.containerless && !h && u !== false;
            if (n && t.childNodes.length > 0) {
                let n = t.firstChild;
                while (n !== null) {
                    n = this.O(n, e);
                }
            }
        }
        return n;
    }
    J(t, e, n, r, i = false) {
        const s = e !== null;
        const o = e?.capture;
        const l = o != null && typeof o !== "boolean";
        const a = r.ep;
        const u = t.attributes;
        let c = u.length;
        let f = 0;
        let m;
        let p;
        let h;
        let y;
        let b = null;
        let A;
        let w;
        let C;
        let P;
        let T;
        let B;
        let S = null;
        let v;
        let R;
        let I;
        let _;
        let E = false;
        let k = false;
        let D = 0;
        const $ = this.debug ? g : () => {
            t.removeAttribute(p);
            --f;
            --c;
        };
        for (;c > f; ++f) {
            m = u[f];
            p = m.name;
            h = m.value;
            switch (p) {
              case "as-element":
              case "containerless":
                $();
                E = E || p === "containerless";
                continue;
            }
            y = r.F.parse(p, h);
            b = r.U(y);
            A = y.target;
            w = y.rawValue;
            if (o && (!l || l && o(A))) {
                if (b != null && b.ignoreAttr) {
                    $();
                    n.push(y);
                    continue;
                }
                k = A !== Tt && A !== "slot" && ((D = A.indexOf("...")) === -1 || D === 0 && A === "...$attrs");
                if (k) {
                    I = r.H(e);
                    if (I.attrs[A] == null && !r.q(A)?.isTemplateController) {
                        $();
                        n.push(y);
                        continue;
                    }
                }
            }
            if (A === "...$attrs") {
                (B ??= []).push({
                    type: mt
                });
                $();
                continue;
            }
            if (b?.ignoreAttr) {
                (B ??= []).push(b.build({
                    node: t,
                    attr: y,
                    bindable: null,
                    def: null
                }, r.ep, r.m));
                $();
                continue;
            }
            if (A.indexOf("...") === 0) {
                if (s && (A = A.slice(3)) !== "$element") {
                    (T ??= []).push({
                        type: pt,
                        target: "$bindables",
                        from: A === "$bindables" ? w : A
                    });
                    $();
                    continue;
                }
                throw createMappedError(720, A);
            }
            if (s) {
                I = r.H(e);
                v = I.attrs[A];
                if (v !== void 0) {
                    if (b === null) {
                        _ = a.parse(w, x);
                        (T ??= []).push(_ == null ? {
                            type: Y,
                            value: w,
                            to: v.name
                        } : {
                            type: Z,
                            from: _,
                            to: v.name
                        });
                    } else {
                        (T ??= []).push(b.build({
                            node: t,
                            attr: y,
                            bindable: v,
                            def: e
                        }, r.ep, r.m));
                    }
                    $();
                    continue;
                }
                if (A === "$bindables") {
                    if (b != null) {
                        const e = b.build({
                            node: t,
                            attr: y,
                            bindable: null,
                            def: null
                        }, r.ep, r.m);
                        (T ??= []).push(e);
                    }
                    $();
                    continue;
                }
            }
            if (A === "$bindables") {
                throw createMappedError(721, t.nodeName, A, w);
            }
            S = r.q(A);
            if (S !== null) {
                R = this.G(t, S, y, w, b, r, true);
                $();
                if (S.isTemplateController) {
                    (C ??= []).push({
                        type: Q,
                        def: Rt,
                        res: this.resolveResources ? S : S.name,
                        alias: void 0,
                        props: R
                    });
                } else {
                    (P ??= []).push({
                        type: K,
                        res: this.resolveResources ? S : S.name,
                        alias: S.aliases != null && S.aliases.includes(A) ? A : void 0,
                        props: R
                    });
                }
                continue;
            }
            if (b === null) {
                _ = a.parse(w, x);
                if (_ != null) {
                    $();
                    (B ??= []).push({
                        type: Z,
                        from: _,
                        to: r.m.map(t, A) ?? d(A)
                    });
                } else if (i) {
                    switch (p) {
                      case "class":
                        (B ??= []).push({
                            type: ct,
                            value: w
                        });
                        break;

                      case "style":
                        (B ??= []).push({
                            type: ft,
                            value: w
                        });
                        break;

                      default:
                        (B ??= []).push({
                            type: ut,
                            value: w,
                            to: p
                        });
                    }
                }
                continue;
            }
            (B ??= []).push(b.build({
                node: t,
                attr: y,
                bindable: null,
                def: null
            }, r.ep, r.m));
            $();
        }
        return {
            tcInstructions: C,
            attrInstructions: P,
            elBindableInstructions: T,
            plainAttrInstructions: B,
            hasContainerless: E
        };
    }
    Y(t, e) {
        const n = t.parentNode;
        const r = e.ep.parse(t.textContent, x);
        const i = t.nextSibling;
        let s;
        let o;
        let l;
        let a;
        let u;
        if (r !== null) {
            ({parts: s, expressions: o} = r);
            if (u = s[0]) {
                insertBefore(n, e.lt(u), t);
            }
            for (l = 0, a = o.length; a > l; ++l) {
                insertManyBefore(n, t, [ e.st(), e.lt(" ") ]);
                if (u = s[l + 1]) {
                    insertBefore(n, e.lt(u), t);
                }
                e.rows.push([ {
                    type: st,
                    from: o[l]
                } ]);
            }
            n.removeChild(t);
        }
        return i;
    }
    ut(t, e, n, r) {
        const i = r.H(n);
        const s = e.length;
        const o = [];
        let l = void 0;
        let a = void 0;
        let u = 0;
        let c = 0;
        let f;
        let m;
        let d;
        let p;
        for (let h = 0; h < s; ++h) {
            c = e.charCodeAt(h);
            if (c === 92) {
                ++h;
            } else if (c === 58) {
                l = e.slice(u, h);
                while (e.charCodeAt(++h) <= 32) ;
                u = h;
                for (;h < s; ++h) {
                    c = e.charCodeAt(h);
                    if (c === 92) {
                        ++h;
                    } else if (c === 59) {
                        a = e.slice(u, h);
                        break;
                    }
                }
                if (a === void 0) {
                    a = e.slice(u);
                }
                m = r.F.parse(l, a);
                d = r.U(m);
                p = i.attrs[m.target];
                if (p == null) {
                    throw createMappedError(707, m.target, n.name);
                }
                if (d === null) {
                    f = r.ep.parse(a, x);
                    o.push(f === null ? {
                        type: Y,
                        value: a,
                        to: p.name
                    } : {
                        type: Z,
                        from: f,
                        to: p.name
                    });
                } else {
                    o.push(d.build({
                        node: t,
                        attr: m,
                        bindable: p,
                        def: n
                    }, r.ep, r.m));
                }
                while (h < s && e.charCodeAt(++h) <= 32) ;
                u = h;
                l = void 0;
                a = void 0;
            }
        }
        return o;
    }
    W(t, e) {
        const n = e.root.def.name;
        const r = t;
        const s = y(r.querySelectorAll("template[as-custom-element]"));
        const o = s.length;
        if (o === 0) {
            return;
        }
        if (o === r.childElementCount) {
            throw createMappedError(708, n);
        }
        const l = new Set;
        const a = [];
        for (const t of s) {
            if (t.parentNode !== r) {
                throw createMappedError(709, n);
            }
            const e = processTemplateName(n, t, l);
            const i = t.content;
            const s = y(i.querySelectorAll("bindable"));
            const o = new Set;
            const u = new Set;
            const c = s.reduce((t, n) => {
                if (n.parentNode !== i) {
                    throw createMappedError(710, e);
                }
                const r = n.getAttribute("name");
                if (r === null) {
                    throw createMappedError(711, n, e);
                }
                const s = n.getAttribute("attribute");
                if (s !== null && u.has(s) || o.has(r)) {
                    throw createMappedError(712, o, s);
                } else {
                    if (s !== null) {
                        u.add(s);
                    }
                    o.add(r);
                }
                const l = y(n.attributes).filter(t => !kt.includes(t.name));
                if (l.length > 0) ;
                n.remove();
                t[r] = {
                    name: r,
                    attribute: s ?? void 0,
                    mode: n.getAttribute("mode") ?? "default"
                };
                return t;
            }, {});
            class LocalDepType {}
            LocalDepType.$au = {
                type: E,
                name: e,
                template: t,
                bindables: c
            };
            Reflect.defineProperty(LocalDepType, "name", {
                value: b(e)
            });
            a.push(LocalDepType);
            r.removeChild(t);
        }
        const u = (e.root.def.dependencies ?? []).concat(e.root.def.Type == null ? i : [ e.root.def.Type ]);
        for (const t of a) {
            t.dependencies = u.concat(a.filter(e => e !== t));
            e.ct(t);
        }
    }
    tt(t, e) {
        const n = t.nodeName;
        return n === "INPUT" && It[t.type] === 1 || n === "SELECT" && (t.hasAttribute("multiple") || e?.some(t => t.type === tt && t.to === "multiple"));
    }
    et(t, e) {
        switch (t.nodeName) {
          case "INPUT":
            {
                const t = e;
                let n = void 0;
                let r = void 0;
                let i = 0;
                let s;
                for (let e = 0; e < t.length && i < 3; e++) {
                    s = t[e];
                    switch (s.to) {
                      case "model":
                      case "value":
                      case "matcher":
                        n = e;
                        i++;
                        break;

                      case "checked":
                        r = e;
                        i++;
                        break;
                    }
                }
                if (r !== void 0 && n !== void 0 && r < n) {
                    [t[n], t[r]] = [ t[r], t[n] ];
                }
                break;
            }

          case "SELECT":
            {
                const t = e;
                let n = 0;
                let r = 0;
                let i = 0;
                let s;
                for (let e = 0; e < t.length && i < 2; ++e) {
                    s = t[e];
                    switch (s.to) {
                      case "multiple":
                        r = e;
                        i++;
                        break;

                      case "value":
                        n = e;
                        i++;
                        break;
                    }
                    if (i === 2 && n < r) {
                        [t[r], t[n]] = [ t[n], t[r] ];
                    }
                }
            }
        }
    }
    Z(t, e) {
        insertBefore(t.parentNode, e.st(), t);
        return t;
    }
    nt(t, e) {
        if (isMarker(t)) {
            return t;
        }
        const n = t.parentNode;
        const r = e.st();
        insertManyBefore(n, t, [ r, e.ot(Ct), e.ot(Pt) ]);
        n.removeChild(t);
        return r;
    }
    G(t, e, n, r, i, s, o) {
        const l = s.H(e);
        const a = e.noMultiBindings === false && i === null && hasInlineBindings(r);
        if (a) {
            return this.ut(t, r, e, s);
        }
        const u = l.primary;
        if (i === null) {
            const t = s.ep.parse(r, x);
            if (t === null) {
                if (o && r === "") {
                    return [];
                }
                return [ {
                    type: Y,
                    value: r,
                    to: u.name
                } ];
            }
            return [ {
                type: Z,
                from: t,
                to: u.name
            } ];
        }
        return [ i.build({
            node: t,
            attr: n,
            bindable: u,
            def: e
        }, s.ep, s.m) ];
    }
    it(t, e, n, r, i) {
        let s = t.firstChild;
        let o;
        let l;
        let a;
        let u;
        let c;
        while (s !== null) {
            l = isElement(s) ? s.getAttribute(Tt) : null;
            a = l !== null || e && !n;
            o = s.nextSibling;
            if (a) {
                if (!e) {
                    throw createMappedError(706, l, r);
                }
                s.removeAttribute?.(Tt);
                c = isTextNode(s) && s.textContent.trim() === "";
                if (!c) {
                    ((u ??= {})[l || Bt] ??= []).push(s);
                }
                t.removeChild(s);
            }
            s = o;
        }
        if (u == null) {
            return null;
        }
        const f = {};
        let m;
        let d;
        let p;
        let h;
        for (l in u) {
            m = i.t();
            d = u[l];
            for (let t = 0, e = d.length; e > t; ++t) {
                p = d[t];
                if (p.nodeName === vt) {
                    if (p.attributes.length > 0) {
                        appendToTemplate(m, p);
                    } else {
                        appendToTemplate(m, p.content);
                    }
                } else {
                    appendToTemplate(m, p);
                }
            }
            h = i.rt();
            this.O(m.content, h);
            f[l] = {
                name: St(),
                type: E,
                template: m,
                instructions: h.rows,
                needsCompile: false
            };
        }
        return f;
    }
}

TemplateCompiler.register = h(D);

const vt = "TEMPLATE";

const isMarker = t => t.nodeType === 8 && t.textContent === "au";

class CompilationContext {
    constructor(t, e, n, r, i) {
        this.hasSlot = false;
        this.deps = null;
        const s = n !== null;
        this.c = e;
        this.root = r === null ? this : r;
        this.def = t;
        this.parent = n;
        this.ft = s ? n.ft : e.get(_t);
        this.dt = s ? n.dt : e.get(Et);
        this.M = s ? n.M : e.get(At);
        this.F = s ? n.F : e.get(H);
        this.ep = s ? n.ep : e.get(T);
        this.m = s ? n.m : e.get($);
        this.ht = s ? n.ht : e.get(A);
        if (typeof (this.p = s ? n.p : e.get(p)).document?.nodeType !== "number") {
            throw createMappedError(722);
        }
        this.localEls = s ? n.localEls : new Set;
        this.rows = i ?? [];
    }
    ct(t) {
        (this.root.deps ??= []).push(t);
        this.root.c.register(t);
        return this;
    }
    lt(t) {
        return this.p.document.createTextNode(t);
    }
    ot(t) {
        return this.p.document.createComment(t);
    }
    st() {
        return this.ot("au");
    }
    h(t) {
        const e = this.p.document.createElement(t);
        if (t === "template") {
            this.p.document.adoptNode(e.content);
        }
        return e;
    }
    t() {
        return this.h("template");
    }
    N(t) {
        return this.ft.el(this.c, t);
    }
    q(t) {
        return this.ft.attr(this.c, t);
    }
    H(t) {
        return this.ft.bindables(t);
    }
    rt(t) {
        return new CompilationContext(this.def, this.c, this, this.root, t);
    }
    U(t) {
        const e = t.command;
        if (e === null) {
            return null;
        }
        return this.dt.get(this.c, e);
    }
}

const hasInlineBindings = t => {
    const e = t.length;
    let n = 0;
    let r = 0;
    while (e > r) {
        n = t.charCodeAt(r);
        if (n === 92) {
            ++r;
        } else if (n === 58) {
            return true;
        } else if (n === 36 && t.charCodeAt(r + 1) === 123) {
            return false;
        }
        ++r;
    }
    return false;
};

const Rt = {
    name: "unnamed",
    type: E
};

const xt = {
    id: true,
    name: true,
    "au-slot": true,
    "as-element": true
};

const It = {
    checkbox: 1,
    radio: 1
};

const _t = /*@__PURE__*/ B("IResourceResolver");

const Et = /*@__PURE__*/ B("IBindingCommandResolver", t => {
    class DefaultBindingCommandResolver {
        constructor() {
            this.R = new WeakMap;
        }
        get(t, e) {
            let n = this.R.get(t);
            if (!n) {
                this.R.set(t, n = {});
            }
            return e in n ? n[e] : n[e] = bt.get(t, e);
        }
    }
    return t.singleton(DefaultBindingCommandResolver);
});

const kt = S([ "name", "attribute", "mode" ]);

const Dt = "as-custom-element";

const processTemplateName = (t, e, n) => {
    const r = e.getAttribute(Dt);
    if (r === null || r === "") {
        throw createMappedError(715, t);
    }
    if (n.has(r)) {
        throw createMappedError(716, r, t);
    } else {
        n.add(r);
        e.removeAttribute(Dt);
    }
    return r;
};

const $t = /*@__PURE__*/ B("ITemplateCompilerHooks");

const Lt = S({
    name: /*@__PURE__*/ n("compiler-hooks"),
    define(t) {
        return {
            register(e) {
                R($t, t).register(e);
            }
        };
    },
    findAll(t) {
        return t.get(w($t));
    }
});

const templateCompilerHooks = (t, e) => {
    return t === void 0 ? decorator : decorator(t, e);
    function decorator(t, e) {
        e.metadata[r] = Lt.define(t);
        return t;
    }
};

export { AtPrefixedTriggerAttributePattern, AttrBindingCommand, AttrSyntax, AttributeParser, q as AttributePattern, bt as BindingCommand, BindingCommandDefinition, k as BindingMode, CaptureBindingCommand, ClassBindingCommand, ColonPrefixedBindAttributePattern, DefaultBindingCommand, DotSeparatedAttributePattern, EventAttributePattern, ForBindingCommand, FromViewBindingCommand, $ as IAttrMapper, H as IAttributeParser, O as IAttributePattern, Et as IBindingCommandResolver, ht as IInstruction, _t as IResourceResolver, U as ISyntaxInterpreter, D as ITemplateCompiler, $t as ITemplateCompilerHooks, At as ITemplateElementFactory, Interpretation, OneTimeBindingCommand, RefAttributePattern, RefBindingCommand, SpreadValueBindingCommand, StyleBindingCommand, SyntaxInterpreter, TemplateCompiler, Lt as TemplateCompilerHooks, ToViewBindingCommand, TriggerBindingCommand, TwoWayBindingCommand, attributePattern, bindingCommand, St as generateElementName, isInstruction, lt as itAttributeBinding, K as itHydrateAttribute, J as itHydrateElement, X as itHydrateLetElement, Q as itHydrateTemplateController, Z as itInterpolation, rt as itIteratorBinding, et as itLetBinding, ot as itListenerBinding, it as itMultiAttr, tt as itPropertyBinding, nt as itRefBinding, ut as itSetAttribute, ct as itSetClassAttribute, Y as itSetProperty, ft as itSetStyleAttribute, dt as itSpreadElementProp, mt as itSpreadTransferedBinding, pt as itSpreadValueBinding, at as itStylePropertyBinding, st as itTextBinding, templateCompilerHooks };
//# sourceMappingURL=index.mjs.map
