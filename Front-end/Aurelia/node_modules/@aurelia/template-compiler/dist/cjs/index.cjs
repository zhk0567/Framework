"use strict";

var t = require("@aurelia/kernel");

var e = require("@aurelia/metadata");

var n = require("@aurelia/expression-parser");

const r = t.DI.createInterface;

const s = Object.freeze;

const {aliasTo: o, singleton: i} = t.Registration;

const l = "Interpolation";

const a = "IsFunction";

const u = "IsProperty";

const c = "custom-element";

const f = /*@__PURE__*/ s({
    default: 0,
    oneTime: 1,
    toView: 2,
    fromView: 4,
    twoWay: 6
});

const m = /*@__PURE__*/ r("ITemplateCompiler");

const p = /*@__PURE__*/ r("IAttrMapper");

const createMappedError = (t, ...e) => {
    const n = String(t).padStart(4, "0");
    return new Error(`AUR${n}:${e.map(String)}`);
};

var d, h, g, y, b;

class AttrSyntax {
    constructor(t, e, n, r, s = null) {
        this.rawName = t;
        this.rawValue = e;
        this.target = n;
        this.command = r;
        this.parts = s;
    }
}

const A = /*@__PURE__*/ r("IAttributePattern");

const x = 0;

const w = 1;

function createSymbolSet(t) {
    const e = new Set;
    for (let n = 0; n < t.length; n++) {
        e.add(t[n]);
    }
    return e;
}

function compilePattern(t, e) {
    const n = [];
    const r = createSymbolSet(e);
    let s = 0;
    let o = 0;
    let i = 0;
    let l = 0;
    while (l < t.length) {
        if (t.startsWith("PART", l)) {
            n.push({
                kind: x
            });
            o++;
            l += 4;
            continue;
        }
        const e = l;
        while (l < t.length && !t.startsWith("PART", l)) l++;
        const a = t.slice(e, l);
        let u = 0;
        while (u < a.length) {
            const t = r.has(a[u]);
            let e = u + 1;
            while (e < a.length && r.has(a[e]) === t) e++;
            n.push({
                kind: w,
                value: a.slice(u, e)
            });
            if (t) i++; else s++;
            u = e;
        }
    }
    return {
        tokens: n,
        score: {
            statics: s,
            dynamics: o,
            symbols: i
        }
    };
}

class CompiledPattern {
    constructor(t) {
        this.def = t;
        const {tokens: e, score: n} = compilePattern(t.pattern, t.symbols);
        this.i = e;
        this.u = n;
        this.A = createSymbolSet(t.symbols);
    }
    C(t) {
        const e = [];
        const n = this.i;
        const r = this.A;
        let s = 0;
        let o = "";
        for (let i = 0; i < n.length; i++) {
            const l = n[i];
            if (l.kind === w) {
                const {value: n} = l;
                if (!t.startsWith(n, s)) return null;
                for (let t = 0; t < n.length; t++) {
                    const s = n[t];
                    if (r.has(s)) {
                        if (o.length > 0) {
                            e.push(o);
                            o = "";
                        }
                    } else {
                        o += s;
                    }
                }
                s += n.length;
            } else {
                const e = s;
                while (s < t.length && !r.has(t[s])) s++;
                if (s === e) return null;
                o += t.slice(e, s);
            }
        }
        if (o.length > 0) e.push(o);
        return s === t.length ? e : null;
    }
}

function isBetterScore(t, e) {
    if (t.statics !== e.statics) return t.statics > e.statics;
    if (t.dynamics !== e.dynamics) return t.dynamics > e.dynamics;
    return t.symbols > e.symbols;
}

class Interpretation {
    constructor() {
        this.parts = t.emptyArray;
        this.P = null;
    }
    get pattern() {
        return this.P;
    }
    set pattern(t) {
        this.P = t;
    }
    T(t, e) {
        this.P = t;
        this.parts = e;
    }
}

const C = /*@__PURE__*/ r("ISyntaxInterpreter", t => t.singleton(SyntaxInterpreter));

class SyntaxInterpreter {
    constructor() {
        this.B = [];
        this.R = new Map;
    }
    add(t) {
        for (const e of t) {
            this.B.push(new CompiledPattern(e));
        }
    }
    interpret(e) {
        const n = new Interpretation;
        const r = this.R.get(e);
        if (r !== void 0) {
            n.T(r.pattern, r.parts);
            return n;
        }
        let s = null;
        let o = null;
        for (let t = 0; t < this.B.length; t++) {
            const n = this.B[t];
            const r = n.C(e);
            if (r !== null) {
                if (s === null || isBetterScore(n.u, s.u)) {
                    s = n;
                    o = r;
                }
            }
        }
        if (s !== null) {
            const t = {
                pattern: s.def.pattern,
                parts: o
            };
            this.R.set(e, t);
            n.T(t.pattern, t.parts);
        } else {
            this.R.set(e, {
                pattern: null,
                parts: t.emptyArray
            });
        }
        return n;
    }
}

const P = /*@__PURE__*/ r("IAttributeParser", t => t.singleton(AttributeParser));

class AttributeParser {
    constructor() {
        this.I = t.resolve(C);
        this.c = t.resolve(t.IContainer);
        this._ = {};
        this.$ = [];
        this.L = false;
    }
    registerPattern(t, e) {
        if (this.L) throw createMappedError(88);
        const n = this._;
        for (const r of t) {
            if (n[r.pattern] != null) throw createMappedError(89, r.pattern);
            n[r.pattern] = {
                type: e
            };
        }
        this.$.push(...t);
    }
    parse(t, e) {
        if (!this.L) {
            this.I.add(this.$);
            this.L = true;
        }
        const n = this.I.interpret(t);
        const r = n.pattern;
        if (r === null) {
            return new AttrSyntax(t, e, t, null, null);
        }
        const s = this._[r];
        if (s.instance === void 0) {
            s.instance = this.c.get(s.type);
        }
        return s.instance[r](t, e, n.parts);
    }
}

function attributePattern(...e) {
    return function decorator(n, r) {
        r.metadata[t.registrableMetadataKey] = T.create(e, n);
        return n;
    };
}

const T = /*@__PURE__*/ s({
    name: t.getResourceKeyFor("attribute-pattern"),
    create(t, e) {
        return {
            register(n) {
                n.get(P).registerPattern(t, e);
                i(A, e).register(n);
            }
        };
    }
});

class DotSeparatedAttributePattern {
    "PART.PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], n[1]);
    }
    "PART.PART.PART"(t, e, n) {
        return new AttrSyntax(t, e, `${n[0]}.${n[1]}`, n[2]);
    }
}

d = Symbol.metadata;

DotSeparatedAttributePattern[d] = {
    [t.registrableMetadataKey]: /*@__PURE__*/ T.create([ {
        pattern: "PART.PART",
        symbols: "."
    }, {
        pattern: "PART.PART.PART",
        symbols: "."
    } ], DotSeparatedAttributePattern)
};

class RefAttributePattern {
    ref(t, e, n) {
        return new AttrSyntax(t, e, "element", "ref");
    }
    "PART.ref"(t, e, n) {
        let r = n[0];
        if (r === "view-model") {
            r = "component";
        }
        return new AttrSyntax(t, e, r, "ref");
    }
}

h = Symbol.metadata;

RefAttributePattern[h] = {
    [t.registrableMetadataKey]: /*@__PURE__*/ T.create([ {
        pattern: "ref",
        symbols: ""
    }, {
        pattern: "PART.ref",
        symbols: "."
    } ], RefAttributePattern)
};

class EventAttributePattern {
    "PART.trigger:PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "trigger", n);
    }
    "PART.capture:PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "capture", n);
    }
}

g = Symbol.metadata;

EventAttributePattern[g] = {
    [t.registrableMetadataKey]: /*@__PURE__*/ T.create([ {
        pattern: "PART.trigger:PART",
        symbols: ".:"
    }, {
        pattern: "PART.capture:PART",
        symbols: ".:"
    } ], EventAttributePattern)
};

class ColonPrefixedBindAttributePattern {
    ":PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "bind");
    }
}

y = Symbol.metadata;

ColonPrefixedBindAttributePattern[y] = {
    [t.registrableMetadataKey]: /*@__PURE__*/ T.create([ {
        pattern: ":PART",
        symbols: ":"
    } ], ColonPrefixedBindAttributePattern)
};

class AtPrefixedTriggerAttributePattern {
    "@PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "trigger");
    }
    "@PART:PART"(t, e, n) {
        return new AttrSyntax(t, e, n[0], "trigger", [ n[0], "trigger", ...n.slice(1) ]);
    }
}

b = Symbol.metadata;

AtPrefixedTriggerAttributePattern[b] = {
    [t.registrableMetadataKey]: /*@__PURE__*/ T.create([ {
        pattern: "@PART",
        symbols: "@"
    }, {
        pattern: "@PART:PART",
        symbols: "@:"
    } ], AtPrefixedTriggerAttributePattern)
};

const B = e.Metadata.get;

e.Metadata.has;

const S = e.Metadata.define;

const v = 0;

const R = 1;

const I = 2;

const _ = 3;

const E = 10;

const k = 11;

const D = 12;

const $ = 13;

const L = 14;

const F = 15;

const V = 16;

const M = 30;

const W = 31;

const O = 32;

const j = 33;

const q = 34;

const N = 35;

const U = 36;

const H = 50;

const z = 51;

const G = 52;

const J = /*@__PURE__*/ r("Instruction");

function isInstruction(t) {
    const e = t.type;
    return typeof e === "number" && e >= 0 && e < 100;
}

function bindingCommand(t) {
    return function(e, n) {
        n.addInitializer(function() {
            X.define(t, e);
        });
        return e;
    };
}

class BindingCommandDefinition {
    constructor(t, e, n, r) {
        this.Type = t;
        this.name = e;
        this.aliases = n;
        this.key = r;
    }
    static create(e, n) {
        let r;
        let s;
        if (t.isString(e)) {
            r = e;
            s = {
                name: r
            };
        } else {
            r = e.name;
            s = e;
        }
        return new BindingCommandDefinition(n, t.firstDefined(getCommandAnnotation(n, "name"), r), t.mergeArrays(getCommandAnnotation(n, "aliases"), s.aliases, n.aliases), getCommandKeyFrom(r));
    }
    register(t, e) {
        const n = this.Type;
        const r = typeof e === "string" ? getCommandKeyFrom(e) : this.key;
        const s = this.aliases;
        if (!t.has(r, false)) {
            t.register(t.has(n, false) ? null : i(n, n), o(n, r), ...s.map(t => o(n, getCommandKeyFrom(t))));
        }
    }
}

const K = "binding-command";

const Q = /*@__PURE__*/ t.getResourceKeyFor(K);

const getCommandKeyFrom = t => `${Q}:${t}`;

const getCommandAnnotation = (e, n) => B(t.Protocol.annotation.keyFor(n), e);

const X = /*@__PURE__*/ (() => {
    const e = "__au_static_resource__";
    const getDefinitionFromStaticAu = (t, n, r) => {
        let s = B(e, t);
        if (s == null) {
            if (t.$au?.type === n) {
                s = r(t.$au, t);
                S(s, t, e);
            }
        }
        return s;
    };
    return s({
        name: Q,
        keyFrom: getCommandKeyFrom,
        define(e, n) {
            const r = BindingCommandDefinition.create(e, n);
            const s = r.Type;
            S(r, s, Q, t.resourceBaseName);
            return s;
        },
        getAnnotation: getCommandAnnotation,
        find(t, e) {
            const n = t.find(K, e);
            return n == null ? null : B(Q, n) ?? getDefinitionFromStaticAu(n, K, BindingCommandDefinition.create) ?? null;
        },
        get(e, n) {
            return e.get(t.resource(getCommandKeyFrom(n)));
        }
    });
})();

class OneTimeBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(e, n, r) {
        const s = e.attr;
        let o = s.target;
        let i = e.attr.rawValue;
        i = i === "" ? t.camelCase(o) : i;
        if (e.bindable == null) {
            o = r.map(e.node, o) ?? t.camelCase(o);
        } else {
            o = e.bindable.name;
        }
        return {
            type: D,
            from: n.parse(i, u),
            to: o,
            mode: 1
        };
    }
}

OneTimeBindingCommand.$au = {
    type: K,
    name: "one-time"
};

class ToViewBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(e, n, r) {
        const s = e.attr;
        let o = s.target;
        let i = e.attr.rawValue;
        i = i === "" ? t.camelCase(o) : i;
        if (e.bindable == null) {
            o = r.map(e.node, o) ?? t.camelCase(o);
        } else {
            o = e.bindable.name;
        }
        return {
            type: D,
            from: n.parse(i, u),
            to: o,
            mode: 2
        };
    }
}

ToViewBindingCommand.$au = {
    type: K,
    name: "to-view"
};

class FromViewBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(e, n, r) {
        const s = e.attr;
        let o = s.target;
        let i = s.rawValue;
        i = i === "" ? t.camelCase(o) : i;
        if (e.bindable == null) {
            o = r.map(e.node, o) ?? t.camelCase(o);
        } else {
            o = e.bindable.name;
        }
        return {
            type: D,
            from: n.parse(i, u),
            to: o,
            mode: 4
        };
    }
}

FromViewBindingCommand.$au = {
    type: K,
    name: "from-view"
};

class TwoWayBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(e, n, r) {
        const s = e.attr;
        let o = s.target;
        let i = s.rawValue;
        i = i === "" ? t.camelCase(o) : i;
        if (e.bindable == null) {
            o = r.map(e.node, o) ?? t.camelCase(o);
        } else {
            o = e.bindable.name;
        }
        return {
            type: D,
            from: n.parse(i, u),
            to: o,
            mode: 6
        };
    }
}

TwoWayBindingCommand.$au = {
    type: K,
    name: "two-way"
};

class DefaultBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(e, n, r) {
        const s = e.attr;
        const o = e.bindable;
        let i = s.rawValue;
        let l = s.target;
        let a;
        i = i === "" ? t.camelCase(l) : i;
        if (o == null) {
            a = r.isTwoWay(e.node, l) ? 6 : 2;
            l = r.map(e.node, l) ?? t.camelCase(l);
        } else {
            a = o.mode === 0 || o.mode == null ? 2 : o.mode;
            l = o.name;
        }
        return {
            type: D,
            from: n.parse(i, u),
            to: l,
            mode: t.isString(a) ? f[a] ?? 0 : a
        };
    }
}

DefaultBindingCommand.$au = {
    type: K,
    name: "bind"
};

class ForBindingCommand {
    constructor() {
        this.F = t.resolve(P);
    }
    get ignoreAttr() {
        return false;
    }
    build(e, n) {
        const r = e.bindable === null ? t.camelCase(e.attr.target) : e.bindable.name;
        const s = n.parse(e.attr.rawValue, "IsIterator");
        let o = t.emptyArray;
        if (s.semiIdx > -1) {
            const t = e.attr.rawValue.slice(s.semiIdx + 1);
            const n = t.split(";");
            const r = [];
            for (let t = 0, e = n.length; t < e; t++) {
                const e = n[t];
                const s = e.indexOf(":");
                if (s > -1) {
                    const t = e.slice(0, s).trim();
                    const n = e.slice(s + 1).trim();
                    const o = this.F.parse(t, n);
                    r.push({
                        type: V,
                        value: n,
                        to: o.target,
                        command: o.command
                    });
                }
            }
            o = r;
        }
        return {
            type: F,
            forOf: s,
            to: r,
            props: o
        };
    }
}

ForBindingCommand.$au = {
    type: K,
    name: "for"
};

class TriggerBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        return {
            type: W,
            from: e.parse(t.attr.rawValue, a),
            to: t.attr.target,
            capture: false,
            modifier: t.attr.parts?.[2] ?? null
        };
    }
}

TriggerBindingCommand.$au = {
    type: K,
    name: "trigger"
};

class CaptureBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        return {
            type: W,
            from: e.parse(t.attr.rawValue, a),
            to: t.attr.target,
            capture: true,
            modifier: t.attr.parts?.[2] ?? null
        };
    }
}

CaptureBindingCommand.$au = {
    type: K,
    name: "capture"
};

class AttrBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(e, n) {
        const r = e.attr;
        const s = r.target;
        let o = r.rawValue;
        o = o === "" ? t.camelCase(s) : o;
        return {
            type: O,
            attr: s,
            from: n.parse(o, u),
            to: s
        };
    }
}

AttrBindingCommand.$au = {
    type: K,
    name: "attr"
};

class StyleBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        return {
            type: O,
            attr: "style",
            from: e.parse(t.attr.rawValue, u),
            to: t.attr.target
        };
    }
}

StyleBindingCommand.$au = {
    type: K,
    name: "style"
};

class ClassBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        let n = t.attr.target;
        if (n.includes(",")) {
            const t = n.split(",").filter(t => t.length > 0);
            if (t.length === 0) {
                throw createMappedError(723);
            }
            n = t.join(" ");
        }
        return {
            type: O,
            attr: "class",
            from: e.parse(t.attr.rawValue, u),
            to: n
        };
    }
}

ClassBindingCommand.$au = {
    type: K,
    name: "class"
};

class RefBindingCommand {
    get ignoreAttr() {
        return true;
    }
    build(t, e) {
        return {
            type: L,
            from: e.parse(t.attr.rawValue, u),
            to: t.attr.target
        };
    }
}

RefBindingCommand.$au = {
    type: K,
    name: "ref"
};

class SpreadValueBindingCommand {
    get ignoreAttr() {
        return false;
    }
    build(t) {
        return {
            type: G,
            target: t.attr.target,
            from: t.attr.rawValue
        };
    }
}

SpreadValueBindingCommand.$au = {
    type: K,
    name: "spread"
};

typeof SuppressedError === "function" ? SuppressedError : function(t, e, n) {
    var r = new Error(n);
    return r.name = "SuppressedError", r.error = t, r.suppressed = e, r;
};

const Y = /*@__PURE__*/ r("ITemplateElementFactory", t => t.singleton(TemplateElementFactory));

const Z = {};

class TemplateElementFactory {
    constructor() {
        this.p = t.resolve(t.IPlatform);
        this.V = this.t();
    }
    t() {
        return this.p.document.createElement("template");
    }
    createTemplate(e) {
        if (t.isString(e)) {
            let t = Z[e];
            if (t === void 0) {
                const n = this.V;
                n.innerHTML = e;
                const r = n.content.firstElementChild;
                if (needsWrapping(r)) {
                    this.V = this.t();
                    t = n;
                } else {
                    n.content.removeChild(r);
                    t = r;
                }
                Z[e] = t;
            }
            return t.cloneNode(true);
        }
        if (e.nodeName !== "TEMPLATE") {
            const t = this.t();
            t.content.appendChild(e);
            return t;
        }
        e.parentNode?.removeChild(e);
        return e.cloneNode(true);
        function needsWrapping(t) {
            if (t == null) return true;
            if (t.nodeName !== "TEMPLATE") return true;
            const e = t.nextElementSibling;
            if (e != null) return true;
            const n = t.previousSibling;
            if (n != null) {
                switch (n.nodeType) {
                  case 3:
                    return n.textContent.trim().length > 0;
                }
            }
            const r = t.nextSibling;
            if (r != null) {
                switch (r.nodeType) {
                  case 3:
                    return r.textContent.trim().length > 0;
                }
            }
            return false;
        }
    }
}

const tt = "au-start";

const et = "au-end";

const insertBefore = (t, e, n) => t.insertBefore(e, n);

const insertManyBefore = (t, e, n) => {
    if (t === null) {
        return;
    }
    const r = n.length;
    let s = 0;
    while (r > s) {
        t.insertBefore(n[s], e);
        ++s;
    }
};

const appendToTemplate = (t, e) => t.content.appendChild(e);

const appendManyToTemplate = (t, e) => {
    const n = e.length;
    let r = 0;
    while (n > r) {
        t.content.appendChild(e[r]);
        ++r;
    }
};

const isElement = t => t.nodeType === 1;

const isTextNode = t => t.nodeType === 3;

const nt = "au-slot";

const rt = "default";

const st = (t => () => `anonymous-${++t}`)(0);

class TemplateCompiler {
    constructor() {
        this.debug = false;
        this.resolveResources = true;
    }
    compile(e, n) {
        if (e.template == null || e.needsCompile === false) {
            return e;
        }
        const r = new CompilationContext(e, n, null, null, void 0);
        const s = t.isString(e.template) || !e.enhance ? r.M.createTemplate(e.template) : e.template;
        const o = s.nodeName === ot && s.content != null;
        const i = o ? s.content : s;
        const l = dt.findAll(n);
        const a = l.length;
        let u = 0;
        if (a > 0) {
            while (a > u) {
                l[u].compiling?.(s);
                ++u;
            }
        }
        if (s.hasAttribute(mt)) {
            throw createMappedError(701, e);
        }
        this.W(i, r);
        this.O(i, r);
        const c = {
            ...e,
            name: e.name || st(),
            dependencies: (e.dependencies ?? t.emptyArray).concat(r.deps ?? t.emptyArray),
            instructions: r.rows,
            surrogates: o ? this.j(s, r) : t.emptyArray,
            template: s,
            hasSlots: r.hasSlot,
            needsCompile: false
        };
        return c;
    }
    compileSpread(e, n, r, s, o) {
        const i = new CompilationContext(e, r, null, null, void 0);
        const a = [];
        const u = o ?? i.q(s.nodeName.toLowerCase());
        const c = u !== null;
        const f = i.ep;
        const m = n.length;
        let p = 0;
        let d;
        let h = null;
        let g;
        let y;
        let b;
        let A;
        let x = null;
        let w;
        let C;
        let P;
        for (;m > p; ++p) {
            d = n[p];
            C = d.target;
            P = d.rawValue;
            if (C === "...$attrs") {
                a.push({
                    type: H
                });
                continue;
            }
            x = i.N(d);
            if (x !== null && x.ignoreAttr) {
                a.push(x.build({
                    node: s,
                    attr: d,
                    bindable: null,
                    def: null
                }, i.ep, i.m));
                continue;
            }
            if (c) {
                b = i.U(u);
                A = b.attrs[C];
                if (A !== void 0) {
                    if (x == null) {
                        w = f.parse(P, l);
                        a.push({
                            type: z,
                            instruction: w == null ? {
                                type: E,
                                value: P,
                                to: A.name
                            } : {
                                type: k,
                                from: w,
                                to: A.name
                            }
                        });
                    } else {
                        a.push({
                            type: z,
                            instruction: x.build({
                                node: s,
                                attr: d,
                                bindable: A,
                                def: u
                            }, i.ep, i.m)
                        });
                    }
                    continue;
                }
            }
            h = i.H(C);
            if (h !== null) {
                if (h.isTemplateController) {
                    throw createMappedError(9998, C);
                }
                y = this.G(s, h, d, P, x, i, false);
                (g ??= []).push({
                    type: R,
                    res: this.resolveResources ? h : h.name,
                    alias: h.aliases != null && h.aliases.includes(C) ? C : void 0,
                    props: y
                });
                continue;
            }
            if (x == null) {
                w = f.parse(P, l);
                if (w != null) {
                    a.push({
                        type: k,
                        from: w,
                        to: i.m.map(s, C) ?? t.camelCase(C)
                    });
                } else {
                    switch (C) {
                      case "class":
                        a.push({
                            type: N,
                            value: P
                        });
                        break;

                      case "style":
                        a.push({
                            type: U,
                            value: P
                        });
                        break;

                      default:
                        a.push({
                            type: q,
                            value: P,
                            to: C
                        });
                    }
                }
            } else {
                a.push(x.build({
                    node: s,
                    attr: d,
                    bindable: null,
                    def: null
                }, i.ep, i.m));
            }
        }
        if (g != null) {
            return g.concat(a);
        }
        return a;
    }
    j(e, n) {
        const r = e.attributes;
        for (let t = 0, e = r.length; t < e; ++t) {
            const e = n.F.parse(r[t].name, r[t].value);
            if (lt[e.target]) {
                throw createMappedError(702, r[t].name);
            }
        }
        const {tcInstructions: s, attrInstructions: o, plainAttrInstructions: i} = this.J(e, null, t.emptyArray, n, true);
        if (s != null && s.length > 0) {
            const t = s[0].res;
            const e = typeof t === "string" ? t : t.name;
            throw createMappedError(703, e);
        }
        if (o != null) {
            return o.concat(i ?? t.emptyArray);
        }
        return i ?? t.emptyArray;
    }
    O(t, e) {
        switch (t.nodeType) {
          case 1:
            switch (t.nodeName) {
              case "LET":
                return this.K(t, e);

              default:
                return this.X(t, e);
            }

          case 3:
            return this.Y(t, e);

          case 11:
            {
                let n = t.firstChild;
                while (n !== null) {
                    n = this.O(n, e);
                }
                break;
            }
        }
        return t.nextSibling;
    }
    K(e, r) {
        const s = e.attributes;
        const o = s.length;
        const i = [];
        const a = r.ep;
        let c = false;
        let f = 0;
        let m;
        let p;
        let d;
        let h;
        let g;
        let y;
        let b;
        let A;
        for (;o > f; ++f) {
            m = s[f];
            d = m.name;
            h = m.value;
            if (d === "to-binding-context") {
                c = true;
                continue;
            }
            p = r.F.parse(d, h);
            y = p.target;
            b = p.rawValue;
            g = r.N(p);
            if (g !== null) {
                if (p.command === "bind") {
                    i.push({
                        type: $,
                        from: a.parse(b, u),
                        to: t.camelCase(y)
                    });
                } else {
                    throw createMappedError(704, p);
                }
                continue;
            }
            A = a.parse(b, l);
            i.push({
                type: $,
                from: A === null ? n.createPrimitiveLiteralExpression(b) : A,
                to: t.camelCase(y)
            });
        }
        r.rows.push([ {
            type: _,
            instructions: i,
            toBindingContext: c
        } ]);
        return this.Z(e, r).nextSibling;
    }
    X(e, n) {
        const r = e.nextSibling;
        const s = (e.getAttribute("as-element") ?? e.nodeName).toLowerCase();
        const o = n.q(s);
        const i = o !== null;
        const l = i && o.shadowOptions != null;
        const a = o?.capture ? [] : t.emptyArray;
        if (s === "slot") {
            if (n.root.def.shadowOptions == null) {
                throw createMappedError(717, n.root.def.name);
            }
            n.root.hasSlot = true;
        }
        let u = true;
        const f = {};
        if (i) {
            u = o.processContent?.call(o.Type, e, n.p, f);
        }
        const {tcInstructions: m, attrInstructions: p, elBindableInstructions: d, plainAttrInstructions: h, hasContainerless: g} = this.J(e, o, a, n);
        if (this.tt(e, h) && h != null && h.length > 1) {
            this.et(e, h);
        }
        let y;
        if (i) {
            y = {
                type: v,
                res: this.resolveResources ? o : o.name,
                props: d ?? t.emptyArray,
                projections: null,
                containerless: g,
                captures: a,
                data: f
            };
        }
        let b;
        let A = false;
        if (h != null || y != null || p != null) {
            b = t.emptyArray.concat(y ?? t.emptyArray, p ?? t.emptyArray, h ?? t.emptyArray);
            A = true;
        }
        if (m != null) {
            const t = m.length;
            let r = t - 1;
            let a = m[r];
            this.nt(e, n);
            let f;
            if (e.nodeName === ot) {
                f = e;
            } else {
                f = n.t();
                appendToTemplate(f, e);
            }
            const p = f;
            const d = n.rt(b == null ? [] : [ b ]);
            if (u !== false) {
                const t = this.st(e, i, l, s, n);
                if (t != null) {
                    y.projections = t;
                }
            }
            if (A) {
                if (i && (g || o.containerless)) {
                    this.nt(e, d);
                } else {
                    this.Z(e, d);
                }
            }
            const h = !i || !o.containerless && !g && u !== false;
            if (h) {
                if (e.nodeName === ot) {
                    this.O(e.content, d);
                } else {
                    let t = e.firstChild;
                    while (t !== null) {
                        t = this.O(t, d);
                    }
                }
            }
            a.def = {
                name: st(),
                type: c,
                template: p,
                instructions: d.rows,
                needsCompile: false
            };
            while (r-- > 0) {
                a = m[r];
                f = n.t();
                appendManyToTemplate(f, [ n.ot(), n.it(tt), n.it(et) ]);
                a.def = {
                    name: st(),
                    type: c,
                    template: f,
                    needsCompile: false,
                    instructions: [ [ m[r + 1] ] ]
                };
            }
            n.rows.push([ a ]);
        } else {
            if (b != null) {
                n.rows.push(b);
            }
            if (u !== false) {
                const t = this.st(e, i, l, s, n);
                if (t != null) {
                    y.projections = t;
                }
            }
            if (A) {
                if (i && (g || o.containerless)) {
                    this.nt(e, n);
                } else {
                    this.Z(e, n);
                }
            }
            const t = !i || !o.containerless && !g && u !== false;
            if (t && e.childNodes.length > 0) {
                let t = e.firstChild;
                while (t !== null) {
                    t = this.O(t, n);
                }
            }
        }
        return r;
    }
    J(e, n, r, s, o = false) {
        const i = n !== null;
        const a = n?.capture;
        const u = a != null && typeof a !== "boolean";
        const c = s.ep;
        const f = e.attributes;
        let m = f.length;
        let p = 0;
        let d;
        let h;
        let g;
        let y;
        let b = null;
        let A;
        let x;
        let w;
        let C;
        let P;
        let T;
        let B = null;
        let S;
        let v;
        let _;
        let D;
        let $ = false;
        let L = false;
        let F = 0;
        const V = this.debug ? t.noop : () => {
            e.removeAttribute(h);
            --p;
            --m;
        };
        for (;m > p; ++p) {
            d = f[p];
            h = d.name;
            g = d.value;
            switch (h) {
              case "as-element":
              case "containerless":
                V();
                $ = $ || h === "containerless";
                continue;
            }
            y = s.F.parse(h, g);
            b = s.N(y);
            A = y.target;
            x = y.rawValue;
            if (a && (!u || u && a(A))) {
                if (b != null && b.ignoreAttr) {
                    V();
                    r.push(y);
                    continue;
                }
                L = A !== nt && A !== "slot" && ((F = A.indexOf("...")) === -1 || F === 0 && A === "...$attrs");
                if (L) {
                    _ = s.U(n);
                    if (_.attrs[A] == null && !s.H(A)?.isTemplateController) {
                        V();
                        r.push(y);
                        continue;
                    }
                }
            }
            if (A === "...$attrs") {
                (T ??= []).push({
                    type: H
                });
                V();
                continue;
            }
            if (b?.ignoreAttr) {
                (T ??= []).push(b.build({
                    node: e,
                    attr: y,
                    bindable: null,
                    def: null
                }, s.ep, s.m));
                V();
                continue;
            }
            if (A.indexOf("...") === 0) {
                if (i && (A = A.slice(3)) !== "$element") {
                    (P ??= []).push({
                        type: G,
                        target: "$bindables",
                        from: A === "$bindables" ? x : A
                    });
                    V();
                    continue;
                }
                throw createMappedError(720, A);
            }
            if (i) {
                _ = s.U(n);
                S = _.attrs[A];
                if (S !== void 0) {
                    if (b === null) {
                        D = c.parse(x, l);
                        (P ??= []).push(D == null ? {
                            type: E,
                            value: x,
                            to: S.name
                        } : {
                            type: k,
                            from: D,
                            to: S.name
                        });
                    } else {
                        (P ??= []).push(b.build({
                            node: e,
                            attr: y,
                            bindable: S,
                            def: n
                        }, s.ep, s.m));
                    }
                    V();
                    continue;
                }
                if (A === "$bindables") {
                    if (b != null) {
                        const t = b.build({
                            node: e,
                            attr: y,
                            bindable: null,
                            def: null
                        }, s.ep, s.m);
                        (P ??= []).push(t);
                    }
                    V();
                    continue;
                }
            }
            if (A === "$bindables") {
                throw createMappedError(721, e.nodeName, A, x);
            }
            B = s.H(A);
            if (B !== null) {
                v = this.G(e, B, y, x, b, s, true);
                V();
                if (B.isTemplateController) {
                    (w ??= []).push({
                        type: I,
                        def: it,
                        res: this.resolveResources ? B : B.name,
                        alias: void 0,
                        props: v
                    });
                } else {
                    (C ??= []).push({
                        type: R,
                        res: this.resolveResources ? B : B.name,
                        alias: B.aliases != null && B.aliases.includes(A) ? A : void 0,
                        props: v
                    });
                }
                continue;
            }
            if (b === null) {
                D = c.parse(x, l);
                if (D != null) {
                    V();
                    (T ??= []).push({
                        type: k,
                        from: D,
                        to: s.m.map(e, A) ?? t.camelCase(A)
                    });
                } else if (o) {
                    switch (h) {
                      case "class":
                        (T ??= []).push({
                            type: N,
                            value: x
                        });
                        break;

                      case "style":
                        (T ??= []).push({
                            type: U,
                            value: x
                        });
                        break;

                      default:
                        (T ??= []).push({
                            type: q,
                            value: x,
                            to: h
                        });
                    }
                }
                continue;
            }
            (T ??= []).push(b.build({
                node: e,
                attr: y,
                bindable: null,
                def: null
            }, s.ep, s.m));
            V();
        }
        return {
            tcInstructions: w,
            attrInstructions: C,
            elBindableInstructions: P,
            plainAttrInstructions: T,
            hasContainerless: $
        };
    }
    Y(t, e) {
        const n = t.parentNode;
        const r = e.ep.parse(t.textContent, l);
        const s = t.nextSibling;
        let o;
        let i;
        let a;
        let u;
        let c;
        if (r !== null) {
            ({parts: o, expressions: i} = r);
            if (c = o[0]) {
                insertBefore(n, e.lt(c), t);
            }
            for (a = 0, u = i.length; u > a; ++a) {
                insertManyBefore(n, t, [ e.ot(), e.lt(" ") ]);
                if (c = o[a + 1]) {
                    insertBefore(n, e.lt(c), t);
                }
                e.rows.push([ {
                    type: M,
                    from: i[a]
                } ]);
            }
            n.removeChild(t);
        }
        return s;
    }
    ut(t, e, n, r) {
        const s = r.U(n);
        const o = e.length;
        const i = [];
        let a = void 0;
        let u = void 0;
        let c = 0;
        let f = 0;
        let m;
        let p;
        let d;
        let h;
        for (let g = 0; g < o; ++g) {
            f = e.charCodeAt(g);
            if (f === 92) {
                ++g;
            } else if (f === 58) {
                a = e.slice(c, g);
                while (e.charCodeAt(++g) <= 32) ;
                c = g;
                for (;g < o; ++g) {
                    f = e.charCodeAt(g);
                    if (f === 92) {
                        ++g;
                    } else if (f === 59) {
                        u = e.slice(c, g);
                        break;
                    }
                }
                if (u === void 0) {
                    u = e.slice(c);
                }
                p = r.F.parse(a, u);
                d = r.N(p);
                h = s.attrs[p.target];
                if (h == null) {
                    throw createMappedError(707, p.target, n.name);
                }
                if (d === null) {
                    m = r.ep.parse(u, l);
                    i.push(m === null ? {
                        type: E,
                        value: u,
                        to: h.name
                    } : {
                        type: k,
                        from: m,
                        to: h.name
                    });
                } else {
                    i.push(d.build({
                        node: t,
                        attr: p,
                        bindable: h,
                        def: n
                    }, r.ep, r.m));
                }
                while (g < o && e.charCodeAt(++g) <= 32) ;
                c = g;
                a = void 0;
                u = void 0;
            }
        }
        return i;
    }
    W(e, n) {
        const r = n.root.def.name;
        const s = e;
        const o = t.toArray(s.querySelectorAll("template[as-custom-element]"));
        const i = o.length;
        if (i === 0) {
            return;
        }
        if (i === s.childElementCount) {
            throw createMappedError(708, r);
        }
        const l = new Set;
        const a = [];
        for (const e of o) {
            if (e.parentNode !== s) {
                throw createMappedError(709, r);
            }
            const n = processTemplateName(r, e, l);
            const o = e.content;
            const i = t.toArray(o.querySelectorAll("bindable"));
            const u = new Set;
            const f = new Set;
            const m = i.reduce((e, r) => {
                if (r.parentNode !== o) {
                    throw createMappedError(710, n);
                }
                const s = r.getAttribute("name");
                if (s === null) {
                    throw createMappedError(711, r, n);
                }
                const i = r.getAttribute("attribute");
                if (i !== null && f.has(i) || u.has(s)) {
                    throw createMappedError(712, u, i);
                } else {
                    if (i !== null) {
                        f.add(i);
                    }
                    u.add(s);
                }
                const l = t.toArray(r.attributes).filter(t => !ft.includes(t.name));
                if (l.length > 0) ;
                r.remove();
                e[s] = {
                    name: s,
                    attribute: i ?? void 0,
                    mode: r.getAttribute("mode") ?? "default"
                };
                return e;
            }, {});
            class LocalDepType {}
            LocalDepType.$au = {
                type: c,
                name: n,
                template: e,
                bindables: m
            };
            Reflect.defineProperty(LocalDepType, "name", {
                value: t.pascalCase(n)
            });
            a.push(LocalDepType);
            s.removeChild(e);
        }
        const u = (n.root.def.dependencies ?? []).concat(n.root.def.Type == null ? t.emptyArray : [ n.root.def.Type ]);
        for (const t of a) {
            t.dependencies = u.concat(a.filter(e => e !== t));
            n.ct(t);
        }
    }
    tt(t, e) {
        const n = t.nodeName;
        return n === "INPUT" && at[t.type] === 1 || n === "SELECT" && (t.hasAttribute("multiple") || e?.some(t => t.type === D && t.to === "multiple"));
    }
    et(t, e) {
        switch (t.nodeName) {
          case "INPUT":
            {
                const t = e;
                let n = void 0;
                let r = void 0;
                let s = 0;
                let o;
                for (let e = 0; e < t.length && s < 3; e++) {
                    o = t[e];
                    switch (o.to) {
                      case "model":
                      case "value":
                      case "matcher":
                        n = e;
                        s++;
                        break;

                      case "checked":
                        r = e;
                        s++;
                        break;
                    }
                }
                if (r !== void 0 && n !== void 0 && r < n) {
                    [t[n], t[r]] = [ t[r], t[n] ];
                }
                break;
            }

          case "SELECT":
            {
                const t = e;
                let n = 0;
                let r = 0;
                let s = 0;
                let o;
                for (let e = 0; e < t.length && s < 2; ++e) {
                    o = t[e];
                    switch (o.to) {
                      case "multiple":
                        r = e;
                        s++;
                        break;

                      case "value":
                        n = e;
                        s++;
                        break;
                    }
                    if (s === 2 && n < r) {
                        [t[r], t[n]] = [ t[n], t[r] ];
                    }
                }
            }
        }
    }
    Z(t, e) {
        insertBefore(t.parentNode, e.ot(), t);
        return t;
    }
    nt(t, e) {
        if (isMarker(t)) {
            return t;
        }
        const n = t.parentNode;
        const r = e.ot();
        insertManyBefore(n, t, [ r, e.it(tt), e.it(et) ]);
        n.removeChild(t);
        return r;
    }
    G(t, e, n, r, s, o, i) {
        const a = o.U(e);
        const u = e.noMultiBindings === false && s === null && hasInlineBindings(r);
        if (u) {
            return this.ut(t, r, e, o);
        }
        const c = a.primary;
        if (s === null) {
            const t = o.ep.parse(r, l);
            if (t === null) {
                if (i && r === "") {
                    return [];
                }
                return [ {
                    type: E,
                    value: r,
                    to: c.name
                } ];
            }
            return [ {
                type: k,
                from: t,
                to: c.name
            } ];
        }
        return [ s.build({
            node: t,
            attr: n,
            bindable: c,
            def: e
        }, o.ep, o.m) ];
    }
    st(t, e, n, r, s) {
        let o = t.firstChild;
        let i;
        let l;
        let a;
        let u;
        let f;
        while (o !== null) {
            l = isElement(o) ? o.getAttribute(nt) : null;
            a = l !== null || e && !n;
            i = o.nextSibling;
            if (a) {
                if (!e) {
                    throw createMappedError(706, l, r);
                }
                o.removeAttribute?.(nt);
                f = isTextNode(o) && o.textContent.trim() === "";
                if (!f) {
                    ((u ??= {})[l || rt] ??= []).push(o);
                }
                t.removeChild(o);
            }
            o = i;
        }
        if (u == null) {
            return null;
        }
        const m = {};
        let p;
        let d;
        let h;
        let g;
        for (l in u) {
            p = s.t();
            d = u[l];
            for (let t = 0, e = d.length; e > t; ++t) {
                h = d[t];
                if (h.nodeName === ot) {
                    if (h.attributes.length > 0) {
                        appendToTemplate(p, h);
                    } else {
                        appendToTemplate(p, h.content);
                    }
                } else {
                    appendToTemplate(p, h);
                }
            }
            g = s.rt();
            this.O(p.content, g);
            m[l] = {
                name: st(),
                type: c,
                template: p,
                instructions: g.rows,
                needsCompile: false
            };
        }
        return m;
    }
}

TemplateCompiler.register = t.createImplementationRegister(m);

const ot = "TEMPLATE";

const isMarker = t => t.nodeType === 8 && t.textContent === "au";

class CompilationContext {
    constructor(e, r, s, o, i) {
        this.hasSlot = false;
        this.deps = null;
        const l = s !== null;
        this.c = r;
        this.root = o === null ? this : o;
        this.def = e;
        this.parent = s;
        this.ft = l ? s.ft : r.get(ut);
        this.dt = l ? s.dt : r.get(ct);
        this.M = l ? s.M : r.get(Y);
        this.F = l ? s.F : r.get(P);
        this.ep = l ? s.ep : r.get(n.IExpressionParser);
        this.m = l ? s.m : r.get(p);
        this.ht = l ? s.ht : r.get(t.ILogger);
        if (typeof (this.p = l ? s.p : r.get(t.IPlatform)).document?.nodeType !== "number") {
            throw createMappedError(722);
        }
        this.localEls = l ? s.localEls : new Set;
        this.rows = i ?? [];
    }
    ct(t) {
        (this.root.deps ??= []).push(t);
        this.root.c.register(t);
        return this;
    }
    lt(t) {
        return this.p.document.createTextNode(t);
    }
    it(t) {
        return this.p.document.createComment(t);
    }
    ot() {
        return this.it("au");
    }
    h(t) {
        const e = this.p.document.createElement(t);
        if (t === "template") {
            this.p.document.adoptNode(e.content);
        }
        return e;
    }
    t() {
        return this.h("template");
    }
    q(t) {
        return this.ft.el(this.c, t);
    }
    H(t) {
        return this.ft.attr(this.c, t);
    }
    U(t) {
        return this.ft.bindables(t);
    }
    rt(t) {
        return new CompilationContext(this.def, this.c, this, this.root, t);
    }
    N(t) {
        const e = t.command;
        if (e === null) {
            return null;
        }
        return this.dt.get(this.c, e);
    }
}

const hasInlineBindings = t => {
    const e = t.length;
    let n = 0;
    let r = 0;
    while (e > r) {
        n = t.charCodeAt(r);
        if (n === 92) {
            ++r;
        } else if (n === 58) {
            return true;
        } else if (n === 36 && t.charCodeAt(r + 1) === 123) {
            return false;
        }
        ++r;
    }
    return false;
};

const it = {
    name: "unnamed",
    type: c
};

const lt = {
    id: true,
    name: true,
    "au-slot": true,
    "as-element": true
};

const at = {
    checkbox: 1,
    radio: 1
};

const ut = /*@__PURE__*/ r("IResourceResolver");

const ct = /*@__PURE__*/ r("IBindingCommandResolver", t => {
    class DefaultBindingCommandResolver {
        constructor() {
            this.R = new WeakMap;
        }
        get(t, e) {
            let n = this.R.get(t);
            if (!n) {
                this.R.set(t, n = {});
            }
            return e in n ? n[e] : n[e] = X.get(t, e);
        }
    }
    return t.singleton(DefaultBindingCommandResolver);
});

const ft = s([ "name", "attribute", "mode" ]);

const mt = "as-custom-element";

const processTemplateName = (t, e, n) => {
    const r = e.getAttribute(mt);
    if (r === null || r === "") {
        throw createMappedError(715, t);
    }
    if (n.has(r)) {
        throw createMappedError(716, r, t);
    } else {
        n.add(r);
        e.removeAttribute(mt);
    }
    return r;
};

const pt = /*@__PURE__*/ r("ITemplateCompilerHooks");

const dt = s({
    name: /*@__PURE__*/ t.getResourceKeyFor("compiler-hooks"),
    define(t) {
        return {
            register(e) {
                i(pt, t).register(e);
            }
        };
    },
    findAll(e) {
        return e.get(t.allResources(pt));
    }
});

const templateCompilerHooks = (e, n) => {
    return e === void 0 ? decorator : decorator(e, n);
    function decorator(e, n) {
        n.metadata[t.registrableMetadataKey] = dt.define(e);
        return e;
    }
};

exports.AtPrefixedTriggerAttributePattern = AtPrefixedTriggerAttributePattern;

exports.AttrBindingCommand = AttrBindingCommand;

exports.AttrSyntax = AttrSyntax;

exports.AttributeParser = AttributeParser;

exports.AttributePattern = T;

exports.BindingCommand = X;

exports.BindingCommandDefinition = BindingCommandDefinition;

exports.BindingMode = f;

exports.CaptureBindingCommand = CaptureBindingCommand;

exports.ClassBindingCommand = ClassBindingCommand;

exports.ColonPrefixedBindAttributePattern = ColonPrefixedBindAttributePattern;

exports.DefaultBindingCommand = DefaultBindingCommand;

exports.DotSeparatedAttributePattern = DotSeparatedAttributePattern;

exports.EventAttributePattern = EventAttributePattern;

exports.ForBindingCommand = ForBindingCommand;

exports.FromViewBindingCommand = FromViewBindingCommand;

exports.IAttrMapper = p;

exports.IAttributeParser = P;

exports.IAttributePattern = A;

exports.IBindingCommandResolver = ct;

exports.IInstruction = J;

exports.IResourceResolver = ut;

exports.ISyntaxInterpreter = C;

exports.ITemplateCompiler = m;

exports.ITemplateCompilerHooks = pt;

exports.ITemplateElementFactory = Y;

exports.Interpretation = Interpretation;

exports.OneTimeBindingCommand = OneTimeBindingCommand;

exports.RefAttributePattern = RefAttributePattern;

exports.RefBindingCommand = RefBindingCommand;

exports.SpreadValueBindingCommand = SpreadValueBindingCommand;

exports.StyleBindingCommand = StyleBindingCommand;

exports.SyntaxInterpreter = SyntaxInterpreter;

exports.TemplateCompiler = TemplateCompiler;

exports.TemplateCompilerHooks = dt;

exports.ToViewBindingCommand = ToViewBindingCommand;

exports.TriggerBindingCommand = TriggerBindingCommand;

exports.TwoWayBindingCommand = TwoWayBindingCommand;

exports.attributePattern = attributePattern;

exports.bindingCommand = bindingCommand;

exports.generateElementName = st;

exports.isInstruction = isInstruction;

exports.itAttributeBinding = O;

exports.itHydrateAttribute = R;

exports.itHydrateElement = v;

exports.itHydrateLetElement = _;

exports.itHydrateTemplateController = I;

exports.itInterpolation = k;

exports.itIteratorBinding = F;

exports.itLetBinding = $;

exports.itListenerBinding = W;

exports.itMultiAttr = V;

exports.itPropertyBinding = D;

exports.itRefBinding = L;

exports.itSetAttribute = q;

exports.itSetClassAttribute = N;

exports.itSetProperty = E;

exports.itSetStyleAttribute = U;

exports.itSpreadElementProp = z;

exports.itSpreadTransferedBinding = H;

exports.itSpreadValueBinding = G;

exports.itStylePropertyBinding = j;

exports.itTextBinding = M;

exports.templateCompilerHooks = templateCompilerHooks;
//# sourceMappingURL=index.cjs.map
