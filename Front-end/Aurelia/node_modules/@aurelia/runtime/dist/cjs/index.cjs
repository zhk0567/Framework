"use strict";

var t = require("@aurelia/kernel");

var e = require("@aurelia/metadata");

var s = require("@aurelia/platform");

var r = require("@aurelia/expression-parser");

const n = Object.prototype.hasOwnProperty;

const i = Reflect.defineProperty;

function rtDefineHiddenProp(t, e, s) {
    i(t, e, {
        configurable: true,
        writable: true,
        value: s
    });
    return s;
}

function ensureProto(t, e, s) {
    if (!(e in t)) {
        rtDefineHiddenProp(t, e, s);
    }
}

const o = Object.assign;

const a = Object.freeze;

const c = Object.getPrototypeOf;

const u = Object.getOwnPropertyDescriptor;

const h = String;

const l = t.DI.createInterface;

const f = e.Metadata.get;

const d = e.Metadata.define;

const createMappedError = (t, ...e) => new Error(`AUR${h(t).padStart(4, "0")}:${e.map(h)}`);

class Scope {
    constructor(t, e, s, r) {
        this.parent = t;
        this.bindingContext = e;
        this.overrideContext = s;
        this.isBoundary = r;
    }
    static getContext(t, e, s) {
        if (t == null) {
            throw createMappedError(203);
        }
        let r = t.overrideContext;
        let n = t;
        if (s > 0) {
            while (s > 0) {
                s--;
                n = n.parent;
                if (n == null) {
                    return void 0;
                }
            }
            r = n.overrideContext;
            return e in r ? r : n.bindingContext;
        }
        while (n != null && !n.isBoundary && !(e in n.overrideContext) && !(e in n.bindingContext)) {
            n = n.parent;
        }
        if (n == null) {
            return t.bindingContext;
        }
        r = n.overrideContext;
        return e in r ? r : n.bindingContext;
    }
    static create(t, e, s) {
        if (t == null) {
            throw createMappedError(204);
        }
        return new Scope(null, t, e ?? new OverrideContext, s ?? false);
    }
    static fromParent(t, e, s = new OverrideContext) {
        if (t == null) {
            throw createMappedError(203);
        }
        return new Scope(t, e, s, false);
    }
}

class BindingContext {
    constructor(t, e) {
        if (t !== void 0) {
            this[t] = e;
        }
    }
}

class OverrideContext {}

let p = null;

const b = [];

let w = false;

function pauseConnecting() {
    w = false;
}

function resumeConnecting() {
    w = true;
}

function currentConnectable() {
    return p;
}

function enterConnectable(t) {
    if (t == null) {
        throw createMappedError(206);
    }
    if (p == null) {
        p = t;
        b[0] = p;
        w = true;
        return;
    }
    if (p === t) {
        throw createMappedError(207);
    }
    b.push(t);
    p = t;
    w = true;
}

function exitConnectable(t) {
    if (t == null) {
        throw createMappedError(208);
    }
    if (p !== t) {
        throw createMappedError(209);
    }
    b.pop();
    p = b.length > 0 ? b[b.length - 1] : null;
    w = p != null;
}

const v = /*@__PURE__*/ a({
    get current() {
        return p;
    },
    get connecting() {
        return w;
    },
    enter: enterConnectable,
    exit: exitConnectable,
    pause: pauseConnecting,
    resume: resumeConnecting
});

const g = Reflect.get;

const y = Object.prototype.toString;

const C = new WeakMap;

const x = "__au_nw__";

const E = "__au_nw";

function canWrap(t) {
    switch (y.call(t)) {
      case "[object Object]":
        return t.constructor[x] !== true;

      case "[object Array]":
      case "[object Map]":
      case "[object Set]":
        return true;

      default:
        return false;
    }
}

const O = "__raw__";

function wrap(t) {
    return canWrap(t) ? getProxy(t) : t;
}

function getProxy(t) {
    return C.get(t) ?? createProxy(t);
}

function getRaw(t) {
    return t[O] ?? t;
}

function unwrap(t) {
    return canWrap(t) && t[O] || t;
}

const A = new Set(Object.getOwnPropertyNames(Symbol).map(t => Symbol[t]).filter(e => t.isSymbol(e) && e !== Symbol.iterator));

function doNotCollect(e, s) {
    if (s === "constructor" || s === "__proto__" || s === "$observers" || t.isSymbol(s) && A.has(s) || e.constructor[`${E}_${h(s)}__`] === true) {
        return true;
    }
    const r = Reflect.getOwnPropertyDescriptor(e, s);
    return r?.configurable === false && r.writable === false;
}

function createProxy(e) {
    const s = t.isArray(e) ? k : t.isMap(e) || t.isSet(e) ? S : m;
    const r = new Proxy(e, s);
    C.set(e, r);
    C.set(r, r);
    return r;
}

function observeTrackableMethodDependencies(e, s, r) {
    if (s == null || typeof s !== "object") {
        return;
    }
    const n = r.deps;
    if (n == null) {
        return;
    }
    const i = t.isFunction(n) ? [ n ] : n;
    for (const t of i) {
        if (typeof t === "string") {
            e.observeExpression(s, t);
        } else {
            t(wrap(s));
        }
    }
}

const m = {
    get(e, s, r) {
        if (s === O) {
            return e;
        }
        const n = p;
        if (!w || n == null || doNotCollect(e, s)) {
            return g(e, s, r);
        }
        n.observe(e, s);
        const i = g(e, s, r);
        if (!t.isFunction(i)) {
            return wrap(i);
        }
        const o = i[P];
        if (o == null) {
            return wrap(i);
        }
        return function trackableMethod(...e) {
            const s = p;
            if (!w || s == null) {
                return i.apply(this, e);
            }
            const n = this ?? r;
            const a = t.isObject(n) ? getRaw(n) : n;
            observeTrackableMethodDependencies(s, a, o);
            const c = o.deps == null;
            const u = c && t.isObject(a) ? wrap(a) : a;
            const h = c ? e.map(t => wrap(t)) : e.map(t => unwrap(t));
            return i.apply(u, h);
        };
    },
    deleteProperty(t, e) {
        return delete t[e];
    }
};

const k = {
    get(t, e, s) {
        if (e === O) {
            return t;
        }
        if (!w || doNotCollect(t, e) || p == null) {
            return g(t, e, s);
        }
        switch (e) {
          case "length":
            p.observe(t, "length");
            return t.length;

          case "map":
            return wrappedArrayMap;

          case "includes":
            return wrappedArrayIncludes;

          case "indexOf":
            return wrappedArrayIndexOf;

          case "lastIndexOf":
            return wrappedArrayLastIndexOf;

          case "every":
            return wrappedArrayEvery;

          case "filter":
            return wrappedArrayFilter;

          case "find":
            return wrappedArrayFind;

          case "findIndex":
            return wrappedArrayFindIndex;

          case "flat":
            return wrappedArrayFlat;

          case "flatMap":
            return wrappedArrayFlatMap;

          case "join":
            return wrappedArrayJoin;

          case "push":
            return wrappedArrayPush;

          case "pop":
            return wrappedArrayPop;

          case "reduce":
            return wrappedReduce;

          case "reduceRight":
            return wrappedReduceRight;

          case "reverse":
            return wrappedArrayReverse;

          case "shift":
            return wrappedArrayShift;

          case "unshift":
            return wrappedArrayUnshift;

          case "slice":
            return wrappedArraySlice;

          case "splice":
            return wrappedArraySplice;

          case "some":
            return wrappedArraySome;

          case "sort":
            return wrappedArraySort;

          case "keys":
            return wrappedKeys;

          case "values":
          case Symbol.iterator:
            return wrappedValues;

          case "entries":
            return wrappedEntries;
        }
        p.observe(t, e);
        return wrap(g(t, e, s));
    },
    ownKeys(t) {
        currentConnectable()?.observe(t, "length");
        return Reflect.ownKeys(t);
    }
};

function wrappedArrayMap(t, e) {
    const s = getRaw(this);
    const r = s.map((s, r) => unwrap(t.call(e, wrap(s), r, this)));
    observeCollection(p, s);
    return wrap(r);
}

function wrappedArrayEvery(t, e) {
    const s = getRaw(this);
    const r = s.every((s, r) => t.call(e, wrap(s), r, this));
    observeCollection(p, s);
    return r;
}

function wrappedArrayFilter(t, e) {
    const s = getRaw(this);
    const r = s.filter((s, r) => unwrap(t.call(e, wrap(s), r, this)));
    observeCollection(p, s);
    return wrap(r);
}

function wrappedArrayIncludes(t) {
    const e = getRaw(this);
    const s = e.includes(unwrap(t));
    observeCollection(p, e);
    return s;
}

function wrappedArrayIndexOf(t) {
    const e = getRaw(this);
    const s = e.indexOf(unwrap(t));
    observeCollection(p, e);
    return s;
}

function wrappedArrayLastIndexOf(t) {
    const e = getRaw(this);
    const s = e.lastIndexOf(unwrap(t));
    observeCollection(p, e);
    return s;
}

function wrappedArrayFindIndex(t, e) {
    const s = getRaw(this);
    const r = s.findIndex((s, r) => unwrap(t.call(e, wrap(s), r, this)));
    observeCollection(p, s);
    return r;
}

function wrappedArrayFind(t, e) {
    const s = getRaw(this);
    const r = s.find((e, s) => t(wrap(e), s, this), e);
    observeCollection(p, s);
    return wrap(r);
}

function wrappedArrayFlat() {
    const t = getRaw(this);
    observeCollection(p, t);
    return wrap(t.flat());
}

function wrappedArrayFlatMap(t, e) {
    const s = getRaw(this);
    observeCollection(p, s);
    return getProxy(s.flatMap((s, r) => wrap(t.call(e, wrap(s), r, this))));
}

function wrappedArrayJoin(t) {
    const e = getRaw(this);
    observeCollection(p, e);
    return e.join(t);
}

function wrappedArrayPop() {
    return wrap(getRaw(this).pop());
}

function wrappedArrayPush(...t) {
    return getRaw(this).push(...t);
}

function wrappedArrayShift() {
    return wrap(getRaw(this).shift());
}

function wrappedArrayUnshift(...t) {
    return getRaw(this).unshift(...t);
}

function wrappedArraySplice(...t) {
    return wrap(getRaw(this).splice(...t));
}

function wrappedArrayReverse(...t) {
    return wrap(getRaw(this).reverse());
}

function wrappedArraySome(t, e) {
    const s = getRaw(this);
    const r = s.some((s, r) => unwrap(t.call(e, wrap(s), r, this)));
    observeCollection(p, s);
    return r;
}

function wrappedArraySort(t) {
    const e = getRaw(this);
    const s = e.sort(t);
    observeCollection(p, e);
    return wrap(s);
}

function wrappedArraySlice(t, e) {
    const s = getRaw(this);
    observeCollection(p, s);
    return getProxy(s.slice(t, e));
}

function wrappedReduce(t, e) {
    const s = getRaw(this);
    const r = s.reduce((e, s, r) => t(e, wrap(s), r, this), e);
    observeCollection(p, s);
    return wrap(r);
}

function wrappedReduceRight(t, e) {
    const s = getRaw(this);
    const r = s.reduceRight((e, s, r) => t(e, wrap(s), r, this), e);
    observeCollection(p, s);
    return wrap(r);
}

const S = {
    get(e, s, r) {
        if (s === O) {
            return e;
        }
        const n = currentConnectable();
        if (!w || doNotCollect(e, s) || n == null) {
            return g(e, s, r);
        }
        switch (s) {
          case "size":
            n.observe(e, "size");
            return e.size;

          case "clear":
            return wrappedClear;

          case "delete":
            return wrappedDelete;

          case "forEach":
            return wrappedForEach;

          case "add":
            if (t.isSet(e)) {
                return wrappedAdd;
            }
            break;

          case "get":
            if (t.isMap(e)) {
                return wrappedGet;
            }
            break;

          case "set":
            if (t.isMap(e)) {
                return wrappedSet;
            }
            break;

          case "has":
            return wrappedHas;

          case "keys":
            return wrappedKeys;

          case "values":
            return wrappedValues;

          case "entries":
            return wrappedEntries;

          case Symbol.iterator:
            return t.isMap(e) ? wrappedEntries : wrappedValues;
        }
        return wrap(g(e, s, r));
    }
};

function wrappedForEach(t, e) {
    const s = getRaw(this);
    observeCollection(p, s);
    return s.forEach((s, r) => {
        t.call(e, wrap(s), wrap(r), this);
    });
}

function wrappedHas(t) {
    const e = getRaw(this);
    observeCollection(p, e);
    return e.has(unwrap(t));
}

function wrappedGet(t) {
    const e = getRaw(this);
    observeCollection(p, e);
    return wrap(e.get(unwrap(t)));
}

function wrappedSet(t, e) {
    return wrap(getRaw(this).set(unwrap(t), unwrap(e)));
}

function wrappedAdd(t) {
    return wrap(getRaw(this).add(unwrap(t)));
}

function wrappedClear() {
    return wrap(getRaw(this).clear());
}

function wrappedDelete(t) {
    return wrap(getRaw(this).delete(unwrap(t)));
}

function wrappedKeys() {
    const t = getRaw(this);
    observeCollection(p, t);
    const e = t.keys();
    return {
        next() {
            const t = e.next();
            const s = t.value;
            const r = t.done;
            return r ? {
                value: void 0,
                done: r
            } : {
                value: wrap(s),
                done: r
            };
        },
        [Symbol.iterator]() {
            return this;
        }
    };
}

function wrappedValues() {
    const t = getRaw(this);
    observeCollection(p, t);
    const e = t.values();
    return {
        next() {
            const t = e.next();
            const s = t.value;
            const r = t.done;
            return r ? {
                value: void 0,
                done: r
            } : {
                value: wrap(s),
                done: r
            };
        },
        [Symbol.iterator]() {
            return this;
        }
    };
}

function wrappedEntries() {
    const t = getRaw(this);
    observeCollection(p, t);
    const e = t.entries();
    return {
        next() {
            const t = e.next();
            const s = t.value;
            const r = t.done;
            return r ? {
                value: void 0,
                done: r
            } : {
                value: [ wrap(s[0]), wrap(s[1]) ],
                done: r
            };
        },
        [Symbol.iterator]() {
            return this;
        }
    };
}

const observeCollection = (t, e) => t?.observeCollection(e);

const R = /*@__PURE__*/ a({
    getProxy: getProxy,
    getRaw: getRaw,
    wrap: wrap,
    unwrap: unwrap,
    rawKey: O
});

const P = "__astt__";

const {astAssign: D, astEvaluate: I, astBind: M, astUnbind: _} = /*@__PURE__*/ (() => {
    const e = "AccessThis";
    const s = "AccessBoundary";
    const r = "AccessGlobal";
    const n = "AccessScope";
    const i = "ArrayLiteral";
    const o = "ObjectLiteral";
    const a = "PrimitiveLiteral";
    const c = "New";
    const u = "Template";
    const l = "Unary";
    const f = "CallScope";
    const d = "CallMember";
    const p = "CallFunction";
    const b = "CallGlobal";
    const w = "AccessMember";
    const v = "AccessKeyed";
    const g = "TaggedTemplate";
    const y = "Binary";
    const C = "Conditional";
    const x = "Assign";
    const E = "ArrowFunction";
    const O = "ValueConverter";
    const A = "BindingBehavior";
    const m = "ArrayBindingPattern";
    const k = "ObjectBindingPattern";
    const S = "BindingIdentifier";
    const R = "ForOfStatement";
    const D = "Interpolation";
    const I = "ArrayDestructuring";
    const M = "ObjectDestructuring";
    const _ = "DestructuringAssignmentLeaf";
    const T = "Custom";
    const V = Scope.getContext;
    function observeTrackableMethodDependencies(e, s, r) {
        if (s == null) {
            return;
        }
        const n = r.deps;
        if (n == null) {
            return;
        }
        const i = t.isFunction(n) ? [ n ] : n;
        for (const t of i) {
            if (typeof t === "string") {
                e.observeExpression(s, t);
                continue;
            }
            try {
                enterConnectable(e);
                t(wrap(s));
            } finally {
                exitConnectable(e);
            }
        }
    }
    function astEvaluate(N, B, L, F) {
        switch (N.$kind) {
          case e:
            {
                let t = B.overrideContext;
                let e = B;
                let s = N.ancestor;
                while (s-- && t) {
                    e = e.parent;
                    t = e?.overrideContext ?? null;
                }
                return s < 1 && e ? e.bindingContext : void 0;
            }

          case s:
            {
                let t = B;
                while (t != null && !t.isBoundary) {
                    t = t.parent;
                }
                return t ? t.bindingContext : void 0;
            }

          case n:
            {
                const e = V(B, N.name, N.ancestor);
                if (F !== null) {
                    F.observe(e, N.name);
                }
                const s = e[N.name];
                if (s == null) {
                    if (N.name === "$host") {
                        throw createMappedError(105);
                    }
                    return s;
                }
                return L?.boundFn && t.isFunction(s) ? s.bind(e) : s;
            }

          case r:
            return globalThis[N.name];

          case b:
            {
                const e = globalThis[N.name];
                if (t.isFunction(e)) {
                    return e(...N.args.map(t => astEvaluate(t, B, L, F)));
                }
                if (!L?.strict && e == null) {
                    return void 0;
                }
                throw createMappedError(107);
            }

          case i:
            return N.elements.map(t => astEvaluate(t, B, L, F));

          case o:
            {
                const t = {};
                for (let e = 0; e < N.keys.length; ++e) {
                    t[N.keys[e]] = astEvaluate(N.values[e], B, L, F);
                }
                return t;
            }

          case a:
            return N.value;

          case c:
            {
                const e = astEvaluate(N.func, B, L, F);
                if (t.isFunction(e)) {
                    return new e(...N.args.map(t => astEvaluate(t, B, L, F)));
                }
                throw createMappedError(107);
            }

          case u:
            {
                let t = N.cooked[0];
                for (let e = 0; e < N.expressions.length; ++e) {
                    t += h(astEvaluate(N.expressions[e], B, L, F));
                    t += N.cooked[e + 1];
                }
                return t;
            }

          case l:
            {
                const t = astEvaluate(N.expression, B, L, F);
                switch (N.operation) {
                  case "void":
                    return void t;

                  case "typeof":
                    return typeof t;

                  case "!":
                    return !t;

                  case "-":
                    return -t;

                  case "+":
                    return +t;

                  case "--":
                    if (F != null) throw createMappedError(113);
                    return astAssign(N.expression, B, L, F, t - 1) + N.pos;

                  case "++":
                    if (F != null) throw createMappedError(113);
                    return astAssign(N.expression, B, L, F, t + 1) - N.pos;

                  default:
                    throw createMappedError(109, N.operation);
                }
            }

          case f:
            {
                const e = V(B, N.name, N.ancestor);
                if (e == null) {
                    if (L?.strict) {
                        throw createMappedError(114, N.name, e);
                    }
                    return void 0;
                }
                const s = e[N.name];
                if (t.isFunction(s)) {
                    if (F != null && s[P] != null) {
                        const t = s[P];
                        observeTrackableMethodDependencies(F, e, t);
                        const r = t?.deps == null;
                        try {
                            enterConnectable(F);
                            return s.apply(r ? wrap(e) : e, N.args.map(t => r ? wrap(astEvaluate(t, B, L, F)) : astEvaluate(t, B, L, F)));
                        } finally {
                            exitConnectable(F);
                        }
                    } else {
                        return s.apply(e, N.args.map(t => astEvaluate(t, B, L, F)));
                    }
                }
                if (s == null) {
                    if (L?.strict && !N.optional) {
                        throw createMappedError(111, N.name);
                    }
                    return void 0;
                }
                throw createMappedError(111, N.name);
            }

          case d:
            {
                const e = astEvaluate(N.object, B, L, F);
                if (e == null) {
                    if (L?.strict && !N.optionalMember) {
                        throw createMappedError(114, N.name, e);
                    }
                }
                const s = e?.[N.name];
                if (s == null) {
                    if (!N.optionalCall && L?.strict) {
                        throw createMappedError(111, N.name);
                    }
                    return void 0;
                }
                if (!t.isFunction(s)) {
                    throw createMappedError(111, N.name);
                }
                if (F != null && s[P] != null) {
                    const t = s[P];
                    observeTrackableMethodDependencies(F, e, t);
                    const r = t?.deps == null;
                    try {
                        enterConnectable(F);
                        return s.apply(r ? wrap(e) : e, N.args.map(t => r ? wrap(astEvaluate(t, B, L, F)) : astEvaluate(t, B, L, F)));
                    } finally {
                        exitConnectable(F);
                    }
                } else {
                    const r = s.apply(e, N.args.map(t => astEvaluate(t, B, L, F)));
                    if (t.isArray(e) && j.includes(N.name)) {
                        F?.observeCollection(e);
                    }
                    return r;
                }
            }

          case p:
            {
                const e = astEvaluate(N.func, B, L, F);
                if (t.isFunction(e)) {
                    return e(...N.args.map(t => astEvaluate(t, B, L, F)));
                }
                if (e == null) {
                    if (!N.optional && L?.strict) {
                        throw createMappedError(107);
                    }
                    return void 0;
                }
                throw createMappedError(107);
            }

          case E:
            {
                const func = (...t) => {
                    const e = N.args;
                    const s = N.rest;
                    const r = e.length - 1;
                    const n = e.reduce((e, n, i) => {
                        if (s && i === r) {
                            e[n.name] = t.slice(i);
                        } else {
                            e[n.name] = t[i];
                        }
                        return e;
                    }, {});
                    const i = Scope.fromParent(B, n);
                    return astEvaluate(N.body, i, L, F);
                };
                return func;
            }

          case w:
            {
                const e = astEvaluate(N.object, B, L, F);
                if (e == null) {
                    if (!N.optional && L?.strict) {
                        throw createMappedError(114, N.name, e);
                    }
                    return void 0;
                }
                if (F !== null && !N.accessGlobal) {
                    F.observe(e, N.name);
                }
                const s = e[N.name];
                return L?.boundFn && t.isFunction(s) ? s.bind(e) : s;
            }

          case v:
            {
                const t = astEvaluate(N.object, B, L, F);
                const e = astEvaluate(N.key, B, L, F);
                if (t == null) {
                    if (!N.optional && L?.strict) {
                        throw createMappedError(115, e, t);
                    }
                    return void 0;
                }
                if (F !== null && !N.accessGlobal) {
                    F.observe(t, e);
                }
                return t[e];
            }

          case g:
            {
                const e = N.expressions.map(t => astEvaluate(t, B, L, F));
                const s = astEvaluate(N.func, B, L, F);
                if (!t.isFunction(s)) {
                    throw createMappedError(110);
                }
                return s(N.cooked, ...e);
            }

          case y:
            {
                const e = N.left;
                const s = N.right;
                switch (N.operation) {
                  case "&&":
                    return astEvaluate(e, B, L, F) && astEvaluate(s, B, L, F);

                  case "||":
                    return astEvaluate(e, B, L, F) || astEvaluate(s, B, L, F);

                  case "??":
                    return astEvaluate(e, B, L, F) ?? astEvaluate(s, B, L, F);

                  case "==":
                    return astEvaluate(e, B, L, F) == astEvaluate(s, B, L, F);

                  case "===":
                    return astEvaluate(e, B, L, F) === astEvaluate(s, B, L, F);

                  case "!=":
                    return astEvaluate(e, B, L, F) != astEvaluate(s, B, L, F);

                  case "!==":
                    return astEvaluate(e, B, L, F) !== astEvaluate(s, B, L, F);

                  case "instanceof":
                    {
                        const r = astEvaluate(s, B, L, F);
                        if (t.isFunction(r)) {
                            return astEvaluate(e, B, L, F) instanceof r;
                        }
                        return false;
                    }

                  case "in":
                    {
                        const r = astEvaluate(s, B, L, F);
                        if (t.isObjectOrFunction(r)) {
                            return astEvaluate(e, B, L, F) in r;
                        }
                        return false;
                    }

                  case "+":
                    return astEvaluate(e, B, L, F) + astEvaluate(s, B, L, F);

                  case "-":
                    return astEvaluate(e, B, L, F) - astEvaluate(s, B, L, F);

                  case "*":
                    return astEvaluate(e, B, L, F) * astEvaluate(s, B, L, F);

                  case "/":
                    return astEvaluate(e, B, L, F) / astEvaluate(s, B, L, F);

                  case "%":
                    return astEvaluate(e, B, L, F) % astEvaluate(s, B, L, F);

                  case "**":
                    return astEvaluate(e, B, L, F) ** astEvaluate(s, B, L, F);

                  case "<":
                    return astEvaluate(e, B, L, F) < astEvaluate(s, B, L, F);

                  case ">":
                    return astEvaluate(e, B, L, F) > astEvaluate(s, B, L, F);

                  case "<=":
                    return astEvaluate(e, B, L, F) <= astEvaluate(s, B, L, F);

                  case ">=":
                    return astEvaluate(e, B, L, F) >= astEvaluate(s, B, L, F);

                  default:
                    throw createMappedError(108, N.operation);
                }
            }

          case C:
            return astEvaluate(N.condition, B, L, F) ? astEvaluate(N.yes, B, L, F) : astEvaluate(N.no, B, L, F);

          case x:
            {
                let t = astEvaluate(N.value, B, L, F);
                if (N.op !== "=") {
                    if (F != null) {
                        throw createMappedError(113);
                    }
                    const e = astEvaluate(N.target, B, L, F);
                    switch (N.op) {
                      case "/=":
                        t = e / t;
                        break;

                      case "*=":
                        t = e * t;
                        break;

                      case "+=":
                        t = e + t;
                        break;

                      case "-=":
                        t = e - t;
                        break;

                      default:
                        throw createMappedError(108, N.op);
                    }
                }
                return astAssign(N.target, B, L, F, t);
            }

          case O:
            {
                return L?.useConverter?.(N.name, "toView", astEvaluate(N.expression, B, L, F), N.args.map(t => astEvaluate(t, B, L, F)));
            }

          case A:
            return astEvaluate(N.expression, B, L, F);

          case S:
            return N.name;

          case R:
            return astEvaluate(N.iterable, B, L, F);

          case D:
            if (N.isMulti) {
                let t = N.parts[0];
                let e = 0;
                for (;e < N.expressions.length; ++e) {
                    t += h(astEvaluate(N.expressions[e], B, L, F));
                    t += N.parts[e + 1];
                }
                return t;
            } else {
                return `${N.parts[0]}${astEvaluate(N.firstExpression, B, L, F)}${N.parts[1]}`;
            }

          case _:
            return astEvaluate(N.target, B, L, F);

          case I:
            {
                return N.list.map(t => astEvaluate(t, B, L, F));
            }

          case m:
          case k:
          case M:
          default:
            return void 0;

          case T:
            return N.evaluate(B, L, F);
        }
    }
    function astAssign(e, s, r, i, o) {
        switch (e.$kind) {
          case n:
            {
                if (e.name === "$host") {
                    throw createMappedError(106);
                }
                const t = V(s, e.name, e.ancestor);
                return t[e.name] = o;
            }

          case w:
            {
                const n = astEvaluate(e.object, s, r, i);
                if (n == null) {
                    if (r?.strict) {
                        throw createMappedError(116, e.name);
                    }
                    astAssign(e.object, s, r, i, {
                        [e.name]: o
                    });
                } else if (t.isObjectOrFunction(n)) {
                    if (e.name === "length" && t.isArray(n) && !isNaN(o)) {
                        n.splice(o);
                    } else {
                        n[e.name] = o;
                    }
                } else ;
                return o;
            }

          case v:
            {
                const n = astEvaluate(e.object, s, r, i);
                const a = astEvaluate(e.key, s, r, i);
                if (n == null) {
                    if (r?.strict) {
                        throw createMappedError(116, a);
                    }
                    astAssign(e.object, s, r, i, {
                        [a]: o
                    });
                    return o;
                }
                if (t.isArray(n)) {
                    if (a === "length" && !isNaN(o)) {
                        n.splice(o);
                        return o;
                    }
                    if (t.isArrayIndex(a)) {
                        n.splice(a, 1, o);
                        return o;
                    }
                }
                return n[a] = o;
            }

          case x:
            astAssign(e.value, s, r, i, o);
            return astAssign(e.target, s, r, i, o);

          case O:
            {
                o = r?.useConverter?.(e.name, "fromView", o, e.args.map(t => astEvaluate(t, s, r, i)));
                return astAssign(e.expression, s, r, i, o);
            }

          case A:
            return astAssign(e.expression, s, r, i, o);

          case I:
          case M:
            {
                const t = e.list;
                const n = t.length;
                let a;
                let c;
                for (a = 0; a < n; a++) {
                    c = t[a];
                    switch (c.$kind) {
                      case _:
                        astAssign(c, s, r, i, o);
                        break;

                      case I:
                      case M:
                        {
                            if (typeof o !== "object" || o === null) {
                                throw createMappedError(112);
                            }
                            let t = astEvaluate(c.source, Scope.create(o), r, null);
                            if (t === void 0 && c.initializer) {
                                t = astEvaluate(c.initializer, s, r, null);
                            }
                            astAssign(c, s, r, i, t);
                            break;
                        }
                    }
                }
                break;
            }

          case _:
            {
                if ("source" in e) {
                    if (o == null) {
                        return;
                    }
                    if (typeof o !== "object") {
                        throw createMappedError(112);
                    }
                    let t = astEvaluate(e.source, Scope.create(o), r, i);
                    if (t === void 0 && e.initializer) {
                        t = astEvaluate(e.initializer, s, r, i);
                    }
                    astAssign(e.target, s, r, i, t);
                } else {
                    if (o == null) {
                        return;
                    }
                    if (typeof o !== "object") {
                        throw createMappedError(112);
                    }
                    const n = e.indexOrProperties;
                    let a;
                    if (t.isArrayIndex(n)) {
                        if (!Array.isArray(o)) {
                            throw createMappedError(112);
                        }
                        a = o.slice(n);
                    } else {
                        a = Object.entries(o).reduce((t, [e, s]) => {
                            if (!n.includes(e)) {
                                t[e] = s;
                            }
                            return t;
                        }, {});
                    }
                    astAssign(e.target, s, r, i, a);
                }
                break;
            }

          case T:
            return e.assign(s, r, o);

          default:
            return void 0;
        }
    }
    function astBind(t, e, s) {
        switch (t.$kind) {
          case A:
            {
                s.bindBehavior?.(t.name, e, t.args.map(t => astEvaluate(t, e, s, null)));
                astBind(t.expression, e, s);
                break;
            }

          case O:
            {
                s.bindConverter?.(t.name);
                astBind(t.expression, e, s);
                break;
            }

          case R:
            {
                astBind(t.iterable, e, s);
                break;
            }

          case T:
            {
                t.bind?.(e, s);
            }
        }
    }
    function astUnbind(t, e, s) {
        switch (t.$kind) {
          case A:
            {
                s.unbindBehavior?.(t.name, e);
                astUnbind(t.expression, e, s);
                break;
            }

          case O:
            {
                s.unbindConverter?.(t.name);
                astUnbind(t.expression, e, s);
                break;
            }

          case R:
            {
                astUnbind(t.iterable, e, s);
                break;
            }

          case T:
            {
                t.unbind?.(e, s);
            }
        }
    }
    const j = "at map filter includes indexOf lastIndexOf findIndex find flat flatMap join reduce reduceRight slice every some sort".split(" ");
    return {
        astEvaluate: astEvaluate,
        astAssign: astAssign,
        astBind: astBind,
        astUnbind: astUnbind
    };
})();

function isClassMethodDecoratorContext$1(t) {
    return t != null && typeof t === "object" && "kind" in t;
}

function astTrack(e, ...s) {
    if (t.isFunction(e) && isClassMethodDecoratorContext$1(s[0])) {
        return applyDecorator(e, s[0], {});
    }
    const r = {};
    if (typeof e === "string") {
        r.deps = [ e, ...s ];
    } else if (t.isFunction(e)) {
        r.deps = [ e ];
    } else if (e != null) {
        const s = e;
        if (Object.prototype.hasOwnProperty.call(s, "deps")) {
            r.deps = s.deps == null ? void 0 : t.isFunction(s.deps) ? [ s.deps ] : s.deps;
        }
    }
    return (t, e) => applyDecorator(t, e, r);
}

function applyDecorator(t, e, s) {
    if (e.kind !== "method") {
        throw createMappedError(117, String(e.name));
    }
    const r = t;
    o(r, {
        [P]: {
            deps: s.deps
        }
    });
}

const T = (() => t => {
    const e = t.prototype;
    [ "bindBehavior", "unbindBehavior", "bindConverter", "unbindConverter", "useConverter" ].forEach(t => {
        rtDefineHiddenProp(e, t, () => {
            throw createMappedError(99, t);
        });
    });
})();

const V = "pending";

const j = "running";

const N = "completed";

const B = "canceled";

const L = Promise.resolve();

let F = false;

let $ = false;

const H = [];

const z = [];

let U = 0;

let q = null;

let W = [];

let K = null;

let G = null;

const requestRun = () => {
    if (!F) {
        F = true;
        void L.then(() => {
            F = false;
            $ = true;
            runTasks();
        });
    }
};

const signalSettled = t => {
    if (q && H.length === 0 && U === 0) {
        q = null;
        if (W.length > 0) {
            const t = W;
            W = [];
            if (t.length === 1) {
                G(t[0]);
            } else {
                G(new AggregateError(t, "One or more tasks failed."));
            }
        } else {
            K(t);
        }
    }
};

const runTasks = () => {
    const t = !$;
    $ = false;
    q ??= new Promise((t, e) => {
        K = t;
        G = e;
    });
    let e = -H.length;
    const s = H.length === 0;
    while (H.length > 0) {
        if (++e > 1e4) {
            const t = new Error(`Potential deadlock detected. More than 10000 extra tasks were queued from within tasks.`);
            H.length = 0;
            G?.(t);
            q = null;
            throw t;
        }
        const t = H.shift();
        if (typeof t === "function") {
            try {
                t();
            } catch (t) {
                W.push(t);
            }
        } else {
            t.run();
        }
    }
    const r = W.slice();
    signalSettled(!s);
    if (t && r.length > 0) {
        if (r.length === 1) {
            throw r[0];
        } else {
            throw new AggregateError(r, "One or more tasks failed.");
        }
    }
};

const getRecurringTasks = () => z.slice();

const isTaskQueueEmpty = () => H.length === 0 && U === 0;

const tasksSettled = () => {
    if (q) {
        return q;
    }
    if (H.length > 0 || U > 0) {
        return q ??= new Promise((t, e) => {
            K = t;
            G = e;
        });
    }
    return L.then(() => {
        if (H.length > 0 || U > 0) {
            return q ??= new Promise((t, e) => {
                K = t;
                G = e;
            });
        }
        return false;
    });
};

const queueTask = t => {
    requestRun();
    H.push(t);
};

const queueAsyncTask = (t, e) => {
    const r = new Task(t, e?.delay);
    if (r.delay != null && r.delay > 0) {
        ++U;
        r.t = s.Platform.getOrCreate(globalThis).setTimeout(() => {
            --U;
            r.t = undefined;
            if (r.status === B) {
                signalSettled(true);
                return;
            }
            H.push(r);
            requestRun();
        }, r.delay);
    } else {
        H.push(r);
        requestRun();
    }
    return r;
};

class TaskAbortError extends Error {
    constructor(t) {
        super(`Task ${t.id} was canceled.`);
        this.task = t;
    }
}

class Task {
    get result() {
        return this.i;
    }
    get status() {
        return this.u;
    }
    constructor(t, e) {
        this.callback = t;
        this.delay = e;
        this.id = ++Task.h;
        this.u = V;
        this.i = new Promise((t, e) => {
            this.C = t;
            this.O = e;
        });
    }
    then(t, e) {
        return this.result.then(t, e);
    }
    catch(t) {
        return this.result.catch(t);
    }
    finally(t) {
        return this.result.finally(t);
    }
    run() {
        if (this.u !== V) {
            throw new Error(`Cannot run task in ${this.u} state`);
        }
        this.u = j;
        let t;
        try {
            t = this.callback();
        } catch (t) {
            this.u = B;
            this.O(t);
            W.push(t);
            return;
        }
        if (t instanceof Promise) {
            ++U;
            t.then(t => {
                this.u = N;
                this.C(t);
            }).catch(t => {
                this.u = B;
                this.O(t);
                W.push(t);
            }).finally(() => {
                --U;
                signalSettled(true);
            });
        } else {
            this.u = N;
            this.C(t);
        }
    }
    cancel() {
        if (this.t !== undefined) {
            s.Platform.getOrCreate(globalThis).clearTimeout(this.t);
            --U;
            this.t = undefined;
            this.u = B;
            const e = new TaskAbortError(this);
            this.O(e);
            void this.i.catch(t.noop);
            signalSettled(true);
            return true;
        }
        if (this.u === V) {
            const e = H.indexOf(this);
            if (e > -1) {
                H.splice(e, 1);
                this.u = B;
                const s = new TaskAbortError(this);
                this.O(s);
                void this.i.catch(t.noop);
                signalSettled(true);
                return true;
            }
        }
        return false;
    }
}

Task.h = 0;

const queueRecurringTask = (t, e) => {
    const s = new RecurringTask(t, Math.max(e?.interval ?? 0, 0));
    z.push(s);
    s.A();
    return s;
};

class RecurringTask {
    constructor(t, e) {
        this.cb = t;
        this.R = e;
        this.id = ++RecurringTask.P;
        this.I = false;
        this.M = [];
    }
    run() {
        try {
            this.cb();
        } catch (t) {
            W.push(t);
            return;
        }
    }
    A() {
        if (this.I) {
            return;
        }
        this.t = s.Platform.getOrCreate(globalThis).setTimeout(() => {
            this._();
            if (!this.I) {
                this.A();
            }
        }, this.R);
    }
    _() {
        H.push(this);
        requestRun();
        const t = this.M.splice(0);
        for (const e of t) {
            e();
        }
    }
    next() {
        if (this.I) {
            return Promise.resolve();
        }
        return new Promise(t => this.M.push(t));
    }
    cancel() {
        this.I = true;
        if (this.t !== undefined) {
            s.Platform.getOrCreate(globalThis).clearTimeout(this.t);
            this.t = undefined;
        }
        const t = z.indexOf(this);
        if (t > -1) {
            z.splice(t, 1);
        }
        const e = this.M.splice(0);
        for (const t of e) {
            t();
        }
    }
}

RecurringTask.P = 0;

const J = /*@__PURE__*/ t.DI.createInterface("ICoercionConfiguration");

const Q = 0;

const X = 1;

const Y = 2;

const Z = 4;

const tt = /*@__PURE__*/ a({
    None: Q,
    Observer: X,
    Node: Y,
    Layout: Z
});

function copyIndexMap(t, e, s) {
    const {length: r} = t;
    const n = Array(r);
    let i = 0;
    while (i < r) {
        n[i] = t[i];
        ++i;
    }
    if (e !== void 0) {
        n.deletedIndices = e.slice(0);
    } else if (t.deletedIndices !== void 0) {
        n.deletedIndices = t.deletedIndices.slice(0);
    } else {
        n.deletedIndices = [];
    }
    if (s !== void 0) {
        n.deletedItems = s.slice(0);
    } else if (t.deletedItems !== void 0) {
        n.deletedItems = t.deletedItems.slice(0);
    } else {
        n.deletedItems = [];
    }
    n.isIndexMap = true;
    return n;
}

function createIndexMap(t = 0) {
    const e = Array(t);
    let s = 0;
    while (s < t) {
        e[s] = s++;
    }
    e.deletedIndices = [];
    e.deletedItems = [];
    e.isIndexMap = true;
    return e;
}

function cloneIndexMap(t) {
    const e = t.slice();
    e.deletedIndices = t.deletedIndices.slice();
    e.deletedItems = t.deletedItems.slice();
    e.isIndexMap = true;
    return e;
}

function isIndexMap(e) {
    return t.isArray(e) && e.isIndexMap === true;
}

let et = new Map;

let st = false;

function batch(t) {
    const e = et;
    const s = et = new Map;
    st = true;
    try {
        t();
    } finally {
        et = null;
        st = false;
        try {
            let t;
            let r;
            let n;
            let i;
            let o;
            let a = false;
            let c;
            let u;
            for (t of s) {
                r = t[0];
                n = t[1];
                if (e?.has(r)) {
                    e.set(r, n);
                }
                if (n[0] === 1) {
                    r.notify(n[1], n[2]);
                } else {
                    i = n[1];
                    o = n[2];
                    a = false;
                    if (o.deletedIndices.length > 0) {
                        a = true;
                    } else {
                        for (c = 0, u = o.length; c < u; ++c) {
                            if (o[c] !== c) {
                                a = true;
                                break;
                            }
                        }
                    }
                    if (a) {
                        r.notifyCollection(i, o);
                    }
                }
            }
        } finally {
            et = e;
        }
    }
}

function addCollectionBatch(t, e, s) {
    if (!et.has(t)) {
        et.set(t, [ 2, e, s ]);
    } else {
        et.get(t)[2] = s;
    }
}

function addValueBatch(t, e, s) {
    const r = et.get(t);
    if (r === void 0) {
        et.set(t, [ 1, e, s ]);
    } else {
        r[1] = e;
        r[2] = s;
    }
}

const rt = /*@__PURE__*/ (() => {
    function subscriberCollection(t, e) {
        return t == null ? subscriberCollectionDeco : subscriberCollectionDeco(t);
    }
    function getSubscriberRecord() {
        return rtDefineHiddenProp(this, "subs", new SubscriberRecord);
    }
    function addSubscriber(t) {
        return this.subs.add(t);
    }
    function removeSubscriber(t) {
        return this.subs.remove(t);
    }
    const t = new WeakSet;
    function subscriberCollectionDeco(e, s) {
        if (!t.has(e)) {
            t.add(e);
            const s = e.prototype;
            i(s, "subs", {
                get: getSubscriberRecord
            });
            ensureProto(s, "subscribe", addSubscriber);
            ensureProto(s, "unsubscribe", removeSubscriber);
        }
        return e;
    }
    class SubscriberRecord {
        constructor() {
            this.count = 0;
            this.T = [];
            this.V = [];
            this.j = false;
        }
        add(t) {
            if (this.T.includes(t)) {
                return false;
            }
            this.T[this.T.length] = t;
            if ("handleDirty" in t) {
                this.V[this.V.length] = t;
                this.j = true;
            }
            ++this.count;
            return true;
        }
        remove(t) {
            let e = this.T.indexOf(t);
            if (e !== -1) {
                this.T.splice(e, 1);
                e = this.V.indexOf(t);
                if (e !== -1) {
                    this.V.splice(e, 1);
                    this.j = this.V.length > 0;
                }
                --this.count;
                return true;
            }
            return false;
        }
        notify(t, e) {
            if (st) {
                addValueBatch(this, t, e);
                return;
            }
            for (const s of this.T.slice(0)) {
                s.handleChange(t, e);
            }
        }
        notifyCollection(t, e) {
            const s = this.T.slice(0);
            const r = s.length;
            let n = 0;
            for (;n < r; ++n) {
                s[n].handleCollectionChange(t, e);
            }
            return;
        }
        notifyDirty() {
            if (this.j) {
                for (const t of this.V.slice(0)) {
                    t.handleDirty();
                }
            }
        }
    }
    return subscriberCollection;
})();

class CollectionLengthObserver {
    constructor(t) {
        this.owner = t;
        this.type = X;
        this.v = (this.o = t.collection).length;
    }
    getValue() {
        return this.o.length;
    }
    setValue(t) {
        if (t !== this.v) {
            if (!Number.isNaN(t)) {
                this.o.splice(t);
                this.v = this.o.length;
            }
        }
    }
    handleDirty() {
        if (this.v !== this.o.length) {
            this.subs.notifyDirty();
        }
    }
    handleCollectionChange(t, e) {
        const s = this.v;
        const r = this.o.length;
        if ((this.v = r) !== s) {
            this.subs.notifyDirty();
            this.subs.notify(this.v, s);
        }
    }
}

(() => {
    implementLengthObserver(CollectionLengthObserver);
})();

class CollectionSizeObserver {
    constructor(t) {
        this.owner = t;
        this.type = X;
        this.v = (this.o = t.collection).size;
    }
    getValue() {
        return this.o.size;
    }
    setValue() {
        throw createMappedError(220);
    }
    handleDirty() {
        if (this.v !== this.o.size) {
            this.subs.notifyDirty();
        }
    }
    handleCollectionChange(t, e) {
        const s = this.v;
        const r = this.o.size;
        if ((this.v = r) !== s) {
            this.subs.notify(this.v, s);
        }
    }
}

(() => {
    implementLengthObserver(CollectionSizeObserver);
})();

function implementLengthObserver(t) {
    const e = t.prototype;
    ensureProto(e, "subscribe", subscribe);
    ensureProto(e, "unsubscribe", unsubscribe);
    return rt(t, null);
}

function subscribe(t) {
    if (this.subs.add(t) && this.subs.count === 1) {
        this.owner.subscribe(this);
    }
}

function unsubscribe(t) {
    if (this.subs.remove(t) && this.subs.count === 0) {
        this.owner.subscribe(this);
    }
}

const nt = /*@__PURE__*/ (() => {
    const e = new WeakMap;
    function sortCompare(t, e) {
        if (t === e) {
            return 0;
        }
        t = t === null ? "null" : t.toString();
        e = e === null ? "null" : e.toString();
        return t < e ? -1 : 1;
    }
    function preSortCompare(t, e) {
        if (t === void 0) {
            if (e === void 0) {
                return 0;
            } else {
                return 1;
            }
        }
        if (e === void 0) {
            return -1;
        }
        return 0;
    }
    function insertionSort(t, e, s, r, n) {
        let i, o, a, c, u;
        let h, l;
        for (h = s + 1; h < r; h++) {
            i = t[h];
            o = e[h];
            for (l = h - 1; l >= s; l--) {
                a = t[l];
                c = e[l];
                u = n(a, i);
                if (u > 0) {
                    t[l + 1] = a;
                    e[l + 1] = c;
                } else {
                    break;
                }
            }
            t[l + 1] = i;
            e[l + 1] = o;
        }
    }
    function quickSort(t, e, s, r, n) {
        let i = 0, o = 0;
        let a, c, u;
        let h, l, f;
        let d, p, b;
        let w, v;
        let g, y, C, x;
        let E, O, A, m;
        while (true) {
            if (r - s <= 10) {
                insertionSort(t, e, s, r, n);
                return;
            }
            i = s + (r - s >> 1);
            a = t[s];
            h = e[s];
            c = t[r - 1];
            l = e[r - 1];
            u = t[i];
            f = e[i];
            d = n(a, c);
            if (d > 0) {
                w = a;
                v = h;
                a = c;
                h = l;
                c = w;
                l = v;
            }
            p = n(a, u);
            if (p >= 0) {
                w = a;
                v = h;
                a = u;
                h = f;
                u = c;
                f = l;
                c = w;
                l = v;
            } else {
                b = n(c, u);
                if (b > 0) {
                    w = c;
                    v = l;
                    c = u;
                    l = f;
                    u = w;
                    f = v;
                }
            }
            t[s] = a;
            e[s] = h;
            t[r - 1] = u;
            e[r - 1] = f;
            g = c;
            y = l;
            C = s + 1;
            x = r - 1;
            t[i] = t[C];
            e[i] = e[C];
            t[C] = g;
            e[C] = y;
            t: for (o = C + 1; o < x; o++) {
                E = t[o];
                O = e[o];
                A = n(E, g);
                if (A < 0) {
                    t[o] = t[C];
                    e[o] = e[C];
                    t[C] = E;
                    e[C] = O;
                    C++;
                } else if (A > 0) {
                    do {
                        x--;
                        if (x == o) {
                            break t;
                        }
                        m = t[x];
                        A = n(m, g);
                    } while (A > 0);
                    t[o] = t[x];
                    e[o] = e[x];
                    t[x] = E;
                    e[x] = O;
                    if (A < 0) {
                        E = t[o];
                        O = e[o];
                        t[o] = t[C];
                        e[o] = e[C];
                        t[C] = E;
                        e[C] = O;
                        C++;
                    }
                }
            }
            if (r - x < C - s) {
                quickSort(t, e, x, r, n);
                r = C;
            } else {
                quickSort(t, e, s, C, n);
                s = x;
            }
        }
    }
    const s = Array.prototype;
    const r = [ "push", "unshift", "pop", "shift", "splice", "reverse", "sort" ];
    let n;
    function overrideArrayPrototypes() {
        const o = s.push;
        const a = s.unshift;
        const c = s.pop;
        const u = s.shift;
        const h = s.splice;
        const l = s.reverse;
        const f = s.sort;
        n = {
            push: function(...t) {
                const s = e.get(this);
                if (s === void 0) {
                    return o.apply(this, t);
                }
                const r = this.length;
                const n = t.length;
                if (n === 0) {
                    return r;
                }
                this.length = s.indexMap.length = r + n;
                let i = r;
                while (i < this.length) {
                    this[i] = t[i - r];
                    s.indexMap[i] = -2;
                    i++;
                }
                s.notify();
                return this.length;
            },
            unshift: function(...t) {
                const s = e.get(this);
                if (s === void 0) {
                    return a.apply(this, t);
                }
                const r = t.length;
                const n = new Array(r);
                let i = 0;
                while (i < r) {
                    n[i++] = -2;
                }
                a.apply(s.indexMap, n);
                const o = a.apply(this, t);
                s.notify();
                return o;
            },
            pop: function() {
                const t = e.get(this);
                if (t === void 0) {
                    return c.call(this);
                }
                const s = t.indexMap;
                const r = c.call(this);
                const n = s.length - 1;
                if (s[n] > -1) {
                    s.deletedIndices.push(s[n]);
                    s.deletedItems.push(r);
                }
                c.call(s);
                t.notify();
                return r;
            },
            shift: function() {
                const t = e.get(this);
                if (t === void 0) {
                    return u.call(this);
                }
                const s = t.indexMap;
                const r = u.call(this);
                if (s[0] > -1) {
                    s.deletedIndices.push(s[0]);
                    s.deletedItems.push(r);
                }
                u.call(s);
                t.notify();
                return r;
            },
            splice: function(...t) {
                const s = t[0];
                const r = t[1];
                const n = e.get(this);
                if (n === void 0) {
                    return h.apply(this, t);
                }
                const i = this.length;
                const o = s | 0;
                const a = o < 0 ? Math.max(i + o, 0) : Math.min(o, i);
                const c = n.indexMap;
                const u = t.length;
                const l = u === 0 ? 0 : u === 1 ? i - a : r;
                let f = a;
                if (l > 0) {
                    const t = f + l;
                    while (f < t) {
                        if (c[f] > -1) {
                            c.deletedIndices.push(c[f]);
                            c.deletedItems.push(this[f]);
                        }
                        f++;
                    }
                }
                f = 0;
                if (u > 2) {
                    const t = u - 2;
                    const e = new Array(t);
                    while (f < t) {
                        e[f++] = -2;
                    }
                    h.call(c, s, r, ...e);
                } else {
                    h.apply(c, t);
                }
                const d = h.apply(this, t);
                if (l > 0 || f > 0) {
                    n.notify();
                }
                return d;
            },
            reverse: function() {
                const t = e.get(this);
                if (t === void 0) {
                    l.call(this);
                    return this;
                }
                const s = this.length;
                const r = s / 2 | 0;
                let n = 0;
                while (n !== r) {
                    const e = s - n - 1;
                    const r = this[n];
                    const i = t.indexMap[n];
                    const o = this[e];
                    const a = t.indexMap[e];
                    this[n] = o;
                    t.indexMap[n] = a;
                    this[e] = r;
                    t.indexMap[e] = i;
                    n++;
                }
                t.notify();
                return this;
            },
            sort: function(s) {
                const r = e.get(this);
                if (r === void 0) {
                    f.call(this, s);
                    return this;
                }
                let n = this.length;
                if (n < 2) {
                    return this;
                }
                quickSort(this, r.indexMap, 0, n, preSortCompare);
                let i = 0;
                while (i < n) {
                    if (this[i] === void 0) {
                        break;
                    }
                    i++;
                }
                if (s === void 0 || !t.isFunction(s)) {
                    s = sortCompare;
                }
                quickSort(this, r.indexMap, 0, i, s);
                let o = false;
                for (i = 0, n = r.indexMap.length; n > i; ++i) {
                    if (r.indexMap[i] !== i) {
                        o = true;
                        break;
                    }
                }
                if (o || st) {
                    r.notify();
                }
                return this;
            }
        };
        for (const t of r) {
            i(n[t], "observing", {
                value: true
            });
        }
    }
    let o = false;
    const a = "__au_arr_on__";
    function enableArrayObservation() {
        if (n === undefined) {
            overrideArrayPrototypes();
        }
        if (!(f(a, Array) ?? false)) {
            d(true, Array, a);
            for (const t of r) {
                if (s[t].observing !== true) {
                    rtDefineHiddenProp(s, t, n[t]);
                }
            }
        }
    }
    class ArrayObserverImpl {
        constructor(t) {
            this.type = X;
            if (!o) {
                o = true;
                enableArrayObservation();
            }
            this.indexObservers = {};
            this.collection = t;
            this.indexMap = createIndexMap(t.length);
            this.lenObs = void 0;
            e.set(t, this);
        }
        notify() {
            const t = this.subs;
            t.notifyDirty();
            const e = this.indexMap;
            if (st) {
                addCollectionBatch(t, this.collection, e);
                return;
            }
            const s = this.collection;
            const r = s.length;
            this.indexMap = createIndexMap(r);
            t.notifyCollection(s, e);
        }
        getLengthObserver() {
            return this.lenObs ??= new CollectionLengthObserver(this);
        }
        getIndexObserver(t) {
            return this.indexObservers[t] ??= new ArrayIndexObserverImpl(this, t);
        }
    }
    (() => {
        rt(ArrayObserverImpl, null);
    })();
    class ArrayIndexObserverImpl {
        constructor(t, e) {
            this.owner = t;
            this.index = e;
            this.doNotCache = true;
            this.value = this.getValue();
        }
        getValue() {
            return this.owner.collection[this.index];
        }
        setValue(t) {
            if (t === this.getValue()) {
                return;
            }
            const e = this.owner;
            const s = this.index;
            const r = e.indexMap;
            if (r[s] > -1) {
                r.deletedIndices.push(r[s]);
            }
            r[s] = -2;
            e.collection[s] = t;
            e.notify();
        }
        handleDirty() {
            if (this.value !== this.getValue()) {
                this.subs.notifyDirty();
            }
        }
        handleCollectionChange(t, e) {
            const s = this.index;
            const r = e[s] === s;
            if (r) {
                return;
            }
            const n = this.value;
            const i = this.value = this.getValue();
            if (n !== i) {
                this.subs.notify(i, n);
            }
        }
        subscribe(t) {
            if (this.subs.add(t) && this.subs.count === 1) {
                this.owner.subscribe(this);
            }
        }
        unsubscribe(t) {
            if (this.subs.remove(t) && this.subs.count === 0) {
                this.owner.unsubscribe(this);
            }
        }
    }
    (() => {
        rt(ArrayIndexObserverImpl, null);
    })();
    return function getArrayObserver(t) {
        let s = e.get(t);
        if (s === void 0) {
            e.set(t, s = new ArrayObserverImpl(t));
            enableArrayObservation();
        }
        return s;
    };
})();

const it = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    const {add: e, clear: s, delete: r} = Set.prototype;
    const n = [ "add", "clear", "delete" ];
    const i = {
        add: function(s) {
            const r = t.get(this);
            if (r === undefined) {
                e.call(this, s);
                return this;
            }
            const n = this.size;
            e.call(this, s);
            const i = this.size;
            if (i === n) {
                return this;
            }
            r.indexMap[n] = -2;
            r.notify();
            return this;
        },
        clear: function() {
            const e = t.get(this);
            if (e === undefined) {
                return s.call(this);
            }
            const r = this.size;
            if (r > 0) {
                const t = e.indexMap;
                let r = 0;
                for (const e of this.keys()) {
                    if (t[r] > -1) {
                        t.deletedIndices.push(t[r]);
                        t.deletedItems.push(e);
                    }
                    r++;
                }
                s.call(this);
                t.length = 0;
                e.notify();
            }
            return undefined;
        },
        delete: function(e) {
            const s = t.get(this);
            if (s === undefined) {
                return r.call(this, e);
            }
            const n = this.size;
            if (n === 0) {
                return false;
            }
            let i = 0;
            const o = s.indexMap;
            for (const t of this.keys()) {
                if (t === e) {
                    if (o[i] > -1) {
                        o.deletedIndices.push(o[i]);
                        o.deletedItems.push(t);
                    }
                    o.splice(i, 1);
                    const n = r.call(this, e);
                    if (n === true) {
                        s.notify();
                    }
                    return n;
                }
                i++;
            }
            return false;
        }
    };
    function enableSetObservation(t) {
        for (const e of n) {
            rtDefineHiddenProp(t, e, i[e]);
        }
    }
    class SetObserverImpl {
        constructor(t) {
            this.type = X;
            this.collection = t;
            this.indexMap = createIndexMap(t.size);
            this.lenObs = void 0;
        }
        notify() {
            const t = this.subs;
            t.notifyDirty();
            const e = this.indexMap;
            if (st) {
                addCollectionBatch(t, this.collection, e);
                return;
            }
            const s = this.collection;
            const r = s.size;
            this.indexMap = createIndexMap(r);
            t.notifyCollection(s, e);
        }
        getLengthObserver() {
            return this.lenObs ??= new CollectionSizeObserver(this);
        }
    }
    rt(SetObserverImpl, null);
    return function getSetObserver(e) {
        let s = t.get(e);
        if (s === void 0) {
            t.set(e, s = new SetObserverImpl(e));
            enableSetObservation(e);
        }
        return s;
    };
})();

const ot = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    const {set: e, clear: s, delete: r} = Map.prototype;
    const n = [ "set", "clear", "delete" ];
    const i = {
        set: function(s, r) {
            const n = t.get(this);
            if (n === undefined) {
                e.call(this, s, r);
                return this;
            }
            const i = this.get(s);
            const o = this.size;
            e.call(this, s, r);
            const a = this.size;
            if (a === o) {
                let t = 0;
                for (const e of this.entries()) {
                    if (e[0] === s) {
                        if (e[1] !== i) {
                            n.indexMap.deletedIndices.push(n.indexMap[t]);
                            n.indexMap.deletedItems.push(e);
                            n.indexMap[t] = -2;
                            n.notify();
                        }
                        return this;
                    }
                    t++;
                }
                return this;
            }
            n.indexMap[o] = -2;
            n.notify();
            return this;
        },
        clear: function() {
            const e = t.get(this);
            if (e === undefined) {
                return s.call(this);
            }
            const r = this.size;
            if (r > 0) {
                const t = e.indexMap;
                let r = 0;
                for (const e of this.keys()) {
                    if (t[r] > -1) {
                        t.deletedIndices.push(t[r]);
                        t.deletedItems.push(e);
                    }
                    r++;
                }
                s.call(this);
                t.length = 0;
                e.notify();
            }
            return undefined;
        },
        delete: function(e) {
            const s = t.get(this);
            if (s === undefined) {
                return r.call(this, e);
            }
            const n = this.size;
            if (n === 0) {
                return false;
            }
            let i = 0;
            const o = s.indexMap;
            for (const t of this.keys()) {
                if (t === e) {
                    if (o[i] > -1) {
                        o.deletedIndices.push(o[i]);
                        o.deletedItems.push(t);
                    }
                    o.splice(i, 1);
                    const n = r.call(this, e);
                    if (n === true) {
                        s.notify();
                    }
                    return n;
                }
                ++i;
            }
            return false;
        }
    };
    function enableMapObservation(t) {
        for (const e of n) {
            rtDefineHiddenProp(t, e, i[e]);
        }
    }
    class MapObserverImpl {
        constructor(t) {
            this.type = X;
            this.collection = t;
            this.indexMap = createIndexMap(t.size);
            this.lenObs = void 0;
        }
        notify() {
            const t = this.subs;
            t.notifyDirty();
            const e = this.indexMap;
            if (st) {
                addCollectionBatch(t, this.collection, e);
                return;
            }
            const s = this.collection;
            const r = s.size;
            this.indexMap = createIndexMap(r);
            t.notifyCollection(s, e);
        }
        getLengthObserver() {
            return this.lenObs ??= new CollectionSizeObserver(this);
        }
    }
    rt(MapObserverImpl, null);
    return function getMapObserver(e) {
        let s = t.get(e);
        if (s === void 0) {
            t.set(e, s = new MapObserverImpl(e));
            enableMapObservation(e);
        }
        return s;
    };
})();

const at = /*@__PURE__*/ (() => {
    class BindingObserverRecord {
        constructor(t) {
            this.version = 0;
            this.count = 0;
            this.o = new Map;
            this.b = t;
        }
        add(t) {
            if (!this.o.has(t)) {
                t.subscribe(this.b);
                ++this.count;
            }
            this.o.set(t, this.version);
        }
        clear() {
            this.o.forEach(unsubscribeStale, this);
            this.count = this.o.size;
        }
        clearAll() {
            this.o.forEach(unsubscribeAll, this);
            this.o.clear();
            this.count = 0;
        }
    }
    function unsubscribeAll(t, e) {
        e.unsubscribe(this.b);
    }
    function unsubscribeStale(t, e) {
        if (this.version !== t) {
            e.unsubscribe(this.b);
            this.o.delete(e);
        }
    }
    function getObserverRecord() {
        return rtDefineHiddenProp(this, "obs", new BindingObserverRecord(this));
    }
    function observe(t, e) {
        this.obs.add(this.oL.getObserver(t, e));
    }
    function observeExpression(t, e) {
        this.obs.add(this.oL.getExpressionObserver(t, e));
    }
    function observeCollection(e) {
        let s;
        if (t.isArray(e)) {
            s = nt(e);
        } else if (t.isSet(e)) {
            s = it(e);
        } else if (t.isMap(e)) {
            s = ot(e);
        } else {
            throw createMappedError(210, e);
        }
        this.obs.add(s);
    }
    function subscribeTo(t) {
        this.obs.add(t);
    }
    function noopHandleChange() {
        throw createMappedError(99, "handleChange");
    }
    function noopHandleCollectionChange() {
        throw createMappedError(99, "handleCollectionChange");
    }
    return function connectableDecorator(t, e) {
        const s = t.prototype;
        ensureProto(s, "observe", observe);
        ensureProto(s, "observeExpression", observeExpression);
        ensureProto(s, "observeCollection", observeCollection);
        ensureProto(s, "subscribeTo", subscribeTo);
        i(s, "obs", {
            get: getObserverRecord
        });
        ensureProto(s, "handleChange", noopHandleChange);
        ensureProto(s, "handleCollectionChange", noopHandleCollectionChange);
        return t;
    };
})();

function connectable(t, e) {
    return t == null ? at : at(t, e);
}

class ComputedObserver {
    constructor(t, e, s, r, n = "async") {
        this.type = X;
        this.ov = void 0;
        this.v = void 0;
        this.N = false;
        this.B = false;
        this.D = false;
        this.cb = void 0;
        this.L = void 0;
        this.F = void 0;
        this.o = t;
        this.$ = wrap(t);
        this.$get = e;
        this.$set = s;
        this.oL = r;
        this.H = n;
    }
    init(t) {
        this.v = t;
        this.D = false;
    }
    getValue() {
        if (this.subs.count === 0) {
            return this.$get.call(this.o, this.o, this);
        }
        if (this.D) {
            this.compute();
            this.N = false;
        }
        return this.v;
    }
    setValue(e) {
        if (t.isFunction(this.$set)) {
            if (this.L !== void 0) {
                e = this.L.call(null, e, this.F);
            }
            if (!t.areEqual(e, this.v)) {
                this.$set.call(this.o, e);
                this.run();
            }
        } else {
            throw createMappedError(221);
        }
    }
    useCoercer(t, e) {
        this.L = t;
        this.F = e;
        return true;
    }
    useCallback(t) {
        this.cb = t;
        return true;
    }
    useFlush(t) {
        this.H = t;
        return true;
    }
    handleDirty() {
        if (!this.D) {
            this.D = true;
            this.subs.notifyDirty();
        }
    }
    handleChange() {
        this.D = true;
        if (this.subs.count > 0) {
            this.run();
        }
    }
    handleCollectionChange() {
        this.D = true;
        if (this.subs.count > 0) {
            this.run();
        }
    }
    subscribe(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            this.ov = this.compute();
            this.D = false;
            this.N = false;
        }
    }
    unsubscribe(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            this.D = true;
            this.obs.clearAll();
            this.ov = void 0;
            this.N = true;
        }
    }
    run() {
        if (this.H === "sync") {
            this.U();
            return;
        }
        if (this.B) {
            return;
        }
        this.B = true;
        queueTask(() => {
            this.B = false;
            this.U();
        });
    }
    U() {
        const e = this.v;
        const s = this.ov;
        const r = this.compute();
        this.D = false;
        if (!this.N || !t.areEqual(r, e)) {
            this.subs.notify(r, s);
            this.ov = this.v = r;
            this.cb?.(r, s);
            this.N = true;
        }
    }
    compute() {
        this.D = false;
        this.obs.version++;
        try {
            enterConnectable(this);
            const t = unwrap(this.$get.call(this.$, this.$, this));
            if (this.D) {
                throw createMappedError(227, this.$get.name ?? this.$get.toString());
            }
            return this.v = t;
        } catch (t) {
            this.D = true;
            throw t;
        } finally {
            this.obs.clear();
            exitConnectable(this);
        }
    }
}

(() => {
    connectable(ComputedObserver, null);
    rt(ComputedObserver, null);
})();

typeof SuppressedError === "function" ? SuppressedError : function(t, e, s) {
    var r = new Error(s);
    return r.name = "SuppressedError", r.error = t, r.suppressed = e, r;
};

function isClassMethodDecoratorContext(t) {
    return t?.kind === "method";
}

function computed(e, ...s) {
    if (t.isFunction(e) && isClassMethodDecoratorContext(s[0])) {
        const t = e;
        o(t, {
            [P]: {
                deps: void 0
            }
        });
        return;
    }
    return function universalDecorator(r, n) {
        if (n.kind === "method") {
            const n = {};
            if (typeof e === "string" || typeof e === "symbol") {
                n.deps = [ e, ...s ];
            } else if (t.isFunction(e)) {
                n.deps = e;
            } else if (e != null && typeof e === "object") {
                const t = e;
                if (Object.prototype.hasOwnProperty.call(t, "deps")) {
                    n.deps = t.deps;
                }
            }
            const i = r;
            o(i, {
                [P]: {
                    deps: n.deps
                }
            });
            return;
        }
        const i = t.isObject(e) ? e : {};
        if (!t.isObject(e)) {
            i.deps = [ e, ...s ];
        }
        const a = r;
        const c = n;
        if (c.kind !== "getter") {
            throw createMappedError(228, c.name);
        }
        const u = new WeakMap;
        return o(function computedGetter() {
            const t = u.get(getRaw(this));
            return t == null ? a.call(getRaw(this)) : t.getValue();
        }, {
            getObserver(t, e) {
                let s = u.get(t);
                if (s == null) {
                    s = e.getComputedObserver(t, c.name, {
                        get: a
                    }, i);
                    u.set(t, s);
                }
                return s;
            }
        });
    };
}

const ct = /*@__PURE__*/ l("IDirtyChecker", void 0);

const ut = {
    timeoutsPerCheck: 25,
    disabled: false,
    throw: false,
    resetToDefault() {
        this.timeoutsPerCheck = 6;
        this.disabled = false;
        this.throw = false;
    }
};

class DirtyChecker {
    static register(e) {
        e.register(t.Registration.singleton(this, this), t.Registration.aliasTo(this, ct));
    }
    constructor() {
        this.tracked = [];
        this.q = null;
        this.W = 0;
        this.p = t.resolve(t.IPlatform);
        this.check = () => {
            if (ut.disabled) {
                return;
            }
            if (++this.W < ut.timeoutsPerCheck) {
                return;
            }
            this.W = 0;
            const t = this.tracked.slice(0);
            const e = t.length;
            let s;
            let r = 0;
            for (;r < e; ++r) {
                s = t[r];
                if (s.isDirty()) {
                    s.flush();
                }
            }
        };
        rt(DirtyCheckProperty, null);
    }
    createProperty(t, e) {
        if (ut.throw) {
            throw createMappedError(218, e);
        }
        return new DirtyCheckProperty(this, t, e);
    }
    addProperty(t) {
        this.tracked.push(t);
        if (this.tracked.length === 1) {
            this.q = queueRecurringTask(this.check, {
                interval: 0
            });
        }
    }
    removeProperty(t) {
        this.tracked.splice(this.tracked.indexOf(t), 1);
        if (this.tracked.length === 0) {
            this.q.cancel();
            this.q = null;
        }
    }
}

class DirtyCheckProperty {
    constructor(t, e, s) {
        this.obj = e;
        this.key = s;
        this.type = Q;
        this.ov = void 0;
        this.K = t;
    }
    getValue() {
        return this.obj[this.key];
    }
    setValue(t) {
        throw createMappedError(219, this.key);
    }
    isDirty() {
        return this.ov !== this.obj[this.key];
    }
    flush() {
        const t = this.ov;
        const e = this.getValue();
        this.ov = e;
        this.subs.notify(e, t);
    }
    subscribe(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            this.ov = this.obj[this.key];
            this.K.addProperty(this);
        }
    }
    unsubscribe(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            this.K.removeProperty(this);
        }
    }
}

class PrimitiveObserver {
    get doNotCache() {
        return true;
    }
    constructor(t, e) {
        this.type = Q;
        this.o = t;
        this.k = e;
    }
    getValue() {
        return this.o[this.k];
    }
    setValue() {}
    subscribe() {}
    unsubscribe() {}
}

class PropertyAccessor {
    constructor() {
        this.type = Q;
    }
    getValue(t, e) {
        return t[e];
    }
    setValue(t, e, s) {
        e[s] = t;
    }
}

class SetterObserver {
    constructor(t, e) {
        this.type = X;
        this.v = void 0;
        this.iO = false;
        this.cb = void 0;
        this.L = void 0;
        this.F = void 0;
        this.o = t;
        this.k = e;
    }
    getValue() {
        return this.v;
    }
    setValue(e) {
        if (this.L !== void 0) {
            e = this.L.call(void 0, e, this.F);
        }
        const s = this.v;
        if (this.iO) {
            if (t.areEqual(e, this.v)) {
                return;
            }
            this.v = e;
            this.subs.notifyDirty();
            this.subs.notify(e, s);
            if (t.areEqual(e, this.v)) {
                this.cb?.(e, s);
            }
        } else {
            this.v = this.o[this.k] = e;
            this.cb?.(e, s);
        }
    }
    useCallback(t) {
        this.cb = t;
        this.start();
        return true;
    }
    useCoercer(t, e) {
        this.L = t;
        this.F = e;
        this.start();
        return true;
    }
    subscribe(t) {
        if (this.iO === false) {
            this.start();
        }
        this.subs.add(t);
    }
    start() {
        if (this.iO === false) {
            this.iO = true;
            this.v = this.o[this.k];
            i(this.o, this.k, {
                enumerable: true,
                configurable: true,
                get: o(() => this.getValue(), {
                    getObserver: () => this
                }),
                set: t => {
                    this.setValue(t);
                }
            });
        }
        return this;
    }
    stop() {
        if (this.iO) {
            i(this.o, this.k, {
                enumerable: true,
                configurable: true,
                writable: true,
                value: this.v
            });
            this.iO = false;
        }
        return this;
    }
}

(() => {
    rt(SetterObserver, null);
})();

const getObserverLookup = e => {
    let s = e.$observers;
    if (s === void 0) {
        i(e, "$observers", {
            value: s = t.createLookup()
        });
    }
    return s;
};

class ExpressionObserver {
    get type() {
        return X;
    }
    constructor(t, e, s) {
        this.oL = e;
        this.v = void 0;
        this.boundFn = false;
        this.s = Scope.create(t);
        this.ast = s;
    }
    getValue() {
        return this.v;
    }
    setValue(t) {
        D(this.ast, this.s, this, null, t);
    }
    useCallback(t) {
        this.cb = t;
        return true;
    }
    handleChange() {
        this.run();
    }
    handleCollectionChange() {
        this.run();
    }
    run() {
        this.obs.version++;
        const e = this.v;
        const s = I(this.ast, this.s, this, this);
        this.obs.clear();
        if (!t.areEqual(s, e)) {
            this.v = s;
            this.subs.notify(s, e);
            this.cb?.call(void 0, s, e);
        }
    }
    subscribe(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            this.A();
        }
    }
    unsubscribe(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            this.stop();
        }
    }
    A() {
        this.obs.version++;
        this.v = I(this.ast, this.s, this, this);
        this.obs.clear();
    }
    stop() {
        this.obs.clearAll();
        this.v = void 0;
    }
}

(() => {
    connectable(ExpressionObserver, null);
    rt(ExpressionObserver, null);
    T(ExpressionObserver);
})();

class ControlledComputedObserver {
    constructor(t, e, s, r, n, i, o) {
        this.obj = t;
        this.key = e;
        this.getter = s;
        this.oL = r;
        this.dependencies = n;
        this.flush = i;
        this.deep = o;
        this.type = X;
        this.doNotCache = false;
        this.v = void 0;
        this.G = [];
        this.J = false;
        this.X = false;
        this.cb = void 0;
        this.L = void 0;
        this.F = void 0;
    }
    getValue() {
        if (!this.X) {
            return this.Y();
        }
        return this.v;
    }
    setValue(t) {
        if (this.L != null) {
            t = this.L.call(null, t, this.F);
        }
        this.obj[this.key] = t;
    }
    useCallback(t) {
        this.cb = t;
        return true;
    }
    useCoercer(t, e) {
        this.L = t;
        this.F = e;
        return true;
    }
    handleChange() {
        if (this.flush === "sync") {
            this.Z();
            return;
        }
        if (this.J) {
            return;
        }
        this.J = true;
        queueTask(() => {
            this.J = false;
            this.Z();
        });
    }
    handleCollectionChange() {
        if (this.flush === "sync") {
            this.Z();
            return;
        }
        if (this.J) {
            return;
        }
        this.J = true;
        queueTask(() => {
            this.J = false;
            this.Z();
        });
    }
    Y() {
        return this.getter.call(this.obj);
    }
    Z() {
        if (!this.X) {
            return;
        }
        const t = this.v;
        const e = this.Y();
        this.tt();
        this.et();
        if (t === e) {
            return;
        }
        this.v = e;
        this.subs.notify(e, t);
        this.cb?.(e, t);
    }
    et() {
        const e = this.G;
        this.dependencies.forEach(s => {
            let r = t.isString(s) ? this.oL.getExpressionObserver(this.obj, s) : this.oL.getObserver(this.obj, s);
            e.push(r);
            r.subscribe(this);
            r.useFlush?.(this.flush);
            if (this.deep) {
                r = observeDeep(r.getValue(), this.oL);
                e.push(r);
                r.subscribe(this);
                r.useFlush?.(this.flush);
            }
        });
    }
    tt() {
        this.G.forEach(t => {
            t.unsubscribe(this);
        });
        this.G.length = 0;
    }
    subscribe(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            this.et();
            this.X = true;
            this.v = this.Y();
        }
    }
    unsubscribe(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            this.tt();
            this.X = false;
            this.v = void 0;
        }
    }
}

(() => {
    rt(ControlledComputedObserver, null);
})();

function observeDeep(e, s) {
    function walk(e, s) {
        const r = unwrap(e);
        if (!t.isObject(r)) {
            return;
        }
        if (t.isArray(r)) {
            s.observeCollection(r);
            for (let t = 0; t < r.length; t++) {
                walk(r[t], s);
            }
            return;
        }
        if (t.isMap(r)) {
            s.observeCollection(r);
            for (const [t, e] of r) {
                walk(t, s);
                walk(e, s);
            }
            return;
        }
        if (t.isSet(r)) {
            s.observeCollection(r);
            for (const t of r) {
                walk(t, s);
            }
            return;
        }
        for (const t of Object.keys(r)) {
            s.observe(r, t);
            walk(r[t], s);
        }
    }
    return s.getObserver(e, (t, e) => {
        walk(t, e);
    });
}

const ht = new PropertyAccessor;

const lt = /*@__PURE__*/ l("IObserverLocator", t => t.singleton(ObserverLocator));

const ft = /*@__PURE__*/ l("INodeObserverLocator", t => t.cachedCallback(t => new DefaultNodeObserverLocator));

class DefaultNodeObserverLocator {
    handles() {
        return false;
    }
    getObserver() {
        return ht;
    }
    getAccessor() {
        return ht;
    }
}

class ObserverLocator {
    constructor() {
        this.st = [];
        this.K = t.resolve(ct);
        this.rt = t.resolve(ft);
        this.nt = t.resolve(t.optional(r.IExpressionParser));
    }
    addAdapter(t) {
        this.st.push(t);
    }
    getObserver(e, s) {
        if (e == null) {
            throw createMappedError(199, s);
        }
        if (!t.isObject(e)) {
            return new PrimitiveObserver(e, t.isFunction(s) ? "" : s);
        }
        if (t.isFunction(s)) {
            return new ComputedObserver(e, s, void 0, this);
        }
        const r = getObserverLookup(e);
        let n = r[s];
        if (n === void 0) {
            n = this.createObserver(e, s);
            if (!n.doNotCache) {
                r[s] = n;
            }
        }
        return n;
    }
    getAccessor(t, e) {
        const s = t.$observers?.[e];
        if (s !== void 0) {
            return s;
        }
        if (this.rt.handles(t, e, this)) {
            return this.rt.getAccessor(t, e, this);
        }
        return ht;
    }
    getExpressionObserver(t, e) {
        if (this.nt == null) {
            throw createMappedError(200, e);
        }
        return new ExpressionObserver(t, this, this.nt.parse(e, "IsProperty"));
    }
    getComputedObserver(t, e, s, r) {
        if (r?.deps == null) {
            const n = new ComputedObserver(t, s.get, s.set, this, r?.flush);
            i(t, e, {
                enumerable: s.enumerable,
                configurable: true,
                get: o(() => n.getValue(), {
                    getObserver: () => n
                }),
                set: t => {
                    n.setValue(t);
                }
            });
            return n;
        }
        return new ControlledComputedObserver(t, e, s.get, this, r.deps, r.flush ?? "async", r.deep === true);
    }
    getArrayObserver(t) {
        return nt(t);
    }
    getMapObserver(t) {
        return ot(t);
    }
    getSetObserver(t) {
        return it(t);
    }
    createObserver(e, s) {
        if (this.rt.handles(e, s, this)) {
            return this.rt.getObserver(e, s, this);
        }
        switch (s) {
          case "length":
            if (t.isArray(e)) {
                return nt(e).getLengthObserver();
            }
            break;

          case "size":
            if (t.isMap(e)) {
                return ot(e).getLengthObserver();
            } else if (t.isSet(e)) {
                return it(e).getLengthObserver();
            }
            break;

          default:
            if (t.isArray(e) && t.isArrayIndex(s)) {
                return nt(e).getIndexObserver(Number(s));
            }
            break;
        }
        let r = u(e, s);
        if (r === void 0) {
            let t = c(e);
            while (t !== null) {
                r = u(t, s);
                if (r === void 0) {
                    t = c(t);
                } else {
                    break;
                }
            }
        }
        if (r !== void 0 && !n.call(r, "value")) {
            let t = this.it(e, s, r);
            if (t == null) {
                t = (r.get?.getObserver)?.(e, this);
            }
            return t == null ? r.configurable ? this.getComputedObserver(e, s, r) : this.K.createProperty(e, s) : t;
        }
        return new SetterObserver(e, s);
    }
    it(t, e, s) {
        if (this.st.length > 0) {
            for (const r of this.st) {
                const n = r.getObserver(t, e, s, this);
                if (n != null) {
                    return n;
                }
            }
        }
        return null;
    }
}

const getCollectionObserver = e => {
    let s;
    if (t.isArray(e)) {
        s = nt(e);
    } else if (t.isMap(e)) {
        s = ot(e);
    } else if (t.isSet(e)) {
        s = it(e);
    }
    return s;
};

const dt = /*@__PURE__*/ l("IObservation", t => t.singleton(Observation));

class Observation {
    constructor() {
        this.oL = t.resolve(lt);
    }
    run(t) {
        const e = new RunEffect(this.oL, t);
        e.run();
        return e;
    }
    watch(t, e, s, r) {
        return this.ot(t, e, s, r);
    }
    ot(e, s, r, n) {
        let i = undefined;
        let o = false;
        let a;
        const handleChange = (e, s) => {
            a?.();
            a = void 0;
            const n = r(e, i = s);
            if (t.isFunction(n)) {
                a = n;
            }
        };
        const c = t.isString(s) ? this.oL.getExpressionObserver(e, s) : this.oL.getObserver(e, s);
        const u = {
            handleChange: handleChange
        };
        const run = () => {
            if (o) {
                return;
            }
            handleChange(c.getValue(), i);
        };
        const stop = () => {
            if (o) {
                throw createMappedError(225);
            }
            o = true;
            c.unsubscribe(u);
            a?.();
            a = void 0;
        };
        c.subscribe(u);
        if (n?.immediate !== false) {
            run();
        }
        return {
            run: run,
            stop: stop
        };
    }
}

class RunEffect {
    constructor(t, e) {
        this.oL = t;
        this.fn = e;
        this.maxRunCount = 10;
        this.queued = false;
        this.running = false;
        this.runCount = 0;
        this.stopped = false;
        this.ct = undefined;
        this.run = () => {
            if (this.running || this.stopped) {
                return;
            }
            ++this.runCount;
            this.running = true;
            this.queued = false;
            ++this.obs.version;
            try {
                this.ct?.call(void 0);
                enterConnectable(this);
                this.ct = this.fn(this);
            } finally {
                this.obs.clear();
                this.running = false;
                exitConnectable(this);
            }
            if (this.queued) {
                if (this.runCount > this.maxRunCount) {
                    this.runCount = 0;
                    throw createMappedError(226);
                }
                this.run();
            } else {
                this.runCount = 0;
            }
        };
        this.stop = () => {
            if (this.stopped) {
                throw createMappedError(225);
            }
            this.stopped = true;
            this.obs.clearAll();
            this.ct?.call(void 0);
            this.ct = void 0;
        };
    }
    handleChange() {
        this.queued = true;
        this.run();
    }
    handleCollectionChange() {
        this.queued = true;
        this.run();
    }
}

(() => {
    connectable(RunEffect, null);
})();

const pt = /*@__PURE__*/ (() => {
    const e = {};
    function observable(s, r) {
        if (!SetterNotifier.mixed) {
            SetterNotifier.mixed = true;
            rt(SetterNotifier, null);
        }
        let n = false;
        let o;
        if (typeof s === "object") {
            o = s;
        } else if (s != null) {
            o = {
                name: s
            };
            n = true;
        } else {
            o = t.emptyObject;
        }
        if (arguments.length === 0) {
            return function(t, e) {
                if (e.kind !== "field") throw createMappedError(224);
                return createFieldInitializer(e);
            };
        }
        if (r?.kind === "field") return createFieldInitializer(r);
        if (n) {
            return function(t, s) {
                createDescriptor(t, o.name, () => e, true);
            };
        }
        return function(t, s) {
            switch (s.kind) {
              case "field":
                return createFieldInitializer(s);

              case "class":
                return createDescriptor(t, o.name, () => e, true);

              default:
                throw createMappedError(224);
            }
        };
        function createFieldInitializer(t) {
            let e;
            t.addInitializer(function() {
                createDescriptor(this, t.name, () => e, false);
            });
            return function(t) {
                return e = t;
            };
        }
        function createDescriptor(t, e, s, r) {
            const n = o.callback || `${h(e)}Changed`;
            const a = o.set;
            const observableGetter = function() {
                const t = getNotifier(this, e, n, s, a);
                currentConnectable()?.subscribeTo(t);
                return t.getValue();
            };
            observableGetter.getObserver = function(t) {
                return getNotifier(t, e, n, s, a);
            };
            const c = {
                enumerable: true,
                configurable: true,
                get: observableGetter,
                set(t) {
                    getNotifier(this, e, n, s, a).setValue(t);
                }
            };
            if (r) i(t.prototype, e, c); else i(t, e, c);
        }
    }
    function getNotifier(t, s, r, n, i) {
        const o = getObserverLookup(t);
        let a = o[s];
        if (a == null) {
            const c = n();
            a = new SetterNotifier(t, r, i, c === e ? void 0 : c);
            o[s] = a;
        }
        return a;
    }
    class SetterNotifier {
        constructor(e, s, r, n) {
            this.type = X;
            this.v = void 0;
            this.ov = void 0;
            this.o = e;
            this.S = r;
            this.hs = t.isFunction(r);
            const i = e[s];
            this.cb = t.isFunction(i) ? i : void 0;
            this.v = n;
        }
        getValue() {
            return this.v;
        }
        setValue(e) {
            if (this.hs) {
                e = this.S(e);
            }
            if (!t.areEqual(e, this.v)) {
                this.ov = this.v;
                this.v = e;
                this.subs.notifyDirty();
                this.subs.notify(this.v, this.ov);
                if (t.areEqual(e, this.v)) {
                    this.cb?.call(this.o, this.v, this.ov);
                }
            }
        }
    }
    SetterNotifier.mixed = false;
    return observable;
})();

function nowrap(t, e) {
    return arguments.length === 0 ? decorator : decorator(t, e);
    function decorator(t, e) {
        switch (e.kind) {
          case "class":
            rtDefineHiddenProp(t, x, true);
            break;

          case "field":
            e.addInitializer(function() {
                const t = this.constructor;
                const s = `${E}_${h(e.name)}__`;
                if (s in t) return;
                rtDefineHiddenProp(t, s, true);
            });
            break;
        }
    }
}

exports.AccessorType = tt;

exports.BindingContext = BindingContext;

exports.CollectionLengthObserver = CollectionLengthObserver;

exports.CollectionSizeObserver = CollectionSizeObserver;

exports.ComputedObserver = ComputedObserver;

exports.ConnectableSwitcher = v;

exports.DirtyCheckProperty = DirtyCheckProperty;

exports.DirtyCheckSettings = ut;

exports.DirtyChecker = DirtyChecker;

exports.ICoercionConfiguration = J;

exports.IDirtyChecker = ct;

exports.INodeObserverLocator = ft;

exports.IObservation = dt;

exports.IObserverLocator = lt;

exports.Observation = Observation;

exports.ObserverLocator = ObserverLocator;

exports.PrimitiveObserver = PrimitiveObserver;

exports.PropertyAccessor = PropertyAccessor;

exports.ProxyObservable = R;

exports.RecurringTask = RecurringTask;

exports.Scope = Scope;

exports.SetterObserver = SetterObserver;

exports.Task = Task;

exports.TaskAbortError = TaskAbortError;

exports.astAssign = D;

exports.astBind = M;

exports.astEvaluate = I;

exports.astTrack = astTrack;

exports.astUnbind = _;

exports.batch = batch;

exports.cloneIndexMap = cloneIndexMap;

exports.computed = computed;

exports.connectable = connectable;

exports.copyIndexMap = copyIndexMap;

exports.createIndexMap = createIndexMap;

exports.getCollectionObserver = getCollectionObserver;

exports.getObserverLookup = getObserverLookup;

exports.getRecurringTasks = getRecurringTasks;

exports.isIndexMap = isIndexMap;

exports.isTaskQueueEmpty = isTaskQueueEmpty;

exports.mixinNoopAstEvaluator = T;

exports.nowrap = nowrap;

exports.observable = pt;

exports.queueAsyncTask = queueAsyncTask;

exports.queueRecurringTask = queueRecurringTask;

exports.queueTask = queueTask;

exports.runTasks = runTasks;

exports.subscriberCollection = rt;

exports.tasksSettled = tasksSettled;
//# sourceMappingURL=index.cjs.map
