import { DI as t, isSymbol as e, isArray as s, isMap as r, isSet as n, isFunction as i, isObject as o, isObjectOrFunction as a, isArrayIndex as c, noop as u, areEqual as h, Registration as l, resolve as f, IPlatform as d, createLookup as p, isString as b, optional as w, emptyObject as v } from "@aurelia/kernel";

import { Metadata as g } from "@aurelia/metadata";

import { Platform as y } from "@aurelia/platform";

import { IExpressionParser as C } from "@aurelia/expression-parser";

const E = Object.prototype.hasOwnProperty;

const O = Reflect.defineProperty;

function rtDefineHiddenProp(t, e, s) {
    O(t, e, {
        configurable: true,
        writable: true,
        value: s
    });
    return s;
}

function ensureProto(t, e, s) {
    if (!(e in t)) {
        rtDefineHiddenProp(t, e, s);
    }
}

const A = Object.assign;

const m = Object.freeze;

const k = Object.getPrototypeOf;

const S = Object.getOwnPropertyDescriptor;

const x = String;

const R = t.createInterface;

const P = g.get;

const D = g.define;

const createMappedError = (t, ...e) => new Error(`AUR${x(t).padStart(4, "0")}:${e.map(x)}`);

class Scope {
    constructor(t, e, s, r) {
        this.parent = t;
        this.bindingContext = e;
        this.overrideContext = s;
        this.isBoundary = r;
    }
    static getContext(t, e, s) {
        if (t == null) {
            throw createMappedError(203);
        }
        let r = t.overrideContext;
        let n = t;
        if (s > 0) {
            while (s > 0) {
                s--;
                n = n.parent;
                if (n == null) {
                    return void 0;
                }
            }
            r = n.overrideContext;
            return e in r ? r : n.bindingContext;
        }
        while (n != null && !n.isBoundary && !(e in n.overrideContext) && !(e in n.bindingContext)) {
            n = n.parent;
        }
        if (n == null) {
            return t.bindingContext;
        }
        r = n.overrideContext;
        return e in r ? r : n.bindingContext;
    }
    static create(t, e, s) {
        if (t == null) {
            throw createMappedError(204);
        }
        return new Scope(null, t, e ?? new OverrideContext, s ?? false);
    }
    static fromParent(t, e, s = new OverrideContext) {
        if (t == null) {
            throw createMappedError(203);
        }
        return new Scope(t, e, s, false);
    }
}

class BindingContext {
    constructor(t, e) {
        if (t !== void 0) {
            this[t] = e;
        }
    }
}

class OverrideContext {}

let I = null;

const M = [];

let _ = false;

function pauseConnecting() {
    _ = false;
}

function resumeConnecting() {
    _ = true;
}

function currentConnectable() {
    return I;
}

function enterConnectable(t) {
    if (t == null) {
        throw createMappedError(206);
    }
    if (I == null) {
        I = t;
        M[0] = I;
        _ = true;
        return;
    }
    if (I === t) {
        throw createMappedError(207);
    }
    M.push(t);
    I = t;
    _ = true;
}

function exitConnectable(t) {
    if (t == null) {
        throw createMappedError(208);
    }
    if (I !== t) {
        throw createMappedError(209);
    }
    M.pop();
    I = M.length > 0 ? M[M.length - 1] : null;
    _ = I != null;
}

const T = /*@__PURE__*/ m({
    get current() {
        return I;
    },
    get connecting() {
        return _;
    },
    enter: enterConnectable,
    exit: exitConnectable,
    pause: pauseConnecting,
    resume: resumeConnecting
});

const V = Reflect.get;

const j = Object.prototype.toString;

const N = new WeakMap;

const B = "__au_nw__";

const L = "__au_nw";

function canWrap(t) {
    switch (j.call(t)) {
      case "[object Object]":
        return t.constructor[B] !== true;

      case "[object Array]":
      case "[object Map]":
      case "[object Set]":
        return true;

      default:
        return false;
    }
}

const F = "__raw__";

function wrap(t) {
    return canWrap(t) ? getProxy(t) : t;
}

function getProxy(t) {
    return N.get(t) ?? createProxy(t);
}

function getRaw(t) {
    return t[F] ?? t;
}

function unwrap(t) {
    return canWrap(t) && t[F] || t;
}

const $ = new Set(Object.getOwnPropertyNames(Symbol).map(t => Symbol[t]).filter(t => e(t) && t !== Symbol.iterator));

function doNotCollect(t, s) {
    if (s === "constructor" || s === "__proto__" || s === "$observers" || e(s) && $.has(s) || t.constructor[`${L}_${x(s)}__`] === true) {
        return true;
    }
    const r = Reflect.getOwnPropertyDescriptor(t, s);
    return r?.configurable === false && r.writable === false;
}

function createProxy(t) {
    const e = s(t) ? z : r(t) || n(t) ? U : H;
    const i = new Proxy(t, e);
    N.set(t, i);
    N.set(i, i);
    return i;
}

function observeTrackableMethodDependencies(t, e, s) {
    if (e == null || typeof e !== "object") {
        return;
    }
    const r = s.deps;
    if (r == null) {
        return;
    }
    const n = i(r) ? [ r ] : r;
    for (const s of n) {
        if (typeof s === "string") {
            t.observeExpression(e, s);
        } else {
            s(wrap(e));
        }
    }
}

const H = {
    get(t, e, s) {
        if (e === F) {
            return t;
        }
        const r = I;
        if (!_ || r == null || doNotCollect(t, e)) {
            return V(t, e, s);
        }
        r.observe(t, e);
        const n = V(t, e, s);
        if (!i(n)) {
            return wrap(n);
        }
        const a = n[q];
        if (a == null) {
            return wrap(n);
        }
        return function trackableMethod(...t) {
            const e = I;
            if (!_ || e == null) {
                return n.apply(this, t);
            }
            const r = this ?? s;
            const i = o(r) ? getRaw(r) : r;
            observeTrackableMethodDependencies(e, i, a);
            const c = a.deps == null;
            const u = c && o(i) ? wrap(i) : i;
            const h = c ? t.map(t => wrap(t)) : t.map(t => unwrap(t));
            return n.apply(u, h);
        };
    },
    deleteProperty(t, e) {
        return delete t[e];
    }
};

const z = {
    get(t, e, s) {
        if (e === F) {
            return t;
        }
        if (!_ || doNotCollect(t, e) || I == null) {
            return V(t, e, s);
        }
        switch (e) {
          case "length":
            I.observe(t, "length");
            return t.length;

          case "map":
            return wrappedArrayMap;

          case "includes":
            return wrappedArrayIncludes;

          case "indexOf":
            return wrappedArrayIndexOf;

          case "lastIndexOf":
            return wrappedArrayLastIndexOf;

          case "every":
            return wrappedArrayEvery;

          case "filter":
            return wrappedArrayFilter;

          case "find":
            return wrappedArrayFind;

          case "findIndex":
            return wrappedArrayFindIndex;

          case "flat":
            return wrappedArrayFlat;

          case "flatMap":
            return wrappedArrayFlatMap;

          case "join":
            return wrappedArrayJoin;

          case "push":
            return wrappedArrayPush;

          case "pop":
            return wrappedArrayPop;

          case "reduce":
            return wrappedReduce;

          case "reduceRight":
            return wrappedReduceRight;

          case "reverse":
            return wrappedArrayReverse;

          case "shift":
            return wrappedArrayShift;

          case "unshift":
            return wrappedArrayUnshift;

          case "slice":
            return wrappedArraySlice;

          case "splice":
            return wrappedArraySplice;

          case "some":
            return wrappedArraySome;

          case "sort":
            return wrappedArraySort;

          case "keys":
            return wrappedKeys;

          case "values":
          case Symbol.iterator:
            return wrappedValues;

          case "entries":
            return wrappedEntries;
        }
        I.observe(t, e);
        return wrap(V(t, e, s));
    },
    ownKeys(t) {
        currentConnectable()?.observe(t, "length");
        return Reflect.ownKeys(t);
    }
};

function wrappedArrayMap(t, e) {
    const s = getRaw(this);
    const r = s.map((s, r) => unwrap(t.call(e, wrap(s), r, this)));
    observeCollection(I, s);
    return wrap(r);
}

function wrappedArrayEvery(t, e) {
    const s = getRaw(this);
    const r = s.every((s, r) => t.call(e, wrap(s), r, this));
    observeCollection(I, s);
    return r;
}

function wrappedArrayFilter(t, e) {
    const s = getRaw(this);
    const r = s.filter((s, r) => unwrap(t.call(e, wrap(s), r, this)));
    observeCollection(I, s);
    return wrap(r);
}

function wrappedArrayIncludes(t) {
    const e = getRaw(this);
    const s = e.includes(unwrap(t));
    observeCollection(I, e);
    return s;
}

function wrappedArrayIndexOf(t) {
    const e = getRaw(this);
    const s = e.indexOf(unwrap(t));
    observeCollection(I, e);
    return s;
}

function wrappedArrayLastIndexOf(t) {
    const e = getRaw(this);
    const s = e.lastIndexOf(unwrap(t));
    observeCollection(I, e);
    return s;
}

function wrappedArrayFindIndex(t, e) {
    const s = getRaw(this);
    const r = s.findIndex((s, r) => unwrap(t.call(e, wrap(s), r, this)));
    observeCollection(I, s);
    return r;
}

function wrappedArrayFind(t, e) {
    const s = getRaw(this);
    const r = s.find((e, s) => t(wrap(e), s, this), e);
    observeCollection(I, s);
    return wrap(r);
}

function wrappedArrayFlat() {
    const t = getRaw(this);
    observeCollection(I, t);
    return wrap(t.flat());
}

function wrappedArrayFlatMap(t, e) {
    const s = getRaw(this);
    observeCollection(I, s);
    return getProxy(s.flatMap((s, r) => wrap(t.call(e, wrap(s), r, this))));
}

function wrappedArrayJoin(t) {
    const e = getRaw(this);
    observeCollection(I, e);
    return e.join(t);
}

function wrappedArrayPop() {
    return wrap(getRaw(this).pop());
}

function wrappedArrayPush(...t) {
    return getRaw(this).push(...t);
}

function wrappedArrayShift() {
    return wrap(getRaw(this).shift());
}

function wrappedArrayUnshift(...t) {
    return getRaw(this).unshift(...t);
}

function wrappedArraySplice(...t) {
    return wrap(getRaw(this).splice(...t));
}

function wrappedArrayReverse(...t) {
    return wrap(getRaw(this).reverse());
}

function wrappedArraySome(t, e) {
    const s = getRaw(this);
    const r = s.some((s, r) => unwrap(t.call(e, wrap(s), r, this)));
    observeCollection(I, s);
    return r;
}

function wrappedArraySort(t) {
    const e = getRaw(this);
    const s = e.sort(t);
    observeCollection(I, e);
    return wrap(s);
}

function wrappedArraySlice(t, e) {
    const s = getRaw(this);
    observeCollection(I, s);
    return getProxy(s.slice(t, e));
}

function wrappedReduce(t, e) {
    const s = getRaw(this);
    const r = s.reduce((e, s, r) => t(e, wrap(s), r, this), e);
    observeCollection(I, s);
    return wrap(r);
}

function wrappedReduceRight(t, e) {
    const s = getRaw(this);
    const r = s.reduceRight((e, s, r) => t(e, wrap(s), r, this), e);
    observeCollection(I, s);
    return wrap(r);
}

const U = {
    get(t, e, s) {
        if (e === F) {
            return t;
        }
        const i = currentConnectable();
        if (!_ || doNotCollect(t, e) || i == null) {
            return V(t, e, s);
        }
        switch (e) {
          case "size":
            i.observe(t, "size");
            return t.size;

          case "clear":
            return wrappedClear;

          case "delete":
            return wrappedDelete;

          case "forEach":
            return wrappedForEach;

          case "add":
            if (n(t)) {
                return wrappedAdd;
            }
            break;

          case "get":
            if (r(t)) {
                return wrappedGet;
            }
            break;

          case "set":
            if (r(t)) {
                return wrappedSet;
            }
            break;

          case "has":
            return wrappedHas;

          case "keys":
            return wrappedKeys;

          case "values":
            return wrappedValues;

          case "entries":
            return wrappedEntries;

          case Symbol.iterator:
            return r(t) ? wrappedEntries : wrappedValues;
        }
        return wrap(V(t, e, s));
    }
};

function wrappedForEach(t, e) {
    const s = getRaw(this);
    observeCollection(I, s);
    return s.forEach((s, r) => {
        t.call(e, wrap(s), wrap(r), this);
    });
}

function wrappedHas(t) {
    const e = getRaw(this);
    observeCollection(I, e);
    return e.has(unwrap(t));
}

function wrappedGet(t) {
    const e = getRaw(this);
    observeCollection(I, e);
    return wrap(e.get(unwrap(t)));
}

function wrappedSet(t, e) {
    return wrap(getRaw(this).set(unwrap(t), unwrap(e)));
}

function wrappedAdd(t) {
    return wrap(getRaw(this).add(unwrap(t)));
}

function wrappedClear() {
    return wrap(getRaw(this).clear());
}

function wrappedDelete(t) {
    return wrap(getRaw(this).delete(unwrap(t)));
}

function wrappedKeys() {
    const t = getRaw(this);
    observeCollection(I, t);
    const e = t.keys();
    return {
        next() {
            const t = e.next();
            const s = t.value;
            const r = t.done;
            return r ? {
                value: void 0,
                done: r
            } : {
                value: wrap(s),
                done: r
            };
        },
        [Symbol.iterator]() {
            return this;
        }
    };
}

function wrappedValues() {
    const t = getRaw(this);
    observeCollection(I, t);
    const e = t.values();
    return {
        next() {
            const t = e.next();
            const s = t.value;
            const r = t.done;
            return r ? {
                value: void 0,
                done: r
            } : {
                value: wrap(s),
                done: r
            };
        },
        [Symbol.iterator]() {
            return this;
        }
    };
}

function wrappedEntries() {
    const t = getRaw(this);
    observeCollection(I, t);
    const e = t.entries();
    return {
        next() {
            const t = e.next();
            const s = t.value;
            const r = t.done;
            return r ? {
                value: void 0,
                done: r
            } : {
                value: [ wrap(s[0]), wrap(s[1]) ],
                done: r
            };
        },
        [Symbol.iterator]() {
            return this;
        }
    };
}

const observeCollection = (t, e) => t?.observeCollection(e);

const W = /*@__PURE__*/ m({
    getProxy: getProxy,
    getRaw: getRaw,
    wrap: wrap,
    unwrap: unwrap,
    rawKey: F
});

const q = "__astt__";

const {astAssign: K, astEvaluate: G, astBind: J, astUnbind: Q} = /*@__PURE__*/ (() => {
    const t = "AccessThis";
    const e = "AccessBoundary";
    const r = "AccessGlobal";
    const n = "AccessScope";
    const o = "ArrayLiteral";
    const u = "ObjectLiteral";
    const h = "PrimitiveLiteral";
    const l = "New";
    const f = "Template";
    const d = "Unary";
    const p = "CallScope";
    const b = "CallMember";
    const w = "CallFunction";
    const v = "CallGlobal";
    const g = "AccessMember";
    const y = "AccessKeyed";
    const C = "TaggedTemplate";
    const E = "Binary";
    const O = "Conditional";
    const A = "Assign";
    const m = "ArrowFunction";
    const k = "ValueConverter";
    const S = "BindingBehavior";
    const R = "ArrayBindingPattern";
    const P = "ObjectBindingPattern";
    const D = "BindingIdentifier";
    const I = "ForOfStatement";
    const M = "Interpolation";
    const _ = "ArrayDestructuring";
    const T = "ObjectDestructuring";
    const V = "DestructuringAssignmentLeaf";
    const j = "Custom";
    const N = Scope.getContext;
    function observeTrackableMethodDependencies(t, e, s) {
        if (e == null) {
            return;
        }
        const r = s.deps;
        if (r == null) {
            return;
        }
        const n = i(r) ? [ r ] : r;
        for (const s of n) {
            if (typeof s === "string") {
                t.observeExpression(e, s);
                continue;
            }
            try {
                enterConnectable(t);
                s(wrap(e));
            } finally {
                exitConnectable(t);
            }
        }
    }
    function astEvaluate(c, L, F, $) {
        switch (c.$kind) {
          case t:
            {
                let t = L.overrideContext;
                let e = L;
                let s = c.ancestor;
                while (s-- && t) {
                    e = e.parent;
                    t = e?.overrideContext ?? null;
                }
                return s < 1 && e ? e.bindingContext : void 0;
            }

          case e:
            {
                let t = L;
                while (t != null && !t.isBoundary) {
                    t = t.parent;
                }
                return t ? t.bindingContext : void 0;
            }

          case n:
            {
                const t = N(L, c.name, c.ancestor);
                if ($ !== null) {
                    $.observe(t, c.name);
                }
                const e = t[c.name];
                if (e == null) {
                    if (c.name === "$host") {
                        throw createMappedError(105);
                    }
                    return e;
                }
                return F?.boundFn && i(e) ? e.bind(t) : e;
            }

          case r:
            return globalThis[c.name];

          case v:
            {
                const t = globalThis[c.name];
                if (i(t)) {
                    return t(...c.args.map(t => astEvaluate(t, L, F, $)));
                }
                if (!F?.strict && t == null) {
                    return void 0;
                }
                throw createMappedError(107);
            }

          case o:
            return c.elements.map(t => astEvaluate(t, L, F, $));

          case u:
            {
                const t = {};
                for (let e = 0; e < c.keys.length; ++e) {
                    t[c.keys[e]] = astEvaluate(c.values[e], L, F, $);
                }
                return t;
            }

          case h:
            return c.value;

          case l:
            {
                const t = astEvaluate(c.func, L, F, $);
                if (i(t)) {
                    return new t(...c.args.map(t => astEvaluate(t, L, F, $)));
                }
                throw createMappedError(107);
            }

          case f:
            {
                let t = c.cooked[0];
                for (let e = 0; e < c.expressions.length; ++e) {
                    t += x(astEvaluate(c.expressions[e], L, F, $));
                    t += c.cooked[e + 1];
                }
                return t;
            }

          case d:
            {
                const t = astEvaluate(c.expression, L, F, $);
                switch (c.operation) {
                  case "void":
                    return void t;

                  case "typeof":
                    return typeof t;

                  case "!":
                    return !t;

                  case "-":
                    return -t;

                  case "+":
                    return +t;

                  case "--":
                    if ($ != null) throw createMappedError(113);
                    return astAssign(c.expression, L, F, $, t - 1) + c.pos;

                  case "++":
                    if ($ != null) throw createMappedError(113);
                    return astAssign(c.expression, L, F, $, t + 1) - c.pos;

                  default:
                    throw createMappedError(109, c.operation);
                }
            }

          case p:
            {
                const t = N(L, c.name, c.ancestor);
                if (t == null) {
                    if (F?.strict) {
                        throw createMappedError(114, c.name, t);
                    }
                    return void 0;
                }
                const e = t[c.name];
                if (i(e)) {
                    if ($ != null && e[q] != null) {
                        const s = e[q];
                        observeTrackableMethodDependencies($, t, s);
                        const r = s?.deps == null;
                        try {
                            enterConnectable($);
                            return e.apply(r ? wrap(t) : t, c.args.map(t => r ? wrap(astEvaluate(t, L, F, $)) : astEvaluate(t, L, F, $)));
                        } finally {
                            exitConnectable($);
                        }
                    } else {
                        return e.apply(t, c.args.map(t => astEvaluate(t, L, F, $)));
                    }
                }
                if (e == null) {
                    if (F?.strict && !c.optional) {
                        throw createMappedError(111, c.name);
                    }
                    return void 0;
                }
                throw createMappedError(111, c.name);
            }

          case b:
            {
                const t = astEvaluate(c.object, L, F, $);
                if (t == null) {
                    if (F?.strict && !c.optionalMember) {
                        throw createMappedError(114, c.name, t);
                    }
                }
                const e = t?.[c.name];
                if (e == null) {
                    if (!c.optionalCall && F?.strict) {
                        throw createMappedError(111, c.name);
                    }
                    return void 0;
                }
                if (!i(e)) {
                    throw createMappedError(111, c.name);
                }
                if ($ != null && e[q] != null) {
                    const s = e[q];
                    observeTrackableMethodDependencies($, t, s);
                    const r = s?.deps == null;
                    try {
                        enterConnectable($);
                        return e.apply(r ? wrap(t) : t, c.args.map(t => r ? wrap(astEvaluate(t, L, F, $)) : astEvaluate(t, L, F, $)));
                    } finally {
                        exitConnectable($);
                    }
                } else {
                    const r = e.apply(t, c.args.map(t => astEvaluate(t, L, F, $)));
                    if (s(t) && B.includes(c.name)) {
                        $?.observeCollection(t);
                    }
                    return r;
                }
            }

          case w:
            {
                const t = astEvaluate(c.func, L, F, $);
                if (i(t)) {
                    return t(...c.args.map(t => astEvaluate(t, L, F, $)));
                }
                if (t == null) {
                    if (!c.optional && F?.strict) {
                        throw createMappedError(107);
                    }
                    return void 0;
                }
                throw createMappedError(107);
            }

          case m:
            {
                const func = (...t) => {
                    const e = c.args;
                    const s = c.rest;
                    const r = e.length - 1;
                    const n = e.reduce((e, n, i) => {
                        if (s && i === r) {
                            e[n.name] = t.slice(i);
                        } else {
                            e[n.name] = t[i];
                        }
                        return e;
                    }, {});
                    const i = Scope.fromParent(L, n);
                    return astEvaluate(c.body, i, F, $);
                };
                return func;
            }

          case g:
            {
                const t = astEvaluate(c.object, L, F, $);
                if (t == null) {
                    if (!c.optional && F?.strict) {
                        throw createMappedError(114, c.name, t);
                    }
                    return void 0;
                }
                if ($ !== null && !c.accessGlobal) {
                    $.observe(t, c.name);
                }
                const e = t[c.name];
                return F?.boundFn && i(e) ? e.bind(t) : e;
            }

          case y:
            {
                const t = astEvaluate(c.object, L, F, $);
                const e = astEvaluate(c.key, L, F, $);
                if (t == null) {
                    if (!c.optional && F?.strict) {
                        throw createMappedError(115, e, t);
                    }
                    return void 0;
                }
                if ($ !== null && !c.accessGlobal) {
                    $.observe(t, e);
                }
                return t[e];
            }

          case C:
            {
                const t = c.expressions.map(t => astEvaluate(t, L, F, $));
                const e = astEvaluate(c.func, L, F, $);
                if (!i(e)) {
                    throw createMappedError(110);
                }
                return e(c.cooked, ...t);
            }

          case E:
            {
                const t = c.left;
                const e = c.right;
                switch (c.operation) {
                  case "&&":
                    return astEvaluate(t, L, F, $) && astEvaluate(e, L, F, $);

                  case "||":
                    return astEvaluate(t, L, F, $) || astEvaluate(e, L, F, $);

                  case "??":
                    return astEvaluate(t, L, F, $) ?? astEvaluate(e, L, F, $);

                  case "==":
                    return astEvaluate(t, L, F, $) == astEvaluate(e, L, F, $);

                  case "===":
                    return astEvaluate(t, L, F, $) === astEvaluate(e, L, F, $);

                  case "!=":
                    return astEvaluate(t, L, F, $) != astEvaluate(e, L, F, $);

                  case "!==":
                    return astEvaluate(t, L, F, $) !== astEvaluate(e, L, F, $);

                  case "instanceof":
                    {
                        const s = astEvaluate(e, L, F, $);
                        if (i(s)) {
                            return astEvaluate(t, L, F, $) instanceof s;
                        }
                        return false;
                    }

                  case "in":
                    {
                        const s = astEvaluate(e, L, F, $);
                        if (a(s)) {
                            return astEvaluate(t, L, F, $) in s;
                        }
                        return false;
                    }

                  case "+":
                    return astEvaluate(t, L, F, $) + astEvaluate(e, L, F, $);

                  case "-":
                    return astEvaluate(t, L, F, $) - astEvaluate(e, L, F, $);

                  case "*":
                    return astEvaluate(t, L, F, $) * astEvaluate(e, L, F, $);

                  case "/":
                    return astEvaluate(t, L, F, $) / astEvaluate(e, L, F, $);

                  case "%":
                    return astEvaluate(t, L, F, $) % astEvaluate(e, L, F, $);

                  case "**":
                    return astEvaluate(t, L, F, $) ** astEvaluate(e, L, F, $);

                  case "<":
                    return astEvaluate(t, L, F, $) < astEvaluate(e, L, F, $);

                  case ">":
                    return astEvaluate(t, L, F, $) > astEvaluate(e, L, F, $);

                  case "<=":
                    return astEvaluate(t, L, F, $) <= astEvaluate(e, L, F, $);

                  case ">=":
                    return astEvaluate(t, L, F, $) >= astEvaluate(e, L, F, $);

                  default:
                    throw createMappedError(108, c.operation);
                }
            }

          case O:
            return astEvaluate(c.condition, L, F, $) ? astEvaluate(c.yes, L, F, $) : astEvaluate(c.no, L, F, $);

          case A:
            {
                let t = astEvaluate(c.value, L, F, $);
                if (c.op !== "=") {
                    if ($ != null) {
                        throw createMappedError(113);
                    }
                    const e = astEvaluate(c.target, L, F, $);
                    switch (c.op) {
                      case "/=":
                        t = e / t;
                        break;

                      case "*=":
                        t = e * t;
                        break;

                      case "+=":
                        t = e + t;
                        break;

                      case "-=":
                        t = e - t;
                        break;

                      default:
                        throw createMappedError(108, c.op);
                    }
                }
                return astAssign(c.target, L, F, $, t);
            }

          case k:
            {
                return F?.useConverter?.(c.name, "toView", astEvaluate(c.expression, L, F, $), c.args.map(t => astEvaluate(t, L, F, $)));
            }

          case S:
            return astEvaluate(c.expression, L, F, $);

          case D:
            return c.name;

          case I:
            return astEvaluate(c.iterable, L, F, $);

          case M:
            if (c.isMulti) {
                let t = c.parts[0];
                let e = 0;
                for (;e < c.expressions.length; ++e) {
                    t += x(astEvaluate(c.expressions[e], L, F, $));
                    t += c.parts[e + 1];
                }
                return t;
            } else {
                return `${c.parts[0]}${astEvaluate(c.firstExpression, L, F, $)}${c.parts[1]}`;
            }

          case V:
            return astEvaluate(c.target, L, F, $);

          case _:
            {
                return c.list.map(t => astEvaluate(t, L, F, $));
            }

          case R:
          case P:
          case T:
          default:
            return void 0;

          case j:
            return c.evaluate(L, F, $);
        }
    }
    function astAssign(t, e, r, i, o) {
        switch (t.$kind) {
          case n:
            {
                if (t.name === "$host") {
                    throw createMappedError(106);
                }
                const s = N(e, t.name, t.ancestor);
                return s[t.name] = o;
            }

          case g:
            {
                const n = astEvaluate(t.object, e, r, i);
                if (n == null) {
                    if (r?.strict) {
                        throw createMappedError(116, t.name);
                    }
                    astAssign(t.object, e, r, i, {
                        [t.name]: o
                    });
                } else if (a(n)) {
                    if (t.name === "length" && s(n) && !isNaN(o)) {
                        n.splice(o);
                    } else {
                        n[t.name] = o;
                    }
                } else ;
                return o;
            }

          case y:
            {
                const n = astEvaluate(t.object, e, r, i);
                const a = astEvaluate(t.key, e, r, i);
                if (n == null) {
                    if (r?.strict) {
                        throw createMappedError(116, a);
                    }
                    astAssign(t.object, e, r, i, {
                        [a]: o
                    });
                    return o;
                }
                if (s(n)) {
                    if (a === "length" && !isNaN(o)) {
                        n.splice(o);
                        return o;
                    }
                    if (c(a)) {
                        n.splice(a, 1, o);
                        return o;
                    }
                }
                return n[a] = o;
            }

          case A:
            astAssign(t.value, e, r, i, o);
            return astAssign(t.target, e, r, i, o);

          case k:
            {
                o = r?.useConverter?.(t.name, "fromView", o, t.args.map(t => astEvaluate(t, e, r, i)));
                return astAssign(t.expression, e, r, i, o);
            }

          case S:
            return astAssign(t.expression, e, r, i, o);

          case _:
          case T:
            {
                const s = t.list;
                const n = s.length;
                let a;
                let c;
                for (a = 0; a < n; a++) {
                    c = s[a];
                    switch (c.$kind) {
                      case V:
                        astAssign(c, e, r, i, o);
                        break;

                      case _:
                      case T:
                        {
                            if (typeof o !== "object" || o === null) {
                                throw createMappedError(112);
                            }
                            let t = astEvaluate(c.source, Scope.create(o), r, null);
                            if (t === void 0 && c.initializer) {
                                t = astEvaluate(c.initializer, e, r, null);
                            }
                            astAssign(c, e, r, i, t);
                            break;
                        }
                    }
                }
                break;
            }

          case V:
            {
                if ("source" in t) {
                    if (o == null) {
                        return;
                    }
                    if (typeof o !== "object") {
                        throw createMappedError(112);
                    }
                    let s = astEvaluate(t.source, Scope.create(o), r, i);
                    if (s === void 0 && t.initializer) {
                        s = astEvaluate(t.initializer, e, r, i);
                    }
                    astAssign(t.target, e, r, i, s);
                } else {
                    if (o == null) {
                        return;
                    }
                    if (typeof o !== "object") {
                        throw createMappedError(112);
                    }
                    const s = t.indexOrProperties;
                    let n;
                    if (c(s)) {
                        if (!Array.isArray(o)) {
                            throw createMappedError(112);
                        }
                        n = o.slice(s);
                    } else {
                        n = Object.entries(o).reduce((t, [e, r]) => {
                            if (!s.includes(e)) {
                                t[e] = r;
                            }
                            return t;
                        }, {});
                    }
                    astAssign(t.target, e, r, i, n);
                }
                break;
            }

          case j:
            return t.assign(e, r, o);

          default:
            return void 0;
        }
    }
    function astBind(t, e, s) {
        switch (t.$kind) {
          case S:
            {
                s.bindBehavior?.(t.name, e, t.args.map(t => astEvaluate(t, e, s, null)));
                astBind(t.expression, e, s);
                break;
            }

          case k:
            {
                s.bindConverter?.(t.name);
                astBind(t.expression, e, s);
                break;
            }

          case I:
            {
                astBind(t.iterable, e, s);
                break;
            }

          case j:
            {
                t.bind?.(e, s);
            }
        }
    }
    function astUnbind(t, e, s) {
        switch (t.$kind) {
          case S:
            {
                s.unbindBehavior?.(t.name, e);
                astUnbind(t.expression, e, s);
                break;
            }

          case k:
            {
                s.unbindConverter?.(t.name);
                astUnbind(t.expression, e, s);
                break;
            }

          case I:
            {
                astUnbind(t.iterable, e, s);
                break;
            }

          case j:
            {
                t.unbind?.(e, s);
            }
        }
    }
    const B = "at map filter includes indexOf lastIndexOf findIndex find flat flatMap join reduce reduceRight slice every some sort".split(" ");
    return {
        astEvaluate: astEvaluate,
        astAssign: astAssign,
        astBind: astBind,
        astUnbind: astUnbind
    };
})();

function isClassMethodDecoratorContext$1(t) {
    return t != null && typeof t === "object" && "kind" in t;
}

function astTrack(t, ...e) {
    if (i(t) && isClassMethodDecoratorContext$1(e[0])) {
        return applyDecorator(t, e[0], {});
    }
    const s = {};
    if (typeof t === "string") {
        s.deps = [ t, ...e ];
    } else if (i(t)) {
        s.deps = [ t ];
    } else if (t != null) {
        const e = t;
        if (Object.prototype.hasOwnProperty.call(e, "deps")) {
            s.deps = e.deps == null ? void 0 : i(e.deps) ? [ e.deps ] : e.deps;
        }
    }
    return (t, e) => applyDecorator(t, e, s);
}

function applyDecorator(t, e, s) {
    if (e.kind !== "method") {
        throw createMappedError(117, String(e.name));
    }
    const r = t;
    A(r, {
        [q]: {
            deps: s.deps
        }
    });
}

const X = (() => t => {
    const e = t.prototype;
    [ "bindBehavior", "unbindBehavior", "bindConverter", "unbindConverter", "useConverter" ].forEach(t => {
        rtDefineHiddenProp(e, t, () => {
            throw createMappedError(99, t);
        });
    });
})();

const Y = "pending";

const Z = "running";

const tt = "completed";

const et = "canceled";

const st = Promise.resolve();

let rt = false;

let nt = false;

const it = [];

const ot = [];

let at = 0;

let ct = null;

let ut = [];

let ht = null;

let lt = null;

const requestRun = () => {
    if (!rt) {
        rt = true;
        void st.then(() => {
            rt = false;
            nt = true;
            runTasks();
        });
    }
};

const signalSettled = t => {
    if (ct && it.length === 0 && at === 0) {
        ct = null;
        if (ut.length > 0) {
            const t = ut;
            ut = [];
            if (t.length === 1) {
                lt(t[0]);
            } else {
                lt(new AggregateError(t, "One or more tasks failed."));
            }
        } else {
            ht(t);
        }
    }
};

const runTasks = () => {
    const t = !nt;
    nt = false;
    ct ??= new Promise((t, e) => {
        ht = t;
        lt = e;
    });
    let e = -it.length;
    const s = it.length === 0;
    while (it.length > 0) {
        if (++e > 1e4) {
            const t = new Error(`Potential deadlock detected. More than 10000 extra tasks were queued from within tasks.`);
            it.length = 0;
            lt?.(t);
            ct = null;
            throw t;
        }
        const t = it.shift();
        if (typeof t === "function") {
            try {
                t();
            } catch (t) {
                ut.push(t);
            }
        } else {
            t.run();
        }
    }
    const r = ut.slice();
    signalSettled(!s);
    if (t && r.length > 0) {
        if (r.length === 1) {
            throw r[0];
        } else {
            throw new AggregateError(r, "One or more tasks failed.");
        }
    }
};

const getRecurringTasks = () => ot.slice();

const isTaskQueueEmpty = () => it.length === 0 && at === 0;

const tasksSettled = () => {
    if (ct) {
        return ct;
    }
    if (it.length > 0 || at > 0) {
        return ct ??= new Promise((t, e) => {
            ht = t;
            lt = e;
        });
    }
    return st.then(() => {
        if (it.length > 0 || at > 0) {
            return ct ??= new Promise((t, e) => {
                ht = t;
                lt = e;
            });
        }
        return false;
    });
};

const queueTask = t => {
    requestRun();
    it.push(t);
};

const queueAsyncTask = (t, e) => {
    const s = new Task(t, e?.delay);
    if (s.delay != null && s.delay > 0) {
        ++at;
        s.t = y.getOrCreate(globalThis).setTimeout(() => {
            --at;
            s.t = undefined;
            if (s.status === et) {
                signalSettled(true);
                return;
            }
            it.push(s);
            requestRun();
        }, s.delay);
    } else {
        it.push(s);
        requestRun();
    }
    return s;
};

class TaskAbortError extends Error {
    constructor(t) {
        super(`Task ${t.id} was canceled.`);
        this.task = t;
    }
}

class Task {
    get result() {
        return this.i;
    }
    get status() {
        return this.u;
    }
    constructor(t, e) {
        this.callback = t;
        this.delay = e;
        this.id = ++Task.h;
        this.u = Y;
        this.i = new Promise((t, e) => {
            this.C = t;
            this.O = e;
        });
    }
    then(t, e) {
        return this.result.then(t, e);
    }
    catch(t) {
        return this.result.catch(t);
    }
    finally(t) {
        return this.result.finally(t);
    }
    run() {
        if (this.u !== Y) {
            throw new Error(`Cannot run task in ${this.u} state`);
        }
        this.u = Z;
        let t;
        try {
            t = this.callback();
        } catch (t) {
            this.u = et;
            this.O(t);
            ut.push(t);
            return;
        }
        if (t instanceof Promise) {
            ++at;
            t.then(t => {
                this.u = tt;
                this.C(t);
            }).catch(t => {
                this.u = et;
                this.O(t);
                ut.push(t);
            }).finally(() => {
                --at;
                signalSettled(true);
            });
        } else {
            this.u = tt;
            this.C(t);
        }
    }
    cancel() {
        if (this.t !== undefined) {
            y.getOrCreate(globalThis).clearTimeout(this.t);
            --at;
            this.t = undefined;
            this.u = et;
            const t = new TaskAbortError(this);
            this.O(t);
            void this.i.catch(u);
            signalSettled(true);
            return true;
        }
        if (this.u === Y) {
            const t = it.indexOf(this);
            if (t > -1) {
                it.splice(t, 1);
                this.u = et;
                const e = new TaskAbortError(this);
                this.O(e);
                void this.i.catch(u);
                signalSettled(true);
                return true;
            }
        }
        return false;
    }
}

Task.h = 0;

const queueRecurringTask = (t, e) => {
    const s = new RecurringTask(t, Math.max(e?.interval ?? 0, 0));
    ot.push(s);
    s.A();
    return s;
};

class RecurringTask {
    constructor(t, e) {
        this.cb = t;
        this.R = e;
        this.id = ++RecurringTask.P;
        this.I = false;
        this.M = [];
    }
    run() {
        try {
            this.cb();
        } catch (t) {
            ut.push(t);
            return;
        }
    }
    A() {
        if (this.I) {
            return;
        }
        this.t = y.getOrCreate(globalThis).setTimeout(() => {
            this._();
            if (!this.I) {
                this.A();
            }
        }, this.R);
    }
    _() {
        it.push(this);
        requestRun();
        const t = this.M.splice(0);
        for (const e of t) {
            e();
        }
    }
    next() {
        if (this.I) {
            return Promise.resolve();
        }
        return new Promise(t => this.M.push(t));
    }
    cancel() {
        this.I = true;
        if (this.t !== undefined) {
            y.getOrCreate(globalThis).clearTimeout(this.t);
            this.t = undefined;
        }
        const t = ot.indexOf(this);
        if (t > -1) {
            ot.splice(t, 1);
        }
        const e = this.M.splice(0);
        for (const t of e) {
            t();
        }
    }
}

RecurringTask.P = 0;

const ft = /*@__PURE__*/ t.createInterface("ICoercionConfiguration");

const dt = 0;

const pt = 1;

const bt = 2;

const wt = 4;

const vt = /*@__PURE__*/ m({
    None: dt,
    Observer: pt,
    Node: bt,
    Layout: wt
});

function copyIndexMap(t, e, s) {
    const {length: r} = t;
    const n = Array(r);
    let i = 0;
    while (i < r) {
        n[i] = t[i];
        ++i;
    }
    if (e !== void 0) {
        n.deletedIndices = e.slice(0);
    } else if (t.deletedIndices !== void 0) {
        n.deletedIndices = t.deletedIndices.slice(0);
    } else {
        n.deletedIndices = [];
    }
    if (s !== void 0) {
        n.deletedItems = s.slice(0);
    } else if (t.deletedItems !== void 0) {
        n.deletedItems = t.deletedItems.slice(0);
    } else {
        n.deletedItems = [];
    }
    n.isIndexMap = true;
    return n;
}

function createIndexMap(t = 0) {
    const e = Array(t);
    let s = 0;
    while (s < t) {
        e[s] = s++;
    }
    e.deletedIndices = [];
    e.deletedItems = [];
    e.isIndexMap = true;
    return e;
}

function cloneIndexMap(t) {
    const e = t.slice();
    e.deletedIndices = t.deletedIndices.slice();
    e.deletedItems = t.deletedItems.slice();
    e.isIndexMap = true;
    return e;
}

function isIndexMap(t) {
    return s(t) && t.isIndexMap === true;
}

let gt = new Map;

let yt = false;

function batch(t) {
    const e = gt;
    const s = gt = new Map;
    yt = true;
    try {
        t();
    } finally {
        gt = null;
        yt = false;
        try {
            let t;
            let r;
            let n;
            let i;
            let o;
            let a = false;
            let c;
            let u;
            for (t of s) {
                r = t[0];
                n = t[1];
                if (e?.has(r)) {
                    e.set(r, n);
                }
                if (n[0] === 1) {
                    r.notify(n[1], n[2]);
                } else {
                    i = n[1];
                    o = n[2];
                    a = false;
                    if (o.deletedIndices.length > 0) {
                        a = true;
                    } else {
                        for (c = 0, u = o.length; c < u; ++c) {
                            if (o[c] !== c) {
                                a = true;
                                break;
                            }
                        }
                    }
                    if (a) {
                        r.notifyCollection(i, o);
                    }
                }
            }
        } finally {
            gt = e;
        }
    }
}

function addCollectionBatch(t, e, s) {
    if (!gt.has(t)) {
        gt.set(t, [ 2, e, s ]);
    } else {
        gt.get(t)[2] = s;
    }
}

function addValueBatch(t, e, s) {
    const r = gt.get(t);
    if (r === void 0) {
        gt.set(t, [ 1, e, s ]);
    } else {
        r[1] = e;
        r[2] = s;
    }
}

const Ct = /*@__PURE__*/ (() => {
    function subscriberCollection(t, e) {
        return t == null ? subscriberCollectionDeco : subscriberCollectionDeco(t);
    }
    function getSubscriberRecord() {
        return rtDefineHiddenProp(this, "subs", new SubscriberRecord);
    }
    function addSubscriber(t) {
        return this.subs.add(t);
    }
    function removeSubscriber(t) {
        return this.subs.remove(t);
    }
    const t = new WeakSet;
    function subscriberCollectionDeco(e, s) {
        if (!t.has(e)) {
            t.add(e);
            const s = e.prototype;
            O(s, "subs", {
                get: getSubscriberRecord
            });
            ensureProto(s, "subscribe", addSubscriber);
            ensureProto(s, "unsubscribe", removeSubscriber);
        }
        return e;
    }
    class SubscriberRecord {
        constructor() {
            this.count = 0;
            this.T = [];
            this.V = [];
            this.j = false;
        }
        add(t) {
            if (this.T.includes(t)) {
                return false;
            }
            this.T[this.T.length] = t;
            if ("handleDirty" in t) {
                this.V[this.V.length] = t;
                this.j = true;
            }
            ++this.count;
            return true;
        }
        remove(t) {
            let e = this.T.indexOf(t);
            if (e !== -1) {
                this.T.splice(e, 1);
                e = this.V.indexOf(t);
                if (e !== -1) {
                    this.V.splice(e, 1);
                    this.j = this.V.length > 0;
                }
                --this.count;
                return true;
            }
            return false;
        }
        notify(t, e) {
            if (yt) {
                addValueBatch(this, t, e);
                return;
            }
            for (const s of this.T.slice(0)) {
                s.handleChange(t, e);
            }
        }
        notifyCollection(t, e) {
            const s = this.T.slice(0);
            const r = s.length;
            let n = 0;
            for (;n < r; ++n) {
                s[n].handleCollectionChange(t, e);
            }
            return;
        }
        notifyDirty() {
            if (this.j) {
                for (const t of this.V.slice(0)) {
                    t.handleDirty();
                }
            }
        }
    }
    return subscriberCollection;
})();

class CollectionLengthObserver {
    constructor(t) {
        this.owner = t;
        this.type = pt;
        this.v = (this.o = t.collection).length;
    }
    getValue() {
        return this.o.length;
    }
    setValue(t) {
        if (t !== this.v) {
            if (!Number.isNaN(t)) {
                this.o.splice(t);
                this.v = this.o.length;
            }
        }
    }
    handleDirty() {
        if (this.v !== this.o.length) {
            this.subs.notifyDirty();
        }
    }
    handleCollectionChange(t, e) {
        const s = this.v;
        const r = this.o.length;
        if ((this.v = r) !== s) {
            this.subs.notifyDirty();
            this.subs.notify(this.v, s);
        }
    }
}

(() => {
    implementLengthObserver(CollectionLengthObserver);
})();

class CollectionSizeObserver {
    constructor(t) {
        this.owner = t;
        this.type = pt;
        this.v = (this.o = t.collection).size;
    }
    getValue() {
        return this.o.size;
    }
    setValue() {
        throw createMappedError(220);
    }
    handleDirty() {
        if (this.v !== this.o.size) {
            this.subs.notifyDirty();
        }
    }
    handleCollectionChange(t, e) {
        const s = this.v;
        const r = this.o.size;
        if ((this.v = r) !== s) {
            this.subs.notify(this.v, s);
        }
    }
}

(() => {
    implementLengthObserver(CollectionSizeObserver);
})();

function implementLengthObserver(t) {
    const e = t.prototype;
    ensureProto(e, "subscribe", subscribe);
    ensureProto(e, "unsubscribe", unsubscribe);
    return Ct(t, null);
}

function subscribe(t) {
    if (this.subs.add(t) && this.subs.count === 1) {
        this.owner.subscribe(this);
    }
}

function unsubscribe(t) {
    if (this.subs.remove(t) && this.subs.count === 0) {
        this.owner.subscribe(this);
    }
}

const Et = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    function sortCompare(t, e) {
        if (t === e) {
            return 0;
        }
        t = t === null ? "null" : t.toString();
        e = e === null ? "null" : e.toString();
        return t < e ? -1 : 1;
    }
    function preSortCompare(t, e) {
        if (t === void 0) {
            if (e === void 0) {
                return 0;
            } else {
                return 1;
            }
        }
        if (e === void 0) {
            return -1;
        }
        return 0;
    }
    function insertionSort(t, e, s, r, n) {
        let i, o, a, c, u;
        let h, l;
        for (h = s + 1; h < r; h++) {
            i = t[h];
            o = e[h];
            for (l = h - 1; l >= s; l--) {
                a = t[l];
                c = e[l];
                u = n(a, i);
                if (u > 0) {
                    t[l + 1] = a;
                    e[l + 1] = c;
                } else {
                    break;
                }
            }
            t[l + 1] = i;
            e[l + 1] = o;
        }
    }
    function quickSort(t, e, s, r, n) {
        let i = 0, o = 0;
        let a, c, u;
        let h, l, f;
        let d, p, b;
        let w, v;
        let g, y, C, E;
        let O, A, m, k;
        while (true) {
            if (r - s <= 10) {
                insertionSort(t, e, s, r, n);
                return;
            }
            i = s + (r - s >> 1);
            a = t[s];
            h = e[s];
            c = t[r - 1];
            l = e[r - 1];
            u = t[i];
            f = e[i];
            d = n(a, c);
            if (d > 0) {
                w = a;
                v = h;
                a = c;
                h = l;
                c = w;
                l = v;
            }
            p = n(a, u);
            if (p >= 0) {
                w = a;
                v = h;
                a = u;
                h = f;
                u = c;
                f = l;
                c = w;
                l = v;
            } else {
                b = n(c, u);
                if (b > 0) {
                    w = c;
                    v = l;
                    c = u;
                    l = f;
                    u = w;
                    f = v;
                }
            }
            t[s] = a;
            e[s] = h;
            t[r - 1] = u;
            e[r - 1] = f;
            g = c;
            y = l;
            C = s + 1;
            E = r - 1;
            t[i] = t[C];
            e[i] = e[C];
            t[C] = g;
            e[C] = y;
            t: for (o = C + 1; o < E; o++) {
                O = t[o];
                A = e[o];
                m = n(O, g);
                if (m < 0) {
                    t[o] = t[C];
                    e[o] = e[C];
                    t[C] = O;
                    e[C] = A;
                    C++;
                } else if (m > 0) {
                    do {
                        E--;
                        if (E == o) {
                            break t;
                        }
                        k = t[E];
                        m = n(k, g);
                    } while (m > 0);
                    t[o] = t[E];
                    e[o] = e[E];
                    t[E] = O;
                    e[E] = A;
                    if (m < 0) {
                        O = t[o];
                        A = e[o];
                        t[o] = t[C];
                        e[o] = e[C];
                        t[C] = O;
                        e[C] = A;
                        C++;
                    }
                }
            }
            if (r - E < C - s) {
                quickSort(t, e, E, r, n);
                r = C;
            } else {
                quickSort(t, e, s, C, n);
                s = E;
            }
        }
    }
    const e = Array.prototype;
    const s = [ "push", "unshift", "pop", "shift", "splice", "reverse", "sort" ];
    let r;
    function overrideArrayPrototypes() {
        const n = e.push;
        const o = e.unshift;
        const a = e.pop;
        const c = e.shift;
        const u = e.splice;
        const h = e.reverse;
        const l = e.sort;
        r = {
            push: function(...e) {
                const s = t.get(this);
                if (s === void 0) {
                    return n.apply(this, e);
                }
                const r = this.length;
                const i = e.length;
                if (i === 0) {
                    return r;
                }
                this.length = s.indexMap.length = r + i;
                let o = r;
                while (o < this.length) {
                    this[o] = e[o - r];
                    s.indexMap[o] = -2;
                    o++;
                }
                s.notify();
                return this.length;
            },
            unshift: function(...e) {
                const s = t.get(this);
                if (s === void 0) {
                    return o.apply(this, e);
                }
                const r = e.length;
                const n = new Array(r);
                let i = 0;
                while (i < r) {
                    n[i++] = -2;
                }
                o.apply(s.indexMap, n);
                const a = o.apply(this, e);
                s.notify();
                return a;
            },
            pop: function() {
                const e = t.get(this);
                if (e === void 0) {
                    return a.call(this);
                }
                const s = e.indexMap;
                const r = a.call(this);
                const n = s.length - 1;
                if (s[n] > -1) {
                    s.deletedIndices.push(s[n]);
                    s.deletedItems.push(r);
                }
                a.call(s);
                e.notify();
                return r;
            },
            shift: function() {
                const e = t.get(this);
                if (e === void 0) {
                    return c.call(this);
                }
                const s = e.indexMap;
                const r = c.call(this);
                if (s[0] > -1) {
                    s.deletedIndices.push(s[0]);
                    s.deletedItems.push(r);
                }
                c.call(s);
                e.notify();
                return r;
            },
            splice: function(...e) {
                const s = e[0];
                const r = e[1];
                const n = t.get(this);
                if (n === void 0) {
                    return u.apply(this, e);
                }
                const i = this.length;
                const o = s | 0;
                const a = o < 0 ? Math.max(i + o, 0) : Math.min(o, i);
                const c = n.indexMap;
                const h = e.length;
                const l = h === 0 ? 0 : h === 1 ? i - a : r;
                let f = a;
                if (l > 0) {
                    const t = f + l;
                    while (f < t) {
                        if (c[f] > -1) {
                            c.deletedIndices.push(c[f]);
                            c.deletedItems.push(this[f]);
                        }
                        f++;
                    }
                }
                f = 0;
                if (h > 2) {
                    const t = h - 2;
                    const e = new Array(t);
                    while (f < t) {
                        e[f++] = -2;
                    }
                    u.call(c, s, r, ...e);
                } else {
                    u.apply(c, e);
                }
                const d = u.apply(this, e);
                if (l > 0 || f > 0) {
                    n.notify();
                }
                return d;
            },
            reverse: function() {
                const e = t.get(this);
                if (e === void 0) {
                    h.call(this);
                    return this;
                }
                const s = this.length;
                const r = s / 2 | 0;
                let n = 0;
                while (n !== r) {
                    const t = s - n - 1;
                    const r = this[n];
                    const i = e.indexMap[n];
                    const o = this[t];
                    const a = e.indexMap[t];
                    this[n] = o;
                    e.indexMap[n] = a;
                    this[t] = r;
                    e.indexMap[t] = i;
                    n++;
                }
                e.notify();
                return this;
            },
            sort: function(e) {
                const s = t.get(this);
                if (s === void 0) {
                    l.call(this, e);
                    return this;
                }
                let r = this.length;
                if (r < 2) {
                    return this;
                }
                quickSort(this, s.indexMap, 0, r, preSortCompare);
                let n = 0;
                while (n < r) {
                    if (this[n] === void 0) {
                        break;
                    }
                    n++;
                }
                if (e === void 0 || !i(e)) {
                    e = sortCompare;
                }
                quickSort(this, s.indexMap, 0, n, e);
                let o = false;
                for (n = 0, r = s.indexMap.length; r > n; ++n) {
                    if (s.indexMap[n] !== n) {
                        o = true;
                        break;
                    }
                }
                if (o || yt) {
                    s.notify();
                }
                return this;
            }
        };
        for (const t of s) {
            O(r[t], "observing", {
                value: true
            });
        }
    }
    let n = false;
    const o = "__au_arr_on__";
    function enableArrayObservation() {
        if (r === undefined) {
            overrideArrayPrototypes();
        }
        if (!(P(o, Array) ?? false)) {
            D(true, Array, o);
            for (const t of s) {
                if (e[t].observing !== true) {
                    rtDefineHiddenProp(e, t, r[t]);
                }
            }
        }
    }
    class ArrayObserverImpl {
        constructor(e) {
            this.type = pt;
            if (!n) {
                n = true;
                enableArrayObservation();
            }
            this.indexObservers = {};
            this.collection = e;
            this.indexMap = createIndexMap(e.length);
            this.lenObs = void 0;
            t.set(e, this);
        }
        notify() {
            const t = this.subs;
            t.notifyDirty();
            const e = this.indexMap;
            if (yt) {
                addCollectionBatch(t, this.collection, e);
                return;
            }
            const s = this.collection;
            const r = s.length;
            this.indexMap = createIndexMap(r);
            t.notifyCollection(s, e);
        }
        getLengthObserver() {
            return this.lenObs ??= new CollectionLengthObserver(this);
        }
        getIndexObserver(t) {
            return this.indexObservers[t] ??= new ArrayIndexObserverImpl(this, t);
        }
    }
    (() => {
        Ct(ArrayObserverImpl, null);
    })();
    class ArrayIndexObserverImpl {
        constructor(t, e) {
            this.owner = t;
            this.index = e;
            this.doNotCache = true;
            this.value = this.getValue();
        }
        getValue() {
            return this.owner.collection[this.index];
        }
        setValue(t) {
            if (t === this.getValue()) {
                return;
            }
            const e = this.owner;
            const s = this.index;
            const r = e.indexMap;
            if (r[s] > -1) {
                r.deletedIndices.push(r[s]);
            }
            r[s] = -2;
            e.collection[s] = t;
            e.notify();
        }
        handleDirty() {
            if (this.value !== this.getValue()) {
                this.subs.notifyDirty();
            }
        }
        handleCollectionChange(t, e) {
            const s = this.index;
            const r = e[s] === s;
            if (r) {
                return;
            }
            const n = this.value;
            const i = this.value = this.getValue();
            if (n !== i) {
                this.subs.notify(i, n);
            }
        }
        subscribe(t) {
            if (this.subs.add(t) && this.subs.count === 1) {
                this.owner.subscribe(this);
            }
        }
        unsubscribe(t) {
            if (this.subs.remove(t) && this.subs.count === 0) {
                this.owner.unsubscribe(this);
            }
        }
    }
    (() => {
        Ct(ArrayIndexObserverImpl, null);
    })();
    return function getArrayObserver(e) {
        let s = t.get(e);
        if (s === void 0) {
            t.set(e, s = new ArrayObserverImpl(e));
            enableArrayObservation();
        }
        return s;
    };
})();

const Ot = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    const {add: e, clear: s, delete: r} = Set.prototype;
    const n = [ "add", "clear", "delete" ];
    const i = {
        add: function(s) {
            const r = t.get(this);
            if (r === undefined) {
                e.call(this, s);
                return this;
            }
            const n = this.size;
            e.call(this, s);
            const i = this.size;
            if (i === n) {
                return this;
            }
            r.indexMap[n] = -2;
            r.notify();
            return this;
        },
        clear: function() {
            const e = t.get(this);
            if (e === undefined) {
                return s.call(this);
            }
            const r = this.size;
            if (r > 0) {
                const t = e.indexMap;
                let r = 0;
                for (const e of this.keys()) {
                    if (t[r] > -1) {
                        t.deletedIndices.push(t[r]);
                        t.deletedItems.push(e);
                    }
                    r++;
                }
                s.call(this);
                t.length = 0;
                e.notify();
            }
            return undefined;
        },
        delete: function(e) {
            const s = t.get(this);
            if (s === undefined) {
                return r.call(this, e);
            }
            const n = this.size;
            if (n === 0) {
                return false;
            }
            let i = 0;
            const o = s.indexMap;
            for (const t of this.keys()) {
                if (t === e) {
                    if (o[i] > -1) {
                        o.deletedIndices.push(o[i]);
                        o.deletedItems.push(t);
                    }
                    o.splice(i, 1);
                    const n = r.call(this, e);
                    if (n === true) {
                        s.notify();
                    }
                    return n;
                }
                i++;
            }
            return false;
        }
    };
    function enableSetObservation(t) {
        for (const e of n) {
            rtDefineHiddenProp(t, e, i[e]);
        }
    }
    class SetObserverImpl {
        constructor(t) {
            this.type = pt;
            this.collection = t;
            this.indexMap = createIndexMap(t.size);
            this.lenObs = void 0;
        }
        notify() {
            const t = this.subs;
            t.notifyDirty();
            const e = this.indexMap;
            if (yt) {
                addCollectionBatch(t, this.collection, e);
                return;
            }
            const s = this.collection;
            const r = s.size;
            this.indexMap = createIndexMap(r);
            t.notifyCollection(s, e);
        }
        getLengthObserver() {
            return this.lenObs ??= new CollectionSizeObserver(this);
        }
    }
    Ct(SetObserverImpl, null);
    return function getSetObserver(e) {
        let s = t.get(e);
        if (s === void 0) {
            t.set(e, s = new SetObserverImpl(e));
            enableSetObservation(e);
        }
        return s;
    };
})();

const At = /*@__PURE__*/ (() => {
    const t = new WeakMap;
    const {set: e, clear: s, delete: r} = Map.prototype;
    const n = [ "set", "clear", "delete" ];
    const i = {
        set: function(s, r) {
            const n = t.get(this);
            if (n === undefined) {
                e.call(this, s, r);
                return this;
            }
            const i = this.get(s);
            const o = this.size;
            e.call(this, s, r);
            const a = this.size;
            if (a === o) {
                let t = 0;
                for (const e of this.entries()) {
                    if (e[0] === s) {
                        if (e[1] !== i) {
                            n.indexMap.deletedIndices.push(n.indexMap[t]);
                            n.indexMap.deletedItems.push(e);
                            n.indexMap[t] = -2;
                            n.notify();
                        }
                        return this;
                    }
                    t++;
                }
                return this;
            }
            n.indexMap[o] = -2;
            n.notify();
            return this;
        },
        clear: function() {
            const e = t.get(this);
            if (e === undefined) {
                return s.call(this);
            }
            const r = this.size;
            if (r > 0) {
                const t = e.indexMap;
                let r = 0;
                for (const e of this.keys()) {
                    if (t[r] > -1) {
                        t.deletedIndices.push(t[r]);
                        t.deletedItems.push(e);
                    }
                    r++;
                }
                s.call(this);
                t.length = 0;
                e.notify();
            }
            return undefined;
        },
        delete: function(e) {
            const s = t.get(this);
            if (s === undefined) {
                return r.call(this, e);
            }
            const n = this.size;
            if (n === 0) {
                return false;
            }
            let i = 0;
            const o = s.indexMap;
            for (const t of this.keys()) {
                if (t === e) {
                    if (o[i] > -1) {
                        o.deletedIndices.push(o[i]);
                        o.deletedItems.push(t);
                    }
                    o.splice(i, 1);
                    const n = r.call(this, e);
                    if (n === true) {
                        s.notify();
                    }
                    return n;
                }
                ++i;
            }
            return false;
        }
    };
    function enableMapObservation(t) {
        for (const e of n) {
            rtDefineHiddenProp(t, e, i[e]);
        }
    }
    class MapObserverImpl {
        constructor(t) {
            this.type = pt;
            this.collection = t;
            this.indexMap = createIndexMap(t.size);
            this.lenObs = void 0;
        }
        notify() {
            const t = this.subs;
            t.notifyDirty();
            const e = this.indexMap;
            if (yt) {
                addCollectionBatch(t, this.collection, e);
                return;
            }
            const s = this.collection;
            const r = s.size;
            this.indexMap = createIndexMap(r);
            t.notifyCollection(s, e);
        }
        getLengthObserver() {
            return this.lenObs ??= new CollectionSizeObserver(this);
        }
    }
    Ct(MapObserverImpl, null);
    return function getMapObserver(e) {
        let s = t.get(e);
        if (s === void 0) {
            t.set(e, s = new MapObserverImpl(e));
            enableMapObservation(e);
        }
        return s;
    };
})();

const mt = /*@__PURE__*/ (() => {
    class BindingObserverRecord {
        constructor(t) {
            this.version = 0;
            this.count = 0;
            this.o = new Map;
            this.b = t;
        }
        add(t) {
            if (!this.o.has(t)) {
                t.subscribe(this.b);
                ++this.count;
            }
            this.o.set(t, this.version);
        }
        clear() {
            this.o.forEach(unsubscribeStale, this);
            this.count = this.o.size;
        }
        clearAll() {
            this.o.forEach(unsubscribeAll, this);
            this.o.clear();
            this.count = 0;
        }
    }
    function unsubscribeAll(t, e) {
        e.unsubscribe(this.b);
    }
    function unsubscribeStale(t, e) {
        if (this.version !== t) {
            e.unsubscribe(this.b);
            this.o.delete(e);
        }
    }
    function getObserverRecord() {
        return rtDefineHiddenProp(this, "obs", new BindingObserverRecord(this));
    }
    function observe(t, e) {
        this.obs.add(this.oL.getObserver(t, e));
    }
    function observeExpression(t, e) {
        this.obs.add(this.oL.getExpressionObserver(t, e));
    }
    function observeCollection(t) {
        let e;
        if (s(t)) {
            e = Et(t);
        } else if (n(t)) {
            e = Ot(t);
        } else if (r(t)) {
            e = At(t);
        } else {
            throw createMappedError(210, t);
        }
        this.obs.add(e);
    }
    function subscribeTo(t) {
        this.obs.add(t);
    }
    function noopHandleChange() {
        throw createMappedError(99, "handleChange");
    }
    function noopHandleCollectionChange() {
        throw createMappedError(99, "handleCollectionChange");
    }
    return function connectableDecorator(t, e) {
        const s = t.prototype;
        ensureProto(s, "observe", observe);
        ensureProto(s, "observeExpression", observeExpression);
        ensureProto(s, "observeCollection", observeCollection);
        ensureProto(s, "subscribeTo", subscribeTo);
        O(s, "obs", {
            get: getObserverRecord
        });
        ensureProto(s, "handleChange", noopHandleChange);
        ensureProto(s, "handleCollectionChange", noopHandleCollectionChange);
        return t;
    };
})();

function connectable(t, e) {
    return t == null ? mt : mt(t, e);
}

class ComputedObserver {
    constructor(t, e, s, r, n = "async") {
        this.type = pt;
        this.ov = void 0;
        this.v = void 0;
        this.N = false;
        this.B = false;
        this.D = false;
        this.cb = void 0;
        this.L = void 0;
        this.F = void 0;
        this.o = t;
        this.$ = wrap(t);
        this.$get = e;
        this.$set = s;
        this.oL = r;
        this.H = n;
    }
    init(t) {
        this.v = t;
        this.D = false;
    }
    getValue() {
        if (this.subs.count === 0) {
            return this.$get.call(this.o, this.o, this);
        }
        if (this.D) {
            this.compute();
            this.N = false;
        }
        return this.v;
    }
    setValue(t) {
        if (i(this.$set)) {
            if (this.L !== void 0) {
                t = this.L.call(null, t, this.F);
            }
            if (!h(t, this.v)) {
                this.$set.call(this.o, t);
                this.run();
            }
        } else {
            throw createMappedError(221);
        }
    }
    useCoercer(t, e) {
        this.L = t;
        this.F = e;
        return true;
    }
    useCallback(t) {
        this.cb = t;
        return true;
    }
    useFlush(t) {
        this.H = t;
        return true;
    }
    handleDirty() {
        if (!this.D) {
            this.D = true;
            this.subs.notifyDirty();
        }
    }
    handleChange() {
        this.D = true;
        if (this.subs.count > 0) {
            this.run();
        }
    }
    handleCollectionChange() {
        this.D = true;
        if (this.subs.count > 0) {
            this.run();
        }
    }
    subscribe(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            this.ov = this.compute();
            this.D = false;
            this.N = false;
        }
    }
    unsubscribe(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            this.D = true;
            this.obs.clearAll();
            this.ov = void 0;
            this.N = true;
        }
    }
    run() {
        if (this.H === "sync") {
            this.U();
            return;
        }
        if (this.B) {
            return;
        }
        this.B = true;
        queueTask(() => {
            this.B = false;
            this.U();
        });
    }
    U() {
        const t = this.v;
        const e = this.ov;
        const s = this.compute();
        this.D = false;
        if (!this.N || !h(s, t)) {
            this.subs.notify(s, e);
            this.ov = this.v = s;
            this.cb?.(s, e);
            this.N = true;
        }
    }
    compute() {
        this.D = false;
        this.obs.version++;
        try {
            enterConnectable(this);
            const t = unwrap(this.$get.call(this.$, this.$, this));
            if (this.D) {
                throw createMappedError(227, this.$get.name ?? this.$get.toString());
            }
            return this.v = t;
        } catch (t) {
            this.D = true;
            throw t;
        } finally {
            this.obs.clear();
            exitConnectable(this);
        }
    }
}

(() => {
    connectable(ComputedObserver, null);
    Ct(ComputedObserver, null);
})();

typeof SuppressedError === "function" ? SuppressedError : function(t, e, s) {
    var r = new Error(s);
    return r.name = "SuppressedError", r.error = t, r.suppressed = e, r;
};

function isClassMethodDecoratorContext(t) {
    return t?.kind === "method";
}

function computed(t, ...e) {
    if (i(t) && isClassMethodDecoratorContext(e[0])) {
        const e = t;
        A(e, {
            [q]: {
                deps: void 0
            }
        });
        return;
    }
    return function universalDecorator(s, r) {
        if (r.kind === "method") {
            const r = {};
            if (typeof t === "string" || typeof t === "symbol") {
                r.deps = [ t, ...e ];
            } else if (i(t)) {
                r.deps = t;
            } else if (t != null && typeof t === "object") {
                const e = t;
                if (Object.prototype.hasOwnProperty.call(e, "deps")) {
                    r.deps = e.deps;
                }
            }
            const n = s;
            A(n, {
                [q]: {
                    deps: r.deps
                }
            });
            return;
        }
        const n = o(t) ? t : {};
        if (!o(t)) {
            n.deps = [ t, ...e ];
        }
        const a = s;
        const c = r;
        if (c.kind !== "getter") {
            throw createMappedError(228, c.name);
        }
        const u = new WeakMap;
        return A(function computedGetter() {
            const t = u.get(getRaw(this));
            return t == null ? a.call(getRaw(this)) : t.getValue();
        }, {
            getObserver(t, e) {
                let s = u.get(t);
                if (s == null) {
                    s = e.getComputedObserver(t, c.name, {
                        get: a
                    }, n);
                    u.set(t, s);
                }
                return s;
            }
        });
    };
}

const kt = /*@__PURE__*/ R("IDirtyChecker", void 0);

const St = {
    timeoutsPerCheck: 25,
    disabled: false,
    throw: false,
    resetToDefault() {
        this.timeoutsPerCheck = 6;
        this.disabled = false;
        this.throw = false;
    }
};

class DirtyChecker {
    static register(t) {
        t.register(l.singleton(this, this), l.aliasTo(this, kt));
    }
    constructor() {
        this.tracked = [];
        this.W = null;
        this.q = 0;
        this.p = f(d);
        this.check = () => {
            if (St.disabled) {
                return;
            }
            if (++this.q < St.timeoutsPerCheck) {
                return;
            }
            this.q = 0;
            const t = this.tracked.slice(0);
            const e = t.length;
            let s;
            let r = 0;
            for (;r < e; ++r) {
                s = t[r];
                if (s.isDirty()) {
                    s.flush();
                }
            }
        };
        Ct(DirtyCheckProperty, null);
    }
    createProperty(t, e) {
        if (St.throw) {
            throw createMappedError(218, e);
        }
        return new DirtyCheckProperty(this, t, e);
    }
    addProperty(t) {
        this.tracked.push(t);
        if (this.tracked.length === 1) {
            this.W = queueRecurringTask(this.check, {
                interval: 0
            });
        }
    }
    removeProperty(t) {
        this.tracked.splice(this.tracked.indexOf(t), 1);
        if (this.tracked.length === 0) {
            this.W.cancel();
            this.W = null;
        }
    }
}

class DirtyCheckProperty {
    constructor(t, e, s) {
        this.obj = e;
        this.key = s;
        this.type = dt;
        this.ov = void 0;
        this.K = t;
    }
    getValue() {
        return this.obj[this.key];
    }
    setValue(t) {
        throw createMappedError(219, this.key);
    }
    isDirty() {
        return this.ov !== this.obj[this.key];
    }
    flush() {
        const t = this.ov;
        const e = this.getValue();
        this.ov = e;
        this.subs.notify(e, t);
    }
    subscribe(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            this.ov = this.obj[this.key];
            this.K.addProperty(this);
        }
    }
    unsubscribe(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            this.K.removeProperty(this);
        }
    }
}

class PrimitiveObserver {
    get doNotCache() {
        return true;
    }
    constructor(t, e) {
        this.type = dt;
        this.o = t;
        this.k = e;
    }
    getValue() {
        return this.o[this.k];
    }
    setValue() {}
    subscribe() {}
    unsubscribe() {}
}

class PropertyAccessor {
    constructor() {
        this.type = dt;
    }
    getValue(t, e) {
        return t[e];
    }
    setValue(t, e, s) {
        e[s] = t;
    }
}

class SetterObserver {
    constructor(t, e) {
        this.type = pt;
        this.v = void 0;
        this.iO = false;
        this.cb = void 0;
        this.L = void 0;
        this.F = void 0;
        this.o = t;
        this.k = e;
    }
    getValue() {
        return this.v;
    }
    setValue(t) {
        if (this.L !== void 0) {
            t = this.L.call(void 0, t, this.F);
        }
        const e = this.v;
        if (this.iO) {
            if (h(t, this.v)) {
                return;
            }
            this.v = t;
            this.subs.notifyDirty();
            this.subs.notify(t, e);
            if (h(t, this.v)) {
                this.cb?.(t, e);
            }
        } else {
            this.v = this.o[this.k] = t;
            this.cb?.(t, e);
        }
    }
    useCallback(t) {
        this.cb = t;
        this.start();
        return true;
    }
    useCoercer(t, e) {
        this.L = t;
        this.F = e;
        this.start();
        return true;
    }
    subscribe(t) {
        if (this.iO === false) {
            this.start();
        }
        this.subs.add(t);
    }
    start() {
        if (this.iO === false) {
            this.iO = true;
            this.v = this.o[this.k];
            O(this.o, this.k, {
                enumerable: true,
                configurable: true,
                get: A(() => this.getValue(), {
                    getObserver: () => this
                }),
                set: t => {
                    this.setValue(t);
                }
            });
        }
        return this;
    }
    stop() {
        if (this.iO) {
            O(this.o, this.k, {
                enumerable: true,
                configurable: true,
                writable: true,
                value: this.v
            });
            this.iO = false;
        }
        return this;
    }
}

(() => {
    Ct(SetterObserver, null);
})();

const getObserverLookup = t => {
    let e = t.$observers;
    if (e === void 0) {
        O(t, "$observers", {
            value: e = p()
        });
    }
    return e;
};

class ExpressionObserver {
    get type() {
        return pt;
    }
    constructor(t, e, s) {
        this.oL = e;
        this.v = void 0;
        this.boundFn = false;
        this.s = Scope.create(t);
        this.ast = s;
    }
    getValue() {
        return this.v;
    }
    setValue(t) {
        K(this.ast, this.s, this, null, t);
    }
    useCallback(t) {
        this.cb = t;
        return true;
    }
    handleChange() {
        this.run();
    }
    handleCollectionChange() {
        this.run();
    }
    run() {
        this.obs.version++;
        const t = this.v;
        const e = G(this.ast, this.s, this, this);
        this.obs.clear();
        if (!h(e, t)) {
            this.v = e;
            this.subs.notify(e, t);
            this.cb?.call(void 0, e, t);
        }
    }
    subscribe(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            this.A();
        }
    }
    unsubscribe(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            this.stop();
        }
    }
    A() {
        this.obs.version++;
        this.v = G(this.ast, this.s, this, this);
        this.obs.clear();
    }
    stop() {
        this.obs.clearAll();
        this.v = void 0;
    }
}

(() => {
    connectable(ExpressionObserver, null);
    Ct(ExpressionObserver, null);
    X(ExpressionObserver);
})();

class ControlledComputedObserver {
    constructor(t, e, s, r, n, i, o) {
        this.obj = t;
        this.key = e;
        this.getter = s;
        this.oL = r;
        this.dependencies = n;
        this.flush = i;
        this.deep = o;
        this.type = pt;
        this.doNotCache = false;
        this.v = void 0;
        this.G = [];
        this.J = false;
        this.X = false;
        this.cb = void 0;
        this.L = void 0;
        this.F = void 0;
    }
    getValue() {
        if (!this.X) {
            return this.Y();
        }
        return this.v;
    }
    setValue(t) {
        if (this.L != null) {
            t = this.L.call(null, t, this.F);
        }
        this.obj[this.key] = t;
    }
    useCallback(t) {
        this.cb = t;
        return true;
    }
    useCoercer(t, e) {
        this.L = t;
        this.F = e;
        return true;
    }
    handleChange() {
        if (this.flush === "sync") {
            this.Z();
            return;
        }
        if (this.J) {
            return;
        }
        this.J = true;
        queueTask(() => {
            this.J = false;
            this.Z();
        });
    }
    handleCollectionChange() {
        if (this.flush === "sync") {
            this.Z();
            return;
        }
        if (this.J) {
            return;
        }
        this.J = true;
        queueTask(() => {
            this.J = false;
            this.Z();
        });
    }
    Y() {
        return this.getter.call(this.obj);
    }
    Z() {
        if (!this.X) {
            return;
        }
        const t = this.v;
        const e = this.Y();
        this.tt();
        this.et();
        if (t === e) {
            return;
        }
        this.v = e;
        this.subs.notify(e, t);
        this.cb?.(e, t);
    }
    et() {
        const t = this.G;
        this.dependencies.forEach(e => {
            let s = b(e) ? this.oL.getExpressionObserver(this.obj, e) : this.oL.getObserver(this.obj, e);
            t.push(s);
            s.subscribe(this);
            s.useFlush?.(this.flush);
            if (this.deep) {
                s = observeDeep(s.getValue(), this.oL);
                t.push(s);
                s.subscribe(this);
                s.useFlush?.(this.flush);
            }
        });
    }
    tt() {
        this.G.forEach(t => {
            t.unsubscribe(this);
        });
        this.G.length = 0;
    }
    subscribe(t) {
        if (this.subs.add(t) && this.subs.count === 1) {
            this.et();
            this.X = true;
            this.v = this.Y();
        }
    }
    unsubscribe(t) {
        if (this.subs.remove(t) && this.subs.count === 0) {
            this.tt();
            this.X = false;
            this.v = void 0;
        }
    }
}

(() => {
    Ct(ControlledComputedObserver, null);
})();

function observeDeep(t, e) {
    function walk(t, e) {
        const i = unwrap(t);
        if (!o(i)) {
            return;
        }
        if (s(i)) {
            e.observeCollection(i);
            for (let t = 0; t < i.length; t++) {
                walk(i[t], e);
            }
            return;
        }
        if (r(i)) {
            e.observeCollection(i);
            for (const [t, s] of i) {
                walk(t, e);
                walk(s, e);
            }
            return;
        }
        if (n(i)) {
            e.observeCollection(i);
            for (const t of i) {
                walk(t, e);
            }
            return;
        }
        for (const t of Object.keys(i)) {
            e.observe(i, t);
            walk(i[t], e);
        }
    }
    return e.getObserver(t, (t, e) => {
        walk(t, e);
    });
}

const xt = new PropertyAccessor;

const Rt = /*@__PURE__*/ R("IObserverLocator", t => t.singleton(ObserverLocator));

const Pt = /*@__PURE__*/ R("INodeObserverLocator", t => t.cachedCallback(t => new DefaultNodeObserverLocator));

class DefaultNodeObserverLocator {
    handles() {
        return false;
    }
    getObserver() {
        return xt;
    }
    getAccessor() {
        return xt;
    }
}

class ObserverLocator {
    constructor() {
        this.st = [];
        this.K = f(kt);
        this.rt = f(Pt);
        this.nt = f(w(C));
    }
    addAdapter(t) {
        this.st.push(t);
    }
    getObserver(t, e) {
        if (t == null) {
            throw createMappedError(199, e);
        }
        if (!o(t)) {
            return new PrimitiveObserver(t, i(e) ? "" : e);
        }
        if (i(e)) {
            return new ComputedObserver(t, e, void 0, this);
        }
        const s = getObserverLookup(t);
        let r = s[e];
        if (r === void 0) {
            r = this.createObserver(t, e);
            if (!r.doNotCache) {
                s[e] = r;
            }
        }
        return r;
    }
    getAccessor(t, e) {
        const s = t.$observers?.[e];
        if (s !== void 0) {
            return s;
        }
        if (this.rt.handles(t, e, this)) {
            return this.rt.getAccessor(t, e, this);
        }
        return xt;
    }
    getExpressionObserver(t, e) {
        if (this.nt == null) {
            throw createMappedError(200, e);
        }
        return new ExpressionObserver(t, this, this.nt.parse(e, "IsProperty"));
    }
    getComputedObserver(t, e, s, r) {
        if (r?.deps == null) {
            const n = new ComputedObserver(t, s.get, s.set, this, r?.flush);
            O(t, e, {
                enumerable: s.enumerable,
                configurable: true,
                get: A(() => n.getValue(), {
                    getObserver: () => n
                }),
                set: t => {
                    n.setValue(t);
                }
            });
            return n;
        }
        return new ControlledComputedObserver(t, e, s.get, this, r.deps, r.flush ?? "async", r.deep === true);
    }
    getArrayObserver(t) {
        return Et(t);
    }
    getMapObserver(t) {
        return At(t);
    }
    getSetObserver(t) {
        return Ot(t);
    }
    createObserver(t, e) {
        if (this.rt.handles(t, e, this)) {
            return this.rt.getObserver(t, e, this);
        }
        switch (e) {
          case "length":
            if (s(t)) {
                return Et(t).getLengthObserver();
            }
            break;

          case "size":
            if (r(t)) {
                return At(t).getLengthObserver();
            } else if (n(t)) {
                return Ot(t).getLengthObserver();
            }
            break;

          default:
            if (s(t) && c(e)) {
                return Et(t).getIndexObserver(Number(e));
            }
            break;
        }
        let i = S(t, e);
        if (i === void 0) {
            let s = k(t);
            while (s !== null) {
                i = S(s, e);
                if (i === void 0) {
                    s = k(s);
                } else {
                    break;
                }
            }
        }
        if (i !== void 0 && !E.call(i, "value")) {
            let s = this.it(t, e, i);
            if (s == null) {
                s = (i.get?.getObserver)?.(t, this);
            }
            return s == null ? i.configurable ? this.getComputedObserver(t, e, i) : this.K.createProperty(t, e) : s;
        }
        return new SetterObserver(t, e);
    }
    it(t, e, s) {
        if (this.st.length > 0) {
            for (const r of this.st) {
                const n = r.getObserver(t, e, s, this);
                if (n != null) {
                    return n;
                }
            }
        }
        return null;
    }
}

const getCollectionObserver = t => {
    let e;
    if (s(t)) {
        e = Et(t);
    } else if (r(t)) {
        e = At(t);
    } else if (n(t)) {
        e = Ot(t);
    }
    return e;
};

const Dt = /*@__PURE__*/ R("IObservation", t => t.singleton(Observation));

class Observation {
    constructor() {
        this.oL = f(Rt);
    }
    run(t) {
        const e = new RunEffect(this.oL, t);
        e.run();
        return e;
    }
    watch(t, e, s, r) {
        return this.ot(t, e, s, r);
    }
    ot(t, e, s, r) {
        let n = undefined;
        let o = false;
        let a;
        const handleChange = (t, e) => {
            a?.();
            a = void 0;
            const r = s(t, n = e);
            if (i(r)) {
                a = r;
            }
        };
        const c = b(e) ? this.oL.getExpressionObserver(t, e) : this.oL.getObserver(t, e);
        const u = {
            handleChange: handleChange
        };
        const run = () => {
            if (o) {
                return;
            }
            handleChange(c.getValue(), n);
        };
        const stop = () => {
            if (o) {
                throw createMappedError(225);
            }
            o = true;
            c.unsubscribe(u);
            a?.();
            a = void 0;
        };
        c.subscribe(u);
        if (r?.immediate !== false) {
            run();
        }
        return {
            run: run,
            stop: stop
        };
    }
}

class RunEffect {
    constructor(t, e) {
        this.oL = t;
        this.fn = e;
        this.maxRunCount = 10;
        this.queued = false;
        this.running = false;
        this.runCount = 0;
        this.stopped = false;
        this.ct = undefined;
        this.run = () => {
            if (this.running || this.stopped) {
                return;
            }
            ++this.runCount;
            this.running = true;
            this.queued = false;
            ++this.obs.version;
            try {
                this.ct?.call(void 0);
                enterConnectable(this);
                this.ct = this.fn(this);
            } finally {
                this.obs.clear();
                this.running = false;
                exitConnectable(this);
            }
            if (this.queued) {
                if (this.runCount > this.maxRunCount) {
                    this.runCount = 0;
                    throw createMappedError(226);
                }
                this.run();
            } else {
                this.runCount = 0;
            }
        };
        this.stop = () => {
            if (this.stopped) {
                throw createMappedError(225);
            }
            this.stopped = true;
            this.obs.clearAll();
            this.ct?.call(void 0);
            this.ct = void 0;
        };
    }
    handleChange() {
        this.queued = true;
        this.run();
    }
    handleCollectionChange() {
        this.queued = true;
        this.run();
    }
}

(() => {
    connectable(RunEffect, null);
})();

const It = /*@__PURE__*/ (() => {
    const t = {};
    function observable(e, s) {
        if (!SetterNotifier.mixed) {
            SetterNotifier.mixed = true;
            Ct(SetterNotifier, null);
        }
        let r = false;
        let n;
        if (typeof e === "object") {
            n = e;
        } else if (e != null) {
            n = {
                name: e
            };
            r = true;
        } else {
            n = v;
        }
        if (arguments.length === 0) {
            return function(t, e) {
                if (e.kind !== "field") throw createMappedError(224);
                return createFieldInitializer(e);
            };
        }
        if (s?.kind === "field") return createFieldInitializer(s);
        if (r) {
            return function(e, s) {
                createDescriptor(e, n.name, () => t, true);
            };
        }
        return function(e, s) {
            switch (s.kind) {
              case "field":
                return createFieldInitializer(s);

              case "class":
                return createDescriptor(e, n.name, () => t, true);

              default:
                throw createMappedError(224);
            }
        };
        function createFieldInitializer(t) {
            let e;
            t.addInitializer(function() {
                createDescriptor(this, t.name, () => e, false);
            });
            return function(t) {
                return e = t;
            };
        }
        function createDescriptor(t, e, s, r) {
            const i = n.callback || `${x(e)}Changed`;
            const o = n.set;
            const observableGetter = function() {
                const t = getNotifier(this, e, i, s, o);
                currentConnectable()?.subscribeTo(t);
                return t.getValue();
            };
            observableGetter.getObserver = function(t) {
                return getNotifier(t, e, i, s, o);
            };
            const a = {
                enumerable: true,
                configurable: true,
                get: observableGetter,
                set(t) {
                    getNotifier(this, e, i, s, o).setValue(t);
                }
            };
            if (r) O(t.prototype, e, a); else O(t, e, a);
        }
    }
    function getNotifier(e, s, r, n, i) {
        const o = getObserverLookup(e);
        let a = o[s];
        if (a == null) {
            const c = n();
            a = new SetterNotifier(e, r, i, c === t ? void 0 : c);
            o[s] = a;
        }
        return a;
    }
    class SetterNotifier {
        constructor(t, e, s, r) {
            this.type = pt;
            this.v = void 0;
            this.ov = void 0;
            this.o = t;
            this.S = s;
            this.hs = i(s);
            const n = t[e];
            this.cb = i(n) ? n : void 0;
            this.v = r;
        }
        getValue() {
            return this.v;
        }
        setValue(t) {
            if (this.hs) {
                t = this.S(t);
            }
            if (!h(t, this.v)) {
                this.ov = this.v;
                this.v = t;
                this.subs.notifyDirty();
                this.subs.notify(this.v, this.ov);
                if (h(t, this.v)) {
                    this.cb?.call(this.o, this.v, this.ov);
                }
            }
        }
    }
    SetterNotifier.mixed = false;
    return observable;
})();

function nowrap(t, e) {
    return arguments.length === 0 ? decorator : decorator(t, e);
    function decorator(t, e) {
        switch (e.kind) {
          case "class":
            rtDefineHiddenProp(t, B, true);
            break;

          case "field":
            e.addInitializer(function() {
                const t = this.constructor;
                const s = `${L}_${x(e.name)}__`;
                if (s in t) return;
                rtDefineHiddenProp(t, s, true);
            });
            break;
        }
    }
}

export { vt as AccessorType, BindingContext, CollectionLengthObserver, CollectionSizeObserver, ComputedObserver, T as ConnectableSwitcher, DirtyCheckProperty, St as DirtyCheckSettings, DirtyChecker, ft as ICoercionConfiguration, kt as IDirtyChecker, Pt as INodeObserverLocator, Dt as IObservation, Rt as IObserverLocator, Observation, ObserverLocator, PrimitiveObserver, PropertyAccessor, W as ProxyObservable, RecurringTask, Scope, SetterObserver, Task, TaskAbortError, K as astAssign, J as astBind, G as astEvaluate, astTrack, Q as astUnbind, batch, cloneIndexMap, computed, connectable, copyIndexMap, createIndexMap, getCollectionObserver, getObserverLookup, getRecurringTasks, isIndexMap, isTaskQueueEmpty, X as mixinNoopAstEvaluator, nowrap, It as observable, queueAsyncTask, queueRecurringTask, queueTask, runTasks, Ct as subscriberCollection, tasksSettled };
//# sourceMappingURL=index.mjs.map
