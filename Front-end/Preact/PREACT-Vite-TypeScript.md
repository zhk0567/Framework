# Preact（Vite + TypeScript）

与 React API 高度兼容、体积极小；本目录为 **Vite 官方 preact-ts** 模板上的「能力展台」单页。

```powershell
Set-Location -LiteralPath 'f:\Study\Framework\Front-end\Preact'
npm install
npm run dev
```

详见仓库根目录 `README.md` 与其它前端子项目对照。
