# Stencil（Web 组件编译器）

产出标准 **Custom Elements**；开发服务器与构建由 `@stencil/core` 驱动（非 Vite）。

```powershell
Set-Location -LiteralPath 'f:\Study\Framework\Front-end\Stencil'
npm install
npm start
```

展台 UI 在 `src/components/my-component`。
